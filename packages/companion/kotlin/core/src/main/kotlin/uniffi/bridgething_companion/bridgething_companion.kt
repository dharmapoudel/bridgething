// This file was autogenerated by some hot garbage in the `uniffi` crate.
// Trust me, you don't want to mess with it!

@file:Suppress("NAME_SHADOWING")

package uniffi.bridgething_companion

// Common helper code.
//
// Ideally this would live in a separate .kt file where it can be unittested etc
// in isolation, and perhaps even published as a re-useable package.
//
// However, it's important that the details of how this helper code works (e.g. the
// way that different builtin types are passed across the FFI) exactly match what's
// expected by the Rust code on the other side of the interface. In practice right
// now that means coming from the exact some version of `uniffi` that was used to
// compile the Rust component. The easiest way to ensure this is to bundle the Kotlin
// helpers directly inline like we're doing here.

import com.sun.jna.Library
import com.sun.jna.IntegerType
import com.sun.jna.Native
import com.sun.jna.Pointer
import com.sun.jna.Structure
import com.sun.jna.Callback
import com.sun.jna.ptr.*
import java.nio.ByteBuffer
import java.nio.ByteOrder
import java.nio.CharBuffer
import java.nio.charset.CodingErrorAction
import java.util.concurrent.atomic.AtomicLong
import java.util.concurrent.ConcurrentHashMap
import java.util.concurrent.atomic.AtomicBoolean
import kotlin.coroutines.resume
import kotlinx.coroutines.CancellableContinuation
import kotlinx.coroutines.DelicateCoroutinesApi
import kotlinx.coroutines.GlobalScope
import kotlinx.coroutines.Job
import kotlinx.coroutines.launch
import kotlinx.coroutines.suspendCancellableCoroutine

// This is a helper for safely working with byte buffers returned from the Rust code.
// A rust-owned buffer is represented by its capacity, its current length, and a
// pointer to the underlying data.

/**
 * @suppress
 */
@Structure.FieldOrder("capacity", "len", "data")
open class RustBuffer : Structure() {
    // Note: `capacity` and `len` are actually `ULong` values, but JVM only supports signed values.
    // When dealing with these fields, make sure to call `toULong()`.
    @JvmField var capacity: Long = 0
    @JvmField var len: Long = 0
    @JvmField var data: Pointer? = null

    class ByValue: RustBuffer(), Structure.ByValue
    class ByReference: RustBuffer(), Structure.ByReference

   internal fun setValue(other: RustBuffer) {
        capacity = other.capacity
        len = other.len
        data = other.data
    }

    companion object {
        internal fun alloc(size: ULong = 0UL) = uniffiRustCall() { status ->
            // Note: need to convert the size to a `Long` value to make this work with JVM.
            UniffiLib.ffi_bridgething_companion_rustbuffer_alloc(size.toLong(), status)
        }.also {
            if(it.data == null) {
               throw RuntimeException("RustBuffer.alloc() returned null data pointer (size=${size})")
           }
        }

        internal fun create(capacity: ULong, len: ULong, data: Pointer?): RustBuffer.ByValue {
            var buf = RustBuffer.ByValue()
            buf.capacity = capacity.toLong()
            buf.len = len.toLong()
            buf.data = data
            return buf
        }

        internal fun free(buf: RustBuffer.ByValue) = uniffiRustCall() { status ->
            UniffiLib.ffi_bridgething_companion_rustbuffer_free(buf, status)
        }
    }

    @Suppress("TooGenericExceptionThrown")
    fun asByteBuffer() =
        this.data?.getByteBuffer(0, this.len)?.also {
            it.order(ByteOrder.BIG_ENDIAN)
        }
}

// This is a helper for safely passing byte references into the rust code.
// It's not actually used at the moment, because there aren't many things that you
// can take a direct pointer to in the JVM, and if we're going to copy something
// then we might as well copy it into a `RustBuffer`. But it's here for API
// completeness.

@Structure.FieldOrder("len", "data")
internal open class ForeignBytes : Structure() {
    @JvmField var len: Int = 0
    @JvmField var data: Pointer? = null

    class ByValue : ForeignBytes(), Structure.ByValue
}

// Converter for `&[u8]` / `[ByRef] bytes` arguments.
//
// Only `lower` is valid — zero-copy byte buffers only flow foreign -> Rust,
// and only in argument position. `lift`, `read`, `write`, and
// `allocationSize` have no sound implementation here and all panic at
// runtime. The `FfiConverter` interface is implemented so that the
// compiler enforces the full method set (rather than relying on eyeball).
//
// The provided `ByteBuffer` MUST be direct — only direct buffers have a
// stable native address that JNA can expose via `getDirectBufferPointer`.
// The returned `ForeignBytes.ByValue` is only valid for the duration of
// the FFI call; the Rust side treats it as a borrow.
internal object FfiConverterByRefBytes : FfiConverter<java.nio.ByteBuffer, ForeignBytes.ByValue> {
    override fun lower(value: java.nio.ByteBuffer): ForeignBytes.ByValue {
        require(value.isDirect) { "UniFFI zero-copy &[u8] requires a direct ByteBuffer. Use ByteBuffer.allocateDirect()." }
        val remaining = value.remaining()
        val fb = ForeignBytes.ByValue()
        fb.len = remaining
        // Zero-length direct buffers: skip getDirectBufferPointer (platform-variable behavior)
        // and pass null. The Rust side treats (null, 0) as &[].
        fb.data = if (remaining == 0) null else com.sun.jna.Native.getDirectBufferPointer(value)
        return fb
    }

    override fun lift(value: ForeignBytes.ByValue): java.nio.ByteBuffer =
        error("ByRef bytes cannot be lifted: zero-copy &[u8] only flows foreign->Rust")

    override fun read(buf: java.nio.ByteBuffer): java.nio.ByteBuffer =
        error("ByRef bytes cannot be read from a buffer: zero-copy &[u8] is only supported in argument position, not nested in records/options/etc.")

    override fun write(value: java.nio.ByteBuffer, buf: java.nio.ByteBuffer): Unit =
        error("ByRef bytes cannot be written to a buffer: zero-copy &[u8] is only supported in argument position, not nested in records/options/etc.")

    override fun allocationSize(value: java.nio.ByteBuffer): ULong =
        error("ByRef bytes have no RustBuffer allocation size: zero-copy &[u8] is only supported in argument position, not nested in records/options/etc.")
}
/**
 * The FfiConverter interface handles converter types to and from the FFI
 *
 * All implementing objects should be public to support external types.  When a
 * type is external we need to import it's FfiConverter.
 *
 * @suppress
 */
public interface FfiConverter<KotlinType, FfiType> {
    // Convert an FFI type to a Kotlin type
    fun lift(value: FfiType): KotlinType

    // Convert an Kotlin type to an FFI type
    fun lower(value: KotlinType): FfiType

    // Read a Kotlin type from a `ByteBuffer`
    fun read(buf: ByteBuffer): KotlinType

    // Calculate bytes to allocate when creating a `RustBuffer`
    //
    // This must return at least as many bytes as the write() function will
    // write. It can return more bytes than needed, for example when writing
    // Strings we can't know the exact bytes needed until we the UTF-8
    // encoding, so we pessimistically allocate the largest size possible (3
    // bytes per codepoint).  Allocating extra bytes is not really a big deal
    // because the `RustBuffer` is short-lived.
    fun allocationSize(value: KotlinType): ULong

    // Write a Kotlin type to a `ByteBuffer`
    fun write(value: KotlinType, buf: ByteBuffer)

    // Lower a value into a `RustBuffer`
    //
    // This method lowers a value into a `RustBuffer` rather than the normal
    // FfiType.  It's used by the callback interface code.  Callback interface
    // returns are always serialized into a `RustBuffer` regardless of their
    // normal FFI type.
    fun lowerIntoRustBuffer(value: KotlinType): RustBuffer.ByValue {
        val rbuf = RustBuffer.alloc(allocationSize(value))
        try {
            val bbuf = rbuf.data!!.getByteBuffer(0, rbuf.capacity).also {
                it.order(ByteOrder.BIG_ENDIAN)
            }
            write(value, bbuf)
            rbuf.writeField("len", bbuf.position().toLong())
            return rbuf
        } catch (e: Throwable) {
            RustBuffer.free(rbuf)
            throw e
        }
    }

    // Lift a value from a `RustBuffer`.
    //
    // This here mostly because of the symmetry with `lowerIntoRustBuffer()`.
    // It's currently only used by the `FfiConverterRustBuffer` class below.
    fun liftFromRustBuffer(rbuf: RustBuffer.ByValue): KotlinType {
        val byteBuf = rbuf.asByteBuffer()!!
        try {
           val item = read(byteBuf)
           if (byteBuf.hasRemaining()) {
               throw RuntimeException("junk remaining in buffer after lifting, something is very wrong!!")
           }
           return item
        } finally {
            RustBuffer.free(rbuf)
        }
    }
}

/**
 * FfiConverter that uses `RustBuffer` as the FfiType
 *
 * @suppress
 */
public interface FfiConverterRustBuffer<KotlinType>: FfiConverter<KotlinType, RustBuffer.ByValue> {
    override fun lift(value: RustBuffer.ByValue) = liftFromRustBuffer(value)
    override fun lower(value: KotlinType) = lowerIntoRustBuffer(value)
}
// A handful of classes and functions to support the generated data structures.
// This would be a good candidate for isolating in its own ffi-support lib.

internal const val UNIFFI_CALL_SUCCESS = 0.toByte()
internal const val UNIFFI_CALL_ERROR = 1.toByte()
internal const val UNIFFI_CALL_UNEXPECTED_ERROR = 2.toByte()

@Structure.FieldOrder("code", "error_buf")
internal open class UniffiRustCallStatus : Structure() {
    @JvmField var code: Byte = 0
    @JvmField var error_buf: RustBuffer.ByValue = RustBuffer.ByValue()

    class ByValue: UniffiRustCallStatus(), Structure.ByValue

    fun isSuccess(): Boolean {
        return code == UNIFFI_CALL_SUCCESS
    }

    fun isError(): Boolean {
        return code == UNIFFI_CALL_ERROR
    }

    fun isPanic(): Boolean {
        return code == UNIFFI_CALL_UNEXPECTED_ERROR
    }

    companion object {
        fun create(code: Byte, errorBuf: RustBuffer.ByValue): UniffiRustCallStatus.ByValue {
            val callStatus = UniffiRustCallStatus.ByValue()
            callStatus.code = code
            callStatus.error_buf = errorBuf
            return callStatus
        }
    }
}

class InternalException(message: String) : kotlin.Exception(message)

/**
 * Each top-level error class has a companion object that can lift the error from the call status's rust buffer
 *
 * @suppress
 */
interface UniffiRustCallStatusErrorHandler<E> {
    fun lift(error_buf: RustBuffer.ByValue): E;
}

// Helpers for calling Rust
// In practice we usually need to be synchronized to call this safely, so it doesn't
// synchronize itself

// Call a rust function that returns a Result<>.  Pass in the Error class companion that corresponds to the Err
private inline fun <U, E: kotlin.Exception> uniffiRustCallWithError(errorHandler: UniffiRustCallStatusErrorHandler<E>, callback: (UniffiRustCallStatus) -> U): U {
    var status = UniffiRustCallStatus()
    val return_value = callback(status)
    uniffiCheckCallStatus(errorHandler, status)
    return return_value
}

// Check UniffiRustCallStatus and throw an error if the call wasn't successful
private fun<E: kotlin.Exception> uniffiCheckCallStatus(errorHandler: UniffiRustCallStatusErrorHandler<E>, status: UniffiRustCallStatus) {
    if (status.isSuccess()) {
        return
    } else if (status.isError()) {
        throw errorHandler.lift(status.error_buf)
    } else if (status.isPanic()) {
        // when the rust code sees a panic, it tries to construct a rustbuffer
        // with the message.  but if that code panics, then it just sends back
        // an empty buffer.
        if (status.error_buf.len > 0) {
            throw InternalException(FfiConverterString.lift(status.error_buf))
        } else {
            throw InternalException("Rust panic")
        }
    } else {
        throw InternalException("Unknown rust call status: $status.code")
    }
}

/**
 * UniffiRustCallStatusErrorHandler implementation for times when we don't expect a CALL_ERROR
 *
 * @suppress
 */
object UniffiNullRustCallStatusErrorHandler: UniffiRustCallStatusErrorHandler<InternalException> {
    override fun lift(error_buf: RustBuffer.ByValue): InternalException {
        RustBuffer.free(error_buf)
        return InternalException("Unexpected CALL_ERROR")
    }
}

// Call a rust function that returns a plain value
private inline fun <U> uniffiRustCall(callback: (UniffiRustCallStatus) -> U): U {
    return uniffiRustCallWithError(UniffiNullRustCallStatusErrorHandler, callback)
}

internal inline fun<T> uniffiTraitInterfaceCall(
    callStatus: UniffiRustCallStatus,
    makeCall: () -> T,
    writeReturn: (T) -> Unit,
) {
    try {
        writeReturn(makeCall())
    } catch(e: kotlin.Exception) {
        val err = try { e.stackTraceToString() } catch(_: Throwable) { "" }
        callStatus.code = UNIFFI_CALL_UNEXPECTED_ERROR
        callStatus.error_buf = FfiConverterString.lower(err)
    }
}

internal inline fun<T, reified E: Throwable> uniffiTraitInterfaceCallWithError(
    callStatus: UniffiRustCallStatus,
    makeCall: () -> T,
    writeReturn: (T) -> Unit,
    lowerError: (E) -> RustBuffer.ByValue
) {
    try {
        writeReturn(makeCall())
    } catch(e: kotlin.Exception) {
        if (e is E) {
            callStatus.code = UNIFFI_CALL_ERROR
            callStatus.error_buf = lowerError(e)
        } else {
            val err = try { e.stackTraceToString() } catch(_: Throwable) { "" }
            callStatus.code = UNIFFI_CALL_UNEXPECTED_ERROR
            callStatus.error_buf = FfiConverterString.lower(err)
        }
    }
}
// Initial value and increment amount for handles. 
// These ensure that Kotlin-generated handles always have the lowest bit set
private const val UNIFFI_HANDLEMAP_INITIAL = 1.toLong()
private const val UNIFFI_HANDLEMAP_DELTA = 2.toLong()

// Map handles to objects
//
// This is used pass an opaque 64-bit handle representing a foreign object to the Rust code.
internal class UniffiHandleMap<T: Any> {
    private val map = ConcurrentHashMap<Long, T>()
    // Start 
    private val counter = java.util.concurrent.atomic.AtomicLong(UNIFFI_HANDLEMAP_INITIAL)

    val size: Int
        get() = map.size

    // Insert a new object into the handle map and get a handle for it
    fun insert(obj: T): Long {
        val handle = counter.getAndAdd(UNIFFI_HANDLEMAP_DELTA)
        map.put(handle, obj)
        return handle
    }

    // Clone a handle, creating a new one
    fun clone(handle: Long): Long {
        val obj = map.get(handle) ?: throw InternalException("UniffiHandleMap.clone: Invalid handle")
        return insert(obj)
    }

    // Get an object from the handle map
    fun get(handle: Long): T {
        return map.get(handle) ?: throw InternalException("UniffiHandleMap.get: Invalid handle")
    }

    // Remove an entry from the handlemap and get the Kotlin object back
    fun remove(handle: Long): T {
        return map.remove(handle) ?: throw InternalException("UniffiHandleMap: Invalid handle")
    }
}

// Contains loading, initialization code,
// and the FFI Function declarations in a com.sun.jna.Library.
@Synchronized
private fun findLibraryName(componentName: String): String {
    val libOverride = System.getProperty("uniffi.component.$componentName.libraryOverride")
    if (libOverride != null) {
        return libOverride
    }
    return "bridgething_companion"
}

// Define FFI callback types
internal interface UniffiRustFutureContinuationCallback : com.sun.jna.Callback {
    fun callback(`data`: Long,`pollResult`: Byte,)
}
internal interface UniffiForeignFutureDroppedCallback : com.sun.jna.Callback {
    fun callback(`handle`: Long,)
}
internal interface UniffiCallbackInterfaceFree : com.sun.jna.Callback {
    fun callback(`handle`: Long,)
}
internal interface UniffiCallbackInterfaceClone : com.sun.jna.Callback {
    fun callback(`handle`: Long,)
    : Long
}
@Structure.FieldOrder("handle", "free")
internal open class UniffiForeignFutureDroppedCallbackStruct(
    @JvmField internal var `handle`: Long = 0.toLong(),
    @JvmField internal var `free`: UniffiForeignFutureDroppedCallback? = null,
) : Structure() {
    class UniffiByValue(
        `handle`: Long = 0.toLong(),
        `free`: UniffiForeignFutureDroppedCallback? = null,
    ): UniffiForeignFutureDroppedCallbackStruct(`handle`,`free`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiForeignFutureDroppedCallbackStruct) {
        `handle` = other.`handle`
        `free` = other.`free`
    }

}
@Structure.FieldOrder("returnValue", "callStatus")
internal open class UniffiForeignFutureResultU8(
    @JvmField internal var `returnValue`: Byte = 0.toByte(),
    @JvmField internal var `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
) : Structure() {
    class UniffiByValue(
        `returnValue`: Byte = 0.toByte(),
        `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
    ): UniffiForeignFutureResultU8(`returnValue`,`callStatus`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiForeignFutureResultU8) {
        `returnValue` = other.`returnValue`
        `callStatus` = other.`callStatus`
    }

}
internal interface UniffiForeignFutureCompleteU8 : com.sun.jna.Callback {
    fun callback(`callbackData`: Long,`result`: UniffiForeignFutureResultU8.UniffiByValue,)
}
@Structure.FieldOrder("returnValue", "callStatus")
internal open class UniffiForeignFutureResultI8(
    @JvmField internal var `returnValue`: Byte = 0.toByte(),
    @JvmField internal var `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
) : Structure() {
    class UniffiByValue(
        `returnValue`: Byte = 0.toByte(),
        `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
    ): UniffiForeignFutureResultI8(`returnValue`,`callStatus`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiForeignFutureResultI8) {
        `returnValue` = other.`returnValue`
        `callStatus` = other.`callStatus`
    }

}
internal interface UniffiForeignFutureCompleteI8 : com.sun.jna.Callback {
    fun callback(`callbackData`: Long,`result`: UniffiForeignFutureResultI8.UniffiByValue,)
}
@Structure.FieldOrder("returnValue", "callStatus")
internal open class UniffiForeignFutureResultU16(
    @JvmField internal var `returnValue`: Short = 0.toShort(),
    @JvmField internal var `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
) : Structure() {
    class UniffiByValue(
        `returnValue`: Short = 0.toShort(),
        `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
    ): UniffiForeignFutureResultU16(`returnValue`,`callStatus`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiForeignFutureResultU16) {
        `returnValue` = other.`returnValue`
        `callStatus` = other.`callStatus`
    }

}
internal interface UniffiForeignFutureCompleteU16 : com.sun.jna.Callback {
    fun callback(`callbackData`: Long,`result`: UniffiForeignFutureResultU16.UniffiByValue,)
}
@Structure.FieldOrder("returnValue", "callStatus")
internal open class UniffiForeignFutureResultI16(
    @JvmField internal var `returnValue`: Short = 0.toShort(),
    @JvmField internal var `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
) : Structure() {
    class UniffiByValue(
        `returnValue`: Short = 0.toShort(),
        `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
    ): UniffiForeignFutureResultI16(`returnValue`,`callStatus`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiForeignFutureResultI16) {
        `returnValue` = other.`returnValue`
        `callStatus` = other.`callStatus`
    }

}
internal interface UniffiForeignFutureCompleteI16 : com.sun.jna.Callback {
    fun callback(`callbackData`: Long,`result`: UniffiForeignFutureResultI16.UniffiByValue,)
}
@Structure.FieldOrder("returnValue", "callStatus")
internal open class UniffiForeignFutureResultU32(
    @JvmField internal var `returnValue`: Int = 0,
    @JvmField internal var `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
) : Structure() {
    class UniffiByValue(
        `returnValue`: Int = 0,
        `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
    ): UniffiForeignFutureResultU32(`returnValue`,`callStatus`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiForeignFutureResultU32) {
        `returnValue` = other.`returnValue`
        `callStatus` = other.`callStatus`
    }

}
internal interface UniffiForeignFutureCompleteU32 : com.sun.jna.Callback {
    fun callback(`callbackData`: Long,`result`: UniffiForeignFutureResultU32.UniffiByValue,)
}
@Structure.FieldOrder("returnValue", "callStatus")
internal open class UniffiForeignFutureResultI32(
    @JvmField internal var `returnValue`: Int = 0,
    @JvmField internal var `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
) : Structure() {
    class UniffiByValue(
        `returnValue`: Int = 0,
        `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
    ): UniffiForeignFutureResultI32(`returnValue`,`callStatus`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiForeignFutureResultI32) {
        `returnValue` = other.`returnValue`
        `callStatus` = other.`callStatus`
    }

}
internal interface UniffiForeignFutureCompleteI32 : com.sun.jna.Callback {
    fun callback(`callbackData`: Long,`result`: UniffiForeignFutureResultI32.UniffiByValue,)
}
@Structure.FieldOrder("returnValue", "callStatus")
internal open class UniffiForeignFutureResultU64(
    @JvmField internal var `returnValue`: Long = 0.toLong(),
    @JvmField internal var `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
) : Structure() {
    class UniffiByValue(
        `returnValue`: Long = 0.toLong(),
        `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
    ): UniffiForeignFutureResultU64(`returnValue`,`callStatus`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiForeignFutureResultU64) {
        `returnValue` = other.`returnValue`
        `callStatus` = other.`callStatus`
    }

}
internal interface UniffiForeignFutureCompleteU64 : com.sun.jna.Callback {
    fun callback(`callbackData`: Long,`result`: UniffiForeignFutureResultU64.UniffiByValue,)
}
@Structure.FieldOrder("returnValue", "callStatus")
internal open class UniffiForeignFutureResultI64(
    @JvmField internal var `returnValue`: Long = 0.toLong(),
    @JvmField internal var `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
) : Structure() {
    class UniffiByValue(
        `returnValue`: Long = 0.toLong(),
        `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
    ): UniffiForeignFutureResultI64(`returnValue`,`callStatus`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiForeignFutureResultI64) {
        `returnValue` = other.`returnValue`
        `callStatus` = other.`callStatus`
    }

}
internal interface UniffiForeignFutureCompleteI64 : com.sun.jna.Callback {
    fun callback(`callbackData`: Long,`result`: UniffiForeignFutureResultI64.UniffiByValue,)
}
@Structure.FieldOrder("returnValue", "callStatus")
internal open class UniffiForeignFutureResultF32(
    @JvmField internal var `returnValue`: Float = 0.0f,
    @JvmField internal var `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
) : Structure() {
    class UniffiByValue(
        `returnValue`: Float = 0.0f,
        `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
    ): UniffiForeignFutureResultF32(`returnValue`,`callStatus`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiForeignFutureResultF32) {
        `returnValue` = other.`returnValue`
        `callStatus` = other.`callStatus`
    }

}
internal interface UniffiForeignFutureCompleteF32 : com.sun.jna.Callback {
    fun callback(`callbackData`: Long,`result`: UniffiForeignFutureResultF32.UniffiByValue,)
}
@Structure.FieldOrder("returnValue", "callStatus")
internal open class UniffiForeignFutureResultF64(
    @JvmField internal var `returnValue`: Double = 0.0,
    @JvmField internal var `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
) : Structure() {
    class UniffiByValue(
        `returnValue`: Double = 0.0,
        `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
    ): UniffiForeignFutureResultF64(`returnValue`,`callStatus`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiForeignFutureResultF64) {
        `returnValue` = other.`returnValue`
        `callStatus` = other.`callStatus`
    }

}
internal interface UniffiForeignFutureCompleteF64 : com.sun.jna.Callback {
    fun callback(`callbackData`: Long,`result`: UniffiForeignFutureResultF64.UniffiByValue,)
}
@Structure.FieldOrder("returnValue", "callStatus")
internal open class UniffiForeignFutureResultRustBuffer(
    @JvmField internal var `returnValue`: RustBuffer.ByValue = RustBuffer.ByValue(),
    @JvmField internal var `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
) : Structure() {
    class UniffiByValue(
        `returnValue`: RustBuffer.ByValue = RustBuffer.ByValue(),
        `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
    ): UniffiForeignFutureResultRustBuffer(`returnValue`,`callStatus`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiForeignFutureResultRustBuffer) {
        `returnValue` = other.`returnValue`
        `callStatus` = other.`callStatus`
    }

}
internal interface UniffiForeignFutureCompleteRustBuffer : com.sun.jna.Callback {
    fun callback(`callbackData`: Long,`result`: UniffiForeignFutureResultRustBuffer.UniffiByValue,)
}
@Structure.FieldOrder("callStatus")
internal open class UniffiForeignFutureResultVoid(
    @JvmField internal var `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
) : Structure() {
    class UniffiByValue(
        `callStatus`: UniffiRustCallStatus.ByValue = UniffiRustCallStatus.ByValue(),
    ): UniffiForeignFutureResultVoid(`callStatus`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiForeignFutureResultVoid) {
        `callStatus` = other.`callStatus`
    }

}
internal interface UniffiForeignFutureCompleteVoid : com.sun.jna.Callback {
    fun callback(`callbackData`: Long,`result`: UniffiForeignFutureResultVoid.UniffiByValue,)
}
internal interface UniffiCallbackInterfaceSessionEventSinkMethod0 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`event`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceWebappBundleSinkMethod0 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`bundle`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceAppleMusicBackendMethod0 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`inbox`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceAppleMusicBackendMethod1 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceAppleMusicBackendMethod2 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceAppleMusicBackendMethod3 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceAppleMusicBackendMethod4 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceAppleMusicBackendMethod5 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceAppleMusicBackendMethod6 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceAppleMusicBackendMethod7 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`contextUri`: RustBuffer.ByValue,`startAtUri`: RustBuffer.ByValue,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceAppleMusicBackendMethod8 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`uri`: RustBuffer.ByValue,`next`: Byte,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceAppleMusicBackendMethod9 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`cmd`: RustBuffer.ByValue,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceAppleMusicBackendMethod10 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`scope`: RustBuffer.ByValue,`limit`: Int,`offset`: Int,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceAppleMusicBackendMethod11 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceAppleMusicBackendMethod12 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`uri`: RustBuffer.ByValue,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceAppleMusicBackendMethod13 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`query`: RustBuffer.ByValue,`limit`: Int,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceAppleMusicBackendMethod14 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`uris`: RustBuffer.ByValue,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceAppleMusicBackendMethod15 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`uri`: RustBuffer.ByValue,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceAudioBackendMethod0 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`id`: RustBuffer.ByValue,`text`: RustBuffer.ByValue,`voice`: RustBuffer.ByValue,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceAudioBackendMethod1 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`id`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceAudioBackendMethod2 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceAudioBackendMethod3 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`name`: RustBuffer.ByValue,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceVolumeBackendMethod0 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`inbox`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceVolumeBackendMethod1 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceVolumeBackendMethod2 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: RustBuffer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceVolumeBackendMethod3 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`level`: Float,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceVolumeBackendMethod4 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`muted`: Byte,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceVolumeBackendMethod5 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceVolumeBackendMethod6 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceVolumeBackendMethod7 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceConnectivityMonitorMethod0 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`inbox`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceConnectivityMonitorMethod1 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceExtensionHostMethod0 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`inbox`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceExtensionHostMethod1 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceExtensionHostMethod2 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`device`: RustBuffer.ByValue,`webapp`: RustBuffer.ByValue,`message`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceExtensionHostMethod3 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`device`: RustBuffer.ByValue,`name`: RustBuffer.ByValue,`config`: RustBuffer.ByValue,`webapps`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceExtensionHostMethod4 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`device`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceExtensionHostMethod5 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`device`: RustBuffer.ByValue,`webapp`: RustBuffer.ByValue,`active`: Byte,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceExtensionHostMethod6 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`device`: RustBuffer.ByValue,`webapp`: RustBuffer.ByValue,`key`: RustBuffer.ByValue,`value`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceGeoProviderMethod0 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: ByteByReference,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceGeoProviderMethod1 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`inbox`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceGeoProviderMethod2 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceGeoProviderMethod3 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`accuracy`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceGeoProviderMethod4 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceGeoProviderMethod5 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceGeoProviderMethod6 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceGeoProviderMethod7 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceGeoProviderMethod8 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceHostEnvironmentMethod0 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: RustBuffer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceImageScalerMethod0 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`bytes`: RustBuffer.ByValue,`maxEdge`: Int,`quality`: Float,`uniffiOutReturn`: RustBuffer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceLinkTransportMethod0 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: IntByReference,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceLinkTransportMethod1 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`inbox`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceLinkTransportMethod2 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceLinkTransportMethod3 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`deviceId`: RustBuffer.ByValue,`batch`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceLinkTransportMethod4 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`deviceId`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceLinkTransportMethod5 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`deviceId`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceLogSinkMethod0 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`level`: RustBuffer.ByValue,`target`: RustBuffer.ByValue,`message`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceMediaSessionBackendMethod0 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: ByteByReference,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceMediaSessionBackendMethod1 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`inbox`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceMediaSessionBackendMethod2 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceMediaSessionBackendMethod3 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceMediaSessionBackendMethod4 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`package`: RustBuffer.ByValue,`cmd`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceMediaSessionBackendMethod5 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`package`: RustBuffer.ByValue,`token`: RustBuffer.ByValue,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceModelArtifactValidatorMethod0 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`kind`: RustBuffer.ByValue,`path`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceTransferPolicyMethod0 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: ByteByReference,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceHttpTransportMethod0 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`request`: RustBuffer.ByValue,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceHttpTransportMethod1 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`request`: RustBuffer.ByValue,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceWsTransportMethod0 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`connect`: RustBuffer.ByValue,`inbox`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceWsTransportMethod1 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`id`: RustBuffer.ByValue,`frame`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceWsTransportMethod2 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`id`: RustBuffer.ByValue,`code`: RustBuffer.ByValue,`reason`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceNluModelRunnerMethod0 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceNluModelRunnerMethod1 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`inputIds`: RustBuffer.ByValue,`attentionMask`: RustBuffer.ByValue,`uniffiOutReturn`: RustBuffer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceNotificationBackendMethod0 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`inbox`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceNotificationBackendMethod1 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceNotificationBackendMethod2 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`id`: RustBuffer.ByValue,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceNotificationBackendMethod3 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`id`: RustBuffer.ByValue,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfacePhoneBackendMethod0 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`inbox`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfacePhoneBackendMethod1 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfacePhoneBackendMethod2 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`cmd`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfacePhoneBackendMethod3 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceSecretStoreMethod0 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`key`: RustBuffer.ByValue,`uniffiOutReturn`: RustBuffer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceSecretStoreMethod1 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`key`: RustBuffer.ByValue,`value`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceSecretStoreMethod2 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`key`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceSecretStoreMethod3 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`key`: RustBuffer.ByValue,`uniffiOutReturn`: RustBuffer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceSpeechRecognizerMethod0 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceSpeechRecognizerMethod1 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`pcm`: RustBuffer.ByValue,`sampleRateHz`: Int,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
internal interface UniffiCallbackInterfaceDeviceWakerMethod0 : com.sun.jna.Callback {
    fun callback(`uniffiHandle`: Long,`reason`: RustBuffer.ByValue,`allowPlayTap`: Byte,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,)
}
@Structure.FieldOrder("uniffiFree", "uniffiClone", "onEvent")
internal open class UniffiVTableCallbackInterfaceSessionEventSink(
    @JvmField internal var `uniffiFree`: UniffiCallbackInterfaceFree? = null,
    @JvmField internal var `uniffiClone`: UniffiCallbackInterfaceClone? = null,
    @JvmField internal var `onEvent`: UniffiCallbackInterfaceSessionEventSinkMethod0? = null,
) : Structure() {
    class UniffiByValue(
        `uniffiFree`: UniffiCallbackInterfaceFree? = null,
        `uniffiClone`: UniffiCallbackInterfaceClone? = null,
        `onEvent`: UniffiCallbackInterfaceSessionEventSinkMethod0? = null,
    ): UniffiVTableCallbackInterfaceSessionEventSink(`uniffiFree`,`uniffiClone`,`onEvent`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiVTableCallbackInterfaceSessionEventSink) {
        `uniffiFree` = other.`uniffiFree`
        `uniffiClone` = other.`uniffiClone`
        `onEvent` = other.`onEvent`
    }

}
@Structure.FieldOrder("uniffiFree", "uniffiClone", "installed")
internal open class UniffiVTableCallbackInterfaceWebappBundleSink(
    @JvmField internal var `uniffiFree`: UniffiCallbackInterfaceFree? = null,
    @JvmField internal var `uniffiClone`: UniffiCallbackInterfaceClone? = null,
    @JvmField internal var `installed`: UniffiCallbackInterfaceWebappBundleSinkMethod0? = null,
) : Structure() {
    class UniffiByValue(
        `uniffiFree`: UniffiCallbackInterfaceFree? = null,
        `uniffiClone`: UniffiCallbackInterfaceClone? = null,
        `installed`: UniffiCallbackInterfaceWebappBundleSinkMethod0? = null,
    ): UniffiVTableCallbackInterfaceWebappBundleSink(`uniffiFree`,`uniffiClone`,`installed`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiVTableCallbackInterfaceWebappBundleSink) {
        `uniffiFree` = other.`uniffiFree`
        `uniffiClone` = other.`uniffiClone`
        `installed` = other.`installed`
    }

}
@Structure.FieldOrder("uniffiFree", "uniffiClone", "start", "stop", "snapshot", "authStatus", "requestAuthorization", "canPlayCatalogContent", "isOtherAudioPlaying", "playContext", "queueInsert", "command", "library", "recommendations", "resolve", "search", "isFavorite", "addFavorite")
internal open class UniffiVTableCallbackInterfaceAppleMusicBackend(
    @JvmField internal var `uniffiFree`: UniffiCallbackInterfaceFree? = null,
    @JvmField internal var `uniffiClone`: UniffiCallbackInterfaceClone? = null,
    @JvmField internal var `start`: UniffiCallbackInterfaceAppleMusicBackendMethod0? = null,
    @JvmField internal var `stop`: UniffiCallbackInterfaceAppleMusicBackendMethod1? = null,
    @JvmField internal var `snapshot`: UniffiCallbackInterfaceAppleMusicBackendMethod2? = null,
    @JvmField internal var `authStatus`: UniffiCallbackInterfaceAppleMusicBackendMethod3? = null,
    @JvmField internal var `requestAuthorization`: UniffiCallbackInterfaceAppleMusicBackendMethod4? = null,
    @JvmField internal var `canPlayCatalogContent`: UniffiCallbackInterfaceAppleMusicBackendMethod5? = null,
    @JvmField internal var `isOtherAudioPlaying`: UniffiCallbackInterfaceAppleMusicBackendMethod6? = null,
    @JvmField internal var `playContext`: UniffiCallbackInterfaceAppleMusicBackendMethod7? = null,
    @JvmField internal var `queueInsert`: UniffiCallbackInterfaceAppleMusicBackendMethod8? = null,
    @JvmField internal var `command`: UniffiCallbackInterfaceAppleMusicBackendMethod9? = null,
    @JvmField internal var `library`: UniffiCallbackInterfaceAppleMusicBackendMethod10? = null,
    @JvmField internal var `recommendations`: UniffiCallbackInterfaceAppleMusicBackendMethod11? = null,
    @JvmField internal var `resolve`: UniffiCallbackInterfaceAppleMusicBackendMethod12? = null,
    @JvmField internal var `search`: UniffiCallbackInterfaceAppleMusicBackendMethod13? = null,
    @JvmField internal var `isFavorite`: UniffiCallbackInterfaceAppleMusicBackendMethod14? = null,
    @JvmField internal var `addFavorite`: UniffiCallbackInterfaceAppleMusicBackendMethod15? = null,
) : Structure() {
    class UniffiByValue(
        `uniffiFree`: UniffiCallbackInterfaceFree? = null,
        `uniffiClone`: UniffiCallbackInterfaceClone? = null,
        `start`: UniffiCallbackInterfaceAppleMusicBackendMethod0? = null,
        `stop`: UniffiCallbackInterfaceAppleMusicBackendMethod1? = null,
        `snapshot`: UniffiCallbackInterfaceAppleMusicBackendMethod2? = null,
        `authStatus`: UniffiCallbackInterfaceAppleMusicBackendMethod3? = null,
        `requestAuthorization`: UniffiCallbackInterfaceAppleMusicBackendMethod4? = null,
        `canPlayCatalogContent`: UniffiCallbackInterfaceAppleMusicBackendMethod5? = null,
        `isOtherAudioPlaying`: UniffiCallbackInterfaceAppleMusicBackendMethod6? = null,
        `playContext`: UniffiCallbackInterfaceAppleMusicBackendMethod7? = null,
        `queueInsert`: UniffiCallbackInterfaceAppleMusicBackendMethod8? = null,
        `command`: UniffiCallbackInterfaceAppleMusicBackendMethod9? = null,
        `library`: UniffiCallbackInterfaceAppleMusicBackendMethod10? = null,
        `recommendations`: UniffiCallbackInterfaceAppleMusicBackendMethod11? = null,
        `resolve`: UniffiCallbackInterfaceAppleMusicBackendMethod12? = null,
        `search`: UniffiCallbackInterfaceAppleMusicBackendMethod13? = null,
        `isFavorite`: UniffiCallbackInterfaceAppleMusicBackendMethod14? = null,
        `addFavorite`: UniffiCallbackInterfaceAppleMusicBackendMethod15? = null,
    ): UniffiVTableCallbackInterfaceAppleMusicBackend(`uniffiFree`,`uniffiClone`,`start`,`stop`,`snapshot`,`authStatus`,`requestAuthorization`,`canPlayCatalogContent`,`isOtherAudioPlaying`,`playContext`,`queueInsert`,`command`,`library`,`recommendations`,`resolve`,`search`,`isFavorite`,`addFavorite`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiVTableCallbackInterfaceAppleMusicBackend) {
        `uniffiFree` = other.`uniffiFree`
        `uniffiClone` = other.`uniffiClone`
        `start` = other.`start`
        `stop` = other.`stop`
        `snapshot` = other.`snapshot`
        `authStatus` = other.`authStatus`
        `requestAuthorization` = other.`requestAuthorization`
        `canPlayCatalogContent` = other.`canPlayCatalogContent`
        `isOtherAudioPlaying` = other.`isOtherAudioPlaying`
        `playContext` = other.`playContext`
        `queueInsert` = other.`queueInsert`
        `command` = other.`command`
        `library` = other.`library`
        `recommendations` = other.`recommendations`
        `resolve` = other.`resolve`
        `search` = other.`search`
        `isFavorite` = other.`isFavorite`
        `addFavorite` = other.`addFavorite`
    }

}
@Structure.FieldOrder("uniffiFree", "uniffiClone", "speak", "cancel", "cancelAll", "playEarcon")
internal open class UniffiVTableCallbackInterfaceAudioBackend(
    @JvmField internal var `uniffiFree`: UniffiCallbackInterfaceFree? = null,
    @JvmField internal var `uniffiClone`: UniffiCallbackInterfaceClone? = null,
    @JvmField internal var `speak`: UniffiCallbackInterfaceAudioBackendMethod0? = null,
    @JvmField internal var `cancel`: UniffiCallbackInterfaceAudioBackendMethod1? = null,
    @JvmField internal var `cancelAll`: UniffiCallbackInterfaceAudioBackendMethod2? = null,
    @JvmField internal var `playEarcon`: UniffiCallbackInterfaceAudioBackendMethod3? = null,
) : Structure() {
    class UniffiByValue(
        `uniffiFree`: UniffiCallbackInterfaceFree? = null,
        `uniffiClone`: UniffiCallbackInterfaceClone? = null,
        `speak`: UniffiCallbackInterfaceAudioBackendMethod0? = null,
        `cancel`: UniffiCallbackInterfaceAudioBackendMethod1? = null,
        `cancelAll`: UniffiCallbackInterfaceAudioBackendMethod2? = null,
        `playEarcon`: UniffiCallbackInterfaceAudioBackendMethod3? = null,
    ): UniffiVTableCallbackInterfaceAudioBackend(`uniffiFree`,`uniffiClone`,`speak`,`cancel`,`cancelAll`,`playEarcon`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiVTableCallbackInterfaceAudioBackend) {
        `uniffiFree` = other.`uniffiFree`
        `uniffiClone` = other.`uniffiClone`
        `speak` = other.`speak`
        `cancel` = other.`cancel`
        `cancelAll` = other.`cancelAll`
        `playEarcon` = other.`playEarcon`
    }

}
@Structure.FieldOrder("uniffiFree", "uniffiClone", "start", "stop", "snapshot", "setVolume", "setMute", "volumeUp", "volumeDown", "muteToggle")
internal open class UniffiVTableCallbackInterfaceVolumeBackend(
    @JvmField internal var `uniffiFree`: UniffiCallbackInterfaceFree? = null,
    @JvmField internal var `uniffiClone`: UniffiCallbackInterfaceClone? = null,
    @JvmField internal var `start`: UniffiCallbackInterfaceVolumeBackendMethod0? = null,
    @JvmField internal var `stop`: UniffiCallbackInterfaceVolumeBackendMethod1? = null,
    @JvmField internal var `snapshot`: UniffiCallbackInterfaceVolumeBackendMethod2? = null,
    @JvmField internal var `setVolume`: UniffiCallbackInterfaceVolumeBackendMethod3? = null,
    @JvmField internal var `setMute`: UniffiCallbackInterfaceVolumeBackendMethod4? = null,
    @JvmField internal var `volumeUp`: UniffiCallbackInterfaceVolumeBackendMethod5? = null,
    @JvmField internal var `volumeDown`: UniffiCallbackInterfaceVolumeBackendMethod6? = null,
    @JvmField internal var `muteToggle`: UniffiCallbackInterfaceVolumeBackendMethod7? = null,
) : Structure() {
    class UniffiByValue(
        `uniffiFree`: UniffiCallbackInterfaceFree? = null,
        `uniffiClone`: UniffiCallbackInterfaceClone? = null,
        `start`: UniffiCallbackInterfaceVolumeBackendMethod0? = null,
        `stop`: UniffiCallbackInterfaceVolumeBackendMethod1? = null,
        `snapshot`: UniffiCallbackInterfaceVolumeBackendMethod2? = null,
        `setVolume`: UniffiCallbackInterfaceVolumeBackendMethod3? = null,
        `setMute`: UniffiCallbackInterfaceVolumeBackendMethod4? = null,
        `volumeUp`: UniffiCallbackInterfaceVolumeBackendMethod5? = null,
        `volumeDown`: UniffiCallbackInterfaceVolumeBackendMethod6? = null,
        `muteToggle`: UniffiCallbackInterfaceVolumeBackendMethod7? = null,
    ): UniffiVTableCallbackInterfaceVolumeBackend(`uniffiFree`,`uniffiClone`,`start`,`stop`,`snapshot`,`setVolume`,`setMute`,`volumeUp`,`volumeDown`,`muteToggle`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiVTableCallbackInterfaceVolumeBackend) {
        `uniffiFree` = other.`uniffiFree`
        `uniffiClone` = other.`uniffiClone`
        `start` = other.`start`
        `stop` = other.`stop`
        `snapshot` = other.`snapshot`
        `setVolume` = other.`setVolume`
        `setMute` = other.`setMute`
        `volumeUp` = other.`volumeUp`
        `volumeDown` = other.`volumeDown`
        `muteToggle` = other.`muteToggle`
    }

}
@Structure.FieldOrder("uniffiFree", "uniffiClone", "start", "stop")
internal open class UniffiVTableCallbackInterfaceConnectivityMonitor(
    @JvmField internal var `uniffiFree`: UniffiCallbackInterfaceFree? = null,
    @JvmField internal var `uniffiClone`: UniffiCallbackInterfaceClone? = null,
    @JvmField internal var `start`: UniffiCallbackInterfaceConnectivityMonitorMethod0? = null,
    @JvmField internal var `stop`: UniffiCallbackInterfaceConnectivityMonitorMethod1? = null,
) : Structure() {
    class UniffiByValue(
        `uniffiFree`: UniffiCallbackInterfaceFree? = null,
        `uniffiClone`: UniffiCallbackInterfaceClone? = null,
        `start`: UniffiCallbackInterfaceConnectivityMonitorMethod0? = null,
        `stop`: UniffiCallbackInterfaceConnectivityMonitorMethod1? = null,
    ): UniffiVTableCallbackInterfaceConnectivityMonitor(`uniffiFree`,`uniffiClone`,`start`,`stop`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiVTableCallbackInterfaceConnectivityMonitor) {
        `uniffiFree` = other.`uniffiFree`
        `uniffiClone` = other.`uniffiClone`
        `start` = other.`start`
        `stop` = other.`stop`
    }

}
@Structure.FieldOrder("uniffiFree", "uniffiClone", "start", "stop", "deliver", "deviceConnected", "deviceDisconnected", "deviceActive", "configChanged")
internal open class UniffiVTableCallbackInterfaceExtensionHost(
    @JvmField internal var `uniffiFree`: UniffiCallbackInterfaceFree? = null,
    @JvmField internal var `uniffiClone`: UniffiCallbackInterfaceClone? = null,
    @JvmField internal var `start`: UniffiCallbackInterfaceExtensionHostMethod0? = null,
    @JvmField internal var `stop`: UniffiCallbackInterfaceExtensionHostMethod1? = null,
    @JvmField internal var `deliver`: UniffiCallbackInterfaceExtensionHostMethod2? = null,
    @JvmField internal var `deviceConnected`: UniffiCallbackInterfaceExtensionHostMethod3? = null,
    @JvmField internal var `deviceDisconnected`: UniffiCallbackInterfaceExtensionHostMethod4? = null,
    @JvmField internal var `deviceActive`: UniffiCallbackInterfaceExtensionHostMethod5? = null,
    @JvmField internal var `configChanged`: UniffiCallbackInterfaceExtensionHostMethod6? = null,
) : Structure() {
    class UniffiByValue(
        `uniffiFree`: UniffiCallbackInterfaceFree? = null,
        `uniffiClone`: UniffiCallbackInterfaceClone? = null,
        `start`: UniffiCallbackInterfaceExtensionHostMethod0? = null,
        `stop`: UniffiCallbackInterfaceExtensionHostMethod1? = null,
        `deliver`: UniffiCallbackInterfaceExtensionHostMethod2? = null,
        `deviceConnected`: UniffiCallbackInterfaceExtensionHostMethod3? = null,
        `deviceDisconnected`: UniffiCallbackInterfaceExtensionHostMethod4? = null,
        `deviceActive`: UniffiCallbackInterfaceExtensionHostMethod5? = null,
        `configChanged`: UniffiCallbackInterfaceExtensionHostMethod6? = null,
    ): UniffiVTableCallbackInterfaceExtensionHost(`uniffiFree`,`uniffiClone`,`start`,`stop`,`deliver`,`deviceConnected`,`deviceDisconnected`,`deviceActive`,`configChanged`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiVTableCallbackInterfaceExtensionHost) {
        `uniffiFree` = other.`uniffiFree`
        `uniffiClone` = other.`uniffiClone`
        `start` = other.`start`
        `stop` = other.`stop`
        `deliver` = other.`deliver`
        `deviceConnected` = other.`deviceConnected`
        `deviceDisconnected` = other.`deviceDisconnected`
        `deviceActive` = other.`deviceActive`
        `configChanged` = other.`configChanged`
    }

}
@Structure.FieldOrder("uniffiFree", "uniffiClone", "canProvideLocation", "start", "stop", "configure", "requestAuthorization", "startUpdating", "stopUpdating", "requestOnce", "cancelOnce")
internal open class UniffiVTableCallbackInterfaceGeoProvider(
    @JvmField internal var `uniffiFree`: UniffiCallbackInterfaceFree? = null,
    @JvmField internal var `uniffiClone`: UniffiCallbackInterfaceClone? = null,
    @JvmField internal var `canProvideLocation`: UniffiCallbackInterfaceGeoProviderMethod0? = null,
    @JvmField internal var `start`: UniffiCallbackInterfaceGeoProviderMethod1? = null,
    @JvmField internal var `stop`: UniffiCallbackInterfaceGeoProviderMethod2? = null,
    @JvmField internal var `configure`: UniffiCallbackInterfaceGeoProviderMethod3? = null,
    @JvmField internal var `requestAuthorization`: UniffiCallbackInterfaceGeoProviderMethod4? = null,
    @JvmField internal var `startUpdating`: UniffiCallbackInterfaceGeoProviderMethod5? = null,
    @JvmField internal var `stopUpdating`: UniffiCallbackInterfaceGeoProviderMethod6? = null,
    @JvmField internal var `requestOnce`: UniffiCallbackInterfaceGeoProviderMethod7? = null,
    @JvmField internal var `cancelOnce`: UniffiCallbackInterfaceGeoProviderMethod8? = null,
) : Structure() {
    class UniffiByValue(
        `uniffiFree`: UniffiCallbackInterfaceFree? = null,
        `uniffiClone`: UniffiCallbackInterfaceClone? = null,
        `canProvideLocation`: UniffiCallbackInterfaceGeoProviderMethod0? = null,
        `start`: UniffiCallbackInterfaceGeoProviderMethod1? = null,
        `stop`: UniffiCallbackInterfaceGeoProviderMethod2? = null,
        `configure`: UniffiCallbackInterfaceGeoProviderMethod3? = null,
        `requestAuthorization`: UniffiCallbackInterfaceGeoProviderMethod4? = null,
        `startUpdating`: UniffiCallbackInterfaceGeoProviderMethod5? = null,
        `stopUpdating`: UniffiCallbackInterfaceGeoProviderMethod6? = null,
        `requestOnce`: UniffiCallbackInterfaceGeoProviderMethod7? = null,
        `cancelOnce`: UniffiCallbackInterfaceGeoProviderMethod8? = null,
    ): UniffiVTableCallbackInterfaceGeoProvider(`uniffiFree`,`uniffiClone`,`canProvideLocation`,`start`,`stop`,`configure`,`requestAuthorization`,`startUpdating`,`stopUpdating`,`requestOnce`,`cancelOnce`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiVTableCallbackInterfaceGeoProvider) {
        `uniffiFree` = other.`uniffiFree`
        `uniffiClone` = other.`uniffiClone`
        `canProvideLocation` = other.`canProvideLocation`
        `start` = other.`start`
        `stop` = other.`stop`
        `configure` = other.`configure`
        `requestAuthorization` = other.`requestAuthorization`
        `startUpdating` = other.`startUpdating`
        `stopUpdating` = other.`stopUpdating`
        `requestOnce` = other.`requestOnce`
        `cancelOnce` = other.`cancelOnce`
    }

}
@Structure.FieldOrder("uniffiFree", "uniffiClone", "clock")
internal open class UniffiVTableCallbackInterfaceHostEnvironment(
    @JvmField internal var `uniffiFree`: UniffiCallbackInterfaceFree? = null,
    @JvmField internal var `uniffiClone`: UniffiCallbackInterfaceClone? = null,
    @JvmField internal var `clock`: UniffiCallbackInterfaceHostEnvironmentMethod0? = null,
) : Structure() {
    class UniffiByValue(
        `uniffiFree`: UniffiCallbackInterfaceFree? = null,
        `uniffiClone`: UniffiCallbackInterfaceClone? = null,
        `clock`: UniffiCallbackInterfaceHostEnvironmentMethod0? = null,
    ): UniffiVTableCallbackInterfaceHostEnvironment(`uniffiFree`,`uniffiClone`,`clock`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiVTableCallbackInterfaceHostEnvironment) {
        `uniffiFree` = other.`uniffiFree`
        `uniffiClone` = other.`uniffiClone`
        `clock` = other.`clock`
    }

}
@Structure.FieldOrder("uniffiFree", "uniffiClone", "downsampleJpeg")
internal open class UniffiVTableCallbackInterfaceImageScaler(
    @JvmField internal var `uniffiFree`: UniffiCallbackInterfaceFree? = null,
    @JvmField internal var `uniffiClone`: UniffiCallbackInterfaceClone? = null,
    @JvmField internal var `downsampleJpeg`: UniffiCallbackInterfaceImageScalerMethod0? = null,
) : Structure() {
    class UniffiByValue(
        `uniffiFree`: UniffiCallbackInterfaceFree? = null,
        `uniffiClone`: UniffiCallbackInterfaceClone? = null,
        `downsampleJpeg`: UniffiCallbackInterfaceImageScalerMethod0? = null,
    ): UniffiVTableCallbackInterfaceImageScaler(`uniffiFree`,`uniffiClone`,`downsampleJpeg`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiVTableCallbackInterfaceImageScaler) {
        `uniffiFree` = other.`uniffiFree`
        `uniffiClone` = other.`uniffiClone`
        `downsampleJpeg` = other.`downsampleJpeg`
    }

}
@Structure.FieldOrder("uniffiFree", "uniffiClone", "maxBatchBytes", "start", "stop", "send", "disconnect", "reconnect")
internal open class UniffiVTableCallbackInterfaceLinkTransport(
    @JvmField internal var `uniffiFree`: UniffiCallbackInterfaceFree? = null,
    @JvmField internal var `uniffiClone`: UniffiCallbackInterfaceClone? = null,
    @JvmField internal var `maxBatchBytes`: UniffiCallbackInterfaceLinkTransportMethod0? = null,
    @JvmField internal var `start`: UniffiCallbackInterfaceLinkTransportMethod1? = null,
    @JvmField internal var `stop`: UniffiCallbackInterfaceLinkTransportMethod2? = null,
    @JvmField internal var `send`: UniffiCallbackInterfaceLinkTransportMethod3? = null,
    @JvmField internal var `disconnect`: UniffiCallbackInterfaceLinkTransportMethod4? = null,
    @JvmField internal var `reconnect`: UniffiCallbackInterfaceLinkTransportMethod5? = null,
) : Structure() {
    class UniffiByValue(
        `uniffiFree`: UniffiCallbackInterfaceFree? = null,
        `uniffiClone`: UniffiCallbackInterfaceClone? = null,
        `maxBatchBytes`: UniffiCallbackInterfaceLinkTransportMethod0? = null,
        `start`: UniffiCallbackInterfaceLinkTransportMethod1? = null,
        `stop`: UniffiCallbackInterfaceLinkTransportMethod2? = null,
        `send`: UniffiCallbackInterfaceLinkTransportMethod3? = null,
        `disconnect`: UniffiCallbackInterfaceLinkTransportMethod4? = null,
        `reconnect`: UniffiCallbackInterfaceLinkTransportMethod5? = null,
    ): UniffiVTableCallbackInterfaceLinkTransport(`uniffiFree`,`uniffiClone`,`maxBatchBytes`,`start`,`stop`,`send`,`disconnect`,`reconnect`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiVTableCallbackInterfaceLinkTransport) {
        `uniffiFree` = other.`uniffiFree`
        `uniffiClone` = other.`uniffiClone`
        `maxBatchBytes` = other.`maxBatchBytes`
        `start` = other.`start`
        `stop` = other.`stop`
        `send` = other.`send`
        `disconnect` = other.`disconnect`
        `reconnect` = other.`reconnect`
    }

}
@Structure.FieldOrder("uniffiFree", "uniffiClone", "onLine")
internal open class UniffiVTableCallbackInterfaceLogSink(
    @JvmField internal var `uniffiFree`: UniffiCallbackInterfaceFree? = null,
    @JvmField internal var `uniffiClone`: UniffiCallbackInterfaceClone? = null,
    @JvmField internal var `onLine`: UniffiCallbackInterfaceLogSinkMethod0? = null,
) : Structure() {
    class UniffiByValue(
        `uniffiFree`: UniffiCallbackInterfaceFree? = null,
        `uniffiClone`: UniffiCallbackInterfaceClone? = null,
        `onLine`: UniffiCallbackInterfaceLogSinkMethod0? = null,
    ): UniffiVTableCallbackInterfaceLogSink(`uniffiFree`,`uniffiClone`,`onLine`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiVTableCallbackInterfaceLogSink) {
        `uniffiFree` = other.`uniffiFree`
        `uniffiClone` = other.`uniffiClone`
        `onLine` = other.`onLine`
    }

}
@Structure.FieldOrder("uniffiFree", "uniffiClone", "isAccessGranted", "start", "stop", "snapshotAll", "control", "art")
internal open class UniffiVTableCallbackInterfaceMediaSessionBackend(
    @JvmField internal var `uniffiFree`: UniffiCallbackInterfaceFree? = null,
    @JvmField internal var `uniffiClone`: UniffiCallbackInterfaceClone? = null,
    @JvmField internal var `isAccessGranted`: UniffiCallbackInterfaceMediaSessionBackendMethod0? = null,
    @JvmField internal var `start`: UniffiCallbackInterfaceMediaSessionBackendMethod1? = null,
    @JvmField internal var `stop`: UniffiCallbackInterfaceMediaSessionBackendMethod2? = null,
    @JvmField internal var `snapshotAll`: UniffiCallbackInterfaceMediaSessionBackendMethod3? = null,
    @JvmField internal var `control`: UniffiCallbackInterfaceMediaSessionBackendMethod4? = null,
    @JvmField internal var `art`: UniffiCallbackInterfaceMediaSessionBackendMethod5? = null,
) : Structure() {
    class UniffiByValue(
        `uniffiFree`: UniffiCallbackInterfaceFree? = null,
        `uniffiClone`: UniffiCallbackInterfaceClone? = null,
        `isAccessGranted`: UniffiCallbackInterfaceMediaSessionBackendMethod0? = null,
        `start`: UniffiCallbackInterfaceMediaSessionBackendMethod1? = null,
        `stop`: UniffiCallbackInterfaceMediaSessionBackendMethod2? = null,
        `snapshotAll`: UniffiCallbackInterfaceMediaSessionBackendMethod3? = null,
        `control`: UniffiCallbackInterfaceMediaSessionBackendMethod4? = null,
        `art`: UniffiCallbackInterfaceMediaSessionBackendMethod5? = null,
    ): UniffiVTableCallbackInterfaceMediaSessionBackend(`uniffiFree`,`uniffiClone`,`isAccessGranted`,`start`,`stop`,`snapshotAll`,`control`,`art`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiVTableCallbackInterfaceMediaSessionBackend) {
        `uniffiFree` = other.`uniffiFree`
        `uniffiClone` = other.`uniffiClone`
        `isAccessGranted` = other.`isAccessGranted`
        `start` = other.`start`
        `stop` = other.`stop`
        `snapshotAll` = other.`snapshotAll`
        `control` = other.`control`
        `art` = other.`art`
    }

}
@Structure.FieldOrder("uniffiFree", "uniffiClone", "validate")
internal open class UniffiVTableCallbackInterfaceModelArtifactValidator(
    @JvmField internal var `uniffiFree`: UniffiCallbackInterfaceFree? = null,
    @JvmField internal var `uniffiClone`: UniffiCallbackInterfaceClone? = null,
    @JvmField internal var `validate`: UniffiCallbackInterfaceModelArtifactValidatorMethod0? = null,
) : Structure() {
    class UniffiByValue(
        `uniffiFree`: UniffiCallbackInterfaceFree? = null,
        `uniffiClone`: UniffiCallbackInterfaceClone? = null,
        `validate`: UniffiCallbackInterfaceModelArtifactValidatorMethod0? = null,
    ): UniffiVTableCallbackInterfaceModelArtifactValidator(`uniffiFree`,`uniffiClone`,`validate`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiVTableCallbackInterfaceModelArtifactValidator) {
        `uniffiFree` = other.`uniffiFree`
        `uniffiClone` = other.`uniffiClone`
        `validate` = other.`validate`
    }

}
@Structure.FieldOrder("uniffiFree", "uniffiClone", "allowsLargeTransfer")
internal open class UniffiVTableCallbackInterfaceTransferPolicy(
    @JvmField internal var `uniffiFree`: UniffiCallbackInterfaceFree? = null,
    @JvmField internal var `uniffiClone`: UniffiCallbackInterfaceClone? = null,
    @JvmField internal var `allowsLargeTransfer`: UniffiCallbackInterfaceTransferPolicyMethod0? = null,
) : Structure() {
    class UniffiByValue(
        `uniffiFree`: UniffiCallbackInterfaceFree? = null,
        `uniffiClone`: UniffiCallbackInterfaceClone? = null,
        `allowsLargeTransfer`: UniffiCallbackInterfaceTransferPolicyMethod0? = null,
    ): UniffiVTableCallbackInterfaceTransferPolicy(`uniffiFree`,`uniffiClone`,`allowsLargeTransfer`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiVTableCallbackInterfaceTransferPolicy) {
        `uniffiFree` = other.`uniffiFree`
        `uniffiClone` = other.`uniffiClone`
        `allowsLargeTransfer` = other.`allowsLargeTransfer`
    }

}
@Structure.FieldOrder("uniffiFree", "uniffiClone", "execute", "download")
internal open class UniffiVTableCallbackInterfaceHttpTransport(
    @JvmField internal var `uniffiFree`: UniffiCallbackInterfaceFree? = null,
    @JvmField internal var `uniffiClone`: UniffiCallbackInterfaceClone? = null,
    @JvmField internal var `execute`: UniffiCallbackInterfaceHttpTransportMethod0? = null,
    @JvmField internal var `download`: UniffiCallbackInterfaceHttpTransportMethod1? = null,
) : Structure() {
    class UniffiByValue(
        `uniffiFree`: UniffiCallbackInterfaceFree? = null,
        `uniffiClone`: UniffiCallbackInterfaceClone? = null,
        `execute`: UniffiCallbackInterfaceHttpTransportMethod0? = null,
        `download`: UniffiCallbackInterfaceHttpTransportMethod1? = null,
    ): UniffiVTableCallbackInterfaceHttpTransport(`uniffiFree`,`uniffiClone`,`execute`,`download`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiVTableCallbackInterfaceHttpTransport) {
        `uniffiFree` = other.`uniffiFree`
        `uniffiClone` = other.`uniffiClone`
        `execute` = other.`execute`
        `download` = other.`download`
    }

}
@Structure.FieldOrder("uniffiFree", "uniffiClone", "connect", "send", "disconnect")
internal open class UniffiVTableCallbackInterfaceWsTransport(
    @JvmField internal var `uniffiFree`: UniffiCallbackInterfaceFree? = null,
    @JvmField internal var `uniffiClone`: UniffiCallbackInterfaceClone? = null,
    @JvmField internal var `connect`: UniffiCallbackInterfaceWsTransportMethod0? = null,
    @JvmField internal var `send`: UniffiCallbackInterfaceWsTransportMethod1? = null,
    @JvmField internal var `disconnect`: UniffiCallbackInterfaceWsTransportMethod2? = null,
) : Structure() {
    class UniffiByValue(
        `uniffiFree`: UniffiCallbackInterfaceFree? = null,
        `uniffiClone`: UniffiCallbackInterfaceClone? = null,
        `connect`: UniffiCallbackInterfaceWsTransportMethod0? = null,
        `send`: UniffiCallbackInterfaceWsTransportMethod1? = null,
        `disconnect`: UniffiCallbackInterfaceWsTransportMethod2? = null,
    ): UniffiVTableCallbackInterfaceWsTransport(`uniffiFree`,`uniffiClone`,`connect`,`send`,`disconnect`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiVTableCallbackInterfaceWsTransport) {
        `uniffiFree` = other.`uniffiFree`
        `uniffiClone` = other.`uniffiClone`
        `connect` = other.`connect`
        `send` = other.`send`
        `disconnect` = other.`disconnect`
    }

}
@Structure.FieldOrder("uniffiFree", "uniffiClone", "prewarm", "predict")
internal open class UniffiVTableCallbackInterfaceNluModelRunner(
    @JvmField internal var `uniffiFree`: UniffiCallbackInterfaceFree? = null,
    @JvmField internal var `uniffiClone`: UniffiCallbackInterfaceClone? = null,
    @JvmField internal var `prewarm`: UniffiCallbackInterfaceNluModelRunnerMethod0? = null,
    @JvmField internal var `predict`: UniffiCallbackInterfaceNluModelRunnerMethod1? = null,
) : Structure() {
    class UniffiByValue(
        `uniffiFree`: UniffiCallbackInterfaceFree? = null,
        `uniffiClone`: UniffiCallbackInterfaceClone? = null,
        `prewarm`: UniffiCallbackInterfaceNluModelRunnerMethod0? = null,
        `predict`: UniffiCallbackInterfaceNluModelRunnerMethod1? = null,
    ): UniffiVTableCallbackInterfaceNluModelRunner(`uniffiFree`,`uniffiClone`,`prewarm`,`predict`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiVTableCallbackInterfaceNluModelRunner) {
        `uniffiFree` = other.`uniffiFree`
        `uniffiClone` = other.`uniffiClone`
        `prewarm` = other.`prewarm`
        `predict` = other.`predict`
    }

}
@Structure.FieldOrder("uniffiFree", "uniffiClone", "start", "stop", "invokePositive", "invokeNegative")
internal open class UniffiVTableCallbackInterfaceNotificationBackend(
    @JvmField internal var `uniffiFree`: UniffiCallbackInterfaceFree? = null,
    @JvmField internal var `uniffiClone`: UniffiCallbackInterfaceClone? = null,
    @JvmField internal var `start`: UniffiCallbackInterfaceNotificationBackendMethod0? = null,
    @JvmField internal var `stop`: UniffiCallbackInterfaceNotificationBackendMethod1? = null,
    @JvmField internal var `invokePositive`: UniffiCallbackInterfaceNotificationBackendMethod2? = null,
    @JvmField internal var `invokeNegative`: UniffiCallbackInterfaceNotificationBackendMethod3? = null,
) : Structure() {
    class UniffiByValue(
        `uniffiFree`: UniffiCallbackInterfaceFree? = null,
        `uniffiClone`: UniffiCallbackInterfaceClone? = null,
        `start`: UniffiCallbackInterfaceNotificationBackendMethod0? = null,
        `stop`: UniffiCallbackInterfaceNotificationBackendMethod1? = null,
        `invokePositive`: UniffiCallbackInterfaceNotificationBackendMethod2? = null,
        `invokeNegative`: UniffiCallbackInterfaceNotificationBackendMethod3? = null,
    ): UniffiVTableCallbackInterfaceNotificationBackend(`uniffiFree`,`uniffiClone`,`start`,`stop`,`invokePositive`,`invokeNegative`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiVTableCallbackInterfaceNotificationBackend) {
        `uniffiFree` = other.`uniffiFree`
        `uniffiClone` = other.`uniffiClone`
        `start` = other.`start`
        `stop` = other.`stop`
        `invokePositive` = other.`invokePositive`
        `invokeNegative` = other.`invokeNegative`
    }

}
@Structure.FieldOrder("uniffiFree", "uniffiClone", "start", "stop", "command", "stateGet")
internal open class UniffiVTableCallbackInterfacePhoneBackend(
    @JvmField internal var `uniffiFree`: UniffiCallbackInterfaceFree? = null,
    @JvmField internal var `uniffiClone`: UniffiCallbackInterfaceClone? = null,
    @JvmField internal var `start`: UniffiCallbackInterfacePhoneBackendMethod0? = null,
    @JvmField internal var `stop`: UniffiCallbackInterfacePhoneBackendMethod1? = null,
    @JvmField internal var `command`: UniffiCallbackInterfacePhoneBackendMethod2? = null,
    @JvmField internal var `stateGet`: UniffiCallbackInterfacePhoneBackendMethod3? = null,
) : Structure() {
    class UniffiByValue(
        `uniffiFree`: UniffiCallbackInterfaceFree? = null,
        `uniffiClone`: UniffiCallbackInterfaceClone? = null,
        `start`: UniffiCallbackInterfacePhoneBackendMethod0? = null,
        `stop`: UniffiCallbackInterfacePhoneBackendMethod1? = null,
        `command`: UniffiCallbackInterfacePhoneBackendMethod2? = null,
        `stateGet`: UniffiCallbackInterfacePhoneBackendMethod3? = null,
    ): UniffiVTableCallbackInterfacePhoneBackend(`uniffiFree`,`uniffiClone`,`start`,`stop`,`command`,`stateGet`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiVTableCallbackInterfacePhoneBackend) {
        `uniffiFree` = other.`uniffiFree`
        `uniffiClone` = other.`uniffiClone`
        `start` = other.`start`
        `stop` = other.`stop`
        `command` = other.`command`
        `stateGet` = other.`stateGet`
    }

}
@Structure.FieldOrder("uniffiFree", "uniffiClone", "get", "set", "remove", "getBlob")
internal open class UniffiVTableCallbackInterfaceSecretStore(
    @JvmField internal var `uniffiFree`: UniffiCallbackInterfaceFree? = null,
    @JvmField internal var `uniffiClone`: UniffiCallbackInterfaceClone? = null,
    @JvmField internal var `get`: UniffiCallbackInterfaceSecretStoreMethod0? = null,
    @JvmField internal var `set`: UniffiCallbackInterfaceSecretStoreMethod1? = null,
    @JvmField internal var `remove`: UniffiCallbackInterfaceSecretStoreMethod2? = null,
    @JvmField internal var `getBlob`: UniffiCallbackInterfaceSecretStoreMethod3? = null,
) : Structure() {
    class UniffiByValue(
        `uniffiFree`: UniffiCallbackInterfaceFree? = null,
        `uniffiClone`: UniffiCallbackInterfaceClone? = null,
        `get`: UniffiCallbackInterfaceSecretStoreMethod0? = null,
        `set`: UniffiCallbackInterfaceSecretStoreMethod1? = null,
        `remove`: UniffiCallbackInterfaceSecretStoreMethod2? = null,
        `getBlob`: UniffiCallbackInterfaceSecretStoreMethod3? = null,
    ): UniffiVTableCallbackInterfaceSecretStore(`uniffiFree`,`uniffiClone`,`get`,`set`,`remove`,`getBlob`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiVTableCallbackInterfaceSecretStore) {
        `uniffiFree` = other.`uniffiFree`
        `uniffiClone` = other.`uniffiClone`
        `get` = other.`get`
        `set` = other.`set`
        `remove` = other.`remove`
        `getBlob` = other.`getBlob`
    }

}
@Structure.FieldOrder("uniffiFree", "uniffiClone", "prepare", "transcribe")
internal open class UniffiVTableCallbackInterfaceSpeechRecognizer(
    @JvmField internal var `uniffiFree`: UniffiCallbackInterfaceFree? = null,
    @JvmField internal var `uniffiClone`: UniffiCallbackInterfaceClone? = null,
    @JvmField internal var `prepare`: UniffiCallbackInterfaceSpeechRecognizerMethod0? = null,
    @JvmField internal var `transcribe`: UniffiCallbackInterfaceSpeechRecognizerMethod1? = null,
) : Structure() {
    class UniffiByValue(
        `uniffiFree`: UniffiCallbackInterfaceFree? = null,
        `uniffiClone`: UniffiCallbackInterfaceClone? = null,
        `prepare`: UniffiCallbackInterfaceSpeechRecognizerMethod0? = null,
        `transcribe`: UniffiCallbackInterfaceSpeechRecognizerMethod1? = null,
    ): UniffiVTableCallbackInterfaceSpeechRecognizer(`uniffiFree`,`uniffiClone`,`prepare`,`transcribe`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiVTableCallbackInterfaceSpeechRecognizer) {
        `uniffiFree` = other.`uniffiFree`
        `uniffiClone` = other.`uniffiClone`
        `prepare` = other.`prepare`
        `transcribe` = other.`transcribe`
    }

}
@Structure.FieldOrder("uniffiFree", "uniffiClone", "wakeDevice")
internal open class UniffiVTableCallbackInterfaceDeviceWaker(
    @JvmField internal var `uniffiFree`: UniffiCallbackInterfaceFree? = null,
    @JvmField internal var `uniffiClone`: UniffiCallbackInterfaceClone? = null,
    @JvmField internal var `wakeDevice`: UniffiCallbackInterfaceDeviceWakerMethod0? = null,
) : Structure() {
    class UniffiByValue(
        `uniffiFree`: UniffiCallbackInterfaceFree? = null,
        `uniffiClone`: UniffiCallbackInterfaceClone? = null,
        `wakeDevice`: UniffiCallbackInterfaceDeviceWakerMethod0? = null,
    ): UniffiVTableCallbackInterfaceDeviceWaker(`uniffiFree`,`uniffiClone`,`wakeDevice`,), Structure.ByValue

   internal fun uniffiSetValue(other: UniffiVTableCallbackInterfaceDeviceWaker) {
        `uniffiFree` = other.`uniffiFree`
        `uniffiClone` = other.`uniffiClone`
        `wakeDevice` = other.`wakeDevice`
    }

}

// A JNA Library to expose the extern-C FFI definitions.
// This is an implementation detail which will be called internally by the public API.

// For large crates we prevent `MethodTooLargeException` (see #2340)
// N.B. the name of the extension is very misleading, since it is
// rather `InterfaceTooLargeException`, caused by too many methods
// in the interface for large crates.
//
// By splitting the otherwise huge interface into two parts
// * UniffiLib (this)
// * IntegrityCheckingUniffiLib
// And all checksum methods are put into `IntegrityCheckingUniffiLib`
// we allow for ~2x as many methods in the UniffiLib interface.
//
// Note: above all written when we used JNA's `loadIndirect` etc.
// We now use JNA's "direct mapping" - unclear if same considerations apply exactly.
internal object IntegrityCheckingUniffiLib {
    init {
        Native.register(IntegrityCheckingUniffiLib::class.java, findLibraryName(componentName = "bridgething_companion"))
        uniffiCheckContractApiVersion(this)
        uniffiCheckApiChecksums(this)
    }
    external fun uniffi_bridgething_companion_checksum_func_parse_lrc(
    ): Int
    external fun uniffi_bridgething_companion_checksum_func_nlu_fast_path_match(
    ): Int
    external fun uniffi_bridgething_companion_checksum_func_nlu_intent_catalog(
    ): Int
    external fun uniffi_bridgething_companion_checksum_func_nlu_rejection_evaluate(
    ): Int
    external fun uniffi_bridgething_companion_checksum_func_ota_artifact_urls(
    ): Int
    external fun uniffi_bridgething_companion_checksum_func_ota_builtin_webapp_url(
    ): Int
    external fun uniffi_bridgething_companion_checksum_func_ota_cancelled_reason(
    ): Int
    external fun uniffi_bridgething_companion_checksum_func_ota_composite_version_string(
    ): Int
    external fun uniffi_bridgething_companion_checksum_func_ota_daemon_patch_url(
    ): Int
    external fun uniffi_bridgething_companion_checksum_func_ota_patch_source_matches(
    ): Int
    external fun uniffi_bridgething_companion_checksum_func_ota_run_progress(
    ): Int
    external fun uniffi_bridgething_companion_checksum_func_parse_ota_composite_version(
    ): Int
    external fun uniffi_bridgething_companion_checksum_func_parse_ota_discover_manifest(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_apply_ota_update(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_available_providers(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_cancel_auth(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_check_for_ota_update(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_collect_screenshot(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_companion_debug(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_complete_provider_auth(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_connect_network(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_connect_provider(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_current_webapp(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_default_resume_target(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_delete_webapp_config_field(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_delete_webapp_doc(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_device_log_snapshot(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_device_set_nickname(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_disconnect_network(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_disconnect_provider(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_dismiss_ota_run(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_download_companion_update(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_download_voice_model(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_fetch_ota_manifest(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_get_webapp_doc(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_install_webapp(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_install_webapp_from_url(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_list_webapp_config(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_list_webapp_doc(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_list_webapps(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_log_inbox(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_ota_run_progress(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_resumed(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_set_capability_flags(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_set_device_auto_resume(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_set_device_log_streaming(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_set_device_resume_target(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_set_ota_poll_config(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_set_provider_priority(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_set_webapp_config_field(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_set_webapp_doc(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_set_webapp_slot(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_snapshot(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_start(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_stop(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_switch_webapp(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_time_changed(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_uninstall_webapp(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_voice_model_paths(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_webapp_resource(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_companionsession_webapp_slots(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_sessioneventsink_on_event(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_webappbundlesink_installed(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_otarunstore_annotate_webapp(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_otarunstore_available(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_otarunstore_dismiss(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_otarunstore_ingest(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_otarunstore_interrupt(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_otarunstore_note_meta(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_otarunstore_open_run_kind(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_otarunstore_poll_status(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_otarunstore_runs(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_amactionsink_fail(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_amactionsink_ok(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_amauthsink_complete(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_amauthsink_fail(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_amcatalogsink_complete(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_amcatalogsink_fail(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_amfavoritessink_complete(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_amfavoritessink_fail(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_amflagsink_complete(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_amflagsink_fail(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_amitemsink_complete(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_amitemsink_fail(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_ampagesink_complete(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_ampagesink_fail(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_amplayerinbox_on_changed(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_amsearchsink_complete(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_amsearchsink_fail(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_amshelvessink_complete(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_amshelvessink_fail(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_amsnapshotsink_complete(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_amsnapshotsink_fail(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_applemusicbackend_start(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_applemusicbackend_stop(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_applemusicbackend_snapshot(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_applemusicbackend_auth_status(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_applemusicbackend_request_authorization(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_applemusicbackend_can_play_catalog_content(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_applemusicbackend_is_other_audio_playing(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_applemusicbackend_play_context(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_applemusicbackend_queue_insert(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_applemusicbackend_command(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_applemusicbackend_library(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_applemusicbackend_recommendations(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_applemusicbackend_resolve(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_applemusicbackend_search(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_applemusicbackend_is_favorite(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_applemusicbackend_add_favorite(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_audiobackend_speak(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_audiobackend_cancel(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_audiobackend_cancel_all(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_audiobackend_play_earcon(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_earconsink_on_finished(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_speaksink_on_finished(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_speaksink_on_start(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_volumebackend_start(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_volumebackend_stop(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_volumebackend_snapshot(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_volumebackend_set_volume(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_volumebackend_set_mute(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_volumebackend_volume_up(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_volumebackend_volume_down(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_volumebackend_mute_toggle(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_volumeinbox_on_changed(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_connectivityinbox_on_changed(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_connectivitymonitor_start(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_connectivitymonitor_stop(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_extensionhost_start(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_extensionhost_stop(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_extensionhost_deliver(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_extensionhost_device_connected(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_extensionhost_device_disconnected(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_extensionhost_device_active(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_extensionhost_config_changed(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_extensionhostinbox_running_changed(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_extensionhostinbox_send_to_device(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_geoinbox_on_authorization_change(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_geoinbox_on_error(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_geoinbox_on_position(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_geoprovider_can_provide_location(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_geoprovider_start(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_geoprovider_stop(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_geoprovider_configure(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_geoprovider_request_authorization(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_geoprovider_start_updating(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_geoprovider_stop_updating(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_geoprovider_request_once(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_geoprovider_cancel_once(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_hostenvironment_clock(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_imagescaler_downsample_jpeg(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_linkinbox_on_bytes(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_linkinbox_on_connected(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_linkinbox_on_disconnected(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_linkinbox_on_link_failed(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_linkinbox_on_send_failed(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_linkinbox_on_write_complete(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_linktransport_max_batch_bytes(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_linktransport_start(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_linktransport_stop(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_linktransport_send(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_linktransport_disconnect(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_linktransport_reconnect(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_loginbox_push(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_logsink_on_line(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_logstore_archives(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_logstore_clear(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_logstore_delete(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_logstore_export_to(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_logstore_flush(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_logstore_read(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_logstore_record(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_logstore_retained_bytes(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_logstore_write(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_mediaartsink_complete(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_mediasessionbackend_is_access_granted(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_mediasessionbackend_start(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_mediasessionbackend_stop(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_mediasessionbackend_snapshot_all(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_mediasessionbackend_control(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_mediasessionbackend_art(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_mediasessioninbox_on_sessions_changed(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_mediasnapshotsink_complete(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_modelartifactvalidator_validate(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_transferpolicy_allows_large_transfer(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_httpdownloadsink_on_chunk(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_httpdownloadsink_on_failed(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_httpdownloadsink_on_finished(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_httpdownloadsink_on_response(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_httpsink_complete(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_httpsink_fail(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_httptransport_execute(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_httptransport_download(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_wsinbox_on_binary(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_wsinbox_on_closed(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_wsinbox_on_open(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_wsinbox_on_text(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_wstransport_connect(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_wstransport_send(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_wstransport_disconnect(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_nlumodelrunner_prewarm(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_nlumodelrunner_predict(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_actionsink_complete(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_actionsink_fail(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_notificationbackend_start(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_notificationbackend_stop(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_notificationbackend_invoke_positive(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_notificationbackend_invoke_negative(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_notificationinbox_on_posted(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_notificationinbox_on_removed(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_phonebackend_start(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_phonebackend_stop(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_phonebackend_command(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_phonebackend_state_get(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_phoneinbox_on_call_ended(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_phoneinbox_on_call_started(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_phoneinbox_on_call_updated(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_phoneinbox_on_communications(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_phoneinbox_on_state(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_phonestatesink_complete(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_phonestatesink_fail(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_secretstore_get(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_secretstore_set(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_secretstore_remove(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_secretstore_get_blob(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_preparesink_on_failed(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_preparesink_on_progress(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_preparesink_on_ready(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_speechrecognizer_prepare(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_speechrecognizer_transcribe(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_transcriptionsink_complete(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_transcriptionsink_fail(
    ): Int
    external fun uniffi_bridgething_companion_checksum_method_devicewaker_wake_device(
    ): Int
    external fun uniffi_bridgething_companion_checksum_constructor_companionsession_create(
    ): Int
    external fun uniffi_bridgething_companion_checksum_constructor_otarunstore_new(
    ): Int
    external fun uniffi_bridgething_companion_checksum_constructor_logstore_install(
    ): Int
    external fun ffi_bridgething_companion_uniffi_contract_version(
    ): Int

        
}

internal object UniffiLib {
    
    // The Cleaner for the whole library
    internal val CLEANER: UniffiCleaner by lazy {
        UniffiCleaner.create()
    }
    

    init {
        Native.register(UniffiLib::class.java, findLibraryName(componentName = "bridgething_companion"))
        uniffiCallbackInterfaceAppleMusicBackend.register(this)
        uniffiCallbackInterfaceAudioBackend.register(this)
        uniffiCallbackInterfaceConnectivityMonitor.register(this)
        uniffiCallbackInterfaceDeviceWaker.register(this)
        uniffiCallbackInterfaceExtensionHost.register(this)
        uniffiCallbackInterfaceGeoProvider.register(this)
        uniffiCallbackInterfaceHostEnvironment.register(this)
        uniffiCallbackInterfaceHttpTransport.register(this)
        uniffiCallbackInterfaceImageScaler.register(this)
        uniffiCallbackInterfaceLinkTransport.register(this)
        uniffiCallbackInterfaceLogSink.register(this)
        uniffiCallbackInterfaceMediaSessionBackend.register(this)
        uniffiCallbackInterfaceModelArtifactValidator.register(this)
        uniffiCallbackInterfaceNluModelRunner.register(this)
        uniffiCallbackInterfaceNotificationBackend.register(this)
        uniffiCallbackInterfacePhoneBackend.register(this)
        uniffiCallbackInterfaceSecretStore.register(this)
        uniffiCallbackInterfaceSessionEventSink.register(this)
        uniffiCallbackInterfaceSpeechRecognizer.register(this)
        uniffiCallbackInterfaceTransferPolicy.register(this)
        uniffiCallbackInterfaceVolumeBackend.register(this)
        uniffiCallbackInterfaceWebappBundleSink.register(this)
        uniffiCallbackInterfaceWsTransport.register(this)
        
    }
    external fun uniffi_bridgething_companion_fn_clone_companionsession(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_companionsession(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_constructor_companionsession_create(`config`: RustBuffer.ByValue,`backends`: RustBuffer.ByValue,`events`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_apply_ota_update(`ptr`: Long,`deviceId`: RustBuffer.ByValue,`channel`: RustBuffer.ByValue,`version`: RustBuffer.ByValue,`rootUrl`: RustBuffer.ByValue,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_available_providers(`ptr`: Long,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_cancel_auth(`ptr`: Long,`id`: RustBuffer.ByValue,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_check_for_ota_update(`ptr`: Long,`rootUrl`: RustBuffer.ByValue,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_collect_screenshot(`ptr`: Long,`deviceId`: RustBuffer.ByValue,`transferId`: RustBuffer.ByValue,`capturedAtMs`: Long,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_companion_debug(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun uniffi_bridgething_companion_fn_method_companionsession_complete_provider_auth(`ptr`: Long,`id`: RustBuffer.ByValue,`tokens`: RustBuffer.ByValue,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_connect_network(`ptr`: Long,`url`: RustBuffer.ByValue,`device`: RustBuffer.ByValue,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_connect_provider(`ptr`: Long,`id`: RustBuffer.ByValue,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_current_webapp(`ptr`: Long,`deviceId`: RustBuffer.ByValue,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_default_resume_target(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun uniffi_bridgething_companion_fn_method_companionsession_delete_webapp_config_field(`ptr`: Long,`deviceId`: RustBuffer.ByValue,`id`: RustBuffer.ByValue,`key`: RustBuffer.ByValue,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_delete_webapp_doc(`ptr`: Long,`deviceId`: RustBuffer.ByValue,`id`: RustBuffer.ByValue,`key`: RustBuffer.ByValue,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_device_log_snapshot(`ptr`: Long,`limit`: Int,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun uniffi_bridgething_companion_fn_method_companionsession_device_set_nickname(`ptr`: Long,`deviceId`: RustBuffer.ByValue,`nickname`: RustBuffer.ByValue,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_disconnect_network(`ptr`: Long,`deviceId`: RustBuffer.ByValue,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_disconnect_provider(`ptr`: Long,`id`: RustBuffer.ByValue,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_dismiss_ota_run(`ptr`: Long,`deviceId`: RustBuffer.ByValue,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_download_companion_update(`ptr`: Long,`url`: RustBuffer.ByValue,`filename`: RustBuffer.ByValue,`expected`: RustBuffer.ByValue,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_download_voice_model(`ptr`: Long,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_fetch_ota_manifest(`ptr`: Long,`rootUrl`: RustBuffer.ByValue,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_get_webapp_doc(`ptr`: Long,`deviceId`: RustBuffer.ByValue,`id`: RustBuffer.ByValue,`key`: RustBuffer.ByValue,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_install_webapp(`ptr`: Long,`deviceId`: RustBuffer.ByValue,`archivePath`: RustBuffer.ByValue,`provenance`: RustBuffer.ByValue,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_install_webapp_from_url(`ptr`: Long,`deviceId`: RustBuffer.ByValue,`url`: RustBuffer.ByValue,`expected`: RustBuffer.ByValue,`provenance`: RustBuffer.ByValue,`sink`: RustBuffer.ByValue,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_list_webapp_config(`ptr`: Long,`deviceId`: RustBuffer.ByValue,`id`: RustBuffer.ByValue,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_list_webapp_doc(`ptr`: Long,`deviceId`: RustBuffer.ByValue,`id`: RustBuffer.ByValue,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_list_webapps(`ptr`: Long,`deviceId`: RustBuffer.ByValue,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_log_inbox(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_ota_run_progress(`ptr`: Long,`deviceId`: RustBuffer.ByValue,`nowMs`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun uniffi_bridgething_companion_fn_method_companionsession_resumed(`ptr`: Long,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_set_capability_flags(`ptr`: Long,`flags`: RustBuffer.ByValue,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_set_device_auto_resume(`ptr`: Long,`deviceId`: RustBuffer.ByValue,`enabled`: Byte,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_set_device_log_streaming(`ptr`: Long,`enabled`: Byte,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_set_device_resume_target(`ptr`: Long,`deviceId`: RustBuffer.ByValue,`target`: RustBuffer.ByValue,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_set_ota_poll_config(`ptr`: Long,`config`: RustBuffer.ByValue,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_set_provider_priority(`ptr`: Long,`ids`: RustBuffer.ByValue,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_set_webapp_config_field(`ptr`: Long,`deviceId`: RustBuffer.ByValue,`id`: RustBuffer.ByValue,`key`: RustBuffer.ByValue,`value`: RustBuffer.ByValue,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_set_webapp_doc(`ptr`: Long,`deviceId`: RustBuffer.ByValue,`id`: RustBuffer.ByValue,`key`: RustBuffer.ByValue,`value`: RustBuffer.ByValue,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_set_webapp_slot(`ptr`: Long,`deviceId`: RustBuffer.ByValue,`slot`: RustBuffer.ByValue,`id`: RustBuffer.ByValue,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_snapshot(`ptr`: Long,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_start(`ptr`: Long,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_stop(`ptr`: Long,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_switch_webapp(`ptr`: Long,`deviceId`: RustBuffer.ByValue,`id`: RustBuffer.ByValue,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_time_changed(`ptr`: Long,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_uninstall_webapp(`ptr`: Long,`deviceId`: RustBuffer.ByValue,`id`: RustBuffer.ByValue,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_voice_model_paths(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun uniffi_bridgething_companion_fn_method_companionsession_webapp_resource(`ptr`: Long,`deviceId`: RustBuffer.ByValue,`id`: RustBuffer.ByValue,`kind`: RustBuffer.ByValue,`origin`: RustBuffer.ByValue,
    ): Long
    external fun uniffi_bridgething_companion_fn_method_companionsession_webapp_slots(`ptr`: Long,`deviceId`: RustBuffer.ByValue,
    ): Long
    external fun uniffi_bridgething_companion_fn_clone_sessioneventsink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_sessioneventsink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_init_callback_vtable_sessioneventsink(`vtable`: UniffiVTableCallbackInterfaceSessionEventSink,
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_sessioneventsink_on_event(`ptr`: Long,`event`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_webappbundlesink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_webappbundlesink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_init_callback_vtable_webappbundlesink(`vtable`: UniffiVTableCallbackInterfaceWebappBundleSink,
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_webappbundlesink_installed(`ptr`: Long,`bundle`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_otarunstore(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_otarunstore(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_constructor_otarunstore_new(uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_method_otarunstore_annotate_webapp(`ptr`: Long,`deviceId`: RustBuffer.ByValue,`webappId`: RustBuffer.ByValue,`webappName`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun uniffi_bridgething_companion_fn_method_otarunstore_available(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun uniffi_bridgething_companion_fn_method_otarunstore_dismiss(`ptr`: Long,`deviceId`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun uniffi_bridgething_companion_fn_method_otarunstore_ingest(`ptr`: Long,`event`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun uniffi_bridgething_companion_fn_method_otarunstore_interrupt(`ptr`: Long,`deviceId`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun uniffi_bridgething_companion_fn_method_otarunstore_note_meta(`ptr`: Long,`deviceId`: RustBuffer.ByValue,`daemonVersion`: RustBuffer.ByValue,`imageVersion`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun uniffi_bridgething_companion_fn_method_otarunstore_open_run_kind(`ptr`: Long,`deviceId`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun uniffi_bridgething_companion_fn_method_otarunstore_poll_status(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun uniffi_bridgething_companion_fn_method_otarunstore_runs(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun uniffi_bridgething_companion_fn_clone_amactionsink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_amactionsink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_amactionsink_fail(`ptr`: Long,`reason`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_amactionsink_ok(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_amauthsink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_amauthsink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_amauthsink_complete(`ptr`: Long,`value`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_amauthsink_fail(`ptr`: Long,`reason`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_amcatalogsink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_amcatalogsink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_amcatalogsink_complete(`ptr`: Long,`value`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_amcatalogsink_fail(`ptr`: Long,`reason`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_amfavoritessink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_amfavoritessink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_amfavoritessink_complete(`ptr`: Long,`value`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_amfavoritessink_fail(`ptr`: Long,`reason`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_amflagsink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_amflagsink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_amflagsink_complete(`ptr`: Long,`value`: Byte,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_amflagsink_fail(`ptr`: Long,`reason`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_amitemsink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_amitemsink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_amitemsink_complete(`ptr`: Long,`value`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_amitemsink_fail(`ptr`: Long,`reason`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_ampagesink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_ampagesink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_ampagesink_complete(`ptr`: Long,`value`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_ampagesink_fail(`ptr`: Long,`reason`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_amplayerinbox(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_amplayerinbox(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_amplayerinbox_on_changed(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_amsearchsink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_amsearchsink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_amsearchsink_complete(`ptr`: Long,`value`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_amsearchsink_fail(`ptr`: Long,`reason`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_amshelvessink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_amshelvessink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_amshelvessink_complete(`ptr`: Long,`value`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_amshelvessink_fail(`ptr`: Long,`reason`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_amsnapshotsink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_amsnapshotsink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_amsnapshotsink_complete(`ptr`: Long,`value`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_amsnapshotsink_fail(`ptr`: Long,`reason`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_applemusicbackend(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_applemusicbackend(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_init_callback_vtable_applemusicbackend(`vtable`: UniffiVTableCallbackInterfaceAppleMusicBackend,
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_applemusicbackend_start(`ptr`: Long,`inbox`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_applemusicbackend_stop(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_applemusicbackend_snapshot(`ptr`: Long,`sink`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_applemusicbackend_auth_status(`ptr`: Long,`sink`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_applemusicbackend_request_authorization(`ptr`: Long,`sink`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_applemusicbackend_can_play_catalog_content(`ptr`: Long,`sink`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_applemusicbackend_is_other_audio_playing(`ptr`: Long,`sink`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_applemusicbackend_play_context(`ptr`: Long,`contextUri`: RustBuffer.ByValue,`startAtUri`: RustBuffer.ByValue,`sink`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_applemusicbackend_queue_insert(`ptr`: Long,`uri`: RustBuffer.ByValue,`next`: Byte,`sink`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_applemusicbackend_command(`ptr`: Long,`cmd`: RustBuffer.ByValue,`sink`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_applemusicbackend_library(`ptr`: Long,`scope`: RustBuffer.ByValue,`limit`: Int,`offset`: Int,`sink`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_applemusicbackend_recommendations(`ptr`: Long,`sink`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_applemusicbackend_resolve(`ptr`: Long,`uri`: RustBuffer.ByValue,`sink`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_applemusicbackend_search(`ptr`: Long,`query`: RustBuffer.ByValue,`limit`: Int,`sink`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_applemusicbackend_is_favorite(`ptr`: Long,`uris`: RustBuffer.ByValue,`sink`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_applemusicbackend_add_favorite(`ptr`: Long,`uri`: RustBuffer.ByValue,`sink`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_audiobackend(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_audiobackend(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_init_callback_vtable_audiobackend(`vtable`: UniffiVTableCallbackInterfaceAudioBackend,
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_audiobackend_speak(`ptr`: Long,`id`: RustBuffer.ByValue,`text`: RustBuffer.ByValue,`voice`: RustBuffer.ByValue,`sink`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_audiobackend_cancel(`ptr`: Long,`id`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_audiobackend_cancel_all(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_audiobackend_play_earcon(`ptr`: Long,`name`: RustBuffer.ByValue,`sink`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_earconsink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_earconsink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_earconsink_on_finished(`ptr`: Long,`ok`: Byte,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_speaksink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_speaksink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_speaksink_on_finished(`ptr`: Long,`ok`: Byte,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_speaksink_on_start(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_volumebackend(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_volumebackend(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_init_callback_vtable_volumebackend(`vtable`: UniffiVTableCallbackInterfaceVolumeBackend,
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_volumebackend_start(`ptr`: Long,`inbox`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_volumebackend_stop(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_volumebackend_snapshot(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun uniffi_bridgething_companion_fn_method_volumebackend_set_volume(`ptr`: Long,`level`: Float,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_volumebackend_set_mute(`ptr`: Long,`muted`: Byte,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_volumebackend_volume_up(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_volumebackend_volume_down(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_volumebackend_mute_toggle(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_volumeinbox(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_volumeinbox(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_volumeinbox_on_changed(`ptr`: Long,`level`: Float,`muted`: Byte,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_connectivityinbox(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_connectivityinbox(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_connectivityinbox_on_changed(`ptr`: Long,`online`: Byte,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_connectivitymonitor(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_connectivitymonitor(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_init_callback_vtable_connectivitymonitor(`vtable`: UniffiVTableCallbackInterfaceConnectivityMonitor,
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_connectivitymonitor_start(`ptr`: Long,`inbox`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_connectivitymonitor_stop(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_extensionhost(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_extensionhost(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_init_callback_vtable_extensionhost(`vtable`: UniffiVTableCallbackInterfaceExtensionHost,
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_extensionhost_start(`ptr`: Long,`inbox`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_extensionhost_stop(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_extensionhost_deliver(`ptr`: Long,`device`: RustBuffer.ByValue,`webapp`: RustBuffer.ByValue,`message`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_extensionhost_device_connected(`ptr`: Long,`device`: RustBuffer.ByValue,`name`: RustBuffer.ByValue,`config`: RustBuffer.ByValue,`webapps`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_extensionhost_device_disconnected(`ptr`: Long,`device`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_extensionhost_device_active(`ptr`: Long,`device`: RustBuffer.ByValue,`webapp`: RustBuffer.ByValue,`active`: Byte,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_extensionhost_config_changed(`ptr`: Long,`device`: RustBuffer.ByValue,`webapp`: RustBuffer.ByValue,`key`: RustBuffer.ByValue,`value`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_extensionhostinbox(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_extensionhostinbox(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_extensionhostinbox_running_changed(`ptr`: Long,`webapps`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_extensionhostinbox_send_to_device(`ptr`: Long,`device`: RustBuffer.ByValue,`webapp`: RustBuffer.ByValue,`message`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_geoinbox(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_geoinbox(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_geoinbox_on_authorization_change(`ptr`: Long,`granted`: Byte,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_geoinbox_on_error(`ptr`: Long,`error`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_geoinbox_on_position(`ptr`: Long,`position`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_geoprovider(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_geoprovider(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_init_callback_vtable_geoprovider(`vtable`: UniffiVTableCallbackInterfaceGeoProvider,
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_geoprovider_can_provide_location(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Byte
    external fun uniffi_bridgething_companion_fn_method_geoprovider_start(`ptr`: Long,`inbox`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_geoprovider_stop(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_geoprovider_configure(`ptr`: Long,`accuracy`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_geoprovider_request_authorization(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_geoprovider_start_updating(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_geoprovider_stop_updating(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_geoprovider_request_once(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_geoprovider_cancel_once(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_hostenvironment(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_hostenvironment(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_init_callback_vtable_hostenvironment(`vtable`: UniffiVTableCallbackInterfaceHostEnvironment,
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_hostenvironment_clock(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun uniffi_bridgething_companion_fn_clone_imagescaler(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_imagescaler(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_init_callback_vtable_imagescaler(`vtable`: UniffiVTableCallbackInterfaceImageScaler,
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_imagescaler_downsample_jpeg(`ptr`: Long,`bytes`: RustBuffer.ByValue,`maxEdge`: Int,`quality`: Float,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun uniffi_bridgething_companion_fn_clone_linkinbox(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_linkinbox(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_linkinbox_on_bytes(`ptr`: Long,`deviceId`: RustBuffer.ByValue,`bytes`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_linkinbox_on_connected(`ptr`: Long,`device`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_linkinbox_on_disconnected(`ptr`: Long,`deviceId`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_linkinbox_on_link_failed(`ptr`: Long,`deviceId`: RustBuffer.ByValue,`name`: RustBuffer.ByValue,`reason`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_linkinbox_on_send_failed(`ptr`: Long,`deviceId`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_linkinbox_on_write_complete(`ptr`: Long,`deviceId`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_linktransport(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_linktransport(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_init_callback_vtable_linktransport(`vtable`: UniffiVTableCallbackInterfaceLinkTransport,
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_linktransport_max_batch_bytes(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Int
    external fun uniffi_bridgething_companion_fn_method_linktransport_start(`ptr`: Long,`inbox`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_linktransport_stop(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_linktransport_send(`ptr`: Long,`deviceId`: RustBuffer.ByValue,`batch`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_linktransport_disconnect(`ptr`: Long,`deviceId`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_linktransport_reconnect(`ptr`: Long,`deviceId`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_loginbox(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_loginbox(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_loginbox_push(`ptr`: Long,`level`: RustBuffer.ByValue,`target`: RustBuffer.ByValue,`message`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_logsink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_logsink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_init_callback_vtable_logsink(`vtable`: UniffiVTableCallbackInterfaceLogSink,
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_logsink_on_line(`ptr`: Long,`level`: RustBuffer.ByValue,`target`: RustBuffer.ByValue,`message`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_logstore(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_logstore(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_constructor_logstore_install(`root`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_method_logstore_archives(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun uniffi_bridgething_companion_fn_method_logstore_clear(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_logstore_delete(`ptr`: Long,`id`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_logstore_export_to(`ptr`: Long,`target`: RustBuffer.ByValue,`id`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun uniffi_bridgething_companion_fn_method_logstore_flush(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_logstore_read(`ptr`: Long,`id`: RustBuffer.ByValue,`limit`: Int,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun uniffi_bridgething_companion_fn_method_logstore_record(`ptr`: Long,`level`: RustBuffer.ByValue,`label`: RustBuffer.ByValue,`message`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_logstore_retained_bytes(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_method_logstore_write(`ptr`: Long,`line`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_mediaartsink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_mediaartsink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_mediaartsink_complete(`ptr`: Long,`art`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_mediasessionbackend(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_mediasessionbackend(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_init_callback_vtable_mediasessionbackend(`vtable`: UniffiVTableCallbackInterfaceMediaSessionBackend,
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_mediasessionbackend_is_access_granted(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Byte
    external fun uniffi_bridgething_companion_fn_method_mediasessionbackend_start(`ptr`: Long,`inbox`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_mediasessionbackend_stop(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_mediasessionbackend_snapshot_all(`ptr`: Long,`sink`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_mediasessionbackend_control(`ptr`: Long,`package`: RustBuffer.ByValue,`cmd`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_mediasessionbackend_art(`ptr`: Long,`package`: RustBuffer.ByValue,`token`: RustBuffer.ByValue,`sink`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_mediasessioninbox(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_mediasessioninbox(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_mediasessioninbox_on_sessions_changed(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_mediasnapshotsink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_mediasnapshotsink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_mediasnapshotsink_complete(`ptr`: Long,`sessions`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_modelartifactvalidator(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_modelartifactvalidator(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_init_callback_vtable_modelartifactvalidator(`vtable`: UniffiVTableCallbackInterfaceModelArtifactValidator,
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_modelartifactvalidator_validate(`ptr`: Long,`kind`: RustBuffer.ByValue,`path`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_transferpolicy(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_transferpolicy(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_init_callback_vtable_transferpolicy(`vtable`: UniffiVTableCallbackInterfaceTransferPolicy,
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_transferpolicy_allows_large_transfer(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Byte
    external fun uniffi_bridgething_companion_fn_clone_httpdownloadsink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_httpdownloadsink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_httpdownloadsink_on_chunk(`ptr`: Long,`chunk`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_httpdownloadsink_on_failed(`ptr`: Long,`reason`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_httpdownloadsink_on_finished(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_httpdownloadsink_on_response(`ptr`: Long,`status`: Short,`headers`: RustBuffer.ByValue,`contentLength`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_httpsink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_httpsink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_httpsink_complete(`ptr`: Long,`response`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_httpsink_fail(`ptr`: Long,`reason`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_httptransport(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_httptransport(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_init_callback_vtable_httptransport(`vtable`: UniffiVTableCallbackInterfaceHttpTransport,
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_httptransport_execute(`ptr`: Long,`request`: RustBuffer.ByValue,`sink`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_httptransport_download(`ptr`: Long,`request`: RustBuffer.ByValue,`sink`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_wsinbox(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_wsinbox(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_wsinbox_on_binary(`ptr`: Long,`id`: RustBuffer.ByValue,`bytes`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_wsinbox_on_closed(`ptr`: Long,`id`: RustBuffer.ByValue,`code`: RustBuffer.ByValue,`reason`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_wsinbox_on_open(`ptr`: Long,`id`: RustBuffer.ByValue,`acceptedProtocol`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_wsinbox_on_text(`ptr`: Long,`id`: RustBuffer.ByValue,`text`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_wstransport(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_wstransport(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_init_callback_vtable_wstransport(`vtable`: UniffiVTableCallbackInterfaceWsTransport,
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_wstransport_connect(`ptr`: Long,`connect`: RustBuffer.ByValue,`inbox`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_wstransport_send(`ptr`: Long,`id`: RustBuffer.ByValue,`frame`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_wstransport_disconnect(`ptr`: Long,`id`: RustBuffer.ByValue,`code`: RustBuffer.ByValue,`reason`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_nlumodelrunner(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_nlumodelrunner(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_init_callback_vtable_nlumodelrunner(`vtable`: UniffiVTableCallbackInterfaceNluModelRunner,
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_nlumodelrunner_prewarm(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_nlumodelrunner_predict(`ptr`: Long,`inputIds`: RustBuffer.ByValue,`attentionMask`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun uniffi_bridgething_companion_fn_clone_actionsink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_actionsink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_actionsink_complete(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_actionsink_fail(`ptr`: Long,`error`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_notificationbackend(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_notificationbackend(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_init_callback_vtable_notificationbackend(`vtable`: UniffiVTableCallbackInterfaceNotificationBackend,
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_notificationbackend_start(`ptr`: Long,`inbox`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_notificationbackend_stop(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_notificationbackend_invoke_positive(`ptr`: Long,`id`: RustBuffer.ByValue,`sink`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_notificationbackend_invoke_negative(`ptr`: Long,`id`: RustBuffer.ByValue,`sink`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_notificationinbox(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_notificationinbox(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_notificationinbox_on_posted(`ptr`: Long,`notification`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_notificationinbox_on_removed(`ptr`: Long,`removed`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_phonebackend(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_phonebackend(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_init_callback_vtable_phonebackend(`vtable`: UniffiVTableCallbackInterfacePhoneBackend,
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_phonebackend_start(`ptr`: Long,`inbox`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_phonebackend_stop(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_phonebackend_command(`ptr`: Long,`cmd`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_phonebackend_state_get(`ptr`: Long,`sink`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_phoneinbox(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_phoneinbox(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_phoneinbox_on_call_ended(`ptr`: Long,`ended`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_phoneinbox_on_call_started(`ptr`: Long,`call`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_phoneinbox_on_call_updated(`ptr`: Long,`call`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_phoneinbox_on_communications(`ptr`: Long,`state`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_phoneinbox_on_state(`ptr`: Long,`state`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_phonestatesink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_phonestatesink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_phonestatesink_complete(`ptr`: Long,`state`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_phonestatesink_fail(`ptr`: Long,`reason`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_secretstore(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_secretstore(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_init_callback_vtable_secretstore(`vtable`: UniffiVTableCallbackInterfaceSecretStore,
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_secretstore_get(`ptr`: Long,`key`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun uniffi_bridgething_companion_fn_method_secretstore_set(`ptr`: Long,`key`: RustBuffer.ByValue,`value`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_secretstore_remove(`ptr`: Long,`key`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_secretstore_get_blob(`ptr`: Long,`key`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun uniffi_bridgething_companion_fn_clone_preparesink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_preparesink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_preparesink_on_failed(`ptr`: Long,`reason`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_preparesink_on_progress(`ptr`: Long,`received`: Long,`total`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_preparesink_on_ready(`ptr`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_speechrecognizer(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_speechrecognizer(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_init_callback_vtable_speechrecognizer(`vtable`: UniffiVTableCallbackInterfaceSpeechRecognizer,
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_speechrecognizer_prepare(`ptr`: Long,`sink`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_speechrecognizer_transcribe(`ptr`: Long,`pcm`: RustBuffer.ByValue,`sampleRateHz`: Int,`sink`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_transcriptionsink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_transcriptionsink(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_transcriptionsink_complete(`ptr`: Long,`transcription`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_transcriptionsink_fail(`ptr`: Long,`reason`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_clone_devicewaker(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun uniffi_bridgething_companion_fn_free_devicewaker(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_init_callback_vtable_devicewaker(`vtable`: UniffiVTableCallbackInterfaceDeviceWaker,
    ): Unit
    external fun uniffi_bridgething_companion_fn_method_devicewaker_wake_device(`ptr`: Long,`reason`: RustBuffer.ByValue,`allowPlayTap`: Byte,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun uniffi_bridgething_companion_fn_func_parse_lrc(`text`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun uniffi_bridgething_companion_fn_func_nlu_fast_path_match(`transcript`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun uniffi_bridgething_companion_fn_func_nlu_intent_catalog(uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun uniffi_bridgething_companion_fn_func_nlu_rejection_evaluate(`intentLogits`: RustBuffer.ByValue,`inDomainLogit`: Double,`policy`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun uniffi_bridgething_companion_fn_func_ota_artifact_urls(`rootUrl`: RustBuffer.ByValue,`channel`: RustBuffer.ByValue,`daemonVersion`: RustBuffer.ByValue,`imageVersion`: RustBuffer.ByValue,`imageVariant`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun uniffi_bridgething_companion_fn_func_ota_builtin_webapp_url(`rootUrl`: RustBuffer.ByValue,`channel`: RustBuffer.ByValue,`name`: RustBuffer.ByValue,`version`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun uniffi_bridgething_companion_fn_func_ota_cancelled_reason(uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun uniffi_bridgething_companion_fn_func_ota_composite_version_string(`version`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun uniffi_bridgething_companion_fn_func_ota_daemon_patch_url(`rootUrl`: RustBuffer.ByValue,`channel`: RustBuffer.ByValue,`toVersion`: RustBuffer.ByValue,`fromVersion`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun uniffi_bridgething_companion_fn_func_ota_patch_source_matches(`declared`: RustBuffer.ByValue,`running`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Byte
    external fun uniffi_bridgething_companion_fn_func_ota_run_progress(`run`: RustBuffer.ByValue,`nowMs`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun uniffi_bridgething_companion_fn_func_parse_ota_composite_version(`raw`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun uniffi_bridgething_companion_fn_func_parse_ota_discover_manifest(`json`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun ffi_bridgething_companion_rustbuffer_alloc(`size`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun ffi_bridgething_companion_rustbuffer_from_bytes(`bytes`: ForeignBytes.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun ffi_bridgething_companion_rustbuffer_free(`buf`: RustBuffer.ByValue,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit
    external fun ffi_bridgething_companion_rustbuffer_reserve(`buf`: RustBuffer.ByValue,`additional`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun ffi_bridgething_companion_rust_future_poll_u8(`handle`: Long,`callback`: UniffiRustFutureContinuationCallback,`callbackData`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_cancel_u8(`handle`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_free_u8(`handle`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_complete_u8(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Int
    external fun ffi_bridgething_companion_rust_future_poll_i8(`handle`: Long,`callback`: UniffiRustFutureContinuationCallback,`callbackData`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_cancel_i8(`handle`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_free_i8(`handle`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_complete_i8(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Byte
    external fun ffi_bridgething_companion_rust_future_poll_u16(`handle`: Long,`callback`: UniffiRustFutureContinuationCallback,`callbackData`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_cancel_u16(`handle`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_free_u16(`handle`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_complete_u16(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Int
    external fun ffi_bridgething_companion_rust_future_poll_i16(`handle`: Long,`callback`: UniffiRustFutureContinuationCallback,`callbackData`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_cancel_i16(`handle`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_free_i16(`handle`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_complete_i16(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Short
    external fun ffi_bridgething_companion_rust_future_poll_u32(`handle`: Long,`callback`: UniffiRustFutureContinuationCallback,`callbackData`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_cancel_u32(`handle`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_free_u32(`handle`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_complete_u32(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Int
    external fun ffi_bridgething_companion_rust_future_poll_i32(`handle`: Long,`callback`: UniffiRustFutureContinuationCallback,`callbackData`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_cancel_i32(`handle`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_free_i32(`handle`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_complete_i32(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Int
    external fun ffi_bridgething_companion_rust_future_poll_u64(`handle`: Long,`callback`: UniffiRustFutureContinuationCallback,`callbackData`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_cancel_u64(`handle`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_free_u64(`handle`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_complete_u64(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun ffi_bridgething_companion_rust_future_poll_i64(`handle`: Long,`callback`: UniffiRustFutureContinuationCallback,`callbackData`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_cancel_i64(`handle`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_free_i64(`handle`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_complete_i64(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Long
    external fun ffi_bridgething_companion_rust_future_poll_f32(`handle`: Long,`callback`: UniffiRustFutureContinuationCallback,`callbackData`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_cancel_f32(`handle`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_free_f32(`handle`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_complete_f32(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Float
    external fun ffi_bridgething_companion_rust_future_poll_f64(`handle`: Long,`callback`: UniffiRustFutureContinuationCallback,`callbackData`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_cancel_f64(`handle`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_free_f64(`handle`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_complete_f64(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Double
    external fun ffi_bridgething_companion_rust_future_poll_rust_buffer(`handle`: Long,`callback`: UniffiRustFutureContinuationCallback,`callbackData`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_cancel_rust_buffer(`handle`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_free_rust_buffer(`handle`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_complete_rust_buffer(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): RustBuffer.ByValue
    external fun ffi_bridgething_companion_rust_future_poll_void(`handle`: Long,`callback`: UniffiRustFutureContinuationCallback,`callbackData`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_cancel_void(`handle`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_free_void(`handle`: Long,
    ): Unit
    external fun ffi_bridgething_companion_rust_future_complete_void(`handle`: Long,uniffi_out_err: UniffiRustCallStatus, 
    ): Unit

        
}

private fun uniffiCheckContractApiVersion(lib: IntegrityCheckingUniffiLib) {
    // Get the bindings contract version from our ComponentInterface
    val bindings_contract_version = 30
    // Get the scaffolding contract version by calling the into the dylib
    val scaffolding_contract_version = lib.ffi_bridgething_companion_uniffi_contract_version()
    if (bindings_contract_version != scaffolding_contract_version) {
        throw RuntimeException("UniFFI contract version mismatch: try cleaning and rebuilding your project")
    }
}
@Suppress("UNUSED_PARAMETER")
private fun uniffiCheckApiChecksums(lib: IntegrityCheckingUniffiLib) {
    if (lib.uniffi_bridgething_companion_checksum_func_parse_lrc() != 15758) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_func_nlu_fast_path_match() != 12404) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_func_nlu_intent_catalog() != 14855) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_func_nlu_rejection_evaluate() != 53692) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_func_ota_artifact_urls() != 33534) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_func_ota_builtin_webapp_url() != 28909) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_func_ota_cancelled_reason() != 63358) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_func_ota_composite_version_string() != 12000) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_func_ota_daemon_patch_url() != 6855) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_func_ota_patch_source_matches() != 1541) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_func_ota_run_progress() != 31804) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_func_parse_ota_composite_version() != 43032) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_func_parse_ota_discover_manifest() != 65227) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_apply_ota_update() != 43986) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_available_providers() != 44636) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_cancel_auth() != 2483) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_check_for_ota_update() != 64188) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_collect_screenshot() != 6174) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_companion_debug() != 26391) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_complete_provider_auth() != 52876) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_connect_network() != 17882) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_connect_provider() != 25749) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_current_webapp() != 44144) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_default_resume_target() != 39836) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_delete_webapp_config_field() != 42200) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_delete_webapp_doc() != 62193) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_device_log_snapshot() != 3371) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_device_set_nickname() != 14738) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_disconnect_network() != 40627) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_disconnect_provider() != 6432) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_dismiss_ota_run() != 20350) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_download_companion_update() != 23445) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_download_voice_model() != 1975) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_fetch_ota_manifest() != 39692) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_get_webapp_doc() != 55247) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_install_webapp() != 50155) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_install_webapp_from_url() != 57765) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_list_webapp_config() != 1816) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_list_webapp_doc() != 62641) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_list_webapps() != 54300) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_log_inbox() != 46480) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_ota_run_progress() != 5569) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_resumed() != 18676) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_set_capability_flags() != 58070) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_set_device_auto_resume() != 2622) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_set_device_log_streaming() != 49632) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_set_device_resume_target() != 16467) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_set_ota_poll_config() != 63985) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_set_provider_priority() != 16699) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_set_webapp_config_field() != 45908) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_set_webapp_doc() != 42842) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_set_webapp_slot() != 44826) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_snapshot() != 31913) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_start() != 64227) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_stop() != 31562) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_switch_webapp() != 43375) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_time_changed() != 9220) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_uninstall_webapp() != 6236) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_voice_model_paths() != 45257) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_webapp_resource() != 54617) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_companionsession_webapp_slots() != 15167) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_sessioneventsink_on_event() != 19676) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_webappbundlesink_installed() != 640) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_otarunstore_annotate_webapp() != 65092) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_otarunstore_available() != 48407) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_otarunstore_dismiss() != 8630) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_otarunstore_ingest() != 26158) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_otarunstore_interrupt() != 47469) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_otarunstore_note_meta() != 19259) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_otarunstore_open_run_kind() != 10159) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_otarunstore_poll_status() != 11209) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_otarunstore_runs() != 51251) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_amactionsink_fail() != 18669) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_amactionsink_ok() != 14718) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_amauthsink_complete() != 6514) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_amauthsink_fail() != 42716) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_amcatalogsink_complete() != 21671) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_amcatalogsink_fail() != 47856) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_amfavoritessink_complete() != 15038) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_amfavoritessink_fail() != 797) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_amflagsink_complete() != 64810) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_amflagsink_fail() != 37547) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_amitemsink_complete() != 31572) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_amitemsink_fail() != 60872) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_ampagesink_complete() != 6407) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_ampagesink_fail() != 39059) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_amplayerinbox_on_changed() != 22514) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_amsearchsink_complete() != 34663) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_amsearchsink_fail() != 54508) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_amshelvessink_complete() != 12813) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_amshelvessink_fail() != 48670) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_amsnapshotsink_complete() != 29849) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_amsnapshotsink_fail() != 51172) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_applemusicbackend_start() != 45406) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_applemusicbackend_stop() != 23567) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_applemusicbackend_snapshot() != 37147) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_applemusicbackend_auth_status() != 40252) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_applemusicbackend_request_authorization() != 46116) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_applemusicbackend_can_play_catalog_content() != 43557) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_applemusicbackend_is_other_audio_playing() != 24142) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_applemusicbackend_play_context() != 53854) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_applemusicbackend_queue_insert() != 50399) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_applemusicbackend_command() != 51206) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_applemusicbackend_library() != 47328) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_applemusicbackend_recommendations() != 17706) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_applemusicbackend_resolve() != 42011) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_applemusicbackend_search() != 56655) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_applemusicbackend_is_favorite() != 20046) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_applemusicbackend_add_favorite() != 26475) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_audiobackend_speak() != 61321) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_audiobackend_cancel() != 62888) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_audiobackend_cancel_all() != 29982) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_audiobackend_play_earcon() != 25031) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_earconsink_on_finished() != 30498) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_speaksink_on_finished() != 30134) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_speaksink_on_start() != 43824) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_volumebackend_start() != 11566) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_volumebackend_stop() != 22914) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_volumebackend_snapshot() != 6320) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_volumebackend_set_volume() != 60392) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_volumebackend_set_mute() != 14436) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_volumebackend_volume_up() != 13866) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_volumebackend_volume_down() != 31743) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_volumebackend_mute_toggle() != 51282) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_volumeinbox_on_changed() != 45635) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_connectivityinbox_on_changed() != 42704) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_connectivitymonitor_start() != 32554) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_connectivitymonitor_stop() != 37048) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_extensionhost_start() != 62617) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_extensionhost_stop() != 61125) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_extensionhost_deliver() != 22605) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_extensionhost_device_connected() != 47175) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_extensionhost_device_disconnected() != 43849) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_extensionhost_device_active() != 1408) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_extensionhost_config_changed() != 43304) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_extensionhostinbox_running_changed() != 25097) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_extensionhostinbox_send_to_device() != 41831) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_geoinbox_on_authorization_change() != 60712) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_geoinbox_on_error() != 62005) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_geoinbox_on_position() != 39668) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_geoprovider_can_provide_location() != 26037) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_geoprovider_start() != 41448) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_geoprovider_stop() != 42159) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_geoprovider_configure() != 28530) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_geoprovider_request_authorization() != 34345) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_geoprovider_start_updating() != 36405) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_geoprovider_stop_updating() != 9046) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_geoprovider_request_once() != 10788) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_geoprovider_cancel_once() != 58045) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_hostenvironment_clock() != 57610) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_imagescaler_downsample_jpeg() != 45721) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_linkinbox_on_bytes() != 13274) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_linkinbox_on_connected() != 48086) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_linkinbox_on_disconnected() != 20440) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_linkinbox_on_link_failed() != 45895) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_linkinbox_on_send_failed() != 59648) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_linkinbox_on_write_complete() != 41154) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_linktransport_max_batch_bytes() != 4543) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_linktransport_start() != 58861) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_linktransport_stop() != 3529) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_linktransport_send() != 58847) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_linktransport_disconnect() != 7584) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_linktransport_reconnect() != 18178) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_loginbox_push() != 65340) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_logsink_on_line() != 29528) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_logstore_archives() != 48006) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_logstore_clear() != 13749) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_logstore_delete() != 14414) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_logstore_export_to() != 6369) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_logstore_flush() != 10383) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_logstore_read() != 15761) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_logstore_record() != 50338) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_logstore_retained_bytes() != 44168) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_logstore_write() != 51258) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_mediaartsink_complete() != 64460) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_mediasessionbackend_is_access_granted() != 39817) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_mediasessionbackend_start() != 13027) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_mediasessionbackend_stop() != 50357) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_mediasessionbackend_snapshot_all() != 50734) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_mediasessionbackend_control() != 40276) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_mediasessionbackend_art() != 1231) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_mediasessioninbox_on_sessions_changed() != 61681) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_mediasnapshotsink_complete() != 46184) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_modelartifactvalidator_validate() != 10005) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_transferpolicy_allows_large_transfer() != 54961) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_httpdownloadsink_on_chunk() != 43867) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_httpdownloadsink_on_failed() != 48694) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_httpdownloadsink_on_finished() != 21755) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_httpdownloadsink_on_response() != 21604) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_httpsink_complete() != 53113) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_httpsink_fail() != 12820) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_httptransport_execute() != 14334) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_httptransport_download() != 30415) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_wsinbox_on_binary() != 64335) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_wsinbox_on_closed() != 61011) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_wsinbox_on_open() != 23351) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_wsinbox_on_text() != 64209) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_wstransport_connect() != 45163) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_wstransport_send() != 2546) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_wstransport_disconnect() != 63449) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_nlumodelrunner_prewarm() != 31313) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_nlumodelrunner_predict() != 5108) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_actionsink_complete() != 27382) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_actionsink_fail() != 55897) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_notificationbackend_start() != 15334) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_notificationbackend_stop() != 45493) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_notificationbackend_invoke_positive() != 8935) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_notificationbackend_invoke_negative() != 38605) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_notificationinbox_on_posted() != 7905) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_notificationinbox_on_removed() != 65513) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_phonebackend_start() != 59733) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_phonebackend_stop() != 52386) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_phonebackend_command() != 55805) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_phonebackend_state_get() != 43289) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_phoneinbox_on_call_ended() != 26956) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_phoneinbox_on_call_started() != 52038) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_phoneinbox_on_call_updated() != 2723) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_phoneinbox_on_communications() != 17160) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_phoneinbox_on_state() != 35226) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_phonestatesink_complete() != 515) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_phonestatesink_fail() != 2127) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_secretstore_get() != 6476) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_secretstore_set() != 8663) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_secretstore_remove() != 50917) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_secretstore_get_blob() != 34854) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_preparesink_on_failed() != 31162) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_preparesink_on_progress() != 18330) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_preparesink_on_ready() != 10510) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_speechrecognizer_prepare() != 63832) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_speechrecognizer_transcribe() != 7921) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_transcriptionsink_complete() != 64389) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_transcriptionsink_fail() != 11687) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_method_devicewaker_wake_device() != 33554) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_constructor_companionsession_create() != 21535) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_constructor_otarunstore_new() != 40210) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
    if (lib.uniffi_bridgething_companion_checksum_constructor_logstore_install() != 54394) {
        throw RuntimeException("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
}

/**
 * @suppress
 */
public fun uniffiEnsureInitialized() {
    IntegrityCheckingUniffiLib
    // UniffiLib() initialized as objects are used, but we still need to explicitly
    // reference it so initialization across crates works as expected.
    UniffiLib
}

// Async support
// Async return type handlers

internal const val UNIFFI_RUST_FUTURE_POLL_READY = 0.toByte()
internal const val UNIFFI_RUST_FUTURE_POLL_WAKE = 1.toByte()

internal val uniffiContinuationHandleMap = UniffiHandleMap<CancellableContinuation<Byte>>()

// FFI type for Rust future continuations
internal object uniffiRustFutureContinuationCallbackImpl: UniffiRustFutureContinuationCallback {
    override fun callback(data: Long, pollResult: Byte) {
        uniffiContinuationHandleMap.remove(data).resume(pollResult)
    }
}

internal suspend fun<T, F, E: kotlin.Exception> uniffiRustCallAsync(
    rustFuture: Long,
    pollFunc: (Long, UniffiRustFutureContinuationCallback, Long) -> Unit,
    completeFunc: (Long, UniffiRustCallStatus) -> F,
    freeFunc: (Long) -> Unit,
    liftFunc: (F) -> T,
    errorHandler: UniffiRustCallStatusErrorHandler<E>
): T {
    try {
        do {
            val pollResult = suspendCancellableCoroutine<Byte> { continuation ->
                pollFunc(
                    rustFuture,
                    uniffiRustFutureContinuationCallbackImpl,
                    uniffiContinuationHandleMap.insert(continuation)
                )
            }
        } while (pollResult != UNIFFI_RUST_FUTURE_POLL_READY);

        return liftFunc(
            uniffiRustCallWithError(errorHandler, { status -> completeFunc(rustFuture, status) })
        )
    } finally {
        freeFunc(rustFuture)
    }
}

// Public interface members begin here.


// Interface implemented by anything that can contain an object reference.
//
// Such types expose a `destroy()` method that must be called to cleanly
// dispose of the contained objects. Failure to call this method may result
// in memory leaks.
//
// The easiest way to ensure this method is called is to use the `.use`
// helper method to execute a block and destroy the object at the end.
interface Disposable {
    fun destroy()
    companion object {
        fun destroy(vararg args: Any?) {
            for (arg in args) {
                when (arg) {
                    is Disposable -> arg.destroy()
                    is ArrayList<*> -> {
                        for (idx in arg.indices) {
                            val element = arg[idx]
                            if (element is Disposable) {
                                element.destroy()
                            }
                        }
                    }
                    is Map<*, *> -> {
                        for (element in arg.values) {
                            if (element is Disposable) {
                                element.destroy()
                            }
                        }
                    }
                    is Iterable<*> -> {
                        for (element in arg) {
                            if (element is Disposable) {
                                element.destroy()
                            }
                        }
                    }
                }
            }
        }
    }
}

/**
 * @suppress
 */
inline fun <T : Disposable?, R> T.use(block: (T) -> R) =
    try {
        block(this)
    } finally {
        try {
            // N.B. our implementation is on the nullable type `Disposable?`.
            this?.destroy()
        } catch (e: Throwable) {
            // swallow
        }
    }

/** 
 * Placeholder object used to signal that we're constructing an interface with a FFI handle.
 *
 * This is the first argument for interface constructors that input a raw handle. It exists is that
 * so we can avoid signature conflicts when an interface has a regular constructor than inputs a
 * Long.
 *
 * @suppress
 * */
object UniffiWithHandle

/** 
 * Used to instantiate an interface without an actual pointer, for fakes in tests, mostly.
 *
 * @suppress
 * */
object NoHandle// Magic number for the Rust proxy to call using the same mechanism as every other method,
// to free the callback once it's dropped by Rust.
internal const val IDX_CALLBACK_FREE = 0
// Callback return codes
internal const val UNIFFI_CALLBACK_SUCCESS = 0
internal const val UNIFFI_CALLBACK_ERROR = 1
internal const val UNIFFI_CALLBACK_UNEXPECTED_ERROR = 2

/**
 * @suppress
 */
public abstract class FfiConverterCallbackInterface<CallbackInterface: Any>: FfiConverter<CallbackInterface, Long> {
    internal val handleMap = UniffiHandleMap<CallbackInterface>()

    internal fun drop(handle: Long) {
        handleMap.remove(handle)
    }

    override fun lift(value: Long): CallbackInterface {
        return handleMap.get(value)
    }

    override fun read(buf: ByteBuffer) = lift(buf.getLong())

    override fun lower(value: CallbackInterface) = handleMap.insert(value)

    override fun allocationSize(value: CallbackInterface) = 8UL

    override fun write(value: CallbackInterface, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}
/**
 * The cleaner interface for Object finalization code to run.
 * This is the entry point to any implementation that we're using.
 *
 * The cleaner registers objects and returns cleanables, so now we are
 * defining a `UniffiCleaner` with a `UniffiClenaer.Cleanable` to abstract the
 * different implmentations available at compile time.
 *
 * @suppress
 */
interface UniffiCleaner {
    interface Cleanable {
        fun clean()
    }

    fun register(value: Any, cleanUpTask: Runnable): UniffiCleaner.Cleanable

    companion object
}

// The fallback Jna cleaner, which is available for both Android, and the JVM.
private class UniffiJnaCleaner : UniffiCleaner {
    private val cleaner = com.sun.jna.internal.Cleaner.getCleaner()

    override fun register(value: Any, cleanUpTask: Runnable): UniffiCleaner.Cleanable =
        UniffiJnaCleanable(cleaner.register(value, cleanUpTask))
}

private class UniffiJnaCleanable(
    private val cleanable: com.sun.jna.internal.Cleaner.Cleanable,
) : UniffiCleaner.Cleanable {
    override fun clean() = cleanable.clean()
}


// We decide at uniffi binding generation time whether we were
// using Android or not.
// There are further runtime checks to chose the correct implementation
// of the cleaner.
private fun UniffiCleaner.Companion.create(): UniffiCleaner =
    try {
        // For safety's sake: if the library hasn't been run in android_cleaner = true
        // mode, but is being run on Android, then we still need to think about
        // Android API versions.
        // So we check if java.lang.ref.Cleaner is there, and use that…
        java.lang.Class.forName("java.lang.ref.Cleaner")
        JavaLangRefCleaner()
    } catch (e: ClassNotFoundException) {
        // … otherwise, fallback to the JNA cleaner.
        UniffiJnaCleaner()
    }

private class JavaLangRefCleaner : UniffiCleaner {
    val cleaner = java.lang.ref.Cleaner.create()

    override fun register(value: Any, cleanUpTask: Runnable): UniffiCleaner.Cleanable =
        JavaLangRefCleanable(cleaner.register(value, cleanUpTask))
}

private class JavaLangRefCleanable(
    val cleanable: java.lang.ref.Cleaner.Cleanable
) : UniffiCleaner.Cleanable {
    override fun clean() = cleanable.clean()
}

/**
 * @suppress
 */
public object FfiConverterUByte: FfiConverter<UByte, Byte> {
    override fun lift(value: Byte): UByte {
        return value.toUByte()
    }

    fun lift(value: Int): UByte {
        return value.toUByte()
    }

    override fun read(buf: ByteBuffer): UByte {
        return lift(buf.get())
    }

    override fun lower(value: UByte): Byte {
        return value.toByte()
    }

    override fun allocationSize(value: UByte) = 1UL

    override fun write(value: UByte, buf: ByteBuffer) {
        buf.put(value.toByte())
    }
}

/**
 * @suppress
 */
public object FfiConverterByte: FfiConverter<Byte, Byte> {
    override fun lift(value: Byte): Byte {
        return value
    }

    override fun read(buf: ByteBuffer): Byte {
        return buf.get()
    }

    override fun lower(value: Byte): Byte {
        return value
    }

    override fun allocationSize(value: Byte) = 1UL

    override fun write(value: Byte, buf: ByteBuffer) {
        buf.put(value)
    }
}

/**
 * @suppress
 */
public object FfiConverterUShort: FfiConverter<UShort, Short> {
    override fun lift(value: Short): UShort {
        return value.toUShort()
    }

    fun lift(value: Int): UShort {
        return value.toUShort()
    }

    override fun read(buf: ByteBuffer): UShort {
        return lift(buf.getShort())
    }

    override fun lower(value: UShort): Short {
        return value.toShort()
    }

    override fun allocationSize(value: UShort) = 2UL

    override fun write(value: UShort, buf: ByteBuffer) {
        buf.putShort(value.toShort())
    }
}

/**
 * @suppress
 */
public object FfiConverterShort: FfiConverter<Short, Short> {
    override fun lift(value: Short): Short {
        return value
    }

    override fun read(buf: ByteBuffer): Short {
        return buf.getShort()
    }

    override fun lower(value: Short): Short {
        return value
    }

    override fun allocationSize(value: Short) = 2UL

    override fun write(value: Short, buf: ByteBuffer) {
        buf.putShort(value)
    }
}

/**
 * @suppress
 */
public object FfiConverterUInt: FfiConverter<UInt, Int> {
    override fun lift(value: Int): UInt {
        return value.toUInt()
    }

    override fun read(buf: ByteBuffer): UInt {
        return lift(buf.getInt())
    }

    override fun lower(value: UInt): Int {
        return value.toInt()
    }

    override fun allocationSize(value: UInt) = 4UL

    override fun write(value: UInt, buf: ByteBuffer) {
        buf.putInt(value.toInt())
    }
}

/**
 * @suppress
 */
public object FfiConverterInt: FfiConverter<Int, Int> {
    override fun lift(value: Int): Int {
        return value
    }

    override fun read(buf: ByteBuffer): Int {
        return buf.getInt()
    }

    override fun lower(value: Int): Int {
        return value
    }

    override fun allocationSize(value: Int) = 4UL

    override fun write(value: Int, buf: ByteBuffer) {
        buf.putInt(value)
    }
}

/**
 * @suppress
 */
public object FfiConverterULong: FfiConverter<ULong, Long> {
    override fun lift(value: Long): ULong {
        return value.toULong()
    }

    override fun read(buf: ByteBuffer): ULong {
        return lift(buf.getLong())
    }

    override fun lower(value: ULong): Long {
        return value.toLong()
    }

    override fun allocationSize(value: ULong) = 8UL

    override fun write(value: ULong, buf: ByteBuffer) {
        buf.putLong(value.toLong())
    }
}

/**
 * @suppress
 */
public object FfiConverterLong: FfiConverter<Long, Long> {
    override fun lift(value: Long): Long {
        return value
    }

    override fun read(buf: ByteBuffer): Long {
        return buf.getLong()
    }

    override fun lower(value: Long): Long {
        return value
    }

    override fun allocationSize(value: Long) = 8UL

    override fun write(value: Long, buf: ByteBuffer) {
        buf.putLong(value)
    }
}

/**
 * @suppress
 */
public object FfiConverterFloat: FfiConverter<Float, Float> {
    override fun lift(value: Float): Float {
        return value
    }

    override fun read(buf: ByteBuffer): Float {
        return buf.getFloat()
    }

    override fun lower(value: Float): Float {
        return value
    }

    override fun allocationSize(value: Float) = 4UL

    override fun write(value: Float, buf: ByteBuffer) {
        buf.putFloat(value)
    }
}

/**
 * @suppress
 */
public object FfiConverterDouble: FfiConverter<Double, Double> {
    override fun lift(value: Double): Double {
        return value
    }

    override fun read(buf: ByteBuffer): Double {
        return buf.getDouble()
    }

    override fun lower(value: Double): Double {
        return value
    }

    override fun allocationSize(value: Double) = 8UL

    override fun write(value: Double, buf: ByteBuffer) {
        buf.putDouble(value)
    }
}

/**
 * @suppress
 */
public object FfiConverterBoolean: FfiConverter<Boolean, Byte> {
    override fun lift(value: Byte): Boolean {
        return value.toInt() != 0
    }

    override fun read(buf: ByteBuffer): Boolean {
        return lift(buf.get())
    }

    override fun lower(value: Boolean): Byte {
        return if (value) 1.toByte() else 0.toByte()
    }

    override fun allocationSize(value: Boolean) = 1UL

    override fun write(value: Boolean, buf: ByteBuffer) {
        buf.put(lower(value))
    }
}

/**
 * @suppress
 */
public object FfiConverterString: FfiConverter<String, RustBuffer.ByValue> {
    // Note: we don't inherit from FfiConverterRustBuffer, because we use a
    // special encoding when lowering/lifting.  We can use `RustBuffer.len` to
    // store our length and avoid writing it out to the buffer.
    override fun lift(value: RustBuffer.ByValue): String {
        try {
            val byteArr = ByteArray(value.len.toInt())
            value.asByteBuffer()!!.get(byteArr)
            return byteArr.toString(Charsets.UTF_8)
        } finally {
            RustBuffer.free(value)
        }
    }

    override fun read(buf: ByteBuffer): String {
        val len = buf.getInt()
        val byteArr = ByteArray(len)
        buf.get(byteArr)
        return byteArr.toString(Charsets.UTF_8)
    }

    fun toUtf8(value: String): ByteBuffer {
        // Make sure we don't have invalid UTF-16, check for lone surrogates.
        return Charsets.UTF_8.newEncoder().run {
            onMalformedInput(CodingErrorAction.REPORT)
            encode(CharBuffer.wrap(value))
        }
    }

    override fun lower(value: String): RustBuffer.ByValue {
        val byteBuf = toUtf8(value)
        // Ideally we'd pass these bytes to `ffi_bytebuffer_from_bytes`, but doing so would require us
        // to copy them into a JNA `Memory`. So we might as well directly copy them into a `RustBuffer`.
        val rbuf = RustBuffer.alloc(byteBuf.limit().toULong())
        rbuf.asByteBuffer()!!.put(byteBuf)
        return rbuf
    }

    // We aren't sure exactly how many bytes our string will be once it's UTF-8
    // encoded.  Allocate 3 bytes per UTF-16 code unit which will always be
    // enough.
    override fun allocationSize(value: String): ULong {
        val sizeForLength = 4UL
        val sizeForString = value.length.toULong() * 3UL
        return sizeForLength + sizeForString
    }

    override fun write(value: String, buf: ByteBuffer) {
        val byteBuf = toUtf8(value)
        buf.putInt(byteBuf.limit())
        buf.put(byteBuf)
    }
}

/**
 * @suppress
 */
public object FfiConverterByteArray: FfiConverterRustBuffer<ByteArray> {
    override fun read(buf: ByteBuffer): ByteArray {
        val len = buf.getInt()
        val byteArr = ByteArray(len)
        buf.get(byteArr)
        return byteArr
    }
    override fun allocationSize(value: ByteArray): ULong {
        return 4UL + value.size.toULong()
    }
    override fun write(value: ByteArray, buf: ByteBuffer) {
        buf.putInt(value.size)
        buf.put(value)
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface ActionSinkInterface {
    
    fun `complete`()
    
    fun `fail`(`error`: NotificationActionError)
    
    companion object
}

open class ActionSink: Disposable, AutoCloseable, ActionSinkInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_actionsink(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_actionsink(handle, status)
        }
    }

    override fun `complete`()
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_actionsink_complete(
        it,
        _status)
}
    }
    
    

    override fun `fail`(`error`: NotificationActionError)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_actionsink_fail(
        it,
        
        FfiConverterTypeNotificationActionError.lower(`error`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeActionSink: FfiConverter<ActionSink, Long> {
    override fun lower(value: ActionSink): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): ActionSink {
        return ActionSink(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): ActionSink {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: ActionSink) = 8UL

    override fun write(value: ActionSink, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface AmActionSinkInterface {
    
    fun `fail`(`reason`: kotlin.String)
    
    fun `ok`()
    
    companion object
}

open class AmActionSink: Disposable, AutoCloseable, AmActionSinkInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_amactionsink(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_amactionsink(handle, status)
        }
    }

    override fun `fail`(`reason`: kotlin.String)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_amactionsink_fail(
        it,
        
        FfiConverterString.lower(`reason`),_status)
}
    }
    
    

    override fun `ok`()
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_amactionsink_ok(
        it,
        _status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeAmActionSink: FfiConverter<AmActionSink, Long> {
    override fun lower(value: AmActionSink): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): AmActionSink {
        return AmActionSink(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): AmActionSink {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: AmActionSink) = 8UL

    override fun write(value: AmActionSink, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface AmAuthSinkInterface {
    
    fun `complete`(`value`: AmAuthStatus)
    
    fun `fail`(`reason`: kotlin.String)
    
    companion object
}

open class AmAuthSink: Disposable, AutoCloseable, AmAuthSinkInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_amauthsink(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_amauthsink(handle, status)
        }
    }

    override fun `complete`(`value`: AmAuthStatus)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_amauthsink_complete(
        it,
        
        FfiConverterTypeAmAuthStatus.lower(`value`),_status)
}
    }
    
    

    override fun `fail`(`reason`: kotlin.String)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_amauthsink_fail(
        it,
        
        FfiConverterString.lower(`reason`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeAmAuthSink: FfiConverter<AmAuthSink, Long> {
    override fun lower(value: AmAuthSink): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): AmAuthSink {
        return AmAuthSink(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): AmAuthSink {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: AmAuthSink) = 8UL

    override fun write(value: AmAuthSink, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface AmCatalogSinkInterface {
    
    fun `complete`(`value`: kotlin.Boolean?)
    
    fun `fail`(`reason`: kotlin.String)
    
    companion object
}

open class AmCatalogSink: Disposable, AutoCloseable, AmCatalogSinkInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_amcatalogsink(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_amcatalogsink(handle, status)
        }
    }

    override fun `complete`(`value`: kotlin.Boolean?)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_amcatalogsink_complete(
        it,
        
        FfiConverterOptionalBoolean.lower(`value`),_status)
}
    }
    
    

    override fun `fail`(`reason`: kotlin.String)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_amcatalogsink_fail(
        it,
        
        FfiConverterString.lower(`reason`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeAmCatalogSink: FfiConverter<AmCatalogSink, Long> {
    override fun lower(value: AmCatalogSink): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): AmCatalogSink {
        return AmCatalogSink(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): AmCatalogSink {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: AmCatalogSink) = 8UL

    override fun write(value: AmCatalogSink, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface AmFavoritesSinkInterface {
    
    fun `complete`(`value`: List<kotlin.Boolean>)
    
    fun `fail`(`reason`: kotlin.String)
    
    companion object
}

open class AmFavoritesSink: Disposable, AutoCloseable, AmFavoritesSinkInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_amfavoritessink(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_amfavoritessink(handle, status)
        }
    }

    override fun `complete`(`value`: List<kotlin.Boolean>)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_amfavoritessink_complete(
        it,
        
        FfiConverterSequenceBoolean.lower(`value`),_status)
}
    }
    
    

    override fun `fail`(`reason`: kotlin.String)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_amfavoritessink_fail(
        it,
        
        FfiConverterString.lower(`reason`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeAmFavoritesSink: FfiConverter<AmFavoritesSink, Long> {
    override fun lower(value: AmFavoritesSink): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): AmFavoritesSink {
        return AmFavoritesSink(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): AmFavoritesSink {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: AmFavoritesSink) = 8UL

    override fun write(value: AmFavoritesSink, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface AmFlagSinkInterface {
    
    fun `complete`(`value`: kotlin.Boolean)
    
    fun `fail`(`reason`: kotlin.String)
    
    companion object
}

open class AmFlagSink: Disposable, AutoCloseable, AmFlagSinkInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_amflagsink(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_amflagsink(handle, status)
        }
    }

    override fun `complete`(`value`: kotlin.Boolean)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_amflagsink_complete(
        it,
        
        FfiConverterBoolean.lower(`value`),_status)
}
    }
    
    

    override fun `fail`(`reason`: kotlin.String)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_amflagsink_fail(
        it,
        
        FfiConverterString.lower(`reason`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeAmFlagSink: FfiConverter<AmFlagSink, Long> {
    override fun lower(value: AmFlagSink): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): AmFlagSink {
        return AmFlagSink(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): AmFlagSink {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: AmFlagSink) = 8UL

    override fun write(value: AmFlagSink, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface AmItemSinkInterface {
    
    fun `complete`(`value`: AmItem)
    
    fun `fail`(`reason`: kotlin.String)
    
    companion object
}

open class AmItemSink: Disposable, AutoCloseable, AmItemSinkInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_amitemsink(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_amitemsink(handle, status)
        }
    }

    override fun `complete`(`value`: AmItem)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_amitemsink_complete(
        it,
        
        FfiConverterTypeAmItem.lower(`value`),_status)
}
    }
    
    

    override fun `fail`(`reason`: kotlin.String)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_amitemsink_fail(
        it,
        
        FfiConverterString.lower(`reason`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeAmItemSink: FfiConverter<AmItemSink, Long> {
    override fun lower(value: AmItemSink): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): AmItemSink {
        return AmItemSink(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): AmItemSink {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: AmItemSink) = 8UL

    override fun write(value: AmItemSink, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface AmPageSinkInterface {
    
    fun `complete`(`value`: AmPage)
    
    fun `fail`(`reason`: kotlin.String)
    
    companion object
}

open class AmPageSink: Disposable, AutoCloseable, AmPageSinkInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_ampagesink(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_ampagesink(handle, status)
        }
    }

    override fun `complete`(`value`: AmPage)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_ampagesink_complete(
        it,
        
        FfiConverterTypeAmPage.lower(`value`),_status)
}
    }
    
    

    override fun `fail`(`reason`: kotlin.String)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_ampagesink_fail(
        it,
        
        FfiConverterString.lower(`reason`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeAmPageSink: FfiConverter<AmPageSink, Long> {
    override fun lower(value: AmPageSink): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): AmPageSink {
        return AmPageSink(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): AmPageSink {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: AmPageSink) = 8UL

    override fun write(value: AmPageSink, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface AmPlayerInboxInterface {
    
    fun `onChanged`()
    
    companion object
}

open class AmPlayerInbox: Disposable, AutoCloseable, AmPlayerInboxInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_amplayerinbox(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_amplayerinbox(handle, status)
        }
    }

    override fun `onChanged`()
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_amplayerinbox_on_changed(
        it,
        _status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeAmPlayerInbox: FfiConverter<AmPlayerInbox, Long> {
    override fun lower(value: AmPlayerInbox): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): AmPlayerInbox {
        return AmPlayerInbox(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): AmPlayerInbox {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: AmPlayerInbox) = 8UL

    override fun write(value: AmPlayerInbox, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface AmSearchSinkInterface {
    
    fun `complete`(`value`: AmSearchResults)
    
    fun `fail`(`reason`: kotlin.String)
    
    companion object
}

open class AmSearchSink: Disposable, AutoCloseable, AmSearchSinkInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_amsearchsink(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_amsearchsink(handle, status)
        }
    }

    override fun `complete`(`value`: AmSearchResults)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_amsearchsink_complete(
        it,
        
        FfiConverterTypeAmSearchResults.lower(`value`),_status)
}
    }
    
    

    override fun `fail`(`reason`: kotlin.String)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_amsearchsink_fail(
        it,
        
        FfiConverterString.lower(`reason`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeAmSearchSink: FfiConverter<AmSearchSink, Long> {
    override fun lower(value: AmSearchSink): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): AmSearchSink {
        return AmSearchSink(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): AmSearchSink {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: AmSearchSink) = 8UL

    override fun write(value: AmSearchSink, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface AmShelvesSinkInterface {
    
    fun `complete`(`value`: List<AmShelf>)
    
    fun `fail`(`reason`: kotlin.String)
    
    companion object
}

open class AmShelvesSink: Disposable, AutoCloseable, AmShelvesSinkInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_amshelvessink(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_amshelvessink(handle, status)
        }
    }

    override fun `complete`(`value`: List<AmShelf>)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_amshelvessink_complete(
        it,
        
        FfiConverterSequenceTypeAmShelf.lower(`value`),_status)
}
    }
    
    

    override fun `fail`(`reason`: kotlin.String)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_amshelvessink_fail(
        it,
        
        FfiConverterString.lower(`reason`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeAmShelvesSink: FfiConverter<AmShelvesSink, Long> {
    override fun lower(value: AmShelvesSink): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): AmShelvesSink {
        return AmShelvesSink(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): AmShelvesSink {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: AmShelvesSink) = 8UL

    override fun write(value: AmShelvesSink, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface AmSnapshotSinkInterface {
    
    fun `complete`(`value`: AmPlayerSnapshot)
    
    fun `fail`(`reason`: kotlin.String)
    
    companion object
}

open class AmSnapshotSink: Disposable, AutoCloseable, AmSnapshotSinkInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_amsnapshotsink(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_amsnapshotsink(handle, status)
        }
    }

    override fun `complete`(`value`: AmPlayerSnapshot)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_amsnapshotsink_complete(
        it,
        
        FfiConverterTypeAmPlayerSnapshot.lower(`value`),_status)
}
    }
    
    

    override fun `fail`(`reason`: kotlin.String)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_amsnapshotsink_fail(
        it,
        
        FfiConverterString.lower(`reason`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeAmSnapshotSink: FfiConverter<AmSnapshotSink, Long> {
    override fun lower(value: AmSnapshotSink): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): AmSnapshotSink {
        return AmSnapshotSink(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): AmSnapshotSink {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: AmSnapshotSink) = 8UL

    override fun write(value: AmSnapshotSink, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface AppleMusicBackend {
    
    fun `start`(`inbox`: AmPlayerInbox)
    
    fun `stop`()
    
    fun `snapshot`(`sink`: AmSnapshotSink)
    
    fun `authStatus`(`sink`: AmAuthSink)
    
    fun `requestAuthorization`(`sink`: AmAuthSink)
    
    fun `canPlayCatalogContent`(`sink`: AmCatalogSink)
    
    fun `isOtherAudioPlaying`(`sink`: AmFlagSink)
    
    fun `playContext`(`contextUri`: kotlin.String, `startAtUri`: kotlin.String?, `sink`: AmActionSink)
    
    fun `queueInsert`(`uri`: kotlin.String, `next`: kotlin.Boolean, `sink`: AmActionSink)
    
    fun `command`(`cmd`: AmPlayerCommand, `sink`: AmActionSink)
    
    fun `library`(`scope`: AmLibraryScope, `limit`: kotlin.UInt, `offset`: kotlin.UInt, `sink`: AmPageSink)
    
    fun `recommendations`(`sink`: AmShelvesSink)
    
    fun `resolve`(`uri`: kotlin.String, `sink`: AmItemSink)
    
    fun `search`(`query`: kotlin.String, `limit`: kotlin.UInt, `sink`: AmSearchSink)
    
    fun `isFavorite`(`uris`: List<kotlin.String>, `sink`: AmFavoritesSink)
    
    fun `addFavorite`(`uri`: kotlin.String, `sink`: AmActionSink)
    
    companion object
}

open class AppleMusicBackendImpl: Disposable, AutoCloseable, AppleMusicBackend
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_applemusicbackend(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_applemusicbackend(handle, status)
        }
    }

    override fun `start`(`inbox`: AmPlayerInbox)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_applemusicbackend_start(
        it,
        
        FfiConverterTypeAmPlayerInbox.lower(`inbox`),_status)
}
    }
    
    

    override fun `stop`()
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_applemusicbackend_stop(
        it,
        _status)
}
    }
    
    

    override fun `snapshot`(`sink`: AmSnapshotSink)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_applemusicbackend_snapshot(
        it,
        
        FfiConverterTypeAmSnapshotSink.lower(`sink`),_status)
}
    }
    
    

    override fun `authStatus`(`sink`: AmAuthSink)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_applemusicbackend_auth_status(
        it,
        
        FfiConverterTypeAmAuthSink.lower(`sink`),_status)
}
    }
    
    

    override fun `requestAuthorization`(`sink`: AmAuthSink)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_applemusicbackend_request_authorization(
        it,
        
        FfiConverterTypeAmAuthSink.lower(`sink`),_status)
}
    }
    
    

    override fun `canPlayCatalogContent`(`sink`: AmCatalogSink)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_applemusicbackend_can_play_catalog_content(
        it,
        
        FfiConverterTypeAmCatalogSink.lower(`sink`),_status)
}
    }
    
    

    override fun `isOtherAudioPlaying`(`sink`: AmFlagSink)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_applemusicbackend_is_other_audio_playing(
        it,
        
        FfiConverterTypeAmFlagSink.lower(`sink`),_status)
}
    }
    
    

    override fun `playContext`(`contextUri`: kotlin.String, `startAtUri`: kotlin.String?, `sink`: AmActionSink)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_applemusicbackend_play_context(
        it,
        
        FfiConverterString.lower(`contextUri`),
        FfiConverterOptionalString.lower(`startAtUri`),
        FfiConverterTypeAmActionSink.lower(`sink`),_status)
}
    }
    
    

    override fun `queueInsert`(`uri`: kotlin.String, `next`: kotlin.Boolean, `sink`: AmActionSink)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_applemusicbackend_queue_insert(
        it,
        
        FfiConverterString.lower(`uri`),
        FfiConverterBoolean.lower(`next`),
        FfiConverterTypeAmActionSink.lower(`sink`),_status)
}
    }
    
    

    override fun `command`(`cmd`: AmPlayerCommand, `sink`: AmActionSink)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_applemusicbackend_command(
        it,
        
        FfiConverterTypeAmPlayerCommand.lower(`cmd`),
        FfiConverterTypeAmActionSink.lower(`sink`),_status)
}
    }
    
    

    override fun `library`(`scope`: AmLibraryScope, `limit`: kotlin.UInt, `offset`: kotlin.UInt, `sink`: AmPageSink)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_applemusicbackend_library(
        it,
        
        FfiConverterTypeAmLibraryScope.lower(`scope`),
        FfiConverterUInt.lower(`limit`),
        FfiConverterUInt.lower(`offset`),
        FfiConverterTypeAmPageSink.lower(`sink`),_status)
}
    }
    
    

    override fun `recommendations`(`sink`: AmShelvesSink)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_applemusicbackend_recommendations(
        it,
        
        FfiConverterTypeAmShelvesSink.lower(`sink`),_status)
}
    }
    
    

    override fun `resolve`(`uri`: kotlin.String, `sink`: AmItemSink)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_applemusicbackend_resolve(
        it,
        
        FfiConverterString.lower(`uri`),
        FfiConverterTypeAmItemSink.lower(`sink`),_status)
}
    }
    
    

    override fun `search`(`query`: kotlin.String, `limit`: kotlin.UInt, `sink`: AmSearchSink)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_applemusicbackend_search(
        it,
        
        FfiConverterString.lower(`query`),
        FfiConverterUInt.lower(`limit`),
        FfiConverterTypeAmSearchSink.lower(`sink`),_status)
}
    }
    
    

    override fun `isFavorite`(`uris`: List<kotlin.String>, `sink`: AmFavoritesSink)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_applemusicbackend_is_favorite(
        it,
        
        FfiConverterSequenceString.lower(`uris`),
        FfiConverterTypeAmFavoritesSink.lower(`sink`),_status)
}
    }
    
    

    override fun `addFavorite`(`uri`: kotlin.String, `sink`: AmActionSink)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_applemusicbackend_add_favorite(
        it,
        
        FfiConverterString.lower(`uri`),
        FfiConverterTypeAmActionSink.lower(`sink`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}



// Put the implementation in an object so we don't pollute the top-level namespace
internal object uniffiCallbackInterfaceAppleMusicBackend {
    internal object `start`: UniffiCallbackInterfaceAppleMusicBackendMethod0 {
        override fun callback(`uniffiHandle`: Long,`inbox`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeAppleMusicBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`start`(
                    FfiConverterTypeAmPlayerInbox.lift(`inbox`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `stop`: UniffiCallbackInterfaceAppleMusicBackendMethod1 {
        override fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeAppleMusicBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`stop`(
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `snapshot`: UniffiCallbackInterfaceAppleMusicBackendMethod2 {
        override fun callback(`uniffiHandle`: Long,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeAppleMusicBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`snapshot`(
                    FfiConverterTypeAmSnapshotSink.lift(`sink`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `authStatus`: UniffiCallbackInterfaceAppleMusicBackendMethod3 {
        override fun callback(`uniffiHandle`: Long,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeAppleMusicBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`authStatus`(
                    FfiConverterTypeAmAuthSink.lift(`sink`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `requestAuthorization`: UniffiCallbackInterfaceAppleMusicBackendMethod4 {
        override fun callback(`uniffiHandle`: Long,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeAppleMusicBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`requestAuthorization`(
                    FfiConverterTypeAmAuthSink.lift(`sink`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `canPlayCatalogContent`: UniffiCallbackInterfaceAppleMusicBackendMethod5 {
        override fun callback(`uniffiHandle`: Long,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeAppleMusicBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`canPlayCatalogContent`(
                    FfiConverterTypeAmCatalogSink.lift(`sink`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `isOtherAudioPlaying`: UniffiCallbackInterfaceAppleMusicBackendMethod6 {
        override fun callback(`uniffiHandle`: Long,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeAppleMusicBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`isOtherAudioPlaying`(
                    FfiConverterTypeAmFlagSink.lift(`sink`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `playContext`: UniffiCallbackInterfaceAppleMusicBackendMethod7 {
        override fun callback(`uniffiHandle`: Long,`contextUri`: RustBuffer.ByValue,`startAtUri`: RustBuffer.ByValue,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeAppleMusicBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`playContext`(
                    FfiConverterString.lift(`contextUri`),
                    FfiConverterOptionalString.lift(`startAtUri`),
                    FfiConverterTypeAmActionSink.lift(`sink`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `queueInsert`: UniffiCallbackInterfaceAppleMusicBackendMethod8 {
        override fun callback(`uniffiHandle`: Long,`uri`: RustBuffer.ByValue,`next`: Byte,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeAppleMusicBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`queueInsert`(
                    FfiConverterString.lift(`uri`),
                    FfiConverterBoolean.lift(`next`),
                    FfiConverterTypeAmActionSink.lift(`sink`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `command`: UniffiCallbackInterfaceAppleMusicBackendMethod9 {
        override fun callback(`uniffiHandle`: Long,`cmd`: RustBuffer.ByValue,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeAppleMusicBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`command`(
                    FfiConverterTypeAmPlayerCommand.lift(`cmd`),
                    FfiConverterTypeAmActionSink.lift(`sink`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `library`: UniffiCallbackInterfaceAppleMusicBackendMethod10 {
        override fun callback(`uniffiHandle`: Long,`scope`: RustBuffer.ByValue,`limit`: Int,`offset`: Int,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeAppleMusicBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`library`(
                    FfiConverterTypeAmLibraryScope.lift(`scope`),
                    FfiConverterUInt.lift(`limit`),
                    FfiConverterUInt.lift(`offset`),
                    FfiConverterTypeAmPageSink.lift(`sink`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `recommendations`: UniffiCallbackInterfaceAppleMusicBackendMethod11 {
        override fun callback(`uniffiHandle`: Long,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeAppleMusicBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`recommendations`(
                    FfiConverterTypeAmShelvesSink.lift(`sink`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `resolve`: UniffiCallbackInterfaceAppleMusicBackendMethod12 {
        override fun callback(`uniffiHandle`: Long,`uri`: RustBuffer.ByValue,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeAppleMusicBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`resolve`(
                    FfiConverterString.lift(`uri`),
                    FfiConverterTypeAmItemSink.lift(`sink`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `search`: UniffiCallbackInterfaceAppleMusicBackendMethod13 {
        override fun callback(`uniffiHandle`: Long,`query`: RustBuffer.ByValue,`limit`: Int,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeAppleMusicBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`search`(
                    FfiConverterString.lift(`query`),
                    FfiConverterUInt.lift(`limit`),
                    FfiConverterTypeAmSearchSink.lift(`sink`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `isFavorite`: UniffiCallbackInterfaceAppleMusicBackendMethod14 {
        override fun callback(`uniffiHandle`: Long,`uris`: RustBuffer.ByValue,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeAppleMusicBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`isFavorite`(
                    FfiConverterSequenceString.lift(`uris`),
                    FfiConverterTypeAmFavoritesSink.lift(`sink`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `addFavorite`: UniffiCallbackInterfaceAppleMusicBackendMethod15 {
        override fun callback(`uniffiHandle`: Long,`uri`: RustBuffer.ByValue,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeAppleMusicBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`addFavorite`(
                    FfiConverterString.lift(`uri`),
                    FfiConverterTypeAmActionSink.lift(`sink`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }

    internal object uniffiFree: UniffiCallbackInterfaceFree {
        override fun callback(handle: Long) {
            FfiConverterTypeAppleMusicBackend.handleMap.remove(handle)
        }
    }

    internal object uniffiClone: UniffiCallbackInterfaceClone {
        override fun callback(handle: Long): Long {
            return FfiConverterTypeAppleMusicBackend.handleMap.clone(handle)
        }
    }

    internal var vtable = UniffiVTableCallbackInterfaceAppleMusicBackend.UniffiByValue(
        uniffiFree,
        uniffiClone,
        `start`,
        `stop`,
        `snapshot`,
        `authStatus`,
        `requestAuthorization`,
        `canPlayCatalogContent`,
        `isOtherAudioPlaying`,
        `playContext`,
        `queueInsert`,
        `command`,
        `library`,
        `recommendations`,
        `resolve`,
        `search`,
        `isFavorite`,
        `addFavorite`,
    )

    // Registers the foreign callback with the Rust side.
    // This method is generated for each callback interface.
    internal fun register(lib: UniffiLib) {
        lib.uniffi_bridgething_companion_fn_init_callback_vtable_applemusicbackend(vtable)
    }
}

/**
 * @suppress
 */
public object FfiConverterTypeAppleMusicBackend: FfiConverter<AppleMusicBackend, Long> {
    internal val handleMap = UniffiHandleMap<AppleMusicBackend>()

    override fun lower(value: AppleMusicBackend): Long {
        if (value is AppleMusicBackendImpl) {
             // Rust-implemented object.  Clone the handle and return it
            return value.uniffiCloneHandle()
         } else {
            // Kotlin object, generate a new vtable handle and return that.
            return handleMap.insert(value)
         }
    }

    override fun lift(value: Long): AppleMusicBackend {
        if ((value and 1.toLong()) == 0.toLong()) {
            // Rust-generated handle, construct a new class that uses the handle to implement the
            // interface
            return AppleMusicBackendImpl(UniffiWithHandle, value)
        } else {
            // Kotlin-generated handle, get the object from the handle map
            return handleMap.remove(value)
        }
    }

    override fun read(buf: ByteBuffer): AppleMusicBackend {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: AppleMusicBackend) = 8UL

    override fun write(value: AppleMusicBackend, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface AudioBackend {
    
    fun `speak`(`id`: kotlin.String, `text`: kotlin.String, `voice`: kotlin.String?, `sink`: SpeakSink)
    
    fun `cancel`(`id`: kotlin.String)
    
    fun `cancelAll`()
    
    fun `playEarcon`(`name`: kotlin.String, `sink`: EarconSink)
    
    companion object
}

open class AudioBackendImpl: Disposable, AutoCloseable, AudioBackend
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_audiobackend(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_audiobackend(handle, status)
        }
    }

    override fun `speak`(`id`: kotlin.String, `text`: kotlin.String, `voice`: kotlin.String?, `sink`: SpeakSink)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_audiobackend_speak(
        it,
        
        FfiConverterString.lower(`id`),
        FfiConverterString.lower(`text`),
        FfiConverterOptionalString.lower(`voice`),
        FfiConverterTypeSpeakSink.lower(`sink`),_status)
}
    }
    
    

    override fun `cancel`(`id`: kotlin.String)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_audiobackend_cancel(
        it,
        
        FfiConverterString.lower(`id`),_status)
}
    }
    
    

    override fun `cancelAll`()
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_audiobackend_cancel_all(
        it,
        _status)
}
    }
    
    

    override fun `playEarcon`(`name`: kotlin.String, `sink`: EarconSink)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_audiobackend_play_earcon(
        it,
        
        FfiConverterString.lower(`name`),
        FfiConverterTypeEarconSink.lower(`sink`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}



// Put the implementation in an object so we don't pollute the top-level namespace
internal object uniffiCallbackInterfaceAudioBackend {
    internal object `speak`: UniffiCallbackInterfaceAudioBackendMethod0 {
        override fun callback(`uniffiHandle`: Long,`id`: RustBuffer.ByValue,`text`: RustBuffer.ByValue,`voice`: RustBuffer.ByValue,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeAudioBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`speak`(
                    FfiConverterString.lift(`id`),
                    FfiConverterString.lift(`text`),
                    FfiConverterOptionalString.lift(`voice`),
                    FfiConverterTypeSpeakSink.lift(`sink`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `cancel`: UniffiCallbackInterfaceAudioBackendMethod1 {
        override fun callback(`uniffiHandle`: Long,`id`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeAudioBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`cancel`(
                    FfiConverterString.lift(`id`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `cancelAll`: UniffiCallbackInterfaceAudioBackendMethod2 {
        override fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeAudioBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`cancelAll`(
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `playEarcon`: UniffiCallbackInterfaceAudioBackendMethod3 {
        override fun callback(`uniffiHandle`: Long,`name`: RustBuffer.ByValue,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeAudioBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`playEarcon`(
                    FfiConverterString.lift(`name`),
                    FfiConverterTypeEarconSink.lift(`sink`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }

    internal object uniffiFree: UniffiCallbackInterfaceFree {
        override fun callback(handle: Long) {
            FfiConverterTypeAudioBackend.handleMap.remove(handle)
        }
    }

    internal object uniffiClone: UniffiCallbackInterfaceClone {
        override fun callback(handle: Long): Long {
            return FfiConverterTypeAudioBackend.handleMap.clone(handle)
        }
    }

    internal var vtable = UniffiVTableCallbackInterfaceAudioBackend.UniffiByValue(
        uniffiFree,
        uniffiClone,
        `speak`,
        `cancel`,
        `cancelAll`,
        `playEarcon`,
    )

    // Registers the foreign callback with the Rust side.
    // This method is generated for each callback interface.
    internal fun register(lib: UniffiLib) {
        lib.uniffi_bridgething_companion_fn_init_callback_vtable_audiobackend(vtable)
    }
}

/**
 * @suppress
 */
public object FfiConverterTypeAudioBackend: FfiConverter<AudioBackend, Long> {
    internal val handleMap = UniffiHandleMap<AudioBackend>()

    override fun lower(value: AudioBackend): Long {
        if (value is AudioBackendImpl) {
             // Rust-implemented object.  Clone the handle and return it
            return value.uniffiCloneHandle()
         } else {
            // Kotlin object, generate a new vtable handle and return that.
            return handleMap.insert(value)
         }
    }

    override fun lift(value: Long): AudioBackend {
        if ((value and 1.toLong()) == 0.toLong()) {
            // Rust-generated handle, construct a new class that uses the handle to implement the
            // interface
            return AudioBackendImpl(UniffiWithHandle, value)
        } else {
            // Kotlin-generated handle, get the object from the handle map
            return handleMap.remove(value)
        }
    }

    override fun read(buf: ByteBuffer): AudioBackend {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: AudioBackend) = 8UL

    override fun write(value: AudioBackend, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface CompanionSessionInterface {
    
    suspend fun `applyOtaUpdate`(`deviceId`: kotlin.String, `channel`: kotlin.String, `version`: kotlin.String, `rootUrl`: kotlin.String)
    
    suspend fun `availableProviders`(): List<ProviderInfo>
    
    suspend fun `cancelAuth`(`id`: kotlin.String)
    
    suspend fun `checkForOtaUpdate`(`rootUrl`: kotlin.String)
    
    suspend fun `collectScreenshot`(`deviceId`: kotlin.String, `transferId`: kotlin.String, `capturedAtMs`: kotlin.ULong): ScreenshotFile
    
    fun `companionDebug`(): CompanionDebug
    
    suspend fun `completeProviderAuth`(`id`: kotlin.String, `tokens`: ProviderTokens)
    
    suspend fun `connectNetwork`(`url`: kotlin.String, `device`: LinkDevice)
    
    suspend fun `connectProvider`(`id`: kotlin.String)
    
    suspend fun `currentWebapp`(`deviceId`: kotlin.String): ActiveWebapp?
    
    fun `defaultResumeTarget`(): ResumeTarget
    
    suspend fun `deleteWebappConfigField`(`deviceId`: kotlin.String, `id`: kotlin.String, `key`: kotlin.String)
    
    suspend fun `deleteWebappDoc`(`deviceId`: kotlin.String, `id`: kotlin.String, `key`: kotlin.String)
    
    fun `deviceLogSnapshot`(`limit`: kotlin.UInt): List<DeviceLogLine>
    
    suspend fun `deviceSetNickname`(`deviceId`: kotlin.String, `nickname`: kotlin.String)
    
    suspend fun `disconnectNetwork`(`deviceId`: kotlin.String)
    
    suspend fun `disconnectProvider`(`id`: kotlin.String)
    
    suspend fun `dismissOtaRun`(`deviceId`: kotlin.String)
    
    suspend fun `downloadCompanionUpdate`(`url`: kotlin.String, `filename`: kotlin.String, `expected`: ArtifactDigest): kotlin.String
    
    suspend fun `downloadVoiceModel`()
    
    suspend fun `fetchOtaManifest`(`rootUrl`: kotlin.String): OtaDiscoverManifest
    
    suspend fun `getWebappDoc`(`deviceId`: kotlin.String, `id`: kotlin.String, `key`: kotlin.String): kotlin.String?
    
    suspend fun `installWebapp`(`deviceId`: kotlin.String, `archivePath`: kotlin.String, `provenance`: kotlin.String?): WebappInfo
    
    suspend fun `installWebappFromUrl`(`deviceId`: kotlin.String, `url`: kotlin.String, `expected`: ArtifactDigest?, `provenance`: kotlin.String?, `sink`: WebappBundleSink? = null): WebappInfo
    
    suspend fun `listWebappConfig`(`deviceId`: kotlin.String, `id`: kotlin.String): List<ConfigEntry>
    
    suspend fun `listWebappDoc`(`deviceId`: kotlin.String, `id`: kotlin.String): List<DocEntry>
    
    suspend fun `listWebapps`(`deviceId`: kotlin.String): List<WebappInfo>
    
    fun `logInbox`(): LogInbox
    
    fun `otaRunProgress`(`deviceId`: kotlin.String, `nowMs`: kotlin.ULong): OtaRunProgress?
    
    suspend fun `resumed`()
    
    suspend fun `setCapabilityFlags`(`flags`: CapabilityFlags)
    
    suspend fun `setDeviceAutoResume`(`deviceId`: kotlin.String, `enabled`: kotlin.Boolean)
    
    suspend fun `setDeviceLogStreaming`(`enabled`: kotlin.Boolean)
    
    suspend fun `setDeviceResumeTarget`(`deviceId`: kotlin.String, `target`: ResumeTarget)
    
    suspend fun `setOtaPollConfig`(`config`: OtaPollConfig?)
    
    suspend fun `setProviderPriority`(`ids`: List<kotlin.String>)
    
    suspend fun `setWebappConfigField`(`deviceId`: kotlin.String, `id`: kotlin.String, `key`: kotlin.String, `value`: kotlin.String)
    
    suspend fun `setWebappDoc`(`deviceId`: kotlin.String, `id`: kotlin.String, `key`: kotlin.String, `value`: kotlin.String)
    
    suspend fun `setWebappSlot`(`deviceId`: kotlin.String, `slot`: WebappSlot, `id`: kotlin.String?): WebappSlots
    
    suspend fun `snapshot`(): SessionSnapshot
    
    suspend fun `start`()
    
    suspend fun `stop`()
    
    suspend fun `switchWebapp`(`deviceId`: kotlin.String, `id`: kotlin.String)
    
    suspend fun `timeChanged`()
    
    suspend fun `uninstallWebapp`(`deviceId`: kotlin.String, `id`: kotlin.String)
    
    fun `voiceModelPaths`(): VoiceModelPaths
    
    suspend fun `webappResource`(`deviceId`: kotlin.String, `id`: kotlin.String, `kind`: WebappResourceKind, `origin`: WebappResourceOrigin?): WebappResourceFile
    
    suspend fun `webappSlots`(`deviceId`: kotlin.String): WebappSlots
    
    companion object
}

open class CompanionSession: Disposable, AutoCloseable, CompanionSessionInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_companionsession(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_companionsession(handle, status)
        }
    }

    
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `applyOtaUpdate`(`deviceId`: kotlin.String, `channel`: kotlin.String, `version`: kotlin.String, `rootUrl`: kotlin.String) {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_apply_ota_update(
                uniffiHandle,
                
        FfiConverterString.lower(`deviceId`),
        FfiConverterString.lower(`channel`),
        FfiConverterString.lower(`version`),
        FfiConverterString.lower(`rootUrl`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_void(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_void(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_void(future) },
        // lift function
        { Unit },
        
        // Error FFI converter
        UniffiNullRustCallStatusErrorHandler,
    )
    }

    
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `availableProviders`() : List<ProviderInfo> {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_available_providers(
                uniffiHandle,
                
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_rust_buffer(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_rust_buffer(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_rust_buffer(future) },
        // lift function
        { FfiConverterSequenceTypeProviderInfo.lift(it) },
        // Error FFI converter
        UniffiNullRustCallStatusErrorHandler,
    )
    }

    
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `cancelAuth`(`id`: kotlin.String) {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_cancel_auth(
                uniffiHandle,
                
        FfiConverterString.lower(`id`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_void(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_void(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_void(future) },
        // lift function
        { Unit },
        
        // Error FFI converter
        UniffiNullRustCallStatusErrorHandler,
    )
    }

    
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `checkForOtaUpdate`(`rootUrl`: kotlin.String) {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_check_for_ota_update(
                uniffiHandle,
                
        FfiConverterString.lower(`rootUrl`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_void(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_void(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_void(future) },
        // lift function
        { Unit },
        
        // Error FFI converter
        UniffiNullRustCallStatusErrorHandler,
    )
    }

    
    @Throws(CompanionException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `collectScreenshot`(`deviceId`: kotlin.String, `transferId`: kotlin.String, `capturedAtMs`: kotlin.ULong) : ScreenshotFile {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_collect_screenshot(
                uniffiHandle,
                
        FfiConverterString.lower(`deviceId`),
        FfiConverterString.lower(`transferId`),
        FfiConverterULong.lower(`capturedAtMs`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_rust_buffer(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_rust_buffer(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_rust_buffer(future) },
        // lift function
        { FfiConverterTypeScreenshotFile.lift(it) },
        // Error FFI converter
        CompanionException.ErrorHandler,
    )
    }

    override fun `companionDebug`(): CompanionDebug {
            return FfiConverterTypeCompanionDebug.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_companion_debug(
        it,
        _status)
}
    }
    )
    }
    

    
    @Throws(CompanionException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `completeProviderAuth`(`id`: kotlin.String, `tokens`: ProviderTokens) {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_complete_provider_auth(
                uniffiHandle,
                
        FfiConverterString.lower(`id`),
        FfiConverterTypeProviderTokens.lower(`tokens`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_void(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_void(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_void(future) },
        // lift function
        { Unit },
        
        // Error FFI converter
        CompanionException.ErrorHandler,
    )
    }

    
    @Throws(CompanionException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `connectNetwork`(`url`: kotlin.String, `device`: LinkDevice) {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_connect_network(
                uniffiHandle,
                
        FfiConverterString.lower(`url`),
        FfiConverterTypeLinkDevice.lower(`device`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_void(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_void(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_void(future) },
        // lift function
        { Unit },
        
        // Error FFI converter
        CompanionException.ErrorHandler,
    )
    }

    
    @Throws(CompanionException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `connectProvider`(`id`: kotlin.String) {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_connect_provider(
                uniffiHandle,
                
        FfiConverterString.lower(`id`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_void(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_void(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_void(future) },
        // lift function
        { Unit },
        
        // Error FFI converter
        CompanionException.ErrorHandler,
    )
    }

    
    @Throws(CompanionException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `currentWebapp`(`deviceId`: kotlin.String) : ActiveWebapp? {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_current_webapp(
                uniffiHandle,
                
        FfiConverterString.lower(`deviceId`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_rust_buffer(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_rust_buffer(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_rust_buffer(future) },
        // lift function
        { FfiConverterOptionalTypeActiveWebapp.lift(it) },
        // Error FFI converter
        CompanionException.ErrorHandler,
    )
    }

    override fun `defaultResumeTarget`(): ResumeTarget {
            return FfiConverterTypeResumeTarget.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_default_resume_target(
        it,
        _status)
}
    }
    )
    }
    

    
    @Throws(CompanionException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `deleteWebappConfigField`(`deviceId`: kotlin.String, `id`: kotlin.String, `key`: kotlin.String) {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_delete_webapp_config_field(
                uniffiHandle,
                
        FfiConverterString.lower(`deviceId`),
        FfiConverterString.lower(`id`),
        FfiConverterString.lower(`key`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_void(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_void(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_void(future) },
        // lift function
        { Unit },
        
        // Error FFI converter
        CompanionException.ErrorHandler,
    )
    }

    
    @Throws(CompanionException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `deleteWebappDoc`(`deviceId`: kotlin.String, `id`: kotlin.String, `key`: kotlin.String) {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_delete_webapp_doc(
                uniffiHandle,
                
        FfiConverterString.lower(`deviceId`),
        FfiConverterString.lower(`id`),
        FfiConverterString.lower(`key`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_void(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_void(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_void(future) },
        // lift function
        { Unit },
        
        // Error FFI converter
        CompanionException.ErrorHandler,
    )
    }

    override fun `deviceLogSnapshot`(`limit`: kotlin.UInt): List<DeviceLogLine> {
            return FfiConverterSequenceTypeDeviceLogLine.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_device_log_snapshot(
        it,
        
        FfiConverterUInt.lower(`limit`),_status)
}
    }
    )
    }
    

    
    @Throws(CompanionException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `deviceSetNickname`(`deviceId`: kotlin.String, `nickname`: kotlin.String) {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_device_set_nickname(
                uniffiHandle,
                
        FfiConverterString.lower(`deviceId`),
        FfiConverterString.lower(`nickname`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_void(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_void(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_void(future) },
        // lift function
        { Unit },
        
        // Error FFI converter
        CompanionException.ErrorHandler,
    )
    }

    
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `disconnectNetwork`(`deviceId`: kotlin.String) {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_disconnect_network(
                uniffiHandle,
                
        FfiConverterString.lower(`deviceId`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_void(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_void(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_void(future) },
        // lift function
        { Unit },
        
        // Error FFI converter
        UniffiNullRustCallStatusErrorHandler,
    )
    }

    
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `disconnectProvider`(`id`: kotlin.String) {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_disconnect_provider(
                uniffiHandle,
                
        FfiConverterString.lower(`id`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_void(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_void(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_void(future) },
        // lift function
        { Unit },
        
        // Error FFI converter
        UniffiNullRustCallStatusErrorHandler,
    )
    }

    
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `dismissOtaRun`(`deviceId`: kotlin.String) {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_dismiss_ota_run(
                uniffiHandle,
                
        FfiConverterString.lower(`deviceId`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_void(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_void(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_void(future) },
        // lift function
        { Unit },
        
        // Error FFI converter
        UniffiNullRustCallStatusErrorHandler,
    )
    }

    
    @Throws(CompanionException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `downloadCompanionUpdate`(`url`: kotlin.String, `filename`: kotlin.String, `expected`: ArtifactDigest) : kotlin.String {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_download_companion_update(
                uniffiHandle,
                
        FfiConverterString.lower(`url`),
        FfiConverterString.lower(`filename`),
        FfiConverterTypeArtifactDigest.lower(`expected`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_rust_buffer(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_rust_buffer(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_rust_buffer(future) },
        // lift function
        { FfiConverterString.lift(it) },
        // Error FFI converter
        CompanionException.ErrorHandler,
    )
    }

    
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `downloadVoiceModel`() {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_download_voice_model(
                uniffiHandle,
                
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_void(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_void(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_void(future) },
        // lift function
        { Unit },
        
        // Error FFI converter
        UniffiNullRustCallStatusErrorHandler,
    )
    }

    
    @Throws(CompanionException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `fetchOtaManifest`(`rootUrl`: kotlin.String) : OtaDiscoverManifest {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_fetch_ota_manifest(
                uniffiHandle,
                
        FfiConverterString.lower(`rootUrl`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_rust_buffer(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_rust_buffer(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_rust_buffer(future) },
        // lift function
        { FfiConverterTypeOtaDiscoverManifest.lift(it) },
        // Error FFI converter
        CompanionException.ErrorHandler,
    )
    }

    
    @Throws(CompanionException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `getWebappDoc`(`deviceId`: kotlin.String, `id`: kotlin.String, `key`: kotlin.String) : kotlin.String? {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_get_webapp_doc(
                uniffiHandle,
                
        FfiConverterString.lower(`deviceId`),
        FfiConverterString.lower(`id`),
        FfiConverterString.lower(`key`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_rust_buffer(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_rust_buffer(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_rust_buffer(future) },
        // lift function
        { FfiConverterOptionalString.lift(it) },
        // Error FFI converter
        CompanionException.ErrorHandler,
    )
    }

    
    @Throws(CompanionException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `installWebapp`(`deviceId`: kotlin.String, `archivePath`: kotlin.String, `provenance`: kotlin.String?) : WebappInfo {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_install_webapp(
                uniffiHandle,
                
        FfiConverterString.lower(`deviceId`),
        FfiConverterString.lower(`archivePath`),
        FfiConverterOptionalString.lower(`provenance`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_rust_buffer(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_rust_buffer(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_rust_buffer(future) },
        // lift function
        { FfiConverterTypeWebappInfo.lift(it) },
        // Error FFI converter
        CompanionException.ErrorHandler,
    )
    }

    
    @Throws(CompanionException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `installWebappFromUrl`(`deviceId`: kotlin.String, `url`: kotlin.String, `expected`: ArtifactDigest?, `provenance`: kotlin.String?, `sink`: WebappBundleSink?) : WebappInfo {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_install_webapp_from_url(
                uniffiHandle,
                
        FfiConverterString.lower(`deviceId`),
        FfiConverterString.lower(`url`),
        FfiConverterOptionalTypeArtifactDigest.lower(`expected`),
        FfiConverterOptionalString.lower(`provenance`),
        FfiConverterOptionalTypeWebappBundleSink.lower(`sink`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_rust_buffer(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_rust_buffer(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_rust_buffer(future) },
        // lift function
        { FfiConverterTypeWebappInfo.lift(it) },
        // Error FFI converter
        CompanionException.ErrorHandler,
    )
    }

    
    @Throws(CompanionException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `listWebappConfig`(`deviceId`: kotlin.String, `id`: kotlin.String) : List<ConfigEntry> {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_list_webapp_config(
                uniffiHandle,
                
        FfiConverterString.lower(`deviceId`),
        FfiConverterString.lower(`id`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_rust_buffer(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_rust_buffer(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_rust_buffer(future) },
        // lift function
        { FfiConverterSequenceTypeConfigEntry.lift(it) },
        // Error FFI converter
        CompanionException.ErrorHandler,
    )
    }

    
    @Throws(CompanionException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `listWebappDoc`(`deviceId`: kotlin.String, `id`: kotlin.String) : List<DocEntry> {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_list_webapp_doc(
                uniffiHandle,
                
        FfiConverterString.lower(`deviceId`),
        FfiConverterString.lower(`id`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_rust_buffer(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_rust_buffer(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_rust_buffer(future) },
        // lift function
        { FfiConverterSequenceTypeDocEntry.lift(it) },
        // Error FFI converter
        CompanionException.ErrorHandler,
    )
    }

    
    @Throws(CompanionException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `listWebapps`(`deviceId`: kotlin.String) : List<WebappInfo> {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_list_webapps(
                uniffiHandle,
                
        FfiConverterString.lower(`deviceId`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_rust_buffer(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_rust_buffer(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_rust_buffer(future) },
        // lift function
        { FfiConverterSequenceTypeWebappInfo.lift(it) },
        // Error FFI converter
        CompanionException.ErrorHandler,
    )
    }

    override fun `logInbox`(): LogInbox {
            return FfiConverterTypeLogInbox.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_log_inbox(
        it,
        _status)
}
    }
    )
    }
    

    override fun `otaRunProgress`(`deviceId`: kotlin.String, `nowMs`: kotlin.ULong): OtaRunProgress? {
            return FfiConverterOptionalTypeOtaRunProgress.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_ota_run_progress(
        it,
        
        FfiConverterString.lower(`deviceId`),
        FfiConverterULong.lower(`nowMs`),_status)
}
    }
    )
    }
    

    
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `resumed`() {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_resumed(
                uniffiHandle,
                
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_void(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_void(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_void(future) },
        // lift function
        { Unit },
        
        // Error FFI converter
        UniffiNullRustCallStatusErrorHandler,
    )
    }

    
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `setCapabilityFlags`(`flags`: CapabilityFlags) {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_set_capability_flags(
                uniffiHandle,
                
        FfiConverterTypeCapabilityFlags.lower(`flags`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_void(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_void(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_void(future) },
        // lift function
        { Unit },
        
        // Error FFI converter
        UniffiNullRustCallStatusErrorHandler,
    )
    }

    
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `setDeviceAutoResume`(`deviceId`: kotlin.String, `enabled`: kotlin.Boolean) {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_set_device_auto_resume(
                uniffiHandle,
                
        FfiConverterString.lower(`deviceId`),
        FfiConverterBoolean.lower(`enabled`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_void(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_void(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_void(future) },
        // lift function
        { Unit },
        
        // Error FFI converter
        UniffiNullRustCallStatusErrorHandler,
    )
    }

    
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `setDeviceLogStreaming`(`enabled`: kotlin.Boolean) {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_set_device_log_streaming(
                uniffiHandle,
                
        FfiConverterBoolean.lower(`enabled`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_void(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_void(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_void(future) },
        // lift function
        { Unit },
        
        // Error FFI converter
        UniffiNullRustCallStatusErrorHandler,
    )
    }

    
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `setDeviceResumeTarget`(`deviceId`: kotlin.String, `target`: ResumeTarget) {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_set_device_resume_target(
                uniffiHandle,
                
        FfiConverterString.lower(`deviceId`),
        FfiConverterTypeResumeTarget.lower(`target`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_void(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_void(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_void(future) },
        // lift function
        { Unit },
        
        // Error FFI converter
        UniffiNullRustCallStatusErrorHandler,
    )
    }

    
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `setOtaPollConfig`(`config`: OtaPollConfig?) {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_set_ota_poll_config(
                uniffiHandle,
                
        FfiConverterOptionalTypeOtaPollConfig.lower(`config`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_void(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_void(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_void(future) },
        // lift function
        { Unit },
        
        // Error FFI converter
        UniffiNullRustCallStatusErrorHandler,
    )
    }

    
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `setProviderPriority`(`ids`: List<kotlin.String>) {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_set_provider_priority(
                uniffiHandle,
                
        FfiConverterSequenceString.lower(`ids`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_void(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_void(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_void(future) },
        // lift function
        { Unit },
        
        // Error FFI converter
        UniffiNullRustCallStatusErrorHandler,
    )
    }

    
    @Throws(CompanionException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `setWebappConfigField`(`deviceId`: kotlin.String, `id`: kotlin.String, `key`: kotlin.String, `value`: kotlin.String) {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_set_webapp_config_field(
                uniffiHandle,
                
        FfiConverterString.lower(`deviceId`),
        FfiConverterString.lower(`id`),
        FfiConverterString.lower(`key`),
        FfiConverterString.lower(`value`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_void(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_void(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_void(future) },
        // lift function
        { Unit },
        
        // Error FFI converter
        CompanionException.ErrorHandler,
    )
    }

    
    @Throws(CompanionException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `setWebappDoc`(`deviceId`: kotlin.String, `id`: kotlin.String, `key`: kotlin.String, `value`: kotlin.String) {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_set_webapp_doc(
                uniffiHandle,
                
        FfiConverterString.lower(`deviceId`),
        FfiConverterString.lower(`id`),
        FfiConverterString.lower(`key`),
        FfiConverterString.lower(`value`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_void(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_void(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_void(future) },
        // lift function
        { Unit },
        
        // Error FFI converter
        CompanionException.ErrorHandler,
    )
    }

    
    @Throws(CompanionException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `setWebappSlot`(`deviceId`: kotlin.String, `slot`: WebappSlot, `id`: kotlin.String?) : WebappSlots {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_set_webapp_slot(
                uniffiHandle,
                
        FfiConverterString.lower(`deviceId`),
        FfiConverterTypeWebappSlot.lower(`slot`),
        FfiConverterOptionalString.lower(`id`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_rust_buffer(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_rust_buffer(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_rust_buffer(future) },
        // lift function
        { FfiConverterTypeWebappSlots.lift(it) },
        // Error FFI converter
        CompanionException.ErrorHandler,
    )
    }

    
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `snapshot`() : SessionSnapshot {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_snapshot(
                uniffiHandle,
                
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_rust_buffer(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_rust_buffer(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_rust_buffer(future) },
        // lift function
        { FfiConverterTypeSessionSnapshot.lift(it) },
        // Error FFI converter
        UniffiNullRustCallStatusErrorHandler,
    )
    }

    
    @Throws(CompanionException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `start`() {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_start(
                uniffiHandle,
                
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_void(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_void(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_void(future) },
        // lift function
        { Unit },
        
        // Error FFI converter
        CompanionException.ErrorHandler,
    )
    }

    
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `stop`() {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_stop(
                uniffiHandle,
                
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_void(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_void(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_void(future) },
        // lift function
        { Unit },
        
        // Error FFI converter
        UniffiNullRustCallStatusErrorHandler,
    )
    }

    
    @Throws(CompanionException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `switchWebapp`(`deviceId`: kotlin.String, `id`: kotlin.String) {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_switch_webapp(
                uniffiHandle,
                
        FfiConverterString.lower(`deviceId`),
        FfiConverterString.lower(`id`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_void(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_void(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_void(future) },
        // lift function
        { Unit },
        
        // Error FFI converter
        CompanionException.ErrorHandler,
    )
    }

    
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `timeChanged`() {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_time_changed(
                uniffiHandle,
                
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_void(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_void(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_void(future) },
        // lift function
        { Unit },
        
        // Error FFI converter
        UniffiNullRustCallStatusErrorHandler,
    )
    }

    
    @Throws(CompanionException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `uninstallWebapp`(`deviceId`: kotlin.String, `id`: kotlin.String) {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_uninstall_webapp(
                uniffiHandle,
                
        FfiConverterString.lower(`deviceId`),
        FfiConverterString.lower(`id`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_void(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_void(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_void(future) },
        // lift function
        { Unit },
        
        // Error FFI converter
        CompanionException.ErrorHandler,
    )
    }

    override fun `voiceModelPaths`(): VoiceModelPaths {
            return FfiConverterTypeVoiceModelPaths.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_voice_model_paths(
        it,
        _status)
}
    }
    )
    }
    

    
    @Throws(CompanionException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `webappResource`(`deviceId`: kotlin.String, `id`: kotlin.String, `kind`: WebappResourceKind, `origin`: WebappResourceOrigin?) : WebappResourceFile {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_webapp_resource(
                uniffiHandle,
                
        FfiConverterString.lower(`deviceId`),
        FfiConverterString.lower(`id`),
        FfiConverterTypeWebappResourceKind.lower(`kind`),
        FfiConverterOptionalTypeWebappResourceOrigin.lower(`origin`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_rust_buffer(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_rust_buffer(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_rust_buffer(future) },
        // lift function
        { FfiConverterTypeWebappResourceFile.lift(it) },
        // Error FFI converter
        CompanionException.ErrorHandler,
    )
    }

    
    @Throws(CompanionException::class)
    @Suppress("ASSIGNED_BUT_NEVER_ACCESSED_VARIABLE")
    override suspend fun `webappSlots`(`deviceId`: kotlin.String) : WebappSlots {
        return uniffiRustCallAsync(
        callWithHandle { uniffiHandle ->
            UniffiLib.uniffi_bridgething_companion_fn_method_companionsession_webapp_slots(
                uniffiHandle,
                
        FfiConverterString.lower(`deviceId`),
            )
        },
        { future, callback, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_poll_rust_buffer(future, callback, continuation) },
        { future, continuation -> UniffiLib.ffi_bridgething_companion_rust_future_complete_rust_buffer(future, continuation) },
        { future -> UniffiLib.ffi_bridgething_companion_rust_future_free_rust_buffer(future) },
        // lift function
        { FfiConverterTypeWebappSlots.lift(it) },
        // Error FFI converter
        CompanionException.ErrorHandler,
    )
    }

    

    


    
    companion object {
         fun `create`(`config`: CompanionConfig, `backends`: CompanionBackends, `events`: SessionEventSink): CompanionSession {
            return FfiConverterTypeCompanionSession.lift(
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_constructor_companionsession_create(
    
        
        FfiConverterTypeCompanionConfig.lower(`config`),
        FfiConverterTypeCompanionBackends.lower(`backends`),
        FfiConverterTypeSessionEventSink.lower(`events`),_status)
}
    )
    }
    

        
    }
    
}


/**
 * @suppress
 */
public object FfiConverterTypeCompanionSession: FfiConverter<CompanionSession, Long> {
    override fun lower(value: CompanionSession): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): CompanionSession {
        return CompanionSession(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): CompanionSession {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: CompanionSession) = 8UL

    override fun write(value: CompanionSession, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface ConnectivityInboxInterface {
    
    fun `onChanged`(`online`: kotlin.Boolean)
    
    companion object
}

open class ConnectivityInbox: Disposable, AutoCloseable, ConnectivityInboxInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_connectivityinbox(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_connectivityinbox(handle, status)
        }
    }

    override fun `onChanged`(`online`: kotlin.Boolean)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_connectivityinbox_on_changed(
        it,
        
        FfiConverterBoolean.lower(`online`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeConnectivityInbox: FfiConverter<ConnectivityInbox, Long> {
    override fun lower(value: ConnectivityInbox): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): ConnectivityInbox {
        return ConnectivityInbox(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): ConnectivityInbox {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: ConnectivityInbox) = 8UL

    override fun write(value: ConnectivityInbox, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface ConnectivityMonitor {
    
    fun `start`(`inbox`: ConnectivityInbox)
    
    fun `stop`()
    
    companion object
}

open class ConnectivityMonitorImpl: Disposable, AutoCloseable, ConnectivityMonitor
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_connectivitymonitor(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_connectivitymonitor(handle, status)
        }
    }

    override fun `start`(`inbox`: ConnectivityInbox)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_connectivitymonitor_start(
        it,
        
        FfiConverterTypeConnectivityInbox.lower(`inbox`),_status)
}
    }
    
    

    override fun `stop`()
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_connectivitymonitor_stop(
        it,
        _status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}



// Put the implementation in an object so we don't pollute the top-level namespace
internal object uniffiCallbackInterfaceConnectivityMonitor {
    internal object `start`: UniffiCallbackInterfaceConnectivityMonitorMethod0 {
        override fun callback(`uniffiHandle`: Long,`inbox`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeConnectivityMonitor.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`start`(
                    FfiConverterTypeConnectivityInbox.lift(`inbox`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `stop`: UniffiCallbackInterfaceConnectivityMonitorMethod1 {
        override fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeConnectivityMonitor.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`stop`(
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }

    internal object uniffiFree: UniffiCallbackInterfaceFree {
        override fun callback(handle: Long) {
            FfiConverterTypeConnectivityMonitor.handleMap.remove(handle)
        }
    }

    internal object uniffiClone: UniffiCallbackInterfaceClone {
        override fun callback(handle: Long): Long {
            return FfiConverterTypeConnectivityMonitor.handleMap.clone(handle)
        }
    }

    internal var vtable = UniffiVTableCallbackInterfaceConnectivityMonitor.UniffiByValue(
        uniffiFree,
        uniffiClone,
        `start`,
        `stop`,
    )

    // Registers the foreign callback with the Rust side.
    // This method is generated for each callback interface.
    internal fun register(lib: UniffiLib) {
        lib.uniffi_bridgething_companion_fn_init_callback_vtable_connectivitymonitor(vtable)
    }
}

/**
 * @suppress
 */
public object FfiConverterTypeConnectivityMonitor: FfiConverter<ConnectivityMonitor, Long> {
    internal val handleMap = UniffiHandleMap<ConnectivityMonitor>()

    override fun lower(value: ConnectivityMonitor): Long {
        if (value is ConnectivityMonitorImpl) {
             // Rust-implemented object.  Clone the handle and return it
            return value.uniffiCloneHandle()
         } else {
            // Kotlin object, generate a new vtable handle and return that.
            return handleMap.insert(value)
         }
    }

    override fun lift(value: Long): ConnectivityMonitor {
        if ((value and 1.toLong()) == 0.toLong()) {
            // Rust-generated handle, construct a new class that uses the handle to implement the
            // interface
            return ConnectivityMonitorImpl(UniffiWithHandle, value)
        } else {
            // Kotlin-generated handle, get the object from the handle map
            return handleMap.remove(value)
        }
    }

    override fun read(buf: ByteBuffer): ConnectivityMonitor {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: ConnectivityMonitor) = 8UL

    override fun write(value: ConnectivityMonitor, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface DeviceWaker {
    
    fun `wakeDevice`(`reason`: WakeReason, `allowPlayTap`: kotlin.Boolean)
    
    companion object
}

open class DeviceWakerImpl: Disposable, AutoCloseable, DeviceWaker
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_devicewaker(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_devicewaker(handle, status)
        }
    }

    override fun `wakeDevice`(`reason`: WakeReason, `allowPlayTap`: kotlin.Boolean)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_devicewaker_wake_device(
        it,
        
        FfiConverterTypeWakeReason.lower(`reason`),
        FfiConverterBoolean.lower(`allowPlayTap`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}



// Put the implementation in an object so we don't pollute the top-level namespace
internal object uniffiCallbackInterfaceDeviceWaker {
    internal object `wakeDevice`: UniffiCallbackInterfaceDeviceWakerMethod0 {
        override fun callback(`uniffiHandle`: Long,`reason`: RustBuffer.ByValue,`allowPlayTap`: Byte,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeDeviceWaker.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`wakeDevice`(
                    FfiConverterTypeWakeReason.lift(`reason`),
                    FfiConverterBoolean.lift(`allowPlayTap`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }

    internal object uniffiFree: UniffiCallbackInterfaceFree {
        override fun callback(handle: Long) {
            FfiConverterTypeDeviceWaker.handleMap.remove(handle)
        }
    }

    internal object uniffiClone: UniffiCallbackInterfaceClone {
        override fun callback(handle: Long): Long {
            return FfiConverterTypeDeviceWaker.handleMap.clone(handle)
        }
    }

    internal var vtable = UniffiVTableCallbackInterfaceDeviceWaker.UniffiByValue(
        uniffiFree,
        uniffiClone,
        `wakeDevice`,
    )

    // Registers the foreign callback with the Rust side.
    // This method is generated for each callback interface.
    internal fun register(lib: UniffiLib) {
        lib.uniffi_bridgething_companion_fn_init_callback_vtable_devicewaker(vtable)
    }
}

/**
 * @suppress
 */
public object FfiConverterTypeDeviceWaker: FfiConverter<DeviceWaker, Long> {
    internal val handleMap = UniffiHandleMap<DeviceWaker>()

    override fun lower(value: DeviceWaker): Long {
        if (value is DeviceWakerImpl) {
             // Rust-implemented object.  Clone the handle and return it
            return value.uniffiCloneHandle()
         } else {
            // Kotlin object, generate a new vtable handle and return that.
            return handleMap.insert(value)
         }
    }

    override fun lift(value: Long): DeviceWaker {
        if ((value and 1.toLong()) == 0.toLong()) {
            // Rust-generated handle, construct a new class that uses the handle to implement the
            // interface
            return DeviceWakerImpl(UniffiWithHandle, value)
        } else {
            // Kotlin-generated handle, get the object from the handle map
            return handleMap.remove(value)
        }
    }

    override fun read(buf: ByteBuffer): DeviceWaker {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: DeviceWaker) = 8UL

    override fun write(value: DeviceWaker, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface EarconSinkInterface {
    
    fun `onFinished`(`ok`: kotlin.Boolean)
    
    companion object
}

open class EarconSink: Disposable, AutoCloseable, EarconSinkInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_earconsink(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_earconsink(handle, status)
        }
    }

    override fun `onFinished`(`ok`: kotlin.Boolean)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_earconsink_on_finished(
        it,
        
        FfiConverterBoolean.lower(`ok`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeEarconSink: FfiConverter<EarconSink, Long> {
    override fun lower(value: EarconSink): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): EarconSink {
        return EarconSink(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): EarconSink {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: EarconSink) = 8UL

    override fun write(value: EarconSink, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface ExtensionHost {
    
    fun `start`(`inbox`: ExtensionHostInbox)
    
    fun `stop`()
    
    fun `deliver`(`device`: kotlin.String, `webapp`: kotlin.String, `message`: ExtensionMessage)
    
    fun `deviceConnected`(`device`: kotlin.String, `name`: kotlin.String, `config`: List<ExtensionConfigEntry>, `webapps`: List<kotlin.String>)
    
    fun `deviceDisconnected`(`device`: kotlin.String)
    
    fun `deviceActive`(`device`: kotlin.String, `webapp`: kotlin.String, `active`: kotlin.Boolean)
    
    fun `configChanged`(`device`: kotlin.String, `webapp`: kotlin.String, `key`: kotlin.String, `value`: kotlin.String?)
    
    companion object
}

open class ExtensionHostImpl: Disposable, AutoCloseable, ExtensionHost
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_extensionhost(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_extensionhost(handle, status)
        }
    }

    override fun `start`(`inbox`: ExtensionHostInbox)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_extensionhost_start(
        it,
        
        FfiConverterTypeExtensionHostInbox.lower(`inbox`),_status)
}
    }
    
    

    override fun `stop`()
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_extensionhost_stop(
        it,
        _status)
}
    }
    
    

    override fun `deliver`(`device`: kotlin.String, `webapp`: kotlin.String, `message`: ExtensionMessage)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_extensionhost_deliver(
        it,
        
        FfiConverterString.lower(`device`),
        FfiConverterString.lower(`webapp`),
        FfiConverterTypeExtensionMessage.lower(`message`),_status)
}
    }
    
    

    override fun `deviceConnected`(`device`: kotlin.String, `name`: kotlin.String, `config`: List<ExtensionConfigEntry>, `webapps`: List<kotlin.String>)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_extensionhost_device_connected(
        it,
        
        FfiConverterString.lower(`device`),
        FfiConverterString.lower(`name`),
        FfiConverterSequenceTypeExtensionConfigEntry.lower(`config`),
        FfiConverterSequenceString.lower(`webapps`),_status)
}
    }
    
    

    override fun `deviceDisconnected`(`device`: kotlin.String)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_extensionhost_device_disconnected(
        it,
        
        FfiConverterString.lower(`device`),_status)
}
    }
    
    

    override fun `deviceActive`(`device`: kotlin.String, `webapp`: kotlin.String, `active`: kotlin.Boolean)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_extensionhost_device_active(
        it,
        
        FfiConverterString.lower(`device`),
        FfiConverterString.lower(`webapp`),
        FfiConverterBoolean.lower(`active`),_status)
}
    }
    
    

    override fun `configChanged`(`device`: kotlin.String, `webapp`: kotlin.String, `key`: kotlin.String, `value`: kotlin.String?)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_extensionhost_config_changed(
        it,
        
        FfiConverterString.lower(`device`),
        FfiConverterString.lower(`webapp`),
        FfiConverterString.lower(`key`),
        FfiConverterOptionalString.lower(`value`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}



// Put the implementation in an object so we don't pollute the top-level namespace
internal object uniffiCallbackInterfaceExtensionHost {
    internal object `start`: UniffiCallbackInterfaceExtensionHostMethod0 {
        override fun callback(`uniffiHandle`: Long,`inbox`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeExtensionHost.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`start`(
                    FfiConverterTypeExtensionHostInbox.lift(`inbox`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `stop`: UniffiCallbackInterfaceExtensionHostMethod1 {
        override fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeExtensionHost.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`stop`(
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `deliver`: UniffiCallbackInterfaceExtensionHostMethod2 {
        override fun callback(`uniffiHandle`: Long,`device`: RustBuffer.ByValue,`webapp`: RustBuffer.ByValue,`message`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeExtensionHost.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`deliver`(
                    FfiConverterString.lift(`device`),
                    FfiConverterString.lift(`webapp`),
                    FfiConverterTypeExtensionMessage.lift(`message`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `deviceConnected`: UniffiCallbackInterfaceExtensionHostMethod3 {
        override fun callback(`uniffiHandle`: Long,`device`: RustBuffer.ByValue,`name`: RustBuffer.ByValue,`config`: RustBuffer.ByValue,`webapps`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeExtensionHost.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`deviceConnected`(
                    FfiConverterString.lift(`device`),
                    FfiConverterString.lift(`name`),
                    FfiConverterSequenceTypeExtensionConfigEntry.lift(`config`),
                    FfiConverterSequenceString.lift(`webapps`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `deviceDisconnected`: UniffiCallbackInterfaceExtensionHostMethod4 {
        override fun callback(`uniffiHandle`: Long,`device`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeExtensionHost.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`deviceDisconnected`(
                    FfiConverterString.lift(`device`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `deviceActive`: UniffiCallbackInterfaceExtensionHostMethod5 {
        override fun callback(`uniffiHandle`: Long,`device`: RustBuffer.ByValue,`webapp`: RustBuffer.ByValue,`active`: Byte,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeExtensionHost.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`deviceActive`(
                    FfiConverterString.lift(`device`),
                    FfiConverterString.lift(`webapp`),
                    FfiConverterBoolean.lift(`active`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `configChanged`: UniffiCallbackInterfaceExtensionHostMethod6 {
        override fun callback(`uniffiHandle`: Long,`device`: RustBuffer.ByValue,`webapp`: RustBuffer.ByValue,`key`: RustBuffer.ByValue,`value`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeExtensionHost.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`configChanged`(
                    FfiConverterString.lift(`device`),
                    FfiConverterString.lift(`webapp`),
                    FfiConverterString.lift(`key`),
                    FfiConverterOptionalString.lift(`value`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }

    internal object uniffiFree: UniffiCallbackInterfaceFree {
        override fun callback(handle: Long) {
            FfiConverterTypeExtensionHost.handleMap.remove(handle)
        }
    }

    internal object uniffiClone: UniffiCallbackInterfaceClone {
        override fun callback(handle: Long): Long {
            return FfiConverterTypeExtensionHost.handleMap.clone(handle)
        }
    }

    internal var vtable = UniffiVTableCallbackInterfaceExtensionHost.UniffiByValue(
        uniffiFree,
        uniffiClone,
        `start`,
        `stop`,
        `deliver`,
        `deviceConnected`,
        `deviceDisconnected`,
        `deviceActive`,
        `configChanged`,
    )

    // Registers the foreign callback with the Rust side.
    // This method is generated for each callback interface.
    internal fun register(lib: UniffiLib) {
        lib.uniffi_bridgething_companion_fn_init_callback_vtable_extensionhost(vtable)
    }
}

/**
 * @suppress
 */
public object FfiConverterTypeExtensionHost: FfiConverter<ExtensionHost, Long> {
    internal val handleMap = UniffiHandleMap<ExtensionHost>()

    override fun lower(value: ExtensionHost): Long {
        if (value is ExtensionHostImpl) {
             // Rust-implemented object.  Clone the handle and return it
            return value.uniffiCloneHandle()
         } else {
            // Kotlin object, generate a new vtable handle and return that.
            return handleMap.insert(value)
         }
    }

    override fun lift(value: Long): ExtensionHost {
        if ((value and 1.toLong()) == 0.toLong()) {
            // Rust-generated handle, construct a new class that uses the handle to implement the
            // interface
            return ExtensionHostImpl(UniffiWithHandle, value)
        } else {
            // Kotlin-generated handle, get the object from the handle map
            return handleMap.remove(value)
        }
    }

    override fun read(buf: ByteBuffer): ExtensionHost {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: ExtensionHost) = 8UL

    override fun write(value: ExtensionHost, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface ExtensionHostInboxInterface {
    
    fun `runningChanged`(`webapps`: List<kotlin.String>)
    
    fun `sendToDevice`(`device`: kotlin.String?, `webapp`: kotlin.String, `message`: ExtensionMessage)
    
    companion object
}

open class ExtensionHostInbox: Disposable, AutoCloseable, ExtensionHostInboxInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_extensionhostinbox(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_extensionhostinbox(handle, status)
        }
    }

    override fun `runningChanged`(`webapps`: List<kotlin.String>)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_extensionhostinbox_running_changed(
        it,
        
        FfiConverterSequenceString.lower(`webapps`),_status)
}
    }
    
    

    override fun `sendToDevice`(`device`: kotlin.String?, `webapp`: kotlin.String, `message`: ExtensionMessage)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_extensionhostinbox_send_to_device(
        it,
        
        FfiConverterOptionalString.lower(`device`),
        FfiConverterString.lower(`webapp`),
        FfiConverterTypeExtensionMessage.lower(`message`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeExtensionHostInbox: FfiConverter<ExtensionHostInbox, Long> {
    override fun lower(value: ExtensionHostInbox): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): ExtensionHostInbox {
        return ExtensionHostInbox(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): ExtensionHostInbox {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: ExtensionHostInbox) = 8UL

    override fun write(value: ExtensionHostInbox, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface GeoInboxInterface {
    
    fun `onAuthorizationChange`(`granted`: kotlin.Boolean)
    
    fun `onError`(`error`: GeoError)
    
    fun `onPosition`(`position`: Position)
    
    companion object
}

open class GeoInbox: Disposable, AutoCloseable, GeoInboxInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_geoinbox(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_geoinbox(handle, status)
        }
    }

    override fun `onAuthorizationChange`(`granted`: kotlin.Boolean)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_geoinbox_on_authorization_change(
        it,
        
        FfiConverterBoolean.lower(`granted`),_status)
}
    }
    
    

    override fun `onError`(`error`: GeoError)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_geoinbox_on_error(
        it,
        
        FfiConverterTypeGeoError.lower(`error`),_status)
}
    }
    
    

    override fun `onPosition`(`position`: Position)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_geoinbox_on_position(
        it,
        
        FfiConverterTypePosition.lower(`position`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeGeoInbox: FfiConverter<GeoInbox, Long> {
    override fun lower(value: GeoInbox): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): GeoInbox {
        return GeoInbox(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): GeoInbox {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: GeoInbox) = 8UL

    override fun write(value: GeoInbox, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface GeoProvider {
    
    fun `canProvideLocation`(): kotlin.Boolean
    
    fun `start`(`inbox`: GeoInbox)
    
    fun `stop`()
    
    fun `configure`(`accuracy`: GeoAccuracy)
    
    fun `requestAuthorization`()
    
    fun `startUpdating`()
    
    fun `stopUpdating`()
    
    fun `requestOnce`()
    
    fun `cancelOnce`()
    
    companion object
}

open class GeoProviderImpl: Disposable, AutoCloseable, GeoProvider
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_geoprovider(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_geoprovider(handle, status)
        }
    }

    override fun `canProvideLocation`(): kotlin.Boolean {
            return FfiConverterBoolean.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_geoprovider_can_provide_location(
        it,
        _status)
}
    }
    )
    }
    

    override fun `start`(`inbox`: GeoInbox)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_geoprovider_start(
        it,
        
        FfiConverterTypeGeoInbox.lower(`inbox`),_status)
}
    }
    
    

    override fun `stop`()
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_geoprovider_stop(
        it,
        _status)
}
    }
    
    

    override fun `configure`(`accuracy`: GeoAccuracy)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_geoprovider_configure(
        it,
        
        FfiConverterTypeGeoAccuracy.lower(`accuracy`),_status)
}
    }
    
    

    override fun `requestAuthorization`()
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_geoprovider_request_authorization(
        it,
        _status)
}
    }
    
    

    override fun `startUpdating`()
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_geoprovider_start_updating(
        it,
        _status)
}
    }
    
    

    override fun `stopUpdating`()
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_geoprovider_stop_updating(
        it,
        _status)
}
    }
    
    

    override fun `requestOnce`()
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_geoprovider_request_once(
        it,
        _status)
}
    }
    
    

    override fun `cancelOnce`()
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_geoprovider_cancel_once(
        it,
        _status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}



// Put the implementation in an object so we don't pollute the top-level namespace
internal object uniffiCallbackInterfaceGeoProvider {
    internal object `canProvideLocation`: UniffiCallbackInterfaceGeoProviderMethod0 {
        override fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: ByteByReference,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeGeoProvider.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`canProvideLocation`(
                )
            }
            val writeReturn = { value: kotlin.Boolean -> uniffiOutReturn.setValue(FfiConverterBoolean.lower(value)) }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `start`: UniffiCallbackInterfaceGeoProviderMethod1 {
        override fun callback(`uniffiHandle`: Long,`inbox`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeGeoProvider.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`start`(
                    FfiConverterTypeGeoInbox.lift(`inbox`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `stop`: UniffiCallbackInterfaceGeoProviderMethod2 {
        override fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeGeoProvider.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`stop`(
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `configure`: UniffiCallbackInterfaceGeoProviderMethod3 {
        override fun callback(`uniffiHandle`: Long,`accuracy`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeGeoProvider.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`configure`(
                    FfiConverterTypeGeoAccuracy.lift(`accuracy`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `requestAuthorization`: UniffiCallbackInterfaceGeoProviderMethod4 {
        override fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeGeoProvider.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`requestAuthorization`(
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `startUpdating`: UniffiCallbackInterfaceGeoProviderMethod5 {
        override fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeGeoProvider.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`startUpdating`(
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `stopUpdating`: UniffiCallbackInterfaceGeoProviderMethod6 {
        override fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeGeoProvider.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`stopUpdating`(
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `requestOnce`: UniffiCallbackInterfaceGeoProviderMethod7 {
        override fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeGeoProvider.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`requestOnce`(
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `cancelOnce`: UniffiCallbackInterfaceGeoProviderMethod8 {
        override fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeGeoProvider.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`cancelOnce`(
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }

    internal object uniffiFree: UniffiCallbackInterfaceFree {
        override fun callback(handle: Long) {
            FfiConverterTypeGeoProvider.handleMap.remove(handle)
        }
    }

    internal object uniffiClone: UniffiCallbackInterfaceClone {
        override fun callback(handle: Long): Long {
            return FfiConverterTypeGeoProvider.handleMap.clone(handle)
        }
    }

    internal var vtable = UniffiVTableCallbackInterfaceGeoProvider.UniffiByValue(
        uniffiFree,
        uniffiClone,
        `canProvideLocation`,
        `start`,
        `stop`,
        `configure`,
        `requestAuthorization`,
        `startUpdating`,
        `stopUpdating`,
        `requestOnce`,
        `cancelOnce`,
    )

    // Registers the foreign callback with the Rust side.
    // This method is generated for each callback interface.
    internal fun register(lib: UniffiLib) {
        lib.uniffi_bridgething_companion_fn_init_callback_vtable_geoprovider(vtable)
    }
}

/**
 * @suppress
 */
public object FfiConverterTypeGeoProvider: FfiConverter<GeoProvider, Long> {
    internal val handleMap = UniffiHandleMap<GeoProvider>()

    override fun lower(value: GeoProvider): Long {
        if (value is GeoProviderImpl) {
             // Rust-implemented object.  Clone the handle and return it
            return value.uniffiCloneHandle()
         } else {
            // Kotlin object, generate a new vtable handle and return that.
            return handleMap.insert(value)
         }
    }

    override fun lift(value: Long): GeoProvider {
        if ((value and 1.toLong()) == 0.toLong()) {
            // Rust-generated handle, construct a new class that uses the handle to implement the
            // interface
            return GeoProviderImpl(UniffiWithHandle, value)
        } else {
            // Kotlin-generated handle, get the object from the handle map
            return handleMap.remove(value)
        }
    }

    override fun read(buf: ByteBuffer): GeoProvider {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: GeoProvider) = 8UL

    override fun write(value: GeoProvider, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface HostEnvironment {
    
    fun `clock`(): HostClock
    
    companion object
}

open class HostEnvironmentImpl: Disposable, AutoCloseable, HostEnvironment
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_hostenvironment(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_hostenvironment(handle, status)
        }
    }

    override fun `clock`(): HostClock {
            return FfiConverterTypeHostClock.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_hostenvironment_clock(
        it,
        _status)
}
    }
    )
    }
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}



// Put the implementation in an object so we don't pollute the top-level namespace
internal object uniffiCallbackInterfaceHostEnvironment {
    internal object `clock`: UniffiCallbackInterfaceHostEnvironmentMethod0 {
        override fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: RustBuffer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeHostEnvironment.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`clock`(
                )
            }
            val writeReturn = { value: HostClock -> uniffiOutReturn.setValue(FfiConverterTypeHostClock.lower(value)) }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }

    internal object uniffiFree: UniffiCallbackInterfaceFree {
        override fun callback(handle: Long) {
            FfiConverterTypeHostEnvironment.handleMap.remove(handle)
        }
    }

    internal object uniffiClone: UniffiCallbackInterfaceClone {
        override fun callback(handle: Long): Long {
            return FfiConverterTypeHostEnvironment.handleMap.clone(handle)
        }
    }

    internal var vtable = UniffiVTableCallbackInterfaceHostEnvironment.UniffiByValue(
        uniffiFree,
        uniffiClone,
        `clock`,
    )

    // Registers the foreign callback with the Rust side.
    // This method is generated for each callback interface.
    internal fun register(lib: UniffiLib) {
        lib.uniffi_bridgething_companion_fn_init_callback_vtable_hostenvironment(vtable)
    }
}

/**
 * @suppress
 */
public object FfiConverterTypeHostEnvironment: FfiConverter<HostEnvironment, Long> {
    internal val handleMap = UniffiHandleMap<HostEnvironment>()

    override fun lower(value: HostEnvironment): Long {
        if (value is HostEnvironmentImpl) {
             // Rust-implemented object.  Clone the handle and return it
            return value.uniffiCloneHandle()
         } else {
            // Kotlin object, generate a new vtable handle and return that.
            return handleMap.insert(value)
         }
    }

    override fun lift(value: Long): HostEnvironment {
        if ((value and 1.toLong()) == 0.toLong()) {
            // Rust-generated handle, construct a new class that uses the handle to implement the
            // interface
            return HostEnvironmentImpl(UniffiWithHandle, value)
        } else {
            // Kotlin-generated handle, get the object from the handle map
            return handleMap.remove(value)
        }
    }

    override fun read(buf: ByteBuffer): HostEnvironment {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: HostEnvironment) = 8UL

    override fun write(value: HostEnvironment, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface HttpDownloadSinkInterface {
    
    fun `onChunk`(`chunk`: kotlin.ByteArray)
    
    fun `onFailed`(`reason`: kotlin.String)
    
    fun `onFinished`()
    
    fun `onResponse`(`status`: kotlin.UShort, `headers`: List<HttpHeader>, `contentLength`: kotlin.ULong?)
    
    companion object
}

open class HttpDownloadSink: Disposable, AutoCloseable, HttpDownloadSinkInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_httpdownloadsink(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_httpdownloadsink(handle, status)
        }
    }

    override fun `onChunk`(`chunk`: kotlin.ByteArray)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_httpdownloadsink_on_chunk(
        it,
        
        FfiConverterByteArray.lower(`chunk`),_status)
}
    }
    
    

    override fun `onFailed`(`reason`: kotlin.String)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_httpdownloadsink_on_failed(
        it,
        
        FfiConverterString.lower(`reason`),_status)
}
    }
    
    

    override fun `onFinished`()
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_httpdownloadsink_on_finished(
        it,
        _status)
}
    }
    
    

    override fun `onResponse`(`status`: kotlin.UShort, `headers`: List<HttpHeader>, `contentLength`: kotlin.ULong?)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_httpdownloadsink_on_response(
        it,
        
        FfiConverterUShort.lower(`status`),
        FfiConverterSequenceTypeHttpHeader.lower(`headers`),
        FfiConverterOptionalULong.lower(`contentLength`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeHttpDownloadSink: FfiConverter<HttpDownloadSink, Long> {
    override fun lower(value: HttpDownloadSink): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): HttpDownloadSink {
        return HttpDownloadSink(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): HttpDownloadSink {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: HttpDownloadSink) = 8UL

    override fun write(value: HttpDownloadSink, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface HttpSinkInterface {
    
    fun `complete`(`response`: HttpResponse)
    
    fun `fail`(`reason`: kotlin.String)
    
    companion object
}

open class HttpSink: Disposable, AutoCloseable, HttpSinkInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_httpsink(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_httpsink(handle, status)
        }
    }

    override fun `complete`(`response`: HttpResponse)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_httpsink_complete(
        it,
        
        FfiConverterTypeHttpResponse.lower(`response`),_status)
}
    }
    
    

    override fun `fail`(`reason`: kotlin.String)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_httpsink_fail(
        it,
        
        FfiConverterString.lower(`reason`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeHttpSink: FfiConverter<HttpSink, Long> {
    override fun lower(value: HttpSink): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): HttpSink {
        return HttpSink(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): HttpSink {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: HttpSink) = 8UL

    override fun write(value: HttpSink, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface HttpTransport {
    
    fun `execute`(`request`: HttpRequest, `sink`: HttpSink)
    
    fun `download`(`request`: HttpRequest, `sink`: HttpDownloadSink)
    
    companion object
}

open class HttpTransportImpl: Disposable, AutoCloseable, HttpTransport
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_httptransport(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_httptransport(handle, status)
        }
    }

    override fun `execute`(`request`: HttpRequest, `sink`: HttpSink)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_httptransport_execute(
        it,
        
        FfiConverterTypeHttpRequest.lower(`request`),
        FfiConverterTypeHttpSink.lower(`sink`),_status)
}
    }
    
    

    override fun `download`(`request`: HttpRequest, `sink`: HttpDownloadSink)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_httptransport_download(
        it,
        
        FfiConverterTypeHttpRequest.lower(`request`),
        FfiConverterTypeHttpDownloadSink.lower(`sink`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}



// Put the implementation in an object so we don't pollute the top-level namespace
internal object uniffiCallbackInterfaceHttpTransport {
    internal object `execute`: UniffiCallbackInterfaceHttpTransportMethod0 {
        override fun callback(`uniffiHandle`: Long,`request`: RustBuffer.ByValue,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeHttpTransport.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`execute`(
                    FfiConverterTypeHttpRequest.lift(`request`),
                    FfiConverterTypeHttpSink.lift(`sink`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `download`: UniffiCallbackInterfaceHttpTransportMethod1 {
        override fun callback(`uniffiHandle`: Long,`request`: RustBuffer.ByValue,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeHttpTransport.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`download`(
                    FfiConverterTypeHttpRequest.lift(`request`),
                    FfiConverterTypeHttpDownloadSink.lift(`sink`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }

    internal object uniffiFree: UniffiCallbackInterfaceFree {
        override fun callback(handle: Long) {
            FfiConverterTypeHttpTransport.handleMap.remove(handle)
        }
    }

    internal object uniffiClone: UniffiCallbackInterfaceClone {
        override fun callback(handle: Long): Long {
            return FfiConverterTypeHttpTransport.handleMap.clone(handle)
        }
    }

    internal var vtable = UniffiVTableCallbackInterfaceHttpTransport.UniffiByValue(
        uniffiFree,
        uniffiClone,
        `execute`,
        `download`,
    )

    // Registers the foreign callback with the Rust side.
    // This method is generated for each callback interface.
    internal fun register(lib: UniffiLib) {
        lib.uniffi_bridgething_companion_fn_init_callback_vtable_httptransport(vtable)
    }
}

/**
 * @suppress
 */
public object FfiConverterTypeHttpTransport: FfiConverter<HttpTransport, Long> {
    internal val handleMap = UniffiHandleMap<HttpTransport>()

    override fun lower(value: HttpTransport): Long {
        if (value is HttpTransportImpl) {
             // Rust-implemented object.  Clone the handle and return it
            return value.uniffiCloneHandle()
         } else {
            // Kotlin object, generate a new vtable handle and return that.
            return handleMap.insert(value)
         }
    }

    override fun lift(value: Long): HttpTransport {
        if ((value and 1.toLong()) == 0.toLong()) {
            // Rust-generated handle, construct a new class that uses the handle to implement the
            // interface
            return HttpTransportImpl(UniffiWithHandle, value)
        } else {
            // Kotlin-generated handle, get the object from the handle map
            return handleMap.remove(value)
        }
    }

    override fun read(buf: ByteBuffer): HttpTransport {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: HttpTransport) = 8UL

    override fun write(value: HttpTransport, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface ImageScaler {
    
    fun `downsampleJpeg`(`bytes`: kotlin.ByteArray, `maxEdge`: kotlin.UInt, `quality`: kotlin.Float): kotlin.ByteArray?
    
    companion object
}

open class ImageScalerImpl: Disposable, AutoCloseable, ImageScaler
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_imagescaler(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_imagescaler(handle, status)
        }
    }

    override fun `downsampleJpeg`(`bytes`: kotlin.ByteArray, `maxEdge`: kotlin.UInt, `quality`: kotlin.Float): kotlin.ByteArray? {
            return FfiConverterOptionalByteArray.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_imagescaler_downsample_jpeg(
        it,
        
        FfiConverterByteArray.lower(`bytes`),
        FfiConverterUInt.lower(`maxEdge`),
        FfiConverterFloat.lower(`quality`),_status)
}
    }
    )
    }
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}



// Put the implementation in an object so we don't pollute the top-level namespace
internal object uniffiCallbackInterfaceImageScaler {
    internal object `downsampleJpeg`: UniffiCallbackInterfaceImageScalerMethod0 {
        override fun callback(`uniffiHandle`: Long,`bytes`: RustBuffer.ByValue,`maxEdge`: Int,`quality`: Float,`uniffiOutReturn`: RustBuffer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeImageScaler.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`downsampleJpeg`(
                    FfiConverterByteArray.lift(`bytes`),
                    FfiConverterUInt.lift(`maxEdge`),
                    FfiConverterFloat.lift(`quality`),
                )
            }
            val writeReturn = { value: kotlin.ByteArray? -> uniffiOutReturn.setValue(FfiConverterOptionalByteArray.lower(value)) }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }

    internal object uniffiFree: UniffiCallbackInterfaceFree {
        override fun callback(handle: Long) {
            FfiConverterTypeImageScaler.handleMap.remove(handle)
        }
    }

    internal object uniffiClone: UniffiCallbackInterfaceClone {
        override fun callback(handle: Long): Long {
            return FfiConverterTypeImageScaler.handleMap.clone(handle)
        }
    }

    internal var vtable = UniffiVTableCallbackInterfaceImageScaler.UniffiByValue(
        uniffiFree,
        uniffiClone,
        `downsampleJpeg`,
    )

    // Registers the foreign callback with the Rust side.
    // This method is generated for each callback interface.
    internal fun register(lib: UniffiLib) {
        lib.uniffi_bridgething_companion_fn_init_callback_vtable_imagescaler(vtable)
    }
}

/**
 * @suppress
 */
public object FfiConverterTypeImageScaler: FfiConverter<ImageScaler, Long> {
    internal val handleMap = UniffiHandleMap<ImageScaler>()

    override fun lower(value: ImageScaler): Long {
        if (value is ImageScalerImpl) {
             // Rust-implemented object.  Clone the handle and return it
            return value.uniffiCloneHandle()
         } else {
            // Kotlin object, generate a new vtable handle and return that.
            return handleMap.insert(value)
         }
    }

    override fun lift(value: Long): ImageScaler {
        if ((value and 1.toLong()) == 0.toLong()) {
            // Rust-generated handle, construct a new class that uses the handle to implement the
            // interface
            return ImageScalerImpl(UniffiWithHandle, value)
        } else {
            // Kotlin-generated handle, get the object from the handle map
            return handleMap.remove(value)
        }
    }

    override fun read(buf: ByteBuffer): ImageScaler {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: ImageScaler) = 8UL

    override fun write(value: ImageScaler, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface LinkInboxInterface {
    
    fun `onBytes`(`deviceId`: kotlin.String, `bytes`: kotlin.ByteArray)
    
    fun `onConnected`(`device`: LinkDevice)
    
    fun `onDisconnected`(`deviceId`: kotlin.String)
    
    fun `onLinkFailed`(`deviceId`: kotlin.String, `name`: kotlin.String, `reason`: kotlin.String)
    
    fun `onSendFailed`(`deviceId`: kotlin.String)
    
    fun `onWriteComplete`(`deviceId`: kotlin.String)
    
    companion object
}

open class LinkInbox: Disposable, AutoCloseable, LinkInboxInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_linkinbox(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_linkinbox(handle, status)
        }
    }

    override fun `onBytes`(`deviceId`: kotlin.String, `bytes`: kotlin.ByteArray)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_linkinbox_on_bytes(
        it,
        
        FfiConverterString.lower(`deviceId`),
        FfiConverterByteArray.lower(`bytes`),_status)
}
    }
    
    

    override fun `onConnected`(`device`: LinkDevice)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_linkinbox_on_connected(
        it,
        
        FfiConverterTypeLinkDevice.lower(`device`),_status)
}
    }
    
    

    override fun `onDisconnected`(`deviceId`: kotlin.String)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_linkinbox_on_disconnected(
        it,
        
        FfiConverterString.lower(`deviceId`),_status)
}
    }
    
    

    override fun `onLinkFailed`(`deviceId`: kotlin.String, `name`: kotlin.String, `reason`: kotlin.String)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_linkinbox_on_link_failed(
        it,
        
        FfiConverterString.lower(`deviceId`),
        FfiConverterString.lower(`name`),
        FfiConverterString.lower(`reason`),_status)
}
    }
    
    

    override fun `onSendFailed`(`deviceId`: kotlin.String)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_linkinbox_on_send_failed(
        it,
        
        FfiConverterString.lower(`deviceId`),_status)
}
    }
    
    

    override fun `onWriteComplete`(`deviceId`: kotlin.String)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_linkinbox_on_write_complete(
        it,
        
        FfiConverterString.lower(`deviceId`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeLinkInbox: FfiConverter<LinkInbox, Long> {
    override fun lower(value: LinkInbox): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): LinkInbox {
        return LinkInbox(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): LinkInbox {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: LinkInbox) = 8UL

    override fun write(value: LinkInbox, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface LinkTransport {
    
    fun `maxBatchBytes`(): kotlin.UInt
    
    fun `start`(`inbox`: LinkInbox)
    
    fun `stop`()
    
    fun `send`(`deviceId`: kotlin.String, `batch`: kotlin.ByteArray)
    
    fun `disconnect`(`deviceId`: kotlin.String)
    
    fun `reconnect`(`deviceId`: kotlin.String)
    
    companion object
}

open class LinkTransportImpl: Disposable, AutoCloseable, LinkTransport
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_linktransport(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_linktransport(handle, status)
        }
    }

    override fun `maxBatchBytes`(): kotlin.UInt {
            return FfiConverterUInt.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_linktransport_max_batch_bytes(
        it,
        _status)
}
    }
    )
    }
    

    override fun `start`(`inbox`: LinkInbox)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_linktransport_start(
        it,
        
        FfiConverterTypeLinkInbox.lower(`inbox`),_status)
}
    }
    
    

    override fun `stop`()
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_linktransport_stop(
        it,
        _status)
}
    }
    
    

    override fun `send`(`deviceId`: kotlin.String, `batch`: kotlin.ByteArray)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_linktransport_send(
        it,
        
        FfiConverterString.lower(`deviceId`),
        FfiConverterByteArray.lower(`batch`),_status)
}
    }
    
    

    override fun `disconnect`(`deviceId`: kotlin.String)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_linktransport_disconnect(
        it,
        
        FfiConverterString.lower(`deviceId`),_status)
}
    }
    
    

    override fun `reconnect`(`deviceId`: kotlin.String)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_linktransport_reconnect(
        it,
        
        FfiConverterString.lower(`deviceId`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}



// Put the implementation in an object so we don't pollute the top-level namespace
internal object uniffiCallbackInterfaceLinkTransport {
    internal object `maxBatchBytes`: UniffiCallbackInterfaceLinkTransportMethod0 {
        override fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: IntByReference,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeLinkTransport.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`maxBatchBytes`(
                )
            }
            val writeReturn = { value: kotlin.UInt -> uniffiOutReturn.setValue(FfiConverterUInt.lower(value)) }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `start`: UniffiCallbackInterfaceLinkTransportMethod1 {
        override fun callback(`uniffiHandle`: Long,`inbox`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeLinkTransport.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`start`(
                    FfiConverterTypeLinkInbox.lift(`inbox`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `stop`: UniffiCallbackInterfaceLinkTransportMethod2 {
        override fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeLinkTransport.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`stop`(
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `send`: UniffiCallbackInterfaceLinkTransportMethod3 {
        override fun callback(`uniffiHandle`: Long,`deviceId`: RustBuffer.ByValue,`batch`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeLinkTransport.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`send`(
                    FfiConverterString.lift(`deviceId`),
                    FfiConverterByteArray.lift(`batch`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `disconnect`: UniffiCallbackInterfaceLinkTransportMethod4 {
        override fun callback(`uniffiHandle`: Long,`deviceId`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeLinkTransport.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`disconnect`(
                    FfiConverterString.lift(`deviceId`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `reconnect`: UniffiCallbackInterfaceLinkTransportMethod5 {
        override fun callback(`uniffiHandle`: Long,`deviceId`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeLinkTransport.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`reconnect`(
                    FfiConverterString.lift(`deviceId`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }

    internal object uniffiFree: UniffiCallbackInterfaceFree {
        override fun callback(handle: Long) {
            FfiConverterTypeLinkTransport.handleMap.remove(handle)
        }
    }

    internal object uniffiClone: UniffiCallbackInterfaceClone {
        override fun callback(handle: Long): Long {
            return FfiConverterTypeLinkTransport.handleMap.clone(handle)
        }
    }

    internal var vtable = UniffiVTableCallbackInterfaceLinkTransport.UniffiByValue(
        uniffiFree,
        uniffiClone,
        `maxBatchBytes`,
        `start`,
        `stop`,
        `send`,
        `disconnect`,
        `reconnect`,
    )

    // Registers the foreign callback with the Rust side.
    // This method is generated for each callback interface.
    internal fun register(lib: UniffiLib) {
        lib.uniffi_bridgething_companion_fn_init_callback_vtable_linktransport(vtable)
    }
}

/**
 * @suppress
 */
public object FfiConverterTypeLinkTransport: FfiConverter<LinkTransport, Long> {
    internal val handleMap = UniffiHandleMap<LinkTransport>()

    override fun lower(value: LinkTransport): Long {
        if (value is LinkTransportImpl) {
             // Rust-implemented object.  Clone the handle and return it
            return value.uniffiCloneHandle()
         } else {
            // Kotlin object, generate a new vtable handle and return that.
            return handleMap.insert(value)
         }
    }

    override fun lift(value: Long): LinkTransport {
        if ((value and 1.toLong()) == 0.toLong()) {
            // Rust-generated handle, construct a new class that uses the handle to implement the
            // interface
            return LinkTransportImpl(UniffiWithHandle, value)
        } else {
            // Kotlin-generated handle, get the object from the handle map
            return handleMap.remove(value)
        }
    }

    override fun read(buf: ByteBuffer): LinkTransport {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: LinkTransport) = 8UL

    override fun write(value: LinkTransport, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface LogInboxInterface {
    
    fun `push`(`level`: LogLevel, `target`: kotlin.String, `message`: kotlin.String)
    
    companion object
}

open class LogInbox: Disposable, AutoCloseable, LogInboxInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_loginbox(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_loginbox(handle, status)
        }
    }

    override fun `push`(`level`: LogLevel, `target`: kotlin.String, `message`: kotlin.String)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_loginbox_push(
        it,
        
        FfiConverterTypeLogLevel.lower(`level`),
        FfiConverterString.lower(`target`),
        FfiConverterString.lower(`message`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeLogInbox: FfiConverter<LogInbox, Long> {
    override fun lower(value: LogInbox): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): LogInbox {
        return LogInbox(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): LogInbox {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: LogInbox) = 8UL

    override fun write(value: LogInbox, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface LogSink {
    
    fun `onLine`(`level`: LogLevel, `target`: kotlin.String, `message`: kotlin.String)
    
    companion object
}

open class LogSinkImpl: Disposable, AutoCloseable, LogSink
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_logsink(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_logsink(handle, status)
        }
    }

    override fun `onLine`(`level`: LogLevel, `target`: kotlin.String, `message`: kotlin.String)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_logsink_on_line(
        it,
        
        FfiConverterTypeLogLevel.lower(`level`),
        FfiConverterString.lower(`target`),
        FfiConverterString.lower(`message`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}



// Put the implementation in an object so we don't pollute the top-level namespace
internal object uniffiCallbackInterfaceLogSink {
    internal object `onLine`: UniffiCallbackInterfaceLogSinkMethod0 {
        override fun callback(`uniffiHandle`: Long,`level`: RustBuffer.ByValue,`target`: RustBuffer.ByValue,`message`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeLogSink.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`onLine`(
                    FfiConverterTypeLogLevel.lift(`level`),
                    FfiConverterString.lift(`target`),
                    FfiConverterString.lift(`message`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }

    internal object uniffiFree: UniffiCallbackInterfaceFree {
        override fun callback(handle: Long) {
            FfiConverterTypeLogSink.handleMap.remove(handle)
        }
    }

    internal object uniffiClone: UniffiCallbackInterfaceClone {
        override fun callback(handle: Long): Long {
            return FfiConverterTypeLogSink.handleMap.clone(handle)
        }
    }

    internal var vtable = UniffiVTableCallbackInterfaceLogSink.UniffiByValue(
        uniffiFree,
        uniffiClone,
        `onLine`,
    )

    // Registers the foreign callback with the Rust side.
    // This method is generated for each callback interface.
    internal fun register(lib: UniffiLib) {
        lib.uniffi_bridgething_companion_fn_init_callback_vtable_logsink(vtable)
    }
}

/**
 * @suppress
 */
public object FfiConverterTypeLogSink: FfiConverter<LogSink, Long> {
    internal val handleMap = UniffiHandleMap<LogSink>()

    override fun lower(value: LogSink): Long {
        if (value is LogSinkImpl) {
             // Rust-implemented object.  Clone the handle and return it
            return value.uniffiCloneHandle()
         } else {
            // Kotlin object, generate a new vtable handle and return that.
            return handleMap.insert(value)
         }
    }

    override fun lift(value: Long): LogSink {
        if ((value and 1.toLong()) == 0.toLong()) {
            // Rust-generated handle, construct a new class that uses the handle to implement the
            // interface
            return LogSinkImpl(UniffiWithHandle, value)
        } else {
            // Kotlin-generated handle, get the object from the handle map
            return handleMap.remove(value)
        }
    }

    override fun read(buf: ByteBuffer): LogSink {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: LogSink) = 8UL

    override fun write(value: LogSink, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface LogStoreInterface {
    
    fun `archives`(): List<LogArchive>
    
    fun `clear`()
    
    fun `delete`(`id`: kotlin.String)
    
    fun `exportTo`(`target`: kotlin.String, `id`: kotlin.String?): kotlin.String
    
    fun `flush`()
    
    fun `read`(`id`: kotlin.String, `limit`: kotlin.UInt): List<LogStoreLine>
    
    fun `record`(`level`: LogStoreLevel, `label`: kotlin.String, `message`: kotlin.String)
    
    fun `retainedBytes`(): kotlin.ULong
    
    fun `write`(`line`: kotlin.String)
    
    companion object
}

open class LogStore: Disposable, AutoCloseable, LogStoreInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_logstore(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_logstore(handle, status)
        }
    }

    override fun `archives`(): List<LogArchive> {
            return FfiConverterSequenceTypeLogArchive.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_logstore_archives(
        it,
        _status)
}
    }
    )
    }
    

    override fun `clear`()
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_logstore_clear(
        it,
        _status)
}
    }
    
    

    override fun `delete`(`id`: kotlin.String)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_logstore_delete(
        it,
        
        FfiConverterString.lower(`id`),_status)
}
    }
    
    

    
    @Throws(CompanionException::class)override fun `exportTo`(`target`: kotlin.String, `id`: kotlin.String?): kotlin.String {
            return FfiConverterString.lift(
    callWithHandle {
    uniffiRustCallWithError(CompanionException) { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_logstore_export_to(
        it,
        
        FfiConverterString.lower(`target`),
        FfiConverterOptionalString.lower(`id`),_status)
}
    }
    )
    }
    

    override fun `flush`()
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_logstore_flush(
        it,
        _status)
}
    }
    
    

    override fun `read`(`id`: kotlin.String, `limit`: kotlin.UInt): List<LogStoreLine> {
            return FfiConverterSequenceTypeLogStoreLine.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_logstore_read(
        it,
        
        FfiConverterString.lower(`id`),
        FfiConverterUInt.lower(`limit`),_status)
}
    }
    )
    }
    

    override fun `record`(`level`: LogStoreLevel, `label`: kotlin.String, `message`: kotlin.String)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_logstore_record(
        it,
        
        FfiConverterTypeLogStoreLevel.lower(`level`),
        FfiConverterString.lower(`label`),
        FfiConverterString.lower(`message`),_status)
}
    }
    
    

    override fun `retainedBytes`(): kotlin.ULong {
            return FfiConverterULong.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_logstore_retained_bytes(
        it,
        _status)
}
    }
    )
    }
    

    override fun `write`(`line`: kotlin.String)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_logstore_write(
        it,
        
        FfiConverterString.lower(`line`),_status)
}
    }
    
    

    

    


    
    companion object {
         fun `install`(`root`: kotlin.String): LogStore {
            return FfiConverterTypeLogStore.lift(
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_constructor_logstore_install(
    
        
        FfiConverterString.lower(`root`),_status)
}
    )
    }
    

        
    }
    
}


/**
 * @suppress
 */
public object FfiConverterTypeLogStore: FfiConverter<LogStore, Long> {
    override fun lower(value: LogStore): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): LogStore {
        return LogStore(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): LogStore {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: LogStore) = 8UL

    override fun write(value: LogStore, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface MediaArtSinkInterface {
    
    fun `complete`(`art`: MediaArt?)
    
    companion object
}

open class MediaArtSink: Disposable, AutoCloseable, MediaArtSinkInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_mediaartsink(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_mediaartsink(handle, status)
        }
    }

    override fun `complete`(`art`: MediaArt?)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_mediaartsink_complete(
        it,
        
        FfiConverterOptionalTypeMediaArt.lower(`art`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeMediaArtSink: FfiConverter<MediaArtSink, Long> {
    override fun lower(value: MediaArtSink): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): MediaArtSink {
        return MediaArtSink(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): MediaArtSink {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: MediaArtSink) = 8UL

    override fun write(value: MediaArtSink, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface MediaSessionBackend {
    
    fun `isAccessGranted`(): kotlin.Boolean
    
    fun `start`(`inbox`: MediaSessionInbox)
    
    fun `stop`()
    
    fun `snapshotAll`(`sink`: MediaSnapshotSink)
    
    fun `control`(`package`: kotlin.String, `cmd`: MediaControl)
    
    fun `art`(`package`: kotlin.String, `token`: kotlin.String, `sink`: MediaArtSink)
    
    companion object
}

open class MediaSessionBackendImpl: Disposable, AutoCloseable, MediaSessionBackend
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_mediasessionbackend(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_mediasessionbackend(handle, status)
        }
    }

    override fun `isAccessGranted`(): kotlin.Boolean {
            return FfiConverterBoolean.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_mediasessionbackend_is_access_granted(
        it,
        _status)
}
    }
    )
    }
    

    override fun `start`(`inbox`: MediaSessionInbox)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_mediasessionbackend_start(
        it,
        
        FfiConverterTypeMediaSessionInbox.lower(`inbox`),_status)
}
    }
    
    

    override fun `stop`()
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_mediasessionbackend_stop(
        it,
        _status)
}
    }
    
    

    override fun `snapshotAll`(`sink`: MediaSnapshotSink)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_mediasessionbackend_snapshot_all(
        it,
        
        FfiConverterTypeMediaSnapshotSink.lower(`sink`),_status)
}
    }
    
    

    override fun `control`(`package`: kotlin.String, `cmd`: MediaControl)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_mediasessionbackend_control(
        it,
        
        FfiConverterString.lower(`package`),
        FfiConverterTypeMediaControl.lower(`cmd`),_status)
}
    }
    
    

    override fun `art`(`package`: kotlin.String, `token`: kotlin.String, `sink`: MediaArtSink)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_mediasessionbackend_art(
        it,
        
        FfiConverterString.lower(`package`),
        FfiConverterString.lower(`token`),
        FfiConverterTypeMediaArtSink.lower(`sink`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}



// Put the implementation in an object so we don't pollute the top-level namespace
internal object uniffiCallbackInterfaceMediaSessionBackend {
    internal object `isAccessGranted`: UniffiCallbackInterfaceMediaSessionBackendMethod0 {
        override fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: ByteByReference,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeMediaSessionBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`isAccessGranted`(
                )
            }
            val writeReturn = { value: kotlin.Boolean -> uniffiOutReturn.setValue(FfiConverterBoolean.lower(value)) }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `start`: UniffiCallbackInterfaceMediaSessionBackendMethod1 {
        override fun callback(`uniffiHandle`: Long,`inbox`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeMediaSessionBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`start`(
                    FfiConverterTypeMediaSessionInbox.lift(`inbox`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `stop`: UniffiCallbackInterfaceMediaSessionBackendMethod2 {
        override fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeMediaSessionBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`stop`(
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `snapshotAll`: UniffiCallbackInterfaceMediaSessionBackendMethod3 {
        override fun callback(`uniffiHandle`: Long,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeMediaSessionBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`snapshotAll`(
                    FfiConverterTypeMediaSnapshotSink.lift(`sink`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `control`: UniffiCallbackInterfaceMediaSessionBackendMethod4 {
        override fun callback(`uniffiHandle`: Long,`package`: RustBuffer.ByValue,`cmd`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeMediaSessionBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`control`(
                    FfiConverterString.lift(`package`),
                    FfiConverterTypeMediaControl.lift(`cmd`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `art`: UniffiCallbackInterfaceMediaSessionBackendMethod5 {
        override fun callback(`uniffiHandle`: Long,`package`: RustBuffer.ByValue,`token`: RustBuffer.ByValue,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeMediaSessionBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`art`(
                    FfiConverterString.lift(`package`),
                    FfiConverterString.lift(`token`),
                    FfiConverterTypeMediaArtSink.lift(`sink`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }

    internal object uniffiFree: UniffiCallbackInterfaceFree {
        override fun callback(handle: Long) {
            FfiConverterTypeMediaSessionBackend.handleMap.remove(handle)
        }
    }

    internal object uniffiClone: UniffiCallbackInterfaceClone {
        override fun callback(handle: Long): Long {
            return FfiConverterTypeMediaSessionBackend.handleMap.clone(handle)
        }
    }

    internal var vtable = UniffiVTableCallbackInterfaceMediaSessionBackend.UniffiByValue(
        uniffiFree,
        uniffiClone,
        `isAccessGranted`,
        `start`,
        `stop`,
        `snapshotAll`,
        `control`,
        `art`,
    )

    // Registers the foreign callback with the Rust side.
    // This method is generated for each callback interface.
    internal fun register(lib: UniffiLib) {
        lib.uniffi_bridgething_companion_fn_init_callback_vtable_mediasessionbackend(vtable)
    }
}

/**
 * @suppress
 */
public object FfiConverterTypeMediaSessionBackend: FfiConverter<MediaSessionBackend, Long> {
    internal val handleMap = UniffiHandleMap<MediaSessionBackend>()

    override fun lower(value: MediaSessionBackend): Long {
        if (value is MediaSessionBackendImpl) {
             // Rust-implemented object.  Clone the handle and return it
            return value.uniffiCloneHandle()
         } else {
            // Kotlin object, generate a new vtable handle and return that.
            return handleMap.insert(value)
         }
    }

    override fun lift(value: Long): MediaSessionBackend {
        if ((value and 1.toLong()) == 0.toLong()) {
            // Rust-generated handle, construct a new class that uses the handle to implement the
            // interface
            return MediaSessionBackendImpl(UniffiWithHandle, value)
        } else {
            // Kotlin-generated handle, get the object from the handle map
            return handleMap.remove(value)
        }
    }

    override fun read(buf: ByteBuffer): MediaSessionBackend {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: MediaSessionBackend) = 8UL

    override fun write(value: MediaSessionBackend, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface MediaSessionInboxInterface {
    
    fun `onSessionsChanged`()
    
    companion object
}

open class MediaSessionInbox: Disposable, AutoCloseable, MediaSessionInboxInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_mediasessioninbox(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_mediasessioninbox(handle, status)
        }
    }

    override fun `onSessionsChanged`()
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_mediasessioninbox_on_sessions_changed(
        it,
        _status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeMediaSessionInbox: FfiConverter<MediaSessionInbox, Long> {
    override fun lower(value: MediaSessionInbox): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): MediaSessionInbox {
        return MediaSessionInbox(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): MediaSessionInbox {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: MediaSessionInbox) = 8UL

    override fun write(value: MediaSessionInbox, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface MediaSnapshotSinkInterface {
    
    fun `complete`(`sessions`: List<MediaSessionSnapshot>)
    
    companion object
}

open class MediaSnapshotSink: Disposable, AutoCloseable, MediaSnapshotSinkInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_mediasnapshotsink(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_mediasnapshotsink(handle, status)
        }
    }

    override fun `complete`(`sessions`: List<MediaSessionSnapshot>)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_mediasnapshotsink_complete(
        it,
        
        FfiConverterSequenceTypeMediaSessionSnapshot.lower(`sessions`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeMediaSnapshotSink: FfiConverter<MediaSnapshotSink, Long> {
    override fun lower(value: MediaSnapshotSink): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): MediaSnapshotSink {
        return MediaSnapshotSink(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): MediaSnapshotSink {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: MediaSnapshotSink) = 8UL

    override fun write(value: MediaSnapshotSink, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface ModelArtifactValidator {
    
    fun `validate`(`kind`: ModelArtifactKind, `path`: kotlin.String)
    
    companion object
}

open class ModelArtifactValidatorImpl: Disposable, AutoCloseable, ModelArtifactValidator
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_modelartifactvalidator(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_modelartifactvalidator(handle, status)
        }
    }

    
    @Throws(ModelValidationException::class)override fun `validate`(`kind`: ModelArtifactKind, `path`: kotlin.String)
        = 
    callWithHandle {
    uniffiRustCallWithError(ModelValidationException) { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_modelartifactvalidator_validate(
        it,
        
        FfiConverterTypeModelArtifactKind.lower(`kind`),
        FfiConverterString.lower(`path`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}



// Put the implementation in an object so we don't pollute the top-level namespace
internal object uniffiCallbackInterfaceModelArtifactValidator {
    internal object `validate`: UniffiCallbackInterfaceModelArtifactValidatorMethod0 {
        override fun callback(`uniffiHandle`: Long,`kind`: RustBuffer.ByValue,`path`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeModelArtifactValidator.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`validate`(
                    FfiConverterTypeModelArtifactKind.lift(`kind`),
                    FfiConverterString.lift(`path`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCallWithError(
                uniffiCallStatus,
                makeCall,
                writeReturn,
                { e: ModelValidationException -> FfiConverterTypeModelValidationError.lower(e) }
            )
        }
    }

    internal object uniffiFree: UniffiCallbackInterfaceFree {
        override fun callback(handle: Long) {
            FfiConverterTypeModelArtifactValidator.handleMap.remove(handle)
        }
    }

    internal object uniffiClone: UniffiCallbackInterfaceClone {
        override fun callback(handle: Long): Long {
            return FfiConverterTypeModelArtifactValidator.handleMap.clone(handle)
        }
    }

    internal var vtable = UniffiVTableCallbackInterfaceModelArtifactValidator.UniffiByValue(
        uniffiFree,
        uniffiClone,
        `validate`,
    )

    // Registers the foreign callback with the Rust side.
    // This method is generated for each callback interface.
    internal fun register(lib: UniffiLib) {
        lib.uniffi_bridgething_companion_fn_init_callback_vtable_modelartifactvalidator(vtable)
    }
}

/**
 * @suppress
 */
public object FfiConverterTypeModelArtifactValidator: FfiConverter<ModelArtifactValidator, Long> {
    internal val handleMap = UniffiHandleMap<ModelArtifactValidator>()

    override fun lower(value: ModelArtifactValidator): Long {
        if (value is ModelArtifactValidatorImpl) {
             // Rust-implemented object.  Clone the handle and return it
            return value.uniffiCloneHandle()
         } else {
            // Kotlin object, generate a new vtable handle and return that.
            return handleMap.insert(value)
         }
    }

    override fun lift(value: Long): ModelArtifactValidator {
        if ((value and 1.toLong()) == 0.toLong()) {
            // Rust-generated handle, construct a new class that uses the handle to implement the
            // interface
            return ModelArtifactValidatorImpl(UniffiWithHandle, value)
        } else {
            // Kotlin-generated handle, get the object from the handle map
            return handleMap.remove(value)
        }
    }

    override fun read(buf: ByteBuffer): ModelArtifactValidator {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: ModelArtifactValidator) = 8UL

    override fun write(value: ModelArtifactValidator, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface NluModelRunner {
    
    fun `prewarm`()
    
    fun `predict`(`inputIds`: List<kotlin.Int>, `attentionMask`: List<kotlin.Int>): NluModelOutputs
    
    companion object
}

open class NluModelRunnerImpl: Disposable, AutoCloseable, NluModelRunner
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_nlumodelrunner(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_nlumodelrunner(handle, status)
        }
    }

    override fun `prewarm`()
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_nlumodelrunner_prewarm(
        it,
        _status)
}
    }
    
    

    
    @Throws(NluRunnerException::class)override fun `predict`(`inputIds`: List<kotlin.Int>, `attentionMask`: List<kotlin.Int>): NluModelOutputs {
            return FfiConverterTypeNluModelOutputs.lift(
    callWithHandle {
    uniffiRustCallWithError(NluRunnerException) { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_nlumodelrunner_predict(
        it,
        
        FfiConverterSequenceInt.lower(`inputIds`),
        FfiConverterSequenceInt.lower(`attentionMask`),_status)
}
    }
    )
    }
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}



// Put the implementation in an object so we don't pollute the top-level namespace
internal object uniffiCallbackInterfaceNluModelRunner {
    internal object `prewarm`: UniffiCallbackInterfaceNluModelRunnerMethod0 {
        override fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeNluModelRunner.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`prewarm`(
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `predict`: UniffiCallbackInterfaceNluModelRunnerMethod1 {
        override fun callback(`uniffiHandle`: Long,`inputIds`: RustBuffer.ByValue,`attentionMask`: RustBuffer.ByValue,`uniffiOutReturn`: RustBuffer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeNluModelRunner.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`predict`(
                    FfiConverterSequenceInt.lift(`inputIds`),
                    FfiConverterSequenceInt.lift(`attentionMask`),
                )
            }
            val writeReturn = { value: NluModelOutputs -> uniffiOutReturn.setValue(FfiConverterTypeNluModelOutputs.lower(value)) }
            uniffiTraitInterfaceCallWithError(
                uniffiCallStatus,
                makeCall,
                writeReturn,
                { e: NluRunnerException -> FfiConverterTypeNluRunnerError.lower(e) }
            )
        }
    }

    internal object uniffiFree: UniffiCallbackInterfaceFree {
        override fun callback(handle: Long) {
            FfiConverterTypeNluModelRunner.handleMap.remove(handle)
        }
    }

    internal object uniffiClone: UniffiCallbackInterfaceClone {
        override fun callback(handle: Long): Long {
            return FfiConverterTypeNluModelRunner.handleMap.clone(handle)
        }
    }

    internal var vtable = UniffiVTableCallbackInterfaceNluModelRunner.UniffiByValue(
        uniffiFree,
        uniffiClone,
        `prewarm`,
        `predict`,
    )

    // Registers the foreign callback with the Rust side.
    // This method is generated for each callback interface.
    internal fun register(lib: UniffiLib) {
        lib.uniffi_bridgething_companion_fn_init_callback_vtable_nlumodelrunner(vtable)
    }
}

/**
 * @suppress
 */
public object FfiConverterTypeNluModelRunner: FfiConverter<NluModelRunner, Long> {
    internal val handleMap = UniffiHandleMap<NluModelRunner>()

    override fun lower(value: NluModelRunner): Long {
        if (value is NluModelRunnerImpl) {
             // Rust-implemented object.  Clone the handle and return it
            return value.uniffiCloneHandle()
         } else {
            // Kotlin object, generate a new vtable handle and return that.
            return handleMap.insert(value)
         }
    }

    override fun lift(value: Long): NluModelRunner {
        if ((value and 1.toLong()) == 0.toLong()) {
            // Rust-generated handle, construct a new class that uses the handle to implement the
            // interface
            return NluModelRunnerImpl(UniffiWithHandle, value)
        } else {
            // Kotlin-generated handle, get the object from the handle map
            return handleMap.remove(value)
        }
    }

    override fun read(buf: ByteBuffer): NluModelRunner {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: NluModelRunner) = 8UL

    override fun write(value: NluModelRunner, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface NotificationBackend {
    
    fun `start`(`inbox`: NotificationInbox)
    
    fun `stop`()
    
    fun `invokePositive`(`id`: kotlin.String, `sink`: ActionSink)
    
    fun `invokeNegative`(`id`: kotlin.String, `sink`: ActionSink)
    
    companion object
}

open class NotificationBackendImpl: Disposable, AutoCloseable, NotificationBackend
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_notificationbackend(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_notificationbackend(handle, status)
        }
    }

    override fun `start`(`inbox`: NotificationInbox)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_notificationbackend_start(
        it,
        
        FfiConverterTypeNotificationInbox.lower(`inbox`),_status)
}
    }
    
    

    override fun `stop`()
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_notificationbackend_stop(
        it,
        _status)
}
    }
    
    

    override fun `invokePositive`(`id`: kotlin.String, `sink`: ActionSink)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_notificationbackend_invoke_positive(
        it,
        
        FfiConverterString.lower(`id`),
        FfiConverterTypeActionSink.lower(`sink`),_status)
}
    }
    
    

    override fun `invokeNegative`(`id`: kotlin.String, `sink`: ActionSink)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_notificationbackend_invoke_negative(
        it,
        
        FfiConverterString.lower(`id`),
        FfiConverterTypeActionSink.lower(`sink`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}



// Put the implementation in an object so we don't pollute the top-level namespace
internal object uniffiCallbackInterfaceNotificationBackend {
    internal object `start`: UniffiCallbackInterfaceNotificationBackendMethod0 {
        override fun callback(`uniffiHandle`: Long,`inbox`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeNotificationBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`start`(
                    FfiConverterTypeNotificationInbox.lift(`inbox`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `stop`: UniffiCallbackInterfaceNotificationBackendMethod1 {
        override fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeNotificationBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`stop`(
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `invokePositive`: UniffiCallbackInterfaceNotificationBackendMethod2 {
        override fun callback(`uniffiHandle`: Long,`id`: RustBuffer.ByValue,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeNotificationBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`invokePositive`(
                    FfiConverterString.lift(`id`),
                    FfiConverterTypeActionSink.lift(`sink`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `invokeNegative`: UniffiCallbackInterfaceNotificationBackendMethod3 {
        override fun callback(`uniffiHandle`: Long,`id`: RustBuffer.ByValue,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeNotificationBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`invokeNegative`(
                    FfiConverterString.lift(`id`),
                    FfiConverterTypeActionSink.lift(`sink`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }

    internal object uniffiFree: UniffiCallbackInterfaceFree {
        override fun callback(handle: Long) {
            FfiConverterTypeNotificationBackend.handleMap.remove(handle)
        }
    }

    internal object uniffiClone: UniffiCallbackInterfaceClone {
        override fun callback(handle: Long): Long {
            return FfiConverterTypeNotificationBackend.handleMap.clone(handle)
        }
    }

    internal var vtable = UniffiVTableCallbackInterfaceNotificationBackend.UniffiByValue(
        uniffiFree,
        uniffiClone,
        `start`,
        `stop`,
        `invokePositive`,
        `invokeNegative`,
    )

    // Registers the foreign callback with the Rust side.
    // This method is generated for each callback interface.
    internal fun register(lib: UniffiLib) {
        lib.uniffi_bridgething_companion_fn_init_callback_vtable_notificationbackend(vtable)
    }
}

/**
 * @suppress
 */
public object FfiConverterTypeNotificationBackend: FfiConverter<NotificationBackend, Long> {
    internal val handleMap = UniffiHandleMap<NotificationBackend>()

    override fun lower(value: NotificationBackend): Long {
        if (value is NotificationBackendImpl) {
             // Rust-implemented object.  Clone the handle and return it
            return value.uniffiCloneHandle()
         } else {
            // Kotlin object, generate a new vtable handle and return that.
            return handleMap.insert(value)
         }
    }

    override fun lift(value: Long): NotificationBackend {
        if ((value and 1.toLong()) == 0.toLong()) {
            // Rust-generated handle, construct a new class that uses the handle to implement the
            // interface
            return NotificationBackendImpl(UniffiWithHandle, value)
        } else {
            // Kotlin-generated handle, get the object from the handle map
            return handleMap.remove(value)
        }
    }

    override fun read(buf: ByteBuffer): NotificationBackend {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: NotificationBackend) = 8UL

    override fun write(value: NotificationBackend, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface NotificationInboxInterface {
    
    fun `onPosted`(`notification`: WireNotification)
    
    fun `onRemoved`(`removed`: NotificationRemoved)
    
    companion object
}

open class NotificationInbox: Disposable, AutoCloseable, NotificationInboxInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_notificationinbox(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_notificationinbox(handle, status)
        }
    }

    override fun `onPosted`(`notification`: WireNotification)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_notificationinbox_on_posted(
        it,
        
        FfiConverterTypeWireNotification.lower(`notification`),_status)
}
    }
    
    

    override fun `onRemoved`(`removed`: NotificationRemoved)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_notificationinbox_on_removed(
        it,
        
        FfiConverterTypeNotificationRemoved.lower(`removed`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeNotificationInbox: FfiConverter<NotificationInbox, Long> {
    override fun lower(value: NotificationInbox): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): NotificationInbox {
        return NotificationInbox(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): NotificationInbox {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: NotificationInbox) = 8UL

    override fun write(value: NotificationInbox, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface OtaRunStoreInterface {
    
    fun `annotateWebapp`(`deviceId`: kotlin.String, `webappId`: kotlin.String?, `webappName`: kotlin.String?): OtaRun?
    
    fun `available`(): List<OtaAvailable>
    
    fun `dismiss`(`deviceId`: kotlin.String): OtaRun?
    
    fun `ingest`(`event`: OtaPollEvent): List<OtaStoreChange>
    
    fun `interrupt`(`deviceId`: kotlin.String): OtaRun?
    
    fun `noteMeta`(`deviceId`: kotlin.String, `daemonVersion`: kotlin.String, `imageVersion`: kotlin.String): OtaRun?
    
    fun `openRunKind`(`deviceId`: kotlin.String): OtaKind?
    
    fun `pollStatus`(): OtaPollStatus
    
    fun `runs`(): List<OtaRun>
    
    companion object
}

open class OtaRunStore: Disposable, AutoCloseable, OtaRunStoreInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }
    constructor() :
        this(UniffiWithHandle, 
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_constructor_otarunstore_new(
    
        _status)
}
    )

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_otarunstore(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_otarunstore(handle, status)
        }
    }

    override fun `annotateWebapp`(`deviceId`: kotlin.String, `webappId`: kotlin.String?, `webappName`: kotlin.String?): OtaRun? {
            return FfiConverterOptionalTypeOtaRun.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_otarunstore_annotate_webapp(
        it,
        
        FfiConverterString.lower(`deviceId`),
        FfiConverterOptionalString.lower(`webappId`),
        FfiConverterOptionalString.lower(`webappName`),_status)
}
    }
    )
    }
    

    override fun `available`(): List<OtaAvailable> {
            return FfiConverterSequenceTypeOtaAvailable.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_otarunstore_available(
        it,
        _status)
}
    }
    )
    }
    

    override fun `dismiss`(`deviceId`: kotlin.String): OtaRun? {
            return FfiConverterOptionalTypeOtaRun.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_otarunstore_dismiss(
        it,
        
        FfiConverterString.lower(`deviceId`),_status)
}
    }
    )
    }
    

    override fun `ingest`(`event`: OtaPollEvent): List<OtaStoreChange> {
            return FfiConverterSequenceTypeOtaStoreChange.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_otarunstore_ingest(
        it,
        
        FfiConverterTypeOtaPollEvent.lower(`event`),_status)
}
    }
    )
    }
    

    override fun `interrupt`(`deviceId`: kotlin.String): OtaRun? {
            return FfiConverterOptionalTypeOtaRun.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_otarunstore_interrupt(
        it,
        
        FfiConverterString.lower(`deviceId`),_status)
}
    }
    )
    }
    

    override fun `noteMeta`(`deviceId`: kotlin.String, `daemonVersion`: kotlin.String, `imageVersion`: kotlin.String): OtaRun? {
            return FfiConverterOptionalTypeOtaRun.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_otarunstore_note_meta(
        it,
        
        FfiConverterString.lower(`deviceId`),
        FfiConverterString.lower(`daemonVersion`),
        FfiConverterString.lower(`imageVersion`),_status)
}
    }
    )
    }
    

    override fun `openRunKind`(`deviceId`: kotlin.String): OtaKind? {
            return FfiConverterOptionalTypeOtaKind.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_otarunstore_open_run_kind(
        it,
        
        FfiConverterString.lower(`deviceId`),_status)
}
    }
    )
    }
    

    override fun `pollStatus`(): OtaPollStatus {
            return FfiConverterTypeOtaPollStatus.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_otarunstore_poll_status(
        it,
        _status)
}
    }
    )
    }
    

    override fun `runs`(): List<OtaRun> {
            return FfiConverterSequenceTypeOtaRun.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_otarunstore_runs(
        it,
        _status)
}
    }
    )
    }
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeOtaRunStore: FfiConverter<OtaRunStore, Long> {
    override fun lower(value: OtaRunStore): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): OtaRunStore {
        return OtaRunStore(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): OtaRunStore {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: OtaRunStore) = 8UL

    override fun write(value: OtaRunStore, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface PhoneBackend {
    
    fun `start`(`inbox`: PhoneInbox)
    
    fun `stop`()
    
    fun `command`(`cmd`: PhoneCommand)
    
    fun `stateGet`(`sink`: PhoneStateSink)
    
    companion object
}

open class PhoneBackendImpl: Disposable, AutoCloseable, PhoneBackend
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_phonebackend(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_phonebackend(handle, status)
        }
    }

    override fun `start`(`inbox`: PhoneInbox)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_phonebackend_start(
        it,
        
        FfiConverterTypePhoneInbox.lower(`inbox`),_status)
}
    }
    
    

    override fun `stop`()
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_phonebackend_stop(
        it,
        _status)
}
    }
    
    

    override fun `command`(`cmd`: PhoneCommand)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_phonebackend_command(
        it,
        
        FfiConverterTypePhoneCommand.lower(`cmd`),_status)
}
    }
    
    

    override fun `stateGet`(`sink`: PhoneStateSink)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_phonebackend_state_get(
        it,
        
        FfiConverterTypePhoneStateSink.lower(`sink`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}



// Put the implementation in an object so we don't pollute the top-level namespace
internal object uniffiCallbackInterfacePhoneBackend {
    internal object `start`: UniffiCallbackInterfacePhoneBackendMethod0 {
        override fun callback(`uniffiHandle`: Long,`inbox`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypePhoneBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`start`(
                    FfiConverterTypePhoneInbox.lift(`inbox`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `stop`: UniffiCallbackInterfacePhoneBackendMethod1 {
        override fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypePhoneBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`stop`(
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `command`: UniffiCallbackInterfacePhoneBackendMethod2 {
        override fun callback(`uniffiHandle`: Long,`cmd`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypePhoneBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`command`(
                    FfiConverterTypePhoneCommand.lift(`cmd`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `stateGet`: UniffiCallbackInterfacePhoneBackendMethod3 {
        override fun callback(`uniffiHandle`: Long,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypePhoneBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`stateGet`(
                    FfiConverterTypePhoneStateSink.lift(`sink`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }

    internal object uniffiFree: UniffiCallbackInterfaceFree {
        override fun callback(handle: Long) {
            FfiConverterTypePhoneBackend.handleMap.remove(handle)
        }
    }

    internal object uniffiClone: UniffiCallbackInterfaceClone {
        override fun callback(handle: Long): Long {
            return FfiConverterTypePhoneBackend.handleMap.clone(handle)
        }
    }

    internal var vtable = UniffiVTableCallbackInterfacePhoneBackend.UniffiByValue(
        uniffiFree,
        uniffiClone,
        `start`,
        `stop`,
        `command`,
        `stateGet`,
    )

    // Registers the foreign callback with the Rust side.
    // This method is generated for each callback interface.
    internal fun register(lib: UniffiLib) {
        lib.uniffi_bridgething_companion_fn_init_callback_vtable_phonebackend(vtable)
    }
}

/**
 * @suppress
 */
public object FfiConverterTypePhoneBackend: FfiConverter<PhoneBackend, Long> {
    internal val handleMap = UniffiHandleMap<PhoneBackend>()

    override fun lower(value: PhoneBackend): Long {
        if (value is PhoneBackendImpl) {
             // Rust-implemented object.  Clone the handle and return it
            return value.uniffiCloneHandle()
         } else {
            // Kotlin object, generate a new vtable handle and return that.
            return handleMap.insert(value)
         }
    }

    override fun lift(value: Long): PhoneBackend {
        if ((value and 1.toLong()) == 0.toLong()) {
            // Rust-generated handle, construct a new class that uses the handle to implement the
            // interface
            return PhoneBackendImpl(UniffiWithHandle, value)
        } else {
            // Kotlin-generated handle, get the object from the handle map
            return handleMap.remove(value)
        }
    }

    override fun read(buf: ByteBuffer): PhoneBackend {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: PhoneBackend) = 8UL

    override fun write(value: PhoneBackend, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface PhoneInboxInterface {
    
    fun `onCallEnded`(`ended`: PhoneCallEnded)
    
    fun `onCallStarted`(`call`: PhoneCall)
    
    fun `onCallUpdated`(`call`: PhoneCall)
    
    fun `onCommunications`(`state`: CommunicationsState)
    
    fun `onState`(`state`: PhoneState)
    
    companion object
}

open class PhoneInbox: Disposable, AutoCloseable, PhoneInboxInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_phoneinbox(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_phoneinbox(handle, status)
        }
    }

    override fun `onCallEnded`(`ended`: PhoneCallEnded)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_phoneinbox_on_call_ended(
        it,
        
        FfiConverterTypePhoneCallEnded.lower(`ended`),_status)
}
    }
    
    

    override fun `onCallStarted`(`call`: PhoneCall)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_phoneinbox_on_call_started(
        it,
        
        FfiConverterTypePhoneCall.lower(`call`),_status)
}
    }
    
    

    override fun `onCallUpdated`(`call`: PhoneCall)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_phoneinbox_on_call_updated(
        it,
        
        FfiConverterTypePhoneCall.lower(`call`),_status)
}
    }
    
    

    override fun `onCommunications`(`state`: CommunicationsState)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_phoneinbox_on_communications(
        it,
        
        FfiConverterTypeCommunicationsState.lower(`state`),_status)
}
    }
    
    

    override fun `onState`(`state`: PhoneState)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_phoneinbox_on_state(
        it,
        
        FfiConverterTypePhoneState.lower(`state`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypePhoneInbox: FfiConverter<PhoneInbox, Long> {
    override fun lower(value: PhoneInbox): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): PhoneInbox {
        return PhoneInbox(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): PhoneInbox {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: PhoneInbox) = 8UL

    override fun write(value: PhoneInbox, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface PhoneStateSinkInterface {
    
    fun `complete`(`state`: PhoneState)
    
    fun `fail`(`reason`: kotlin.String)
    
    companion object
}

open class PhoneStateSink: Disposable, AutoCloseable, PhoneStateSinkInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_phonestatesink(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_phonestatesink(handle, status)
        }
    }

    override fun `complete`(`state`: PhoneState)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_phonestatesink_complete(
        it,
        
        FfiConverterTypePhoneState.lower(`state`),_status)
}
    }
    
    

    override fun `fail`(`reason`: kotlin.String)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_phonestatesink_fail(
        it,
        
        FfiConverterString.lower(`reason`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypePhoneStateSink: FfiConverter<PhoneStateSink, Long> {
    override fun lower(value: PhoneStateSink): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): PhoneStateSink {
        return PhoneStateSink(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): PhoneStateSink {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: PhoneStateSink) = 8UL

    override fun write(value: PhoneStateSink, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface PrepareSinkInterface {
    
    fun `onFailed`(`reason`: kotlin.String)
    
    fun `onProgress`(`received`: kotlin.ULong, `total`: kotlin.ULong)
    
    fun `onReady`()
    
    companion object
}

open class PrepareSink: Disposable, AutoCloseable, PrepareSinkInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_preparesink(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_preparesink(handle, status)
        }
    }

    override fun `onFailed`(`reason`: kotlin.String)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_preparesink_on_failed(
        it,
        
        FfiConverterString.lower(`reason`),_status)
}
    }
    
    

    override fun `onProgress`(`received`: kotlin.ULong, `total`: kotlin.ULong)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_preparesink_on_progress(
        it,
        
        FfiConverterULong.lower(`received`),
        FfiConverterULong.lower(`total`),_status)
}
    }
    
    

    override fun `onReady`()
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_preparesink_on_ready(
        it,
        _status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypePrepareSink: FfiConverter<PrepareSink, Long> {
    override fun lower(value: PrepareSink): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): PrepareSink {
        return PrepareSink(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): PrepareSink {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: PrepareSink) = 8UL

    override fun write(value: PrepareSink, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface SecretStore {
    
    fun `get`(`key`: kotlin.String): kotlin.String?
    
    fun `set`(`key`: kotlin.String, `value`: kotlin.String)
    
    fun `remove`(`key`: kotlin.String)
    
    fun `getBlob`(`key`: kotlin.String): kotlin.ByteArray?
    
    companion object
}

open class SecretStoreImpl: Disposable, AutoCloseable, SecretStore
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_secretstore(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_secretstore(handle, status)
        }
    }

    override fun `get`(`key`: kotlin.String): kotlin.String? {
            return FfiConverterOptionalString.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_secretstore_get(
        it,
        
        FfiConverterString.lower(`key`),_status)
}
    }
    )
    }
    

    override fun `set`(`key`: kotlin.String, `value`: kotlin.String)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_secretstore_set(
        it,
        
        FfiConverterString.lower(`key`),
        FfiConverterString.lower(`value`),_status)
}
    }
    
    

    override fun `remove`(`key`: kotlin.String)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_secretstore_remove(
        it,
        
        FfiConverterString.lower(`key`),_status)
}
    }
    
    

    override fun `getBlob`(`key`: kotlin.String): kotlin.ByteArray? {
            return FfiConverterOptionalByteArray.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_secretstore_get_blob(
        it,
        
        FfiConverterString.lower(`key`),_status)
}
    }
    )
    }
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}



// Put the implementation in an object so we don't pollute the top-level namespace
internal object uniffiCallbackInterfaceSecretStore {
    internal object `get`: UniffiCallbackInterfaceSecretStoreMethod0 {
        override fun callback(`uniffiHandle`: Long,`key`: RustBuffer.ByValue,`uniffiOutReturn`: RustBuffer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeSecretStore.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`get`(
                    FfiConverterString.lift(`key`),
                )
            }
            val writeReturn = { value: kotlin.String? -> uniffiOutReturn.setValue(FfiConverterOptionalString.lower(value)) }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `set`: UniffiCallbackInterfaceSecretStoreMethod1 {
        override fun callback(`uniffiHandle`: Long,`key`: RustBuffer.ByValue,`value`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeSecretStore.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`set`(
                    FfiConverterString.lift(`key`),
                    FfiConverterString.lift(`value`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `remove`: UniffiCallbackInterfaceSecretStoreMethod2 {
        override fun callback(`uniffiHandle`: Long,`key`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeSecretStore.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`remove`(
                    FfiConverterString.lift(`key`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `getBlob`: UniffiCallbackInterfaceSecretStoreMethod3 {
        override fun callback(`uniffiHandle`: Long,`key`: RustBuffer.ByValue,`uniffiOutReturn`: RustBuffer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeSecretStore.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`getBlob`(
                    FfiConverterString.lift(`key`),
                )
            }
            val writeReturn = { value: kotlin.ByteArray? -> uniffiOutReturn.setValue(FfiConverterOptionalByteArray.lower(value)) }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }

    internal object uniffiFree: UniffiCallbackInterfaceFree {
        override fun callback(handle: Long) {
            FfiConverterTypeSecretStore.handleMap.remove(handle)
        }
    }

    internal object uniffiClone: UniffiCallbackInterfaceClone {
        override fun callback(handle: Long): Long {
            return FfiConverterTypeSecretStore.handleMap.clone(handle)
        }
    }

    internal var vtable = UniffiVTableCallbackInterfaceSecretStore.UniffiByValue(
        uniffiFree,
        uniffiClone,
        `get`,
        `set`,
        `remove`,
        `getBlob`,
    )

    // Registers the foreign callback with the Rust side.
    // This method is generated for each callback interface.
    internal fun register(lib: UniffiLib) {
        lib.uniffi_bridgething_companion_fn_init_callback_vtable_secretstore(vtable)
    }
}

/**
 * @suppress
 */
public object FfiConverterTypeSecretStore: FfiConverter<SecretStore, Long> {
    internal val handleMap = UniffiHandleMap<SecretStore>()

    override fun lower(value: SecretStore): Long {
        if (value is SecretStoreImpl) {
             // Rust-implemented object.  Clone the handle and return it
            return value.uniffiCloneHandle()
         } else {
            // Kotlin object, generate a new vtable handle and return that.
            return handleMap.insert(value)
         }
    }

    override fun lift(value: Long): SecretStore {
        if ((value and 1.toLong()) == 0.toLong()) {
            // Rust-generated handle, construct a new class that uses the handle to implement the
            // interface
            return SecretStoreImpl(UniffiWithHandle, value)
        } else {
            // Kotlin-generated handle, get the object from the handle map
            return handleMap.remove(value)
        }
    }

    override fun read(buf: ByteBuffer): SecretStore {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: SecretStore) = 8UL

    override fun write(value: SecretStore, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface SessionEventSink {
    
    fun `onEvent`(`event`: SessionEvent)
    
    companion object
}

open class SessionEventSinkImpl: Disposable, AutoCloseable, SessionEventSink
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_sessioneventsink(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_sessioneventsink(handle, status)
        }
    }

    override fun `onEvent`(`event`: SessionEvent)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_sessioneventsink_on_event(
        it,
        
        FfiConverterTypeSessionEvent.lower(`event`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}



// Put the implementation in an object so we don't pollute the top-level namespace
internal object uniffiCallbackInterfaceSessionEventSink {
    internal object `onEvent`: UniffiCallbackInterfaceSessionEventSinkMethod0 {
        override fun callback(`uniffiHandle`: Long,`event`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeSessionEventSink.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`onEvent`(
                    FfiConverterTypeSessionEvent.lift(`event`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }

    internal object uniffiFree: UniffiCallbackInterfaceFree {
        override fun callback(handle: Long) {
            FfiConverterTypeSessionEventSink.handleMap.remove(handle)
        }
    }

    internal object uniffiClone: UniffiCallbackInterfaceClone {
        override fun callback(handle: Long): Long {
            return FfiConverterTypeSessionEventSink.handleMap.clone(handle)
        }
    }

    internal var vtable = UniffiVTableCallbackInterfaceSessionEventSink.UniffiByValue(
        uniffiFree,
        uniffiClone,
        `onEvent`,
    )

    // Registers the foreign callback with the Rust side.
    // This method is generated for each callback interface.
    internal fun register(lib: UniffiLib) {
        lib.uniffi_bridgething_companion_fn_init_callback_vtable_sessioneventsink(vtable)
    }
}

/**
 * @suppress
 */
public object FfiConverterTypeSessionEventSink: FfiConverter<SessionEventSink, Long> {
    internal val handleMap = UniffiHandleMap<SessionEventSink>()

    override fun lower(value: SessionEventSink): Long {
        if (value is SessionEventSinkImpl) {
             // Rust-implemented object.  Clone the handle and return it
            return value.uniffiCloneHandle()
         } else {
            // Kotlin object, generate a new vtable handle and return that.
            return handleMap.insert(value)
         }
    }

    override fun lift(value: Long): SessionEventSink {
        if ((value and 1.toLong()) == 0.toLong()) {
            // Rust-generated handle, construct a new class that uses the handle to implement the
            // interface
            return SessionEventSinkImpl(UniffiWithHandle, value)
        } else {
            // Kotlin-generated handle, get the object from the handle map
            return handleMap.remove(value)
        }
    }

    override fun read(buf: ByteBuffer): SessionEventSink {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: SessionEventSink) = 8UL

    override fun write(value: SessionEventSink, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface SpeakSinkInterface {
    
    fun `onFinished`(`ok`: kotlin.Boolean)
    
    fun `onStart`()
    
    companion object
}

open class SpeakSink: Disposable, AutoCloseable, SpeakSinkInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_speaksink(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_speaksink(handle, status)
        }
    }

    override fun `onFinished`(`ok`: kotlin.Boolean)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_speaksink_on_finished(
        it,
        
        FfiConverterBoolean.lower(`ok`),_status)
}
    }
    
    

    override fun `onStart`()
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_speaksink_on_start(
        it,
        _status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeSpeakSink: FfiConverter<SpeakSink, Long> {
    override fun lower(value: SpeakSink): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): SpeakSink {
        return SpeakSink(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): SpeakSink {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: SpeakSink) = 8UL

    override fun write(value: SpeakSink, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface SpeechRecognizer {
    
    fun `prepare`(`sink`: PrepareSink)
    
    fun `transcribe`(`pcm`: List<kotlin.Float>, `sampleRateHz`: kotlin.UInt, `sink`: TranscriptionSink)
    
    companion object
}

open class SpeechRecognizerImpl: Disposable, AutoCloseable, SpeechRecognizer
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_speechrecognizer(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_speechrecognizer(handle, status)
        }
    }

    override fun `prepare`(`sink`: PrepareSink)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_speechrecognizer_prepare(
        it,
        
        FfiConverterTypePrepareSink.lower(`sink`),_status)
}
    }
    
    

    override fun `transcribe`(`pcm`: List<kotlin.Float>, `sampleRateHz`: kotlin.UInt, `sink`: TranscriptionSink)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_speechrecognizer_transcribe(
        it,
        
        FfiConverterSequenceFloat.lower(`pcm`),
        FfiConverterUInt.lower(`sampleRateHz`),
        FfiConverterTypeTranscriptionSink.lower(`sink`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}



// Put the implementation in an object so we don't pollute the top-level namespace
internal object uniffiCallbackInterfaceSpeechRecognizer {
    internal object `prepare`: UniffiCallbackInterfaceSpeechRecognizerMethod0 {
        override fun callback(`uniffiHandle`: Long,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeSpeechRecognizer.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`prepare`(
                    FfiConverterTypePrepareSink.lift(`sink`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `transcribe`: UniffiCallbackInterfaceSpeechRecognizerMethod1 {
        override fun callback(`uniffiHandle`: Long,`pcm`: RustBuffer.ByValue,`sampleRateHz`: Int,`sink`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeSpeechRecognizer.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`transcribe`(
                    FfiConverterSequenceFloat.lift(`pcm`),
                    FfiConverterUInt.lift(`sampleRateHz`),
                    FfiConverterTypeTranscriptionSink.lift(`sink`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }

    internal object uniffiFree: UniffiCallbackInterfaceFree {
        override fun callback(handle: Long) {
            FfiConverterTypeSpeechRecognizer.handleMap.remove(handle)
        }
    }

    internal object uniffiClone: UniffiCallbackInterfaceClone {
        override fun callback(handle: Long): Long {
            return FfiConverterTypeSpeechRecognizer.handleMap.clone(handle)
        }
    }

    internal var vtable = UniffiVTableCallbackInterfaceSpeechRecognizer.UniffiByValue(
        uniffiFree,
        uniffiClone,
        `prepare`,
        `transcribe`,
    )

    // Registers the foreign callback with the Rust side.
    // This method is generated for each callback interface.
    internal fun register(lib: UniffiLib) {
        lib.uniffi_bridgething_companion_fn_init_callback_vtable_speechrecognizer(vtable)
    }
}

/**
 * @suppress
 */
public object FfiConverterTypeSpeechRecognizer: FfiConverter<SpeechRecognizer, Long> {
    internal val handleMap = UniffiHandleMap<SpeechRecognizer>()

    override fun lower(value: SpeechRecognizer): Long {
        if (value is SpeechRecognizerImpl) {
             // Rust-implemented object.  Clone the handle and return it
            return value.uniffiCloneHandle()
         } else {
            // Kotlin object, generate a new vtable handle and return that.
            return handleMap.insert(value)
         }
    }

    override fun lift(value: Long): SpeechRecognizer {
        if ((value and 1.toLong()) == 0.toLong()) {
            // Rust-generated handle, construct a new class that uses the handle to implement the
            // interface
            return SpeechRecognizerImpl(UniffiWithHandle, value)
        } else {
            // Kotlin-generated handle, get the object from the handle map
            return handleMap.remove(value)
        }
    }

    override fun read(buf: ByteBuffer): SpeechRecognizer {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: SpeechRecognizer) = 8UL

    override fun write(value: SpeechRecognizer, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface TranscriptionSinkInterface {
    
    fun `complete`(`transcription`: Transcription)
    
    fun `fail`(`reason`: kotlin.String)
    
    companion object
}

open class TranscriptionSink: Disposable, AutoCloseable, TranscriptionSinkInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_transcriptionsink(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_transcriptionsink(handle, status)
        }
    }

    override fun `complete`(`transcription`: Transcription)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_transcriptionsink_complete(
        it,
        
        FfiConverterTypeTranscription.lower(`transcription`),_status)
}
    }
    
    

    override fun `fail`(`reason`: kotlin.String)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_transcriptionsink_fail(
        it,
        
        FfiConverterString.lower(`reason`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeTranscriptionSink: FfiConverter<TranscriptionSink, Long> {
    override fun lower(value: TranscriptionSink): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): TranscriptionSink {
        return TranscriptionSink(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): TranscriptionSink {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: TranscriptionSink) = 8UL

    override fun write(value: TranscriptionSink, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface TransferPolicy {
    
    fun `allowsLargeTransfer`(): kotlin.Boolean
    
    companion object
}

open class TransferPolicyImpl: Disposable, AutoCloseable, TransferPolicy
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_transferpolicy(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_transferpolicy(handle, status)
        }
    }

    override fun `allowsLargeTransfer`(): kotlin.Boolean {
            return FfiConverterBoolean.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_transferpolicy_allows_large_transfer(
        it,
        _status)
}
    }
    )
    }
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}



// Put the implementation in an object so we don't pollute the top-level namespace
internal object uniffiCallbackInterfaceTransferPolicy {
    internal object `allowsLargeTransfer`: UniffiCallbackInterfaceTransferPolicyMethod0 {
        override fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: ByteByReference,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeTransferPolicy.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`allowsLargeTransfer`(
                )
            }
            val writeReturn = { value: kotlin.Boolean -> uniffiOutReturn.setValue(FfiConverterBoolean.lower(value)) }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }

    internal object uniffiFree: UniffiCallbackInterfaceFree {
        override fun callback(handle: Long) {
            FfiConverterTypeTransferPolicy.handleMap.remove(handle)
        }
    }

    internal object uniffiClone: UniffiCallbackInterfaceClone {
        override fun callback(handle: Long): Long {
            return FfiConverterTypeTransferPolicy.handleMap.clone(handle)
        }
    }

    internal var vtable = UniffiVTableCallbackInterfaceTransferPolicy.UniffiByValue(
        uniffiFree,
        uniffiClone,
        `allowsLargeTransfer`,
    )

    // Registers the foreign callback with the Rust side.
    // This method is generated for each callback interface.
    internal fun register(lib: UniffiLib) {
        lib.uniffi_bridgething_companion_fn_init_callback_vtable_transferpolicy(vtable)
    }
}

/**
 * @suppress
 */
public object FfiConverterTypeTransferPolicy: FfiConverter<TransferPolicy, Long> {
    internal val handleMap = UniffiHandleMap<TransferPolicy>()

    override fun lower(value: TransferPolicy): Long {
        if (value is TransferPolicyImpl) {
             // Rust-implemented object.  Clone the handle and return it
            return value.uniffiCloneHandle()
         } else {
            // Kotlin object, generate a new vtable handle and return that.
            return handleMap.insert(value)
         }
    }

    override fun lift(value: Long): TransferPolicy {
        if ((value and 1.toLong()) == 0.toLong()) {
            // Rust-generated handle, construct a new class that uses the handle to implement the
            // interface
            return TransferPolicyImpl(UniffiWithHandle, value)
        } else {
            // Kotlin-generated handle, get the object from the handle map
            return handleMap.remove(value)
        }
    }

    override fun read(buf: ByteBuffer): TransferPolicy {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: TransferPolicy) = 8UL

    override fun write(value: TransferPolicy, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


/**
 * output volume on the host itself. a host that leaves this unimplemented keeps the device
 * routing volume over iAP2 HID instead of over the gateway.
 */
public interface VolumeBackend {
    
    fun `start`(`inbox`: VolumeInbox)
    
    fun `stop`()
    
    fun `snapshot`(): VolumeLevel
    
    fun `setVolume`(`level`: kotlin.Float)
    
    fun `setMute`(`muted`: kotlin.Boolean)
    
    fun `volumeUp`()
    
    fun `volumeDown`()
    
    fun `muteToggle`()
    
    companion object
}

/**
 * output volume on the host itself. a host that leaves this unimplemented keeps the device
 * routing volume over iAP2 HID instead of over the gateway.
 */
open class VolumeBackendImpl: Disposable, AutoCloseable, VolumeBackend
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_volumebackend(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_volumebackend(handle, status)
        }
    }

    override fun `start`(`inbox`: VolumeInbox)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_volumebackend_start(
        it,
        
        FfiConverterTypeVolumeInbox.lower(`inbox`),_status)
}
    }
    
    

    override fun `stop`()
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_volumebackend_stop(
        it,
        _status)
}
    }
    
    

    override fun `snapshot`(): VolumeLevel {
            return FfiConverterTypeVolumeLevel.lift(
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_volumebackend_snapshot(
        it,
        _status)
}
    }
    )
    }
    

    override fun `setVolume`(`level`: kotlin.Float)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_volumebackend_set_volume(
        it,
        
        FfiConverterFloat.lower(`level`),_status)
}
    }
    
    

    override fun `setMute`(`muted`: kotlin.Boolean)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_volumebackend_set_mute(
        it,
        
        FfiConverterBoolean.lower(`muted`),_status)
}
    }
    
    

    override fun `volumeUp`()
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_volumebackend_volume_up(
        it,
        _status)
}
    }
    
    

    override fun `volumeDown`()
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_volumebackend_volume_down(
        it,
        _status)
}
    }
    
    

    override fun `muteToggle`()
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_volumebackend_mute_toggle(
        it,
        _status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}



// Put the implementation in an object so we don't pollute the top-level namespace
internal object uniffiCallbackInterfaceVolumeBackend {
    internal object `start`: UniffiCallbackInterfaceVolumeBackendMethod0 {
        override fun callback(`uniffiHandle`: Long,`inbox`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeVolumeBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`start`(
                    FfiConverterTypeVolumeInbox.lift(`inbox`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `stop`: UniffiCallbackInterfaceVolumeBackendMethod1 {
        override fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeVolumeBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`stop`(
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `snapshot`: UniffiCallbackInterfaceVolumeBackendMethod2 {
        override fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: RustBuffer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeVolumeBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`snapshot`(
                )
            }
            val writeReturn = { value: VolumeLevel -> uniffiOutReturn.setValue(FfiConverterTypeVolumeLevel.lower(value)) }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `setVolume`: UniffiCallbackInterfaceVolumeBackendMethod3 {
        override fun callback(`uniffiHandle`: Long,`level`: Float,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeVolumeBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`setVolume`(
                    FfiConverterFloat.lift(`level`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `setMute`: UniffiCallbackInterfaceVolumeBackendMethod4 {
        override fun callback(`uniffiHandle`: Long,`muted`: Byte,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeVolumeBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`setMute`(
                    FfiConverterBoolean.lift(`muted`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `volumeUp`: UniffiCallbackInterfaceVolumeBackendMethod5 {
        override fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeVolumeBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`volumeUp`(
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `volumeDown`: UniffiCallbackInterfaceVolumeBackendMethod6 {
        override fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeVolumeBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`volumeDown`(
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `muteToggle`: UniffiCallbackInterfaceVolumeBackendMethod7 {
        override fun callback(`uniffiHandle`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeVolumeBackend.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`muteToggle`(
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }

    internal object uniffiFree: UniffiCallbackInterfaceFree {
        override fun callback(handle: Long) {
            FfiConverterTypeVolumeBackend.handleMap.remove(handle)
        }
    }

    internal object uniffiClone: UniffiCallbackInterfaceClone {
        override fun callback(handle: Long): Long {
            return FfiConverterTypeVolumeBackend.handleMap.clone(handle)
        }
    }

    internal var vtable = UniffiVTableCallbackInterfaceVolumeBackend.UniffiByValue(
        uniffiFree,
        uniffiClone,
        `start`,
        `stop`,
        `snapshot`,
        `setVolume`,
        `setMute`,
        `volumeUp`,
        `volumeDown`,
        `muteToggle`,
    )

    // Registers the foreign callback with the Rust side.
    // This method is generated for each callback interface.
    internal fun register(lib: UniffiLib) {
        lib.uniffi_bridgething_companion_fn_init_callback_vtable_volumebackend(vtable)
    }
}

/**
 * @suppress
 */
public object FfiConverterTypeVolumeBackend: FfiConverter<VolumeBackend, Long> {
    internal val handleMap = UniffiHandleMap<VolumeBackend>()

    override fun lower(value: VolumeBackend): Long {
        if (value is VolumeBackendImpl) {
             // Rust-implemented object.  Clone the handle and return it
            return value.uniffiCloneHandle()
         } else {
            // Kotlin object, generate a new vtable handle and return that.
            return handleMap.insert(value)
         }
    }

    override fun lift(value: Long): VolumeBackend {
        if ((value and 1.toLong()) == 0.toLong()) {
            // Rust-generated handle, construct a new class that uses the handle to implement the
            // interface
            return VolumeBackendImpl(UniffiWithHandle, value)
        } else {
            // Kotlin-generated handle, get the object from the handle map
            return handleMap.remove(value)
        }
    }

    override fun read(buf: ByteBuffer): VolumeBackend {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: VolumeBackend) = 8UL

    override fun write(value: VolumeBackend, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface VolumeInboxInterface {
    
    fun `onChanged`(`level`: kotlin.Float, `muted`: kotlin.Boolean)
    
    companion object
}

open class VolumeInbox: Disposable, AutoCloseable, VolumeInboxInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_volumeinbox(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_volumeinbox(handle, status)
        }
    }

    override fun `onChanged`(`level`: kotlin.Float, `muted`: kotlin.Boolean)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_volumeinbox_on_changed(
        it,
        
        FfiConverterFloat.lower(`level`),
        FfiConverterBoolean.lower(`muted`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeVolumeInbox: FfiConverter<VolumeInbox, Long> {
    override fun lower(value: VolumeInbox): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): VolumeInbox {
        return VolumeInbox(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): VolumeInbox {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: VolumeInbox) = 8UL

    override fun write(value: VolumeInbox, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface WebappBundleSink {
    
    fun `installed`(`bundle`: kotlin.String)
    
    companion object
}

open class WebappBundleSinkImpl: Disposable, AutoCloseable, WebappBundleSink
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_webappbundlesink(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_webappbundlesink(handle, status)
        }
    }

    override fun `installed`(`bundle`: kotlin.String)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_webappbundlesink_installed(
        it,
        
        FfiConverterString.lower(`bundle`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}



// Put the implementation in an object so we don't pollute the top-level namespace
internal object uniffiCallbackInterfaceWebappBundleSink {
    internal object `installed`: UniffiCallbackInterfaceWebappBundleSinkMethod0 {
        override fun callback(`uniffiHandle`: Long,`bundle`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeWebappBundleSink.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`installed`(
                    FfiConverterString.lift(`bundle`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }

    internal object uniffiFree: UniffiCallbackInterfaceFree {
        override fun callback(handle: Long) {
            FfiConverterTypeWebappBundleSink.handleMap.remove(handle)
        }
    }

    internal object uniffiClone: UniffiCallbackInterfaceClone {
        override fun callback(handle: Long): Long {
            return FfiConverterTypeWebappBundleSink.handleMap.clone(handle)
        }
    }

    internal var vtable = UniffiVTableCallbackInterfaceWebappBundleSink.UniffiByValue(
        uniffiFree,
        uniffiClone,
        `installed`,
    )

    // Registers the foreign callback with the Rust side.
    // This method is generated for each callback interface.
    internal fun register(lib: UniffiLib) {
        lib.uniffi_bridgething_companion_fn_init_callback_vtable_webappbundlesink(vtable)
    }
}

/**
 * @suppress
 */
public object FfiConverterTypeWebappBundleSink: FfiConverter<WebappBundleSink, Long> {
    internal val handleMap = UniffiHandleMap<WebappBundleSink>()

    override fun lower(value: WebappBundleSink): Long {
        if (value is WebappBundleSinkImpl) {
             // Rust-implemented object.  Clone the handle and return it
            return value.uniffiCloneHandle()
         } else {
            // Kotlin object, generate a new vtable handle and return that.
            return handleMap.insert(value)
         }
    }

    override fun lift(value: Long): WebappBundleSink {
        if ((value and 1.toLong()) == 0.toLong()) {
            // Rust-generated handle, construct a new class that uses the handle to implement the
            // interface
            return WebappBundleSinkImpl(UniffiWithHandle, value)
        } else {
            // Kotlin-generated handle, get the object from the handle map
            return handleMap.remove(value)
        }
    }

    override fun read(buf: ByteBuffer): WebappBundleSink {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: WebappBundleSink) = 8UL

    override fun write(value: WebappBundleSink, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface WsInboxInterface {
    
    fun `onBinary`(`id`: kotlin.String, `bytes`: kotlin.ByteArray)
    
    fun `onClosed`(`id`: kotlin.String, `code`: kotlin.UShort?, `reason`: kotlin.String)
    
    fun `onOpen`(`id`: kotlin.String, `acceptedProtocol`: kotlin.String?)
    
    fun `onText`(`id`: kotlin.String, `text`: kotlin.String)
    
    companion object
}

open class WsInbox: Disposable, AutoCloseable, WsInboxInterface
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_wsinbox(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_wsinbox(handle, status)
        }
    }

    override fun `onBinary`(`id`: kotlin.String, `bytes`: kotlin.ByteArray)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_wsinbox_on_binary(
        it,
        
        FfiConverterString.lower(`id`),
        FfiConverterByteArray.lower(`bytes`),_status)
}
    }
    
    

    override fun `onClosed`(`id`: kotlin.String, `code`: kotlin.UShort?, `reason`: kotlin.String)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_wsinbox_on_closed(
        it,
        
        FfiConverterString.lower(`id`),
        FfiConverterOptionalUShort.lower(`code`),
        FfiConverterString.lower(`reason`),_status)
}
    }
    
    

    override fun `onOpen`(`id`: kotlin.String, `acceptedProtocol`: kotlin.String?)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_wsinbox_on_open(
        it,
        
        FfiConverterString.lower(`id`),
        FfiConverterOptionalString.lower(`acceptedProtocol`),_status)
}
    }
    
    

    override fun `onText`(`id`: kotlin.String, `text`: kotlin.String)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_wsinbox_on_text(
        it,
        
        FfiConverterString.lower(`id`),
        FfiConverterString.lower(`text`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}


/**
 * @suppress
 */
public object FfiConverterTypeWsInbox: FfiConverter<WsInbox, Long> {
    override fun lower(value: WsInbox): Long {
        return value.uniffiCloneHandle()
    }

    override fun lift(value: Long): WsInbox {
        return WsInbox(UniffiWithHandle, value)
    }

    override fun read(buf: ByteBuffer): WsInbox {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: WsInbox) = 8UL

    override fun write(value: WsInbox, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}


// This template implements a class for working with a Rust struct via a handle
// to the live Rust struct on the other side of the FFI.
//
// There's some subtlety here, because we have to be careful not to operate on a Rust
// struct after it has been dropped, and because we must expose a public API for freeing
// theq Kotlin wrapper object in lieu of reliable finalizers. The core requirements are:
//
//   * Each instance holds an opaque handle to the underlying Rust struct.
//     Method calls need to read this handle from the object's state and pass it in to
//     the Rust FFI.
//
//   * When an instance is no longer needed, its handle should be passed to a
//     special destructor function provided by the Rust FFI, which will drop the
//     underlying Rust struct.
//
//   * Given an instance, calling code is expected to call the special
//     `destroy` method in order to free it after use, either by calling it explicitly
//     or by using a higher-level helper like the `use` method. Failing to do so risks
//     leaking the underlying Rust struct.
//
//   * We can't assume that calling code will do the right thing, and must be prepared
//     to handle Kotlin method calls executing concurrently with or even after a call to
//     `destroy`, and to handle multiple (possibly concurrent!) calls to `destroy`.
//
//   * We must never allow Rust code to operate on the underlying Rust struct after
//     the destructor has been called, and must never call the destructor more than once.
//     Doing so may trigger memory unsafety.
//
//   * To mitigate many of the risks of leaking memory and use-after-free unsafety, a `Cleaner`
//     is implemented to call the destructor when the Kotlin object becomes unreachable.
//     This is done in a background thread. This is not a panacea, and client code should be aware that
//      1. the thread may starve if some there are objects that have poorly performing
//     `drop` methods or do significant work in their `drop` methods.
//      2. the thread is shared across the whole library. This can be tuned by using `android_cleaner = true`,
//         or `android = true` in the [`kotlin` section of the `uniffi.toml` file](https://mozilla.github.io/uniffi-rs/kotlin/configuration.html).
//
// If we try to implement this with mutual exclusion on access to the handle, there is the
// possibility of a race between a method call and a concurrent call to `destroy`:
//
//    * Thread A starts a method call, reads the value of the handle, but is interrupted
//      before it can pass the handle over the FFI to Rust.
//    * Thread B calls `destroy` and frees the underlying Rust struct.
//    * Thread A resumes, passing the already-read handle value to Rust and triggering
//      a use-after-free.
//
// One possible solution would be to use a `ReadWriteLock`, with each method call taking
// a read lock (and thus allowed to run concurrently) and the special `destroy` method
// taking a write lock (and thus blocking on live method calls). However, we aim not to
// generate methods with any hidden blocking semantics, and a `destroy` method that might
// block if called incorrectly seems to meet that bar.
//
// So, we achieve our goals by giving each instance an associated `AtomicLong` counter to track
// the number of in-flight method calls, and an `AtomicBoolean` flag to indicate whether `destroy`
// has been called. These are updated according to the following rules:
//
//    * The initial value of the counter is 1, indicating a live object with no in-flight calls.
//      The initial value for the flag is false.
//
//    * At the start of each method call, we atomically check the counter.
//      If it is 0 then the underlying Rust struct has already been destroyed and the call is aborted.
//      If it is nonzero them we atomically increment it by 1 and proceed with the method call.
//
//    * At the end of each method call, we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
//    * When `destroy` is called, we atomically flip the flag from false to true.
//      If the flag was already true we silently fail.
//      Otherwise we atomically decrement and check the counter.
//      If it has reached zero then we destroy the underlying Rust struct.
//
// Astute readers may observe that this all sounds very similar to the way that Rust's `Arc<T>` works,
// and indeed it is, with the addition of a flag to guard against multiple calls to `destroy`.
//
// The overall effect is that the underlying Rust struct is destroyed only when `destroy` has been
// called *and* all in-flight method calls have completed, avoiding violating any of the expectations
// of the underlying Rust code.
//
// This makes a cleaner a better alternative to _not_ calling `destroy()` as
// and when the object is finished with, but the abstraction is not perfect: if the Rust object's `drop`
// method is slow, and/or there are many objects to cleanup, and it's on a low end Android device, then the cleaner
// thread may be starved, and the app will leak memory.
//
// In this case, `destroy`ing manually may be a better solution.
//
// The cleaner can live side by side with the manual calling of `destroy`. In the order of responsiveness, uniffi objects
// with Rust peers are reclaimed:
//
// 1. By calling the `destroy` method of the object, which calls `rustObject.free()`. If that doesn't happen:
// 2. When the object becomes unreachable, AND the Cleaner thread gets to call `rustObject.free()`. If the thread is starved then:
// 3. The memory is reclaimed when the process terminates.
//
// [1] https://stackoverflow.com/questions/24376768/can-java-finalize-an-object-when-it-is-still-in-scope/24380219
//


public interface WsTransport {
    
    fun `connect`(`connect`: WsConnect, `inbox`: WsInbox)
    
    fun `send`(`id`: kotlin.String, `frame`: WsFrame)
    
    fun `disconnect`(`id`: kotlin.String, `code`: kotlin.UShort?, `reason`: kotlin.String?)
    
    companion object
}

open class WsTransportImpl: Disposable, AutoCloseable, WsTransport
{

    @Suppress("UNUSED_PARAMETER")
    /**
     * @suppress
     */
    constructor(withHandle: UniffiWithHandle, handle: Long) {
        this.handle = handle
        this.cleanable = UniffiLib.CLEANER.register(this, UniffiCleanAction(handle))
    }

    /**
     * @suppress
     *
     * This constructor can be used to instantiate a fake object. Only used for tests. Any
     * attempt to actually use an object constructed this way will fail as there is no
     * connected Rust object.
     */
    @Suppress("UNUSED_PARAMETER")
    constructor(noHandle: NoHandle) {
        this.handle = 0
        this.cleanable = null
    }

    protected val handle: Long
    protected val cleanable: UniffiCleaner.Cleanable?

    private val wasDestroyed = AtomicBoolean(false)
    private val callCounter = AtomicLong(1)

    /**
     * Whether the current object has been destroyed and its reference is gone in the Rust side.
     */
    val uniffiIsDestroyed: Boolean get() = wasDestroyed.get()

    override fun destroy() {
        // Only allow a single call to this method.
        // TODO: maybe we should log a warning if called more than once?
        if (this.wasDestroyed.compareAndSet(false, true)) {
            // This decrement always matches the initial count of 1 given at creation time.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    @Synchronized
    override fun close() {
        this.destroy()
    }

    internal inline fun <R> callWithHandle(block: (handle: Long) -> R): R {
        // Check and increment the call counter, to keep the object alive.
        // This needs a compare-and-set retry loop in case of concurrent updates.
        do {
            val c = this.callCounter.get()
            if (c == 0L) {
                throw IllegalStateException("${this.javaClass.simpleName} object has already been destroyed")
            }
            if (c == Long.MAX_VALUE) {
                throw IllegalStateException("${this.javaClass.simpleName} call counter would overflow")
            }
        } while (! this.callCounter.compareAndSet(c, c + 1L))
        // Now we can safely do the method call without the handle being freed concurrently.
        try {
            return block(this.uniffiCloneHandle())
        } finally {
            // This decrement always matches the increment we performed above.
            if (this.callCounter.decrementAndGet() == 0L) {
                cleanable?.clean()
            }
        }
    }

    // Use a static inner class instead of a closure so as not to accidentally
    // capture `this` as part of the cleanable's action.
    private class UniffiCleanAction(private val handle: Long) : Runnable {
        override fun run() {
            if (handle == 0.toLong()) {
                // Fake object created with `NoHandle`, don't try to free.
                return;
            }
            uniffiRustCall { status ->
                UniffiLib.uniffi_bridgething_companion_fn_free_wstransport(handle, status)
            }
        }
    }

    /**
     * @suppress
     */
    fun uniffiCloneHandle(): Long {
        if (handle == 0.toLong()) {
            throw InternalException("uniffiCloneHandle() called on NoHandle object");
        }
        return uniffiRustCall() { status ->
            UniffiLib.uniffi_bridgething_companion_fn_clone_wstransport(handle, status)
        }
    }

    override fun `connect`(`connect`: WsConnect, `inbox`: WsInbox)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_wstransport_connect(
        it,
        
        FfiConverterTypeWsConnect.lower(`connect`),
        FfiConverterTypeWsInbox.lower(`inbox`),_status)
}
    }
    
    

    override fun `send`(`id`: kotlin.String, `frame`: WsFrame)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_wstransport_send(
        it,
        
        FfiConverterString.lower(`id`),
        FfiConverterTypeWsFrame.lower(`frame`),_status)
}
    }
    
    

    override fun `disconnect`(`id`: kotlin.String, `code`: kotlin.UShort?, `reason`: kotlin.String?)
        = 
    callWithHandle {
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_method_wstransport_disconnect(
        it,
        
        FfiConverterString.lower(`id`),
        FfiConverterOptionalUShort.lower(`code`),
        FfiConverterOptionalString.lower(`reason`),_status)
}
    }
    
    

    

    


    
    
    /**
     * @suppress
     */
    companion object
    
}



// Put the implementation in an object so we don't pollute the top-level namespace
internal object uniffiCallbackInterfaceWsTransport {
    internal object `connect`: UniffiCallbackInterfaceWsTransportMethod0 {
        override fun callback(`uniffiHandle`: Long,`connect`: RustBuffer.ByValue,`inbox`: Long,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeWsTransport.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`connect`(
                    FfiConverterTypeWsConnect.lift(`connect`),
                    FfiConverterTypeWsInbox.lift(`inbox`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `send`: UniffiCallbackInterfaceWsTransportMethod1 {
        override fun callback(`uniffiHandle`: Long,`id`: RustBuffer.ByValue,`frame`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeWsTransport.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`send`(
                    FfiConverterString.lift(`id`),
                    FfiConverterTypeWsFrame.lift(`frame`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }
    internal object `disconnect`: UniffiCallbackInterfaceWsTransportMethod2 {
        override fun callback(`uniffiHandle`: Long,`id`: RustBuffer.ByValue,`code`: RustBuffer.ByValue,`reason`: RustBuffer.ByValue,`uniffiOutReturn`: Pointer,uniffiCallStatus: UniffiRustCallStatus,) {
            val uniffiObj = FfiConverterTypeWsTransport.handleMap.get(uniffiHandle)
            val makeCall = { ->
                uniffiObj.`disconnect`(
                    FfiConverterString.lift(`id`),
                    FfiConverterOptionalUShort.lift(`code`),
                    FfiConverterOptionalString.lift(`reason`),
                )
            }
            val writeReturn = { _: Unit -> Unit }
            uniffiTraitInterfaceCall(uniffiCallStatus, makeCall, writeReturn)
        }
    }

    internal object uniffiFree: UniffiCallbackInterfaceFree {
        override fun callback(handle: Long) {
            FfiConverterTypeWsTransport.handleMap.remove(handle)
        }
    }

    internal object uniffiClone: UniffiCallbackInterfaceClone {
        override fun callback(handle: Long): Long {
            return FfiConverterTypeWsTransport.handleMap.clone(handle)
        }
    }

    internal var vtable = UniffiVTableCallbackInterfaceWsTransport.UniffiByValue(
        uniffiFree,
        uniffiClone,
        `connect`,
        `send`,
        `disconnect`,
    )

    // Registers the foreign callback with the Rust side.
    // This method is generated for each callback interface.
    internal fun register(lib: UniffiLib) {
        lib.uniffi_bridgething_companion_fn_init_callback_vtable_wstransport(vtable)
    }
}

/**
 * @suppress
 */
public object FfiConverterTypeWsTransport: FfiConverter<WsTransport, Long> {
    internal val handleMap = UniffiHandleMap<WsTransport>()

    override fun lower(value: WsTransport): Long {
        if (value is WsTransportImpl) {
             // Rust-implemented object.  Clone the handle and return it
            return value.uniffiCloneHandle()
         } else {
            // Kotlin object, generate a new vtable handle and return that.
            return handleMap.insert(value)
         }
    }

    override fun lift(value: Long): WsTransport {
        if ((value and 1.toLong()) == 0.toLong()) {
            // Rust-generated handle, construct a new class that uses the handle to implement the
            // interface
            return WsTransportImpl(UniffiWithHandle, value)
        } else {
            // Kotlin-generated handle, get the object from the handle map
            return handleMap.remove(value)
        }
    }

    override fun read(buf: ByteBuffer): WsTransport {
        return lift(buf.getLong())
    }

    override fun allocationSize(value: WsTransport) = 8UL

    override fun write(value: WsTransport, buf: ByteBuffer) {
        buf.putLong(lower(value))
    }
}



data class ActiveWebapp (
    var `id`: kotlin.String
    , 
    var `name`: kotlin.String?
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeActiveWebapp: FfiConverterRustBuffer<ActiveWebapp> {
    override fun read(buf: ByteBuffer): ActiveWebapp {
        return ActiveWebapp(
            FfiConverterString.read(buf),
            FfiConverterOptionalString.read(buf),
        )
    }

    override fun allocationSize(value: ActiveWebapp) = (
            FfiConverterString.allocationSize(value.`id`) +
            FfiConverterOptionalString.allocationSize(value.`name`)
    )

    override fun write(value: ActiveWebapp, buf: ByteBuffer) {
            FfiConverterString.write(value.`id`, buf)
            FfiConverterOptionalString.write(value.`name`, buf)
    }
}



data class AmEntry (
    var `uri`: kotlin.String?
    , 
    var `title`: kotlin.String
    , 
    var `artistName`: kotlin.String?
    , 
    var `albumName`: kotlin.String?
    , 
    var `artworkUrl`: kotlin.String?
    , 
    var `durationMs`: kotlin.UInt?
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeAmEntry: FfiConverterRustBuffer<AmEntry> {
    override fun read(buf: ByteBuffer): AmEntry {
        return AmEntry(
            FfiConverterOptionalString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalUInt.read(buf),
        )
    }

    override fun allocationSize(value: AmEntry) = (
            FfiConverterOptionalString.allocationSize(value.`uri`) +
            FfiConverterString.allocationSize(value.`title`) +
            FfiConverterOptionalString.allocationSize(value.`artistName`) +
            FfiConverterOptionalString.allocationSize(value.`albumName`) +
            FfiConverterOptionalString.allocationSize(value.`artworkUrl`) +
            FfiConverterOptionalUInt.allocationSize(value.`durationMs`)
    )

    override fun write(value: AmEntry, buf: ByteBuffer) {
            FfiConverterOptionalString.write(value.`uri`, buf)
            FfiConverterString.write(value.`title`, buf)
            FfiConverterOptionalString.write(value.`artistName`, buf)
            FfiConverterOptionalString.write(value.`albumName`, buf)
            FfiConverterOptionalString.write(value.`artworkUrl`, buf)
            FfiConverterOptionalUInt.write(value.`durationMs`, buf)
    }
}



data class AmItem (
    var `uri`: kotlin.String
    , 
    var `kind`: AmKind
    , 
    var `title`: kotlin.String
    , 
    var `subtitle`: kotlin.String?
    , 
    var `artistName`: kotlin.String?
    , 
    var `artistUri`: kotlin.String?
    , 
    var `albumName`: kotlin.String?
    , 
    var `albumUri`: kotlin.String?
    , 
    var `artworkUrl`: kotlin.String?
    , 
    var `durationMs`: kotlin.UInt?
    , 
    var `trackCount`: kotlin.UInt?
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeAmItem: FfiConverterRustBuffer<AmItem> {
    override fun read(buf: ByteBuffer): AmItem {
        return AmItem(
            FfiConverterString.read(buf),
            FfiConverterTypeAmKind.read(buf),
            FfiConverterString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalUInt.read(buf),
            FfiConverterOptionalUInt.read(buf),
        )
    }

    override fun allocationSize(value: AmItem) = (
            FfiConverterString.allocationSize(value.`uri`) +
            FfiConverterTypeAmKind.allocationSize(value.`kind`) +
            FfiConverterString.allocationSize(value.`title`) +
            FfiConverterOptionalString.allocationSize(value.`subtitle`) +
            FfiConverterOptionalString.allocationSize(value.`artistName`) +
            FfiConverterOptionalString.allocationSize(value.`artistUri`) +
            FfiConverterOptionalString.allocationSize(value.`albumName`) +
            FfiConverterOptionalString.allocationSize(value.`albumUri`) +
            FfiConverterOptionalString.allocationSize(value.`artworkUrl`) +
            FfiConverterOptionalUInt.allocationSize(value.`durationMs`) +
            FfiConverterOptionalUInt.allocationSize(value.`trackCount`)
    )

    override fun write(value: AmItem, buf: ByteBuffer) {
            FfiConverterString.write(value.`uri`, buf)
            FfiConverterTypeAmKind.write(value.`kind`, buf)
            FfiConverterString.write(value.`title`, buf)
            FfiConverterOptionalString.write(value.`subtitle`, buf)
            FfiConverterOptionalString.write(value.`artistName`, buf)
            FfiConverterOptionalString.write(value.`artistUri`, buf)
            FfiConverterOptionalString.write(value.`albumName`, buf)
            FfiConverterOptionalString.write(value.`albumUri`, buf)
            FfiConverterOptionalString.write(value.`artworkUrl`, buf)
            FfiConverterOptionalUInt.write(value.`durationMs`, buf)
            FfiConverterOptionalUInt.write(value.`trackCount`, buf)
    }
}



data class AmPage (
    var `items`: List<AmItem>
    , 
    var `total`: kotlin.UInt?
    , 
    var `hasMore`: kotlin.Boolean
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeAmPage: FfiConverterRustBuffer<AmPage> {
    override fun read(buf: ByteBuffer): AmPage {
        return AmPage(
            FfiConverterSequenceTypeAmItem.read(buf),
            FfiConverterOptionalUInt.read(buf),
            FfiConverterBoolean.read(buf),
        )
    }

    override fun allocationSize(value: AmPage) = (
            FfiConverterSequenceTypeAmItem.allocationSize(value.`items`) +
            FfiConverterOptionalUInt.allocationSize(value.`total`) +
            FfiConverterBoolean.allocationSize(value.`hasMore`)
    )

    override fun write(value: AmPage, buf: ByteBuffer) {
            FfiConverterSequenceTypeAmItem.write(value.`items`, buf)
            FfiConverterOptionalUInt.write(value.`total`, buf)
            FfiConverterBoolean.write(value.`hasMore`, buf)
    }
}



data class AmPlayerSnapshot (
    var `entry`: AmEntry?
    , 
    var `playing`: kotlin.Boolean
    , 
    var `positionMs`: kotlin.UInt
    , 
    var `shuffle`: kotlin.Boolean
    , 
    var `repeat`: AmRepeatMode
    , 
    var `canSeek`: kotlin.Boolean
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeAmPlayerSnapshot: FfiConverterRustBuffer<AmPlayerSnapshot> {
    override fun read(buf: ByteBuffer): AmPlayerSnapshot {
        return AmPlayerSnapshot(
            FfiConverterOptionalTypeAmEntry.read(buf),
            FfiConverterBoolean.read(buf),
            FfiConverterUInt.read(buf),
            FfiConverterBoolean.read(buf),
            FfiConverterTypeAmRepeatMode.read(buf),
            FfiConverterBoolean.read(buf),
        )
    }

    override fun allocationSize(value: AmPlayerSnapshot) = (
            FfiConverterOptionalTypeAmEntry.allocationSize(value.`entry`) +
            FfiConverterBoolean.allocationSize(value.`playing`) +
            FfiConverterUInt.allocationSize(value.`positionMs`) +
            FfiConverterBoolean.allocationSize(value.`shuffle`) +
            FfiConverterTypeAmRepeatMode.allocationSize(value.`repeat`) +
            FfiConverterBoolean.allocationSize(value.`canSeek`)
    )

    override fun write(value: AmPlayerSnapshot, buf: ByteBuffer) {
            FfiConverterOptionalTypeAmEntry.write(value.`entry`, buf)
            FfiConverterBoolean.write(value.`playing`, buf)
            FfiConverterUInt.write(value.`positionMs`, buf)
            FfiConverterBoolean.write(value.`shuffle`, buf)
            FfiConverterTypeAmRepeatMode.write(value.`repeat`, buf)
            FfiConverterBoolean.write(value.`canSeek`, buf)
    }
}



data class AmSearchResults (
    var `songs`: List<AmItem>
    , 
    var `albums`: List<AmItem>
    , 
    var `artists`: List<AmItem>
    , 
    var `playlists`: List<AmItem>
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeAmSearchResults: FfiConverterRustBuffer<AmSearchResults> {
    override fun read(buf: ByteBuffer): AmSearchResults {
        return AmSearchResults(
            FfiConverterSequenceTypeAmItem.read(buf),
            FfiConverterSequenceTypeAmItem.read(buf),
            FfiConverterSequenceTypeAmItem.read(buf),
            FfiConverterSequenceTypeAmItem.read(buf),
        )
    }

    override fun allocationSize(value: AmSearchResults) = (
            FfiConverterSequenceTypeAmItem.allocationSize(value.`songs`) +
            FfiConverterSequenceTypeAmItem.allocationSize(value.`albums`) +
            FfiConverterSequenceTypeAmItem.allocationSize(value.`artists`) +
            FfiConverterSequenceTypeAmItem.allocationSize(value.`playlists`)
    )

    override fun write(value: AmSearchResults, buf: ByteBuffer) {
            FfiConverterSequenceTypeAmItem.write(value.`songs`, buf)
            FfiConverterSequenceTypeAmItem.write(value.`albums`, buf)
            FfiConverterSequenceTypeAmItem.write(value.`artists`, buf)
            FfiConverterSequenceTypeAmItem.write(value.`playlists`, buf)
    }
}



data class AmShelf (
    var `id`: kotlin.String
    , 
    var `title`: kotlin.String
    , 
    var `items`: List<AmItem>
    , 
    var `total`: kotlin.UInt?
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeAmShelf: FfiConverterRustBuffer<AmShelf> {
    override fun read(buf: ByteBuffer): AmShelf {
        return AmShelf(
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterSequenceTypeAmItem.read(buf),
            FfiConverterOptionalUInt.read(buf),
        )
    }

    override fun allocationSize(value: AmShelf) = (
            FfiConverterString.allocationSize(value.`id`) +
            FfiConverterString.allocationSize(value.`title`) +
            FfiConverterSequenceTypeAmItem.allocationSize(value.`items`) +
            FfiConverterOptionalUInt.allocationSize(value.`total`)
    )

    override fun write(value: AmShelf, buf: ByteBuffer) {
            FfiConverterString.write(value.`id`, buf)
            FfiConverterString.write(value.`title`, buf)
            FfiConverterSequenceTypeAmItem.write(value.`items`, buf)
            FfiConverterOptionalUInt.write(value.`total`, buf)
    }
}



data class AncsAuthStatusEntry (
    var `deviceId`: kotlin.String
    , 
    var `status`: AncsAuthStatus
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeAncsAuthStatusEntry: FfiConverterRustBuffer<AncsAuthStatusEntry> {
    override fun read(buf: ByteBuffer): AncsAuthStatusEntry {
        return AncsAuthStatusEntry(
            FfiConverterString.read(buf),
            FfiConverterTypeAncsAuthStatus.read(buf),
        )
    }

    override fun allocationSize(value: AncsAuthStatusEntry) = (
            FfiConverterString.allocationSize(value.`deviceId`) +
            FfiConverterTypeAncsAuthStatus.allocationSize(value.`status`)
    )

    override fun write(value: AncsAuthStatusEntry, buf: ByteBuffer) {
            FfiConverterString.write(value.`deviceId`, buf)
            FfiConverterTypeAncsAuthStatus.write(value.`status`, buf)
    }
}



data class ArtifactDigest (
    var `size`: kotlin.ULong
    , 
    var `sha256`: kotlin.String
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeArtifactDigest: FfiConverterRustBuffer<ArtifactDigest> {
    override fun read(buf: ByteBuffer): ArtifactDigest {
        return ArtifactDigest(
            FfiConverterULong.read(buf),
            FfiConverterString.read(buf),
        )
    }

    override fun allocationSize(value: ArtifactDigest) = (
            FfiConverterULong.allocationSize(value.`size`) +
            FfiConverterString.allocationSize(value.`sha256`)
    )

    override fun write(value: ArtifactDigest, buf: ByteBuffer) {
            FfiConverterULong.write(value.`size`, buf)
            FfiConverterString.write(value.`sha256`, buf)
    }
}



data class AuthState (
    var `kind`: AuthKind
    , 
    var `userCode`: kotlin.String?
    , 
    var `verificationUrl`: kotlin.String?
    , 
    var `verificationUrlComplete`: kotlin.String?
    , 
    var `message`: kotlin.String?
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeAuthState: FfiConverterRustBuffer<AuthState> {
    override fun read(buf: ByteBuffer): AuthState {
        return AuthState(
            FfiConverterTypeAuthKind.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
        )
    }

    override fun allocationSize(value: AuthState) = (
            FfiConverterTypeAuthKind.allocationSize(value.`kind`) +
            FfiConverterOptionalString.allocationSize(value.`userCode`) +
            FfiConverterOptionalString.allocationSize(value.`verificationUrl`) +
            FfiConverterOptionalString.allocationSize(value.`verificationUrlComplete`) +
            FfiConverterOptionalString.allocationSize(value.`message`)
    )

    override fun write(value: AuthState, buf: ByteBuffer) {
            FfiConverterTypeAuthKind.write(value.`kind`, buf)
            FfiConverterOptionalString.write(value.`userCode`, buf)
            FfiConverterOptionalString.write(value.`verificationUrl`, buf)
            FfiConverterOptionalString.write(value.`verificationUrlComplete`, buf)
            FfiConverterOptionalString.write(value.`message`, buf)
    }
}



data class CapabilityFlags (
    var `geo`: kotlin.Boolean
    , 
    var `notifications`: kotlin.Boolean
    , 
    var `netFetch`: kotlin.Boolean
    , 
    var `netWs`: kotlin.Boolean
    , 
    var `audioTts`: kotlin.Boolean
    , 
    var `voiceModel`: kotlin.Boolean
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeCapabilityFlags: FfiConverterRustBuffer<CapabilityFlags> {
    override fun read(buf: ByteBuffer): CapabilityFlags {
        return CapabilityFlags(
            FfiConverterBoolean.read(buf),
            FfiConverterBoolean.read(buf),
            FfiConverterBoolean.read(buf),
            FfiConverterBoolean.read(buf),
            FfiConverterBoolean.read(buf),
            FfiConverterBoolean.read(buf),
        )
    }

    override fun allocationSize(value: CapabilityFlags) = (
            FfiConverterBoolean.allocationSize(value.`geo`) +
            FfiConverterBoolean.allocationSize(value.`notifications`) +
            FfiConverterBoolean.allocationSize(value.`netFetch`) +
            FfiConverterBoolean.allocationSize(value.`netWs`) +
            FfiConverterBoolean.allocationSize(value.`audioTts`) +
            FfiConverterBoolean.allocationSize(value.`voiceModel`)
    )

    override fun write(value: CapabilityFlags, buf: ByteBuffer) {
            FfiConverterBoolean.write(value.`geo`, buf)
            FfiConverterBoolean.write(value.`notifications`, buf)
            FfiConverterBoolean.write(value.`netFetch`, buf)
            FfiConverterBoolean.write(value.`netWs`, buf)
            FfiConverterBoolean.write(value.`audioTts`, buf)
            FfiConverterBoolean.write(value.`voiceModel`, buf)
    }
}



data class CommunicationsState (
    var `signalStrength`: kotlin.UByte?
    , 
    var `registrationStatus`: RegistrationStatus?
    , 
    var `airplaneMode`: kotlin.Boolean?
    , 
    var `carrierName`: kotlin.String?
    , 
    var `cellularSupported`: kotlin.Boolean?
    , 
    var `telephonyEnabled`: kotlin.Boolean?
    , 
    var `faceTimeAudioEnabled`: kotlin.Boolean?
    , 
    var `faceTimeVideoEnabled`: kotlin.Boolean?
    , 
    var `muteStatus`: kotlin.Boolean?
    , 
    var `currentCallCount`: kotlin.UByte?
    , 
    var `newVoicemailCount`: kotlin.UByte?
    , 
    var `initiateCallAvailable`: kotlin.Boolean?
    , 
    var `endAndAcceptAvailable`: kotlin.Boolean?
    , 
    var `holdAndAcceptAvailable`: kotlin.Boolean?
    , 
    var `swapAvailable`: kotlin.Boolean?
    , 
    var `mergeAvailable`: kotlin.Boolean?
    , 
    var `holdAvailable`: kotlin.Boolean?
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeCommunicationsState: FfiConverterRustBuffer<CommunicationsState> {
    override fun read(buf: ByteBuffer): CommunicationsState {
        return CommunicationsState(
            FfiConverterOptionalUByte.read(buf),
            FfiConverterOptionalTypeRegistrationStatus.read(buf),
            FfiConverterOptionalBoolean.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalBoolean.read(buf),
            FfiConverterOptionalBoolean.read(buf),
            FfiConverterOptionalBoolean.read(buf),
            FfiConverterOptionalBoolean.read(buf),
            FfiConverterOptionalBoolean.read(buf),
            FfiConverterOptionalUByte.read(buf),
            FfiConverterOptionalUByte.read(buf),
            FfiConverterOptionalBoolean.read(buf),
            FfiConverterOptionalBoolean.read(buf),
            FfiConverterOptionalBoolean.read(buf),
            FfiConverterOptionalBoolean.read(buf),
            FfiConverterOptionalBoolean.read(buf),
            FfiConverterOptionalBoolean.read(buf),
        )
    }

    override fun allocationSize(value: CommunicationsState) = (
            FfiConverterOptionalUByte.allocationSize(value.`signalStrength`) +
            FfiConverterOptionalTypeRegistrationStatus.allocationSize(value.`registrationStatus`) +
            FfiConverterOptionalBoolean.allocationSize(value.`airplaneMode`) +
            FfiConverterOptionalString.allocationSize(value.`carrierName`) +
            FfiConverterOptionalBoolean.allocationSize(value.`cellularSupported`) +
            FfiConverterOptionalBoolean.allocationSize(value.`telephonyEnabled`) +
            FfiConverterOptionalBoolean.allocationSize(value.`faceTimeAudioEnabled`) +
            FfiConverterOptionalBoolean.allocationSize(value.`faceTimeVideoEnabled`) +
            FfiConverterOptionalBoolean.allocationSize(value.`muteStatus`) +
            FfiConverterOptionalUByte.allocationSize(value.`currentCallCount`) +
            FfiConverterOptionalUByte.allocationSize(value.`newVoicemailCount`) +
            FfiConverterOptionalBoolean.allocationSize(value.`initiateCallAvailable`) +
            FfiConverterOptionalBoolean.allocationSize(value.`endAndAcceptAvailable`) +
            FfiConverterOptionalBoolean.allocationSize(value.`holdAndAcceptAvailable`) +
            FfiConverterOptionalBoolean.allocationSize(value.`swapAvailable`) +
            FfiConverterOptionalBoolean.allocationSize(value.`mergeAvailable`) +
            FfiConverterOptionalBoolean.allocationSize(value.`holdAvailable`)
    )

    override fun write(value: CommunicationsState, buf: ByteBuffer) {
            FfiConverterOptionalUByte.write(value.`signalStrength`, buf)
            FfiConverterOptionalTypeRegistrationStatus.write(value.`registrationStatus`, buf)
            FfiConverterOptionalBoolean.write(value.`airplaneMode`, buf)
            FfiConverterOptionalString.write(value.`carrierName`, buf)
            FfiConverterOptionalBoolean.write(value.`cellularSupported`, buf)
            FfiConverterOptionalBoolean.write(value.`telephonyEnabled`, buf)
            FfiConverterOptionalBoolean.write(value.`faceTimeAudioEnabled`, buf)
            FfiConverterOptionalBoolean.write(value.`faceTimeVideoEnabled`, buf)
            FfiConverterOptionalBoolean.write(value.`muteStatus`, buf)
            FfiConverterOptionalUByte.write(value.`currentCallCount`, buf)
            FfiConverterOptionalUByte.write(value.`newVoicemailCount`, buf)
            FfiConverterOptionalBoolean.write(value.`initiateCallAvailable`, buf)
            FfiConverterOptionalBoolean.write(value.`endAndAcceptAvailable`, buf)
            FfiConverterOptionalBoolean.write(value.`holdAndAcceptAvailable`, buf)
            FfiConverterOptionalBoolean.write(value.`swapAvailable`, buf)
            FfiConverterOptionalBoolean.write(value.`mergeAvailable`, buf)
            FfiConverterOptionalBoolean.write(value.`holdAvailable`, buf)
    }
}



data class CompanionBackends (
    var `link`: LinkTransport?
    , 
    var `host`: HostEnvironment
    , 
    var `http`: HttpTransport
    , 
    var `ws`: WsTransport
    , 
    var `secrets`: SecretStore
    , 
    var `log`: LogSink
    , 
    var `audio`: AudioBackend? = null 
    , 
    var `volume`: VolumeBackend? = null 
    , 
    var `geo`: GeoProvider? = null 
    , 
    var `notifications`: NotificationBackend? = null 
    , 
    var `phone`: PhoneBackend? = null 
    , 
    var `mediaSessions`: MediaSessionBackend? = null 
    , 
    var `speech`: SpeechRecognizer? = null 
    , 
    var `nlu`: NluModelRunner? = null 
    , 
    var `appleMusic`: AppleMusicBackend? = null 
    , 
    var `image`: ImageScaler? = null 
    , 
    var `modelValidator`: ModelArtifactValidator? = null 
    , 
    var `transferPolicy`: TransferPolicy? = null 
    , 
    var `connectivity`: ConnectivityMonitor? = null 
    , 
    var `deviceWaker`: DeviceWaker? = null 
    , 
    var `extensions`: ExtensionHost? = null 
    
): Disposable{
    

    

    
    @Suppress("UNNECESSARY_SAFE_CALL") // codegen is much simpler if we unconditionally emit safe calls here
    override fun destroy() {
        
    Disposable.destroy(
        this.`link`,
        this.`host`,
        this.`http`,
        this.`ws`,
        this.`secrets`,
        this.`log`,
        this.`audio`,
        this.`volume`,
        this.`geo`,
        this.`notifications`,
        this.`phone`,
        this.`mediaSessions`,
        this.`speech`,
        this.`nlu`,
        this.`appleMusic`,
        this.`image`,
        this.`modelValidator`,
        this.`transferPolicy`,
        this.`connectivity`,
        this.`deviceWaker`,
        this.`extensions`
    )
    }
    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeCompanionBackends: FfiConverterRustBuffer<CompanionBackends> {
    override fun read(buf: ByteBuffer): CompanionBackends {
        return CompanionBackends(
            FfiConverterOptionalTypeLinkTransport.read(buf),
            FfiConverterTypeHostEnvironment.read(buf),
            FfiConverterTypeHttpTransport.read(buf),
            FfiConverterTypeWsTransport.read(buf),
            FfiConverterTypeSecretStore.read(buf),
            FfiConverterTypeLogSink.read(buf),
            FfiConverterOptionalTypeAudioBackend.read(buf),
            FfiConverterOptionalTypeVolumeBackend.read(buf),
            FfiConverterOptionalTypeGeoProvider.read(buf),
            FfiConverterOptionalTypeNotificationBackend.read(buf),
            FfiConverterOptionalTypePhoneBackend.read(buf),
            FfiConverterOptionalTypeMediaSessionBackend.read(buf),
            FfiConverterOptionalTypeSpeechRecognizer.read(buf),
            FfiConverterOptionalTypeNluModelRunner.read(buf),
            FfiConverterOptionalTypeAppleMusicBackend.read(buf),
            FfiConverterOptionalTypeImageScaler.read(buf),
            FfiConverterOptionalTypeModelArtifactValidator.read(buf),
            FfiConverterOptionalTypeTransferPolicy.read(buf),
            FfiConverterOptionalTypeConnectivityMonitor.read(buf),
            FfiConverterOptionalTypeDeviceWaker.read(buf),
            FfiConverterOptionalTypeExtensionHost.read(buf),
        )
    }

    override fun allocationSize(value: CompanionBackends) = (
            FfiConverterOptionalTypeLinkTransport.allocationSize(value.`link`) +
            FfiConverterTypeHostEnvironment.allocationSize(value.`host`) +
            FfiConverterTypeHttpTransport.allocationSize(value.`http`) +
            FfiConverterTypeWsTransport.allocationSize(value.`ws`) +
            FfiConverterTypeSecretStore.allocationSize(value.`secrets`) +
            FfiConverterTypeLogSink.allocationSize(value.`log`) +
            FfiConverterOptionalTypeAudioBackend.allocationSize(value.`audio`) +
            FfiConverterOptionalTypeVolumeBackend.allocationSize(value.`volume`) +
            FfiConverterOptionalTypeGeoProvider.allocationSize(value.`geo`) +
            FfiConverterOptionalTypeNotificationBackend.allocationSize(value.`notifications`) +
            FfiConverterOptionalTypePhoneBackend.allocationSize(value.`phone`) +
            FfiConverterOptionalTypeMediaSessionBackend.allocationSize(value.`mediaSessions`) +
            FfiConverterOptionalTypeSpeechRecognizer.allocationSize(value.`speech`) +
            FfiConverterOptionalTypeNluModelRunner.allocationSize(value.`nlu`) +
            FfiConverterOptionalTypeAppleMusicBackend.allocationSize(value.`appleMusic`) +
            FfiConverterOptionalTypeImageScaler.allocationSize(value.`image`) +
            FfiConverterOptionalTypeModelArtifactValidator.allocationSize(value.`modelValidator`) +
            FfiConverterOptionalTypeTransferPolicy.allocationSize(value.`transferPolicy`) +
            FfiConverterOptionalTypeConnectivityMonitor.allocationSize(value.`connectivity`) +
            FfiConverterOptionalTypeDeviceWaker.allocationSize(value.`deviceWaker`) +
            FfiConverterOptionalTypeExtensionHost.allocationSize(value.`extensions`)
    )

    override fun write(value: CompanionBackends, buf: ByteBuffer) {
            FfiConverterOptionalTypeLinkTransport.write(value.`link`, buf)
            FfiConverterTypeHostEnvironment.write(value.`host`, buf)
            FfiConverterTypeHttpTransport.write(value.`http`, buf)
            FfiConverterTypeWsTransport.write(value.`ws`, buf)
            FfiConverterTypeSecretStore.write(value.`secrets`, buf)
            FfiConverterTypeLogSink.write(value.`log`, buf)
            FfiConverterOptionalTypeAudioBackend.write(value.`audio`, buf)
            FfiConverterOptionalTypeVolumeBackend.write(value.`volume`, buf)
            FfiConverterOptionalTypeGeoProvider.write(value.`geo`, buf)
            FfiConverterOptionalTypeNotificationBackend.write(value.`notifications`, buf)
            FfiConverterOptionalTypePhoneBackend.write(value.`phone`, buf)
            FfiConverterOptionalTypeMediaSessionBackend.write(value.`mediaSessions`, buf)
            FfiConverterOptionalTypeSpeechRecognizer.write(value.`speech`, buf)
            FfiConverterOptionalTypeNluModelRunner.write(value.`nlu`, buf)
            FfiConverterOptionalTypeAppleMusicBackend.write(value.`appleMusic`, buf)
            FfiConverterOptionalTypeImageScaler.write(value.`image`, buf)
            FfiConverterOptionalTypeModelArtifactValidator.write(value.`modelValidator`, buf)
            FfiConverterOptionalTypeTransferPolicy.write(value.`transferPolicy`, buf)
            FfiConverterOptionalTypeConnectivityMonitor.write(value.`connectivity`, buf)
            FfiConverterOptionalTypeDeviceWaker.write(value.`deviceWaker`, buf)
            FfiConverterOptionalTypeExtensionHost.write(value.`extensions`, buf)
    }
}



data class CompanionConfig (
    var `host`: HostInfo
    , 
    var `capabilities`: CapabilityFlags
    , 
    var `stateDir`: kotlin.String
    , 
    var `cacheDir`: kotlin.String
    , 
    var `modelPlatform`: ModelPlatform? = null 
    , 
    var `spotify`: SpotifyProviderConfig? = null 
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeCompanionConfig: FfiConverterRustBuffer<CompanionConfig> {
    override fun read(buf: ByteBuffer): CompanionConfig {
        return CompanionConfig(
            FfiConverterTypeHostInfo.read(buf),
            FfiConverterTypeCapabilityFlags.read(buf),
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterOptionalTypeModelPlatform.read(buf),
            FfiConverterOptionalTypeSpotifyProviderConfig.read(buf),
        )
    }

    override fun allocationSize(value: CompanionConfig) = (
            FfiConverterTypeHostInfo.allocationSize(value.`host`) +
            FfiConverterTypeCapabilityFlags.allocationSize(value.`capabilities`) +
            FfiConverterString.allocationSize(value.`stateDir`) +
            FfiConverterString.allocationSize(value.`cacheDir`) +
            FfiConverterOptionalTypeModelPlatform.allocationSize(value.`modelPlatform`) +
            FfiConverterOptionalTypeSpotifyProviderConfig.allocationSize(value.`spotify`)
    )

    override fun write(value: CompanionConfig, buf: ByteBuffer) {
            FfiConverterTypeHostInfo.write(value.`host`, buf)
            FfiConverterTypeCapabilityFlags.write(value.`capabilities`, buf)
            FfiConverterString.write(value.`stateDir`, buf)
            FfiConverterString.write(value.`cacheDir`, buf)
            FfiConverterOptionalTypeModelPlatform.write(value.`modelPlatform`, buf)
            FfiConverterOptionalTypeSpotifyProviderConfig.write(value.`spotify`, buf)
    }
}



data class CompanionDebug (
    var `authorityPlaybackHeld`: kotlin.Boolean
    , 
    var `authorityMetadataHeld`: kotlin.Boolean
    , 
    var `authorityVolumeHeld`: kotlin.Boolean
    , 
    var `authorityAppBundle`: kotlin.String?
    , 
    var `arbitratedSource`: kotlin.String?
    , 
    var `librarySource`: kotlin.String?
    , 
    var `lastPlayedFrom`: kotlin.String?
    , 
    var `attachedProviders`: List<kotlin.String>
    , 
    var `attachedSchemes`: List<kotlin.String>
    , 
    var `linkedDevices`: List<kotlin.String>
    , 
    var `autoResume`: List<DeviceAutoResume>
    , 
    var `resumeTargets`: List<DeviceResumeTarget>
    , 
    var `voice`: VoiceDebug
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeCompanionDebug: FfiConverterRustBuffer<CompanionDebug> {
    override fun read(buf: ByteBuffer): CompanionDebug {
        return CompanionDebug(
            FfiConverterBoolean.read(buf),
            FfiConverterBoolean.read(buf),
            FfiConverterBoolean.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterSequenceString.read(buf),
            FfiConverterSequenceString.read(buf),
            FfiConverterSequenceString.read(buf),
            FfiConverterSequenceTypeDeviceAutoResume.read(buf),
            FfiConverterSequenceTypeDeviceResumeTarget.read(buf),
            FfiConverterTypeVoiceDebug.read(buf),
        )
    }

    override fun allocationSize(value: CompanionDebug) = (
            FfiConverterBoolean.allocationSize(value.`authorityPlaybackHeld`) +
            FfiConverterBoolean.allocationSize(value.`authorityMetadataHeld`) +
            FfiConverterBoolean.allocationSize(value.`authorityVolumeHeld`) +
            FfiConverterOptionalString.allocationSize(value.`authorityAppBundle`) +
            FfiConverterOptionalString.allocationSize(value.`arbitratedSource`) +
            FfiConverterOptionalString.allocationSize(value.`librarySource`) +
            FfiConverterOptionalString.allocationSize(value.`lastPlayedFrom`) +
            FfiConverterSequenceString.allocationSize(value.`attachedProviders`) +
            FfiConverterSequenceString.allocationSize(value.`attachedSchemes`) +
            FfiConverterSequenceString.allocationSize(value.`linkedDevices`) +
            FfiConverterSequenceTypeDeviceAutoResume.allocationSize(value.`autoResume`) +
            FfiConverterSequenceTypeDeviceResumeTarget.allocationSize(value.`resumeTargets`) +
            FfiConverterTypeVoiceDebug.allocationSize(value.`voice`)
    )

    override fun write(value: CompanionDebug, buf: ByteBuffer) {
            FfiConverterBoolean.write(value.`authorityPlaybackHeld`, buf)
            FfiConverterBoolean.write(value.`authorityMetadataHeld`, buf)
            FfiConverterBoolean.write(value.`authorityVolumeHeld`, buf)
            FfiConverterOptionalString.write(value.`authorityAppBundle`, buf)
            FfiConverterOptionalString.write(value.`arbitratedSource`, buf)
            FfiConverterOptionalString.write(value.`librarySource`, buf)
            FfiConverterOptionalString.write(value.`lastPlayedFrom`, buf)
            FfiConverterSequenceString.write(value.`attachedProviders`, buf)
            FfiConverterSequenceString.write(value.`attachedSchemes`, buf)
            FfiConverterSequenceString.write(value.`linkedDevices`, buf)
            FfiConverterSequenceTypeDeviceAutoResume.write(value.`autoResume`, buf)
            FfiConverterSequenceTypeDeviceResumeTarget.write(value.`resumeTargets`, buf)
            FfiConverterTypeVoiceDebug.write(value.`voice`, buf)
    }
}



data class ConfigEntry (
    var `key`: kotlin.String
    , 
    var `value`: kotlin.String
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeConfigEntry: FfiConverterRustBuffer<ConfigEntry> {
    override fun read(buf: ByteBuffer): ConfigEntry {
        return ConfigEntry(
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
        )
    }

    override fun allocationSize(value: ConfigEntry) = (
            FfiConverterString.allocationSize(value.`key`) +
            FfiConverterString.allocationSize(value.`value`)
    )

    override fun write(value: ConfigEntry, buf: ByteBuffer) {
            FfiConverterString.write(value.`key`, buf)
            FfiConverterString.write(value.`value`, buf)
    }
}



data class ConfigField (
    var `kind`: ConfigKind
    , 
    var `key`: kotlin.String
    , 
    var `label`: kotlin.String
    , 
    var `pattern`: kotlin.String?
    , 
    var `minLength`: kotlin.UInt?
    , 
    var `maxLength`: kotlin.UInt?
    , 
    var `min`: kotlin.Double?
    , 
    var `max`: kotlin.Double?
    , 
    var `step`: kotlin.Double?
    , 
    var `choices`: List<kotlin.String>
    , 
    var `defaultValue`: kotlin.String?
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeConfigField: FfiConverterRustBuffer<ConfigField> {
    override fun read(buf: ByteBuffer): ConfigField {
        return ConfigField(
            FfiConverterTypeConfigKind.read(buf),
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalUInt.read(buf),
            FfiConverterOptionalUInt.read(buf),
            FfiConverterOptionalDouble.read(buf),
            FfiConverterOptionalDouble.read(buf),
            FfiConverterOptionalDouble.read(buf),
            FfiConverterSequenceString.read(buf),
            FfiConverterOptionalString.read(buf),
        )
    }

    override fun allocationSize(value: ConfigField) = (
            FfiConverterTypeConfigKind.allocationSize(value.`kind`) +
            FfiConverterString.allocationSize(value.`key`) +
            FfiConverterString.allocationSize(value.`label`) +
            FfiConverterOptionalString.allocationSize(value.`pattern`) +
            FfiConverterOptionalUInt.allocationSize(value.`minLength`) +
            FfiConverterOptionalUInt.allocationSize(value.`maxLength`) +
            FfiConverterOptionalDouble.allocationSize(value.`min`) +
            FfiConverterOptionalDouble.allocationSize(value.`max`) +
            FfiConverterOptionalDouble.allocationSize(value.`step`) +
            FfiConverterSequenceString.allocationSize(value.`choices`) +
            FfiConverterOptionalString.allocationSize(value.`defaultValue`)
    )

    override fun write(value: ConfigField, buf: ByteBuffer) {
            FfiConverterTypeConfigKind.write(value.`kind`, buf)
            FfiConverterString.write(value.`key`, buf)
            FfiConverterString.write(value.`label`, buf)
            FfiConverterOptionalString.write(value.`pattern`, buf)
            FfiConverterOptionalUInt.write(value.`minLength`, buf)
            FfiConverterOptionalUInt.write(value.`maxLength`, buf)
            FfiConverterOptionalDouble.write(value.`min`, buf)
            FfiConverterOptionalDouble.write(value.`max`, buf)
            FfiConverterOptionalDouble.write(value.`step`, buf)
            FfiConverterSequenceString.write(value.`choices`, buf)
            FfiConverterOptionalString.write(value.`defaultValue`, buf)
    }
}



data class DeviceAutoResume (
    var `deviceId`: kotlin.String
    , 
    var `enabled`: kotlin.Boolean
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeDeviceAutoResume: FfiConverterRustBuffer<DeviceAutoResume> {
    override fun read(buf: ByteBuffer): DeviceAutoResume {
        return DeviceAutoResume(
            FfiConverterString.read(buf),
            FfiConverterBoolean.read(buf),
        )
    }

    override fun allocationSize(value: DeviceAutoResume) = (
            FfiConverterString.allocationSize(value.`deviceId`) +
            FfiConverterBoolean.allocationSize(value.`enabled`)
    )

    override fun write(value: DeviceAutoResume, buf: ByteBuffer) {
            FfiConverterString.write(value.`deviceId`, buf)
            FfiConverterBoolean.write(value.`enabled`, buf)
    }
}



data class DeviceLogLine (
    var `seq`: kotlin.ULong
    , 
    var `tsUnixMs`: kotlin.ULong
    , 
    var `origin`: LogOrigin
    , 
    var `level`: LogLevel
    , 
    var `target`: kotlin.String
    , 
    var `message`: kotlin.String
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeDeviceLogLine: FfiConverterRustBuffer<DeviceLogLine> {
    override fun read(buf: ByteBuffer): DeviceLogLine {
        return DeviceLogLine(
            FfiConverterULong.read(buf),
            FfiConverterULong.read(buf),
            FfiConverterTypeLogOrigin.read(buf),
            FfiConverterTypeLogLevel.read(buf),
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
        )
    }

    override fun allocationSize(value: DeviceLogLine) = (
            FfiConverterULong.allocationSize(value.`seq`) +
            FfiConverterULong.allocationSize(value.`tsUnixMs`) +
            FfiConverterTypeLogOrigin.allocationSize(value.`origin`) +
            FfiConverterTypeLogLevel.allocationSize(value.`level`) +
            FfiConverterString.allocationSize(value.`target`) +
            FfiConverterString.allocationSize(value.`message`)
    )

    override fun write(value: DeviceLogLine, buf: ByteBuffer) {
            FfiConverterULong.write(value.`seq`, buf)
            FfiConverterULong.write(value.`tsUnixMs`, buf)
            FfiConverterTypeLogOrigin.write(value.`origin`, buf)
            FfiConverterTypeLogLevel.write(value.`level`, buf)
            FfiConverterString.write(value.`target`, buf)
            FfiConverterString.write(value.`message`, buf)
    }
}



data class DeviceMeta (
    var `daemonVersion`: kotlin.String
    , 
    var `libbridgethingVersion`: kotlin.String
    , 
    var `imageVersion`: kotlin.String
    , 
    var `appName`: kotlin.String
    , 
    var `osName`: kotlin.String
    , 
    var `osVersion`: kotlin.String
    , 
    var `channel`: kotlin.String
    , 
    var `modelName`: kotlin.String
    , 
    var `serialNumber`: kotlin.String
    , 
    var `nickname`: kotlin.String?
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeDeviceMeta: FfiConverterRustBuffer<DeviceMeta> {
    override fun read(buf: ByteBuffer): DeviceMeta {
        return DeviceMeta(
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterOptionalString.read(buf),
        )
    }

    override fun allocationSize(value: DeviceMeta) = (
            FfiConverterString.allocationSize(value.`daemonVersion`) +
            FfiConverterString.allocationSize(value.`libbridgethingVersion`) +
            FfiConverterString.allocationSize(value.`imageVersion`) +
            FfiConverterString.allocationSize(value.`appName`) +
            FfiConverterString.allocationSize(value.`osName`) +
            FfiConverterString.allocationSize(value.`osVersion`) +
            FfiConverterString.allocationSize(value.`channel`) +
            FfiConverterString.allocationSize(value.`modelName`) +
            FfiConverterString.allocationSize(value.`serialNumber`) +
            FfiConverterOptionalString.allocationSize(value.`nickname`)
    )

    override fun write(value: DeviceMeta, buf: ByteBuffer) {
            FfiConverterString.write(value.`daemonVersion`, buf)
            FfiConverterString.write(value.`libbridgethingVersion`, buf)
            FfiConverterString.write(value.`imageVersion`, buf)
            FfiConverterString.write(value.`appName`, buf)
            FfiConverterString.write(value.`osName`, buf)
            FfiConverterString.write(value.`osVersion`, buf)
            FfiConverterString.write(value.`channel`, buf)
            FfiConverterString.write(value.`modelName`, buf)
            FfiConverterString.write(value.`serialNumber`, buf)
            FfiConverterOptionalString.write(value.`nickname`, buf)
    }
}



data class DeviceMetaEntry (
    var `deviceId`: kotlin.String
    , 
    var `meta`: DeviceMeta
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeDeviceMetaEntry: FfiConverterRustBuffer<DeviceMetaEntry> {
    override fun read(buf: ByteBuffer): DeviceMetaEntry {
        return DeviceMetaEntry(
            FfiConverterString.read(buf),
            FfiConverterTypeDeviceMeta.read(buf),
        )
    }

    override fun allocationSize(value: DeviceMetaEntry) = (
            FfiConverterString.allocationSize(value.`deviceId`) +
            FfiConverterTypeDeviceMeta.allocationSize(value.`meta`)
    )

    override fun write(value: DeviceMetaEntry, buf: ByteBuffer) {
            FfiConverterString.write(value.`deviceId`, buf)
            FfiConverterTypeDeviceMeta.write(value.`meta`, buf)
    }
}



data class DeviceResumeTarget (
    var `deviceId`: kotlin.String
    , 
    var `target`: ResumeTarget
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeDeviceResumeTarget: FfiConverterRustBuffer<DeviceResumeTarget> {
    override fun read(buf: ByteBuffer): DeviceResumeTarget {
        return DeviceResumeTarget(
            FfiConverterString.read(buf),
            FfiConverterTypeResumeTarget.read(buf),
        )
    }

    override fun allocationSize(value: DeviceResumeTarget) = (
            FfiConverterString.allocationSize(value.`deviceId`) +
            FfiConverterTypeResumeTarget.allocationSize(value.`target`)
    )

    override fun write(value: DeviceResumeTarget, buf: ByteBuffer) {
            FfiConverterString.write(value.`deviceId`, buf)
            FfiConverterTypeResumeTarget.write(value.`target`, buf)
    }
}



data class DeviceWebappsEntry (
    var `deviceId`: kotlin.String
    , 
    var `webapps`: List<WebappInfo>
    , 
    var `active`: ActiveWebapp?
    , 
    var `listed`: kotlin.Boolean
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeDeviceWebappsEntry: FfiConverterRustBuffer<DeviceWebappsEntry> {
    override fun read(buf: ByteBuffer): DeviceWebappsEntry {
        return DeviceWebappsEntry(
            FfiConverterString.read(buf),
            FfiConverterSequenceTypeWebappInfo.read(buf),
            FfiConverterOptionalTypeActiveWebapp.read(buf),
            FfiConverterBoolean.read(buf),
        )
    }

    override fun allocationSize(value: DeviceWebappsEntry) = (
            FfiConverterString.allocationSize(value.`deviceId`) +
            FfiConverterSequenceTypeWebappInfo.allocationSize(value.`webapps`) +
            FfiConverterOptionalTypeActiveWebapp.allocationSize(value.`active`) +
            FfiConverterBoolean.allocationSize(value.`listed`)
    )

    override fun write(value: DeviceWebappsEntry, buf: ByteBuffer) {
            FfiConverterString.write(value.`deviceId`, buf)
            FfiConverterSequenceTypeWebappInfo.write(value.`webapps`, buf)
            FfiConverterOptionalTypeActiveWebapp.write(value.`active`, buf)
            FfiConverterBoolean.write(value.`listed`, buf)
    }
}



data class DocEntry (
    var `key`: kotlin.String
    , 
    var `value`: kotlin.String
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeDocEntry: FfiConverterRustBuffer<DocEntry> {
    override fun read(buf: ByteBuffer): DocEntry {
        return DocEntry(
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
        )
    }

    override fun allocationSize(value: DocEntry) = (
            FfiConverterString.allocationSize(value.`key`) +
            FfiConverterString.allocationSize(value.`value`)
    )

    override fun write(value: DocEntry, buf: ByteBuffer) {
            FfiConverterString.write(value.`key`, buf)
            FfiConverterString.write(value.`value`, buf)
    }
}



data class ExtensionConfigEntry (
    var `webapp`: kotlin.String
    , 
    var `key`: kotlin.String
    , 
    var `value`: kotlin.String
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeExtensionConfigEntry: FfiConverterRustBuffer<ExtensionConfigEntry> {
    override fun read(buf: ByteBuffer): ExtensionConfigEntry {
        return ExtensionConfigEntry(
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
        )
    }

    override fun allocationSize(value: ExtensionConfigEntry) = (
            FfiConverterString.allocationSize(value.`webapp`) +
            FfiConverterString.allocationSize(value.`key`) +
            FfiConverterString.allocationSize(value.`value`)
    )

    override fun write(value: ExtensionConfigEntry, buf: ByteBuffer) {
            FfiConverterString.write(value.`webapp`, buf)
            FfiConverterString.write(value.`key`, buf)
            FfiConverterString.write(value.`value`, buf)
    }
}



data class ExtensionInfo (
    var `permissions`: List<kotlin.String>
    , 
    var `api`: kotlin.UInt
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeExtensionInfo: FfiConverterRustBuffer<ExtensionInfo> {
    override fun read(buf: ByteBuffer): ExtensionInfo {
        return ExtensionInfo(
            FfiConverterSequenceString.read(buf),
            FfiConverterUInt.read(buf),
        )
    }

    override fun allocationSize(value: ExtensionInfo) = (
            FfiConverterSequenceString.allocationSize(value.`permissions`) +
            FfiConverterUInt.allocationSize(value.`api`)
    )

    override fun write(value: ExtensionInfo, buf: ByteBuffer) {
            FfiConverterSequenceString.write(value.`permissions`, buf)
            FfiConverterUInt.write(value.`api`, buf)
    }
}



data class HostClock (
    var `tzIana`: kotlin.String
    , 
    var `locale`: kotlin.String
    , 
    var `unixSeconds`: kotlin.ULong
    , 
    var `utcOffsetMinutes`: kotlin.Short
    , 
    var `dstOffsetMinutes`: kotlin.Byte
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeHostClock: FfiConverterRustBuffer<HostClock> {
    override fun read(buf: ByteBuffer): HostClock {
        return HostClock(
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterULong.read(buf),
            FfiConverterShort.read(buf),
            FfiConverterByte.read(buf),
        )
    }

    override fun allocationSize(value: HostClock) = (
            FfiConverterString.allocationSize(value.`tzIana`) +
            FfiConverterString.allocationSize(value.`locale`) +
            FfiConverterULong.allocationSize(value.`unixSeconds`) +
            FfiConverterShort.allocationSize(value.`utcOffsetMinutes`) +
            FfiConverterByte.allocationSize(value.`dstOffsetMinutes`)
    )

    override fun write(value: HostClock, buf: ByteBuffer) {
            FfiConverterString.write(value.`tzIana`, buf)
            FfiConverterString.write(value.`locale`, buf)
            FfiConverterULong.write(value.`unixSeconds`, buf)
            FfiConverterShort.write(value.`utcOffsetMinutes`, buf)
            FfiConverterByte.write(value.`dstOffsetMinutes`, buf)
    }
}



data class HostInfo (
    var `appName`: kotlin.String
    , 
    var `appVersion`: kotlin.String
    , 
    var `osName`: kotlin.String
    , 
    var `osVersion`: kotlin.String
    , 
    var `hostIdentifier`: kotlin.String
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeHostInfo: FfiConverterRustBuffer<HostInfo> {
    override fun read(buf: ByteBuffer): HostInfo {
        return HostInfo(
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
        )
    }

    override fun allocationSize(value: HostInfo) = (
            FfiConverterString.allocationSize(value.`appName`) +
            FfiConverterString.allocationSize(value.`appVersion`) +
            FfiConverterString.allocationSize(value.`osName`) +
            FfiConverterString.allocationSize(value.`osVersion`) +
            FfiConverterString.allocationSize(value.`hostIdentifier`)
    )

    override fun write(value: HostInfo, buf: ByteBuffer) {
            FfiConverterString.write(value.`appName`, buf)
            FfiConverterString.write(value.`appVersion`, buf)
            FfiConverterString.write(value.`osName`, buf)
            FfiConverterString.write(value.`osVersion`, buf)
            FfiConverterString.write(value.`hostIdentifier`, buf)
    }
}



data class HttpHeader (
    var `name`: kotlin.String
    , 
    var `value`: kotlin.String
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeHttpHeader: FfiConverterRustBuffer<HttpHeader> {
    override fun read(buf: ByteBuffer): HttpHeader {
        return HttpHeader(
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
        )
    }

    override fun allocationSize(value: HttpHeader) = (
            FfiConverterString.allocationSize(value.`name`) +
            FfiConverterString.allocationSize(value.`value`)
    )

    override fun write(value: HttpHeader, buf: ByteBuffer) {
            FfiConverterString.write(value.`name`, buf)
            FfiConverterString.write(value.`value`, buf)
    }
}



data class HttpRequest (
    var `method`: HttpMethod
    , 
    var `url`: kotlin.String
    , 
    var `headers`: List<HttpHeader>
    , 
    var `body`: kotlin.ByteArray
    , 
    var `timeoutMs`: kotlin.UInt
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeHttpRequest: FfiConverterRustBuffer<HttpRequest> {
    override fun read(buf: ByteBuffer): HttpRequest {
        return HttpRequest(
            FfiConverterTypeHttpMethod.read(buf),
            FfiConverterString.read(buf),
            FfiConverterSequenceTypeHttpHeader.read(buf),
            FfiConverterByteArray.read(buf),
            FfiConverterUInt.read(buf),
        )
    }

    override fun allocationSize(value: HttpRequest) = (
            FfiConverterTypeHttpMethod.allocationSize(value.`method`) +
            FfiConverterString.allocationSize(value.`url`) +
            FfiConverterSequenceTypeHttpHeader.allocationSize(value.`headers`) +
            FfiConverterByteArray.allocationSize(value.`body`) +
            FfiConverterUInt.allocationSize(value.`timeoutMs`)
    )

    override fun write(value: HttpRequest, buf: ByteBuffer) {
            FfiConverterTypeHttpMethod.write(value.`method`, buf)
            FfiConverterString.write(value.`url`, buf)
            FfiConverterSequenceTypeHttpHeader.write(value.`headers`, buf)
            FfiConverterByteArray.write(value.`body`, buf)
            FfiConverterUInt.write(value.`timeoutMs`, buf)
    }
}



data class HttpResponse (
    var `status`: kotlin.UShort
    , 
    var `headers`: List<HttpHeader>
    , 
    var `body`: kotlin.ByteArray
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeHttpResponse: FfiConverterRustBuffer<HttpResponse> {
    override fun read(buf: ByteBuffer): HttpResponse {
        return HttpResponse(
            FfiConverterUShort.read(buf),
            FfiConverterSequenceTypeHttpHeader.read(buf),
            FfiConverterByteArray.read(buf),
        )
    }

    override fun allocationSize(value: HttpResponse) = (
            FfiConverterUShort.allocationSize(value.`status`) +
            FfiConverterSequenceTypeHttpHeader.allocationSize(value.`headers`) +
            FfiConverterByteArray.allocationSize(value.`body`)
    )

    override fun write(value: HttpResponse, buf: ByteBuffer) {
            FfiConverterUShort.write(value.`status`, buf)
            FfiConverterSequenceTypeHttpHeader.write(value.`headers`, buf)
            FfiConverterByteArray.write(value.`body`, buf)
    }
}



data class LinkDevice (
    var `id`: kotlin.String
    , 
    var `name`: kotlin.String
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeLinkDevice: FfiConverterRustBuffer<LinkDevice> {
    override fun read(buf: ByteBuffer): LinkDevice {
        return LinkDevice(
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
        )
    }

    override fun allocationSize(value: LinkDevice) = (
            FfiConverterString.allocationSize(value.`id`) +
            FfiConverterString.allocationSize(value.`name`)
    )

    override fun write(value: LinkDevice, buf: ByteBuffer) {
            FfiConverterString.write(value.`id`, buf)
            FfiConverterString.write(value.`name`, buf)
    }
}



data class LogArchive (
    var `id`: kotlin.String
    , 
    var `startedAtMs`: kotlin.ULong
    , 
    var `bytes`: kotlin.ULong
    , 
    var `pinned`: kotlin.Boolean
    , 
    var `current`: kotlin.Boolean
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeLogArchive: FfiConverterRustBuffer<LogArchive> {
    override fun read(buf: ByteBuffer): LogArchive {
        return LogArchive(
            FfiConverterString.read(buf),
            FfiConverterULong.read(buf),
            FfiConverterULong.read(buf),
            FfiConverterBoolean.read(buf),
            FfiConverterBoolean.read(buf),
        )
    }

    override fun allocationSize(value: LogArchive) = (
            FfiConverterString.allocationSize(value.`id`) +
            FfiConverterULong.allocationSize(value.`startedAtMs`) +
            FfiConverterULong.allocationSize(value.`bytes`) +
            FfiConverterBoolean.allocationSize(value.`pinned`) +
            FfiConverterBoolean.allocationSize(value.`current`)
    )

    override fun write(value: LogArchive, buf: ByteBuffer) {
            FfiConverterString.write(value.`id`, buf)
            FfiConverterULong.write(value.`startedAtMs`, buf)
            FfiConverterULong.write(value.`bytes`, buf)
            FfiConverterBoolean.write(value.`pinned`, buf)
            FfiConverterBoolean.write(value.`current`, buf)
    }
}



data class LogStoreLine (
    var `tsUnixMs`: kotlin.ULong
    , 
    var `level`: LogStoreLevel
    , 
    var `label`: kotlin.String
    , 
    var `message`: kotlin.String
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeLogStoreLine: FfiConverterRustBuffer<LogStoreLine> {
    override fun read(buf: ByteBuffer): LogStoreLine {
        return LogStoreLine(
            FfiConverterULong.read(buf),
            FfiConverterTypeLogStoreLevel.read(buf),
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
        )
    }

    override fun allocationSize(value: LogStoreLine) = (
            FfiConverterULong.allocationSize(value.`tsUnixMs`) +
            FfiConverterTypeLogStoreLevel.allocationSize(value.`level`) +
            FfiConverterString.allocationSize(value.`label`) +
            FfiConverterString.allocationSize(value.`message`)
    )

    override fun write(value: LogStoreLine, buf: ByteBuffer) {
            FfiConverterULong.write(value.`tsUnixMs`, buf)
            FfiConverterTypeLogStoreLevel.write(value.`level`, buf)
            FfiConverterString.write(value.`label`, buf)
            FfiConverterString.write(value.`message`, buf)
    }
}



data class LrcLine (
    var `startMs`: kotlin.UInt
    , 
    var `text`: kotlin.String
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeLrcLine: FfiConverterRustBuffer<LrcLine> {
    override fun read(buf: ByteBuffer): LrcLine {
        return LrcLine(
            FfiConverterUInt.read(buf),
            FfiConverterString.read(buf),
        )
    }

    override fun allocationSize(value: LrcLine) = (
            FfiConverterUInt.allocationSize(value.`startMs`) +
            FfiConverterString.allocationSize(value.`text`)
    )

    override fun write(value: LrcLine, buf: ByteBuffer) {
            FfiConverterUInt.write(value.`startMs`, buf)
            FfiConverterString.write(value.`text`, buf)
    }
}



data class MediaArt (
    var `bytes`: kotlin.ByteArray
    , 
    var `mime`: kotlin.String
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeMediaArt: FfiConverterRustBuffer<MediaArt> {
    override fun read(buf: ByteBuffer): MediaArt {
        return MediaArt(
            FfiConverterByteArray.read(buf),
            FfiConverterString.read(buf),
        )
    }

    override fun allocationSize(value: MediaArt) = (
            FfiConverterByteArray.allocationSize(value.`bytes`) +
            FfiConverterString.allocationSize(value.`mime`)
    )

    override fun write(value: MediaArt, buf: ByteBuffer) {
            FfiConverterByteArray.write(value.`bytes`, buf)
            FfiConverterString.write(value.`mime`, buf)
    }
}



data class MediaQueueEntry (
    var `queueId`: kotlin.Long
    , 
    var `title`: kotlin.String?
    , 
    var `subtitle`: kotlin.String?
    , 
    var `artToken`: kotlin.String?
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeMediaQueueEntry: FfiConverterRustBuffer<MediaQueueEntry> {
    override fun read(buf: ByteBuffer): MediaQueueEntry {
        return MediaQueueEntry(
            FfiConverterLong.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
        )
    }

    override fun allocationSize(value: MediaQueueEntry) = (
            FfiConverterLong.allocationSize(value.`queueId`) +
            FfiConverterOptionalString.allocationSize(value.`title`) +
            FfiConverterOptionalString.allocationSize(value.`subtitle`) +
            FfiConverterOptionalString.allocationSize(value.`artToken`)
    )

    override fun write(value: MediaQueueEntry, buf: ByteBuffer) {
            FfiConverterLong.write(value.`queueId`, buf)
            FfiConverterOptionalString.write(value.`title`, buf)
            FfiConverterOptionalString.write(value.`subtitle`, buf)
            FfiConverterOptionalString.write(value.`artToken`, buf)
    }
}



data class MediaSessionSnapshot (
    var `package`: kotlin.String
    , 
    var `title`: kotlin.String?
    , 
    var `artist`: kotlin.String?
    , 
    var `album`: kotlin.String?
    , 
    var `durationMs`: kotlin.Long?
    , 
    var `positionMs`: kotlin.Long
    , 
    var `playing`: kotlin.Boolean
    , 
    var `canSeek`: kotlin.Boolean
    , 
    var `artToken`: kotlin.String?
    , 
    var `queue`: List<MediaQueueEntry>
    , 
    var `activeQueueId`: kotlin.Long?
    , 
    var `shuffle`: kotlin.Boolean?
    , 
    var `repeat`: MediaRepeatMode?
    , 
    var `speed`: kotlin.Float?
    , 
    var `positionAgeMs`: kotlin.Long?
    , 
    var `liked`: kotlin.Boolean?
    , 
    var `likeSupported`: kotlin.Boolean
    , 
    var `queueTitle`: kotlin.String?
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeMediaSessionSnapshot: FfiConverterRustBuffer<MediaSessionSnapshot> {
    override fun read(buf: ByteBuffer): MediaSessionSnapshot {
        return MediaSessionSnapshot(
            FfiConverterString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalLong.read(buf),
            FfiConverterLong.read(buf),
            FfiConverterBoolean.read(buf),
            FfiConverterBoolean.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterSequenceTypeMediaQueueEntry.read(buf),
            FfiConverterOptionalLong.read(buf),
            FfiConverterOptionalBoolean.read(buf),
            FfiConverterOptionalTypeMediaRepeatMode.read(buf),
            FfiConverterOptionalFloat.read(buf),
            FfiConverterOptionalLong.read(buf),
            FfiConverterOptionalBoolean.read(buf),
            FfiConverterBoolean.read(buf),
            FfiConverterOptionalString.read(buf),
        )
    }

    override fun allocationSize(value: MediaSessionSnapshot) = (
            FfiConverterString.allocationSize(value.`package`) +
            FfiConverterOptionalString.allocationSize(value.`title`) +
            FfiConverterOptionalString.allocationSize(value.`artist`) +
            FfiConverterOptionalString.allocationSize(value.`album`) +
            FfiConverterOptionalLong.allocationSize(value.`durationMs`) +
            FfiConverterLong.allocationSize(value.`positionMs`) +
            FfiConverterBoolean.allocationSize(value.`playing`) +
            FfiConverterBoolean.allocationSize(value.`canSeek`) +
            FfiConverterOptionalString.allocationSize(value.`artToken`) +
            FfiConverterSequenceTypeMediaQueueEntry.allocationSize(value.`queue`) +
            FfiConverterOptionalLong.allocationSize(value.`activeQueueId`) +
            FfiConverterOptionalBoolean.allocationSize(value.`shuffle`) +
            FfiConverterOptionalTypeMediaRepeatMode.allocationSize(value.`repeat`) +
            FfiConverterOptionalFloat.allocationSize(value.`speed`) +
            FfiConverterOptionalLong.allocationSize(value.`positionAgeMs`) +
            FfiConverterOptionalBoolean.allocationSize(value.`liked`) +
            FfiConverterBoolean.allocationSize(value.`likeSupported`) +
            FfiConverterOptionalString.allocationSize(value.`queueTitle`)
    )

    override fun write(value: MediaSessionSnapshot, buf: ByteBuffer) {
            FfiConverterString.write(value.`package`, buf)
            FfiConverterOptionalString.write(value.`title`, buf)
            FfiConverterOptionalString.write(value.`artist`, buf)
            FfiConverterOptionalString.write(value.`album`, buf)
            FfiConverterOptionalLong.write(value.`durationMs`, buf)
            FfiConverterLong.write(value.`positionMs`, buf)
            FfiConverterBoolean.write(value.`playing`, buf)
            FfiConverterBoolean.write(value.`canSeek`, buf)
            FfiConverterOptionalString.write(value.`artToken`, buf)
            FfiConverterSequenceTypeMediaQueueEntry.write(value.`queue`, buf)
            FfiConverterOptionalLong.write(value.`activeQueueId`, buf)
            FfiConverterOptionalBoolean.write(value.`shuffle`, buf)
            FfiConverterOptionalTypeMediaRepeatMode.write(value.`repeat`, buf)
            FfiConverterOptionalFloat.write(value.`speed`, buf)
            FfiConverterOptionalLong.write(value.`positionAgeMs`, buf)
            FfiConverterOptionalBoolean.write(value.`liked`, buf)
            FfiConverterBoolean.write(value.`likeSupported`, buf)
            FfiConverterOptionalString.write(value.`queueTitle`, buf)
    }
}



data class NluFastPathHit (
    var `intent`: kotlin.String
    , 
    var `slots`: NluFastPathSlots
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeNluFastPathHit: FfiConverterRustBuffer<NluFastPathHit> {
    override fun read(buf: ByteBuffer): NluFastPathHit {
        return NluFastPathHit(
            FfiConverterString.read(buf),
            FfiConverterTypeNluFastPathSlots.read(buf),
        )
    }

    override fun allocationSize(value: NluFastPathHit) = (
            FfiConverterString.allocationSize(value.`intent`) +
            FfiConverterTypeNluFastPathSlots.allocationSize(value.`slots`)
    )

    override fun write(value: NluFastPathHit, buf: ByteBuffer) {
            FfiConverterString.write(value.`intent`, buf)
            FfiConverterTypeNluFastPathSlots.write(value.`slots`, buf)
    }
}



data class NluFastPathSlots (
    var `preset`: kotlin.String?
    , 
    var `level`: kotlin.UInt?
    , 
    var `speed`: NluFastPathSpeed?
    , 
    var `seconds`: kotlin.Int?
    , 
    var `repeatMode`: NluFastPathRepeatMode?
    , 
    var `enabled`: kotlin.Boolean?
    , 
    var `view`: NluFastPathView?
    , 
    var `mute`: kotlin.Boolean?
    , 
    var `direction`: NluFastPathDirection?
    , 
    var `amount`: NluFastPathAmount?
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeNluFastPathSlots: FfiConverterRustBuffer<NluFastPathSlots> {
    override fun read(buf: ByteBuffer): NluFastPathSlots {
        return NluFastPathSlots(
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalUInt.read(buf),
            FfiConverterOptionalTypeNluFastPathSpeed.read(buf),
            FfiConverterOptionalInt.read(buf),
            FfiConverterOptionalTypeNluFastPathRepeatMode.read(buf),
            FfiConverterOptionalBoolean.read(buf),
            FfiConverterOptionalTypeNluFastPathView.read(buf),
            FfiConverterOptionalBoolean.read(buf),
            FfiConverterOptionalTypeNluFastPathDirection.read(buf),
            FfiConverterOptionalTypeNluFastPathAmount.read(buf),
        )
    }

    override fun allocationSize(value: NluFastPathSlots) = (
            FfiConverterOptionalString.allocationSize(value.`preset`) +
            FfiConverterOptionalUInt.allocationSize(value.`level`) +
            FfiConverterOptionalTypeNluFastPathSpeed.allocationSize(value.`speed`) +
            FfiConverterOptionalInt.allocationSize(value.`seconds`) +
            FfiConverterOptionalTypeNluFastPathRepeatMode.allocationSize(value.`repeatMode`) +
            FfiConverterOptionalBoolean.allocationSize(value.`enabled`) +
            FfiConverterOptionalTypeNluFastPathView.allocationSize(value.`view`) +
            FfiConverterOptionalBoolean.allocationSize(value.`mute`) +
            FfiConverterOptionalTypeNluFastPathDirection.allocationSize(value.`direction`) +
            FfiConverterOptionalTypeNluFastPathAmount.allocationSize(value.`amount`)
    )

    override fun write(value: NluFastPathSlots, buf: ByteBuffer) {
            FfiConverterOptionalString.write(value.`preset`, buf)
            FfiConverterOptionalUInt.write(value.`level`, buf)
            FfiConverterOptionalTypeNluFastPathSpeed.write(value.`speed`, buf)
            FfiConverterOptionalInt.write(value.`seconds`, buf)
            FfiConverterOptionalTypeNluFastPathRepeatMode.write(value.`repeatMode`, buf)
            FfiConverterOptionalBoolean.write(value.`enabled`, buf)
            FfiConverterOptionalTypeNluFastPathView.write(value.`view`, buf)
            FfiConverterOptionalBoolean.write(value.`mute`, buf)
            FfiConverterOptionalTypeNluFastPathDirection.write(value.`direction`, buf)
            FfiConverterOptionalTypeNluFastPathAmount.write(value.`amount`, buf)
    }
}



data class NluIntentCatalog (
    var `surfaceNames`: List<kotlin.String>
    , 
    var `noIntent`: kotlin.String
    , 
    var `clarify`: kotlin.String
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeNluIntentCatalog: FfiConverterRustBuffer<NluIntentCatalog> {
    override fun read(buf: ByteBuffer): NluIntentCatalog {
        return NluIntentCatalog(
            FfiConverterSequenceString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
        )
    }

    override fun allocationSize(value: NluIntentCatalog) = (
            FfiConverterSequenceString.allocationSize(value.`surfaceNames`) +
            FfiConverterString.allocationSize(value.`noIntent`) +
            FfiConverterString.allocationSize(value.`clarify`)
    )

    override fun write(value: NluIntentCatalog, buf: ByteBuffer) {
            FfiConverterSequenceString.write(value.`surfaceNames`, buf)
            FfiConverterString.write(value.`noIntent`, buf)
            FfiConverterString.write(value.`clarify`, buf)
    }
}



data class NluModelOutputs (
    var `intentLogits`: List<kotlin.Float>
    , 
    var `oodLogit`: kotlin.Float
    , 
    var `bioLogits`: List<kotlin.Float>
    , 
    var `closedLogits`: List<List<kotlin.Float>>
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeNluModelOutputs: FfiConverterRustBuffer<NluModelOutputs> {
    override fun read(buf: ByteBuffer): NluModelOutputs {
        return NluModelOutputs(
            FfiConverterSequenceFloat.read(buf),
            FfiConverterFloat.read(buf),
            FfiConverterSequenceFloat.read(buf),
            FfiConverterSequenceSequenceFloat.read(buf),
        )
    }

    override fun allocationSize(value: NluModelOutputs) = (
            FfiConverterSequenceFloat.allocationSize(value.`intentLogits`) +
            FfiConverterFloat.allocationSize(value.`oodLogit`) +
            FfiConverterSequenceFloat.allocationSize(value.`bioLogits`) +
            FfiConverterSequenceSequenceFloat.allocationSize(value.`closedLogits`)
    )

    override fun write(value: NluModelOutputs, buf: ByteBuffer) {
            FfiConverterSequenceFloat.write(value.`intentLogits`, buf)
            FfiConverterFloat.write(value.`oodLogit`, buf)
            FfiConverterSequenceFloat.write(value.`bioLogits`, buf)
            FfiConverterSequenceSequenceFloat.write(value.`closedLogits`, buf)
    }
}



data class NluRejectionPolicy (
    var `inDomainThreshold`: kotlin.Double = 0.5 
    , 
    var `clarifyMargin`: kotlin.Double = 0.15 
    , 
    var `maxAlternates`: kotlin.UInt = 2u 
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeNluRejectionPolicy: FfiConverterRustBuffer<NluRejectionPolicy> {
    override fun read(buf: ByteBuffer): NluRejectionPolicy {
        return NluRejectionPolicy(
            FfiConverterDouble.read(buf),
            FfiConverterDouble.read(buf),
            FfiConverterUInt.read(buf),
        )
    }

    override fun allocationSize(value: NluRejectionPolicy) = (
            FfiConverterDouble.allocationSize(value.`inDomainThreshold`) +
            FfiConverterDouble.allocationSize(value.`clarifyMargin`) +
            FfiConverterUInt.allocationSize(value.`maxAlternates`)
    )

    override fun write(value: NluRejectionPolicy, buf: ByteBuffer) {
            FfiConverterDouble.write(value.`inDomainThreshold`, buf)
            FfiConverterDouble.write(value.`clarifyMargin`, buf)
            FfiConverterUInt.write(value.`maxAlternates`, buf)
    }
}



data class NotificationAction (
    var `label`: kotlin.String
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeNotificationAction: FfiConverterRustBuffer<NotificationAction> {
    override fun read(buf: ByteBuffer): NotificationAction {
        return NotificationAction(
            FfiConverterString.read(buf),
        )
    }

    override fun allocationSize(value: NotificationAction) = (
            FfiConverterString.allocationSize(value.`label`)
    )

    override fun write(value: NotificationAction, buf: ByteBuffer) {
            FfiConverterString.write(value.`label`, buf)
    }
}



data class NotificationApp (
    var `bundleId`: kotlin.String
    , 
    var `displayName`: kotlin.String?
    , 
    var `iconAssetId`: kotlin.String?
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeNotificationApp: FfiConverterRustBuffer<NotificationApp> {
    override fun read(buf: ByteBuffer): NotificationApp {
        return NotificationApp(
            FfiConverterString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
        )
    }

    override fun allocationSize(value: NotificationApp) = (
            FfiConverterString.allocationSize(value.`bundleId`) +
            FfiConverterOptionalString.allocationSize(value.`displayName`) +
            FfiConverterOptionalString.allocationSize(value.`iconAssetId`)
    )

    override fun write(value: NotificationApp, buf: ByteBuffer) {
            FfiConverterString.write(value.`bundleId`, buf)
            FfiConverterOptionalString.write(value.`displayName`, buf)
            FfiConverterOptionalString.write(value.`iconAssetId`, buf)
    }
}



data class NotificationFlags (
    var `silent`: kotlin.Boolean
    , 
    var `important`: kotlin.Boolean
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeNotificationFlags: FfiConverterRustBuffer<NotificationFlags> {
    override fun read(buf: ByteBuffer): NotificationFlags {
        return NotificationFlags(
            FfiConverterBoolean.read(buf),
            FfiConverterBoolean.read(buf),
        )
    }

    override fun allocationSize(value: NotificationFlags) = (
            FfiConverterBoolean.allocationSize(value.`silent`) +
            FfiConverterBoolean.allocationSize(value.`important`)
    )

    override fun write(value: NotificationFlags, buf: ByteBuffer) {
            FfiConverterBoolean.write(value.`silent`, buf)
            FfiConverterBoolean.write(value.`important`, buf)
    }
}



data class NotificationRemoved (
    var `id`: kotlin.String
    , 
    var `reason`: DismissReason
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeNotificationRemoved: FfiConverterRustBuffer<NotificationRemoved> {
    override fun read(buf: ByteBuffer): NotificationRemoved {
        return NotificationRemoved(
            FfiConverterString.read(buf),
            FfiConverterTypeDismissReason.read(buf),
        )
    }

    override fun allocationSize(value: NotificationRemoved) = (
            FfiConverterString.allocationSize(value.`id`) +
            FfiConverterTypeDismissReason.allocationSize(value.`reason`)
    )

    override fun write(value: NotificationRemoved, buf: ByteBuffer) {
            FfiConverterString.write(value.`id`, buf)
            FfiConverterTypeDismissReason.write(value.`reason`, buf)
    }
}



data class NowPlaying (
    var `track`: NowPlayingTrack?
    , 
    var `playback`: NowPlayingPlayback
    , 
    var `appName`: kotlin.String?
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeNowPlaying: FfiConverterRustBuffer<NowPlaying> {
    override fun read(buf: ByteBuffer): NowPlaying {
        return NowPlaying(
            FfiConverterOptionalTypeNowPlayingTrack.read(buf),
            FfiConverterTypeNowPlayingPlayback.read(buf),
            FfiConverterOptionalString.read(buf),
        )
    }

    override fun allocationSize(value: NowPlaying) = (
            FfiConverterOptionalTypeNowPlayingTrack.allocationSize(value.`track`) +
            FfiConverterTypeNowPlayingPlayback.allocationSize(value.`playback`) +
            FfiConverterOptionalString.allocationSize(value.`appName`)
    )

    override fun write(value: NowPlaying, buf: ByteBuffer) {
            FfiConverterOptionalTypeNowPlayingTrack.write(value.`track`, buf)
            FfiConverterTypeNowPlayingPlayback.write(value.`playback`, buf)
            FfiConverterOptionalString.write(value.`appName`, buf)
    }
}



data class NowPlayingPlayback (
    var `playing`: kotlin.Boolean
    , 
    var `positionMs`: kotlin.ULong
    , 
    var `shuffle`: kotlin.Boolean
    , 
    var `repeatMode`: RepeatMode
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeNowPlayingPlayback: FfiConverterRustBuffer<NowPlayingPlayback> {
    override fun read(buf: ByteBuffer): NowPlayingPlayback {
        return NowPlayingPlayback(
            FfiConverterBoolean.read(buf),
            FfiConverterULong.read(buf),
            FfiConverterBoolean.read(buf),
            FfiConverterTypeRepeatMode.read(buf),
        )
    }

    override fun allocationSize(value: NowPlayingPlayback) = (
            FfiConverterBoolean.allocationSize(value.`playing`) +
            FfiConverterULong.allocationSize(value.`positionMs`) +
            FfiConverterBoolean.allocationSize(value.`shuffle`) +
            FfiConverterTypeRepeatMode.allocationSize(value.`repeatMode`)
    )

    override fun write(value: NowPlayingPlayback, buf: ByteBuffer) {
            FfiConverterBoolean.write(value.`playing`, buf)
            FfiConverterULong.write(value.`positionMs`, buf)
            FfiConverterBoolean.write(value.`shuffle`, buf)
            FfiConverterTypeRepeatMode.write(value.`repeatMode`, buf)
    }
}



data class NowPlayingTrack (
    var `id`: kotlin.String?
    , 
    var `title`: kotlin.String?
    , 
    var `artist`: kotlin.String?
    , 
    var `album`: kotlin.String?
    , 
    var `artworkUrl`: kotlin.String?
    , 
    var `durationMs`: kotlin.ULong?
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeNowPlayingTrack: FfiConverterRustBuffer<NowPlayingTrack> {
    override fun read(buf: ByteBuffer): NowPlayingTrack {
        return NowPlayingTrack(
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalULong.read(buf),
        )
    }

    override fun allocationSize(value: NowPlayingTrack) = (
            FfiConverterOptionalString.allocationSize(value.`id`) +
            FfiConverterOptionalString.allocationSize(value.`title`) +
            FfiConverterOptionalString.allocationSize(value.`artist`) +
            FfiConverterOptionalString.allocationSize(value.`album`) +
            FfiConverterOptionalString.allocationSize(value.`artworkUrl`) +
            FfiConverterOptionalULong.allocationSize(value.`durationMs`)
    )

    override fun write(value: NowPlayingTrack, buf: ByteBuffer) {
            FfiConverterOptionalString.write(value.`id`, buf)
            FfiConverterOptionalString.write(value.`title`, buf)
            FfiConverterOptionalString.write(value.`artist`, buf)
            FfiConverterOptionalString.write(value.`album`, buf)
            FfiConverterOptionalString.write(value.`artworkUrl`, buf)
            FfiConverterOptionalULong.write(value.`durationMs`, buf)
    }
}



data class OtaArtifactUrls (
    var `daemonBinary`: kotlin.String
    , 
    var `daemonBinaryZst`: kotlin.String
    , 
    var `imageSwu`: kotlin.String
    , 
    var `imageZck`: kotlin.String
    , 
    var `imageBootZck`: kotlin.String
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeOtaArtifactUrls: FfiConverterRustBuffer<OtaArtifactUrls> {
    override fun read(buf: ByteBuffer): OtaArtifactUrls {
        return OtaArtifactUrls(
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
        )
    }

    override fun allocationSize(value: OtaArtifactUrls) = (
            FfiConverterString.allocationSize(value.`daemonBinary`) +
            FfiConverterString.allocationSize(value.`daemonBinaryZst`) +
            FfiConverterString.allocationSize(value.`imageSwu`) +
            FfiConverterString.allocationSize(value.`imageZck`) +
            FfiConverterString.allocationSize(value.`imageBootZck`)
    )

    override fun write(value: OtaArtifactUrls, buf: ByteBuffer) {
            FfiConverterString.write(value.`daemonBinary`, buf)
            FfiConverterString.write(value.`daemonBinaryZst`, buf)
            FfiConverterString.write(value.`imageSwu`, buf)
            FfiConverterString.write(value.`imageZck`, buf)
            FfiConverterString.write(value.`imageBootZck`, buf)
    }
}



data class OtaAvailable (
    var `deviceId`: kotlin.String
    , 
    var `releaseVersion`: kotlin.String?
    , 
    var `daemonVersion`: kotlin.String?
    , 
    var `imageVersion`: kotlin.String?
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeOtaAvailable: FfiConverterRustBuffer<OtaAvailable> {
    override fun read(buf: ByteBuffer): OtaAvailable {
        return OtaAvailable(
            FfiConverterString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
        )
    }

    override fun allocationSize(value: OtaAvailable) = (
            FfiConverterString.allocationSize(value.`deviceId`) +
            FfiConverterOptionalString.allocationSize(value.`releaseVersion`) +
            FfiConverterOptionalString.allocationSize(value.`daemonVersion`) +
            FfiConverterOptionalString.allocationSize(value.`imageVersion`)
    )

    override fun write(value: OtaAvailable, buf: ByteBuffer) {
            FfiConverterString.write(value.`deviceId`, buf)
            FfiConverterOptionalString.write(value.`releaseVersion`, buf)
            FfiConverterOptionalString.write(value.`daemonVersion`, buf)
            FfiConverterOptionalString.write(value.`imageVersion`, buf)
    }
}



data class OtaCompositeVersion (
    var `daemon`: kotlin.String
    , 
    var `image`: kotlin.String
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeOtaCompositeVersion: FfiConverterRustBuffer<OtaCompositeVersion> {
    override fun read(buf: ByteBuffer): OtaCompositeVersion {
        return OtaCompositeVersion(
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
        )
    }

    override fun allocationSize(value: OtaCompositeVersion) = (
            FfiConverterString.allocationSize(value.`daemon`) +
            FfiConverterString.allocationSize(value.`image`)
    )

    override fun write(value: OtaCompositeVersion, buf: ByteBuffer) {
            FfiConverterString.write(value.`daemon`, buf)
            FfiConverterString.write(value.`image`, buf)
    }
}



data class OtaDiscoverManifest (
    var `manifestVersion`: kotlin.UInt
    , 
    var `updatedAt`: kotlin.String
    , 
    var `channels`: Map<kotlin.String, OtaManifestChannel>
    , 
    var `releases`: Map<kotlin.String, OtaManifestRelease>
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeOtaDiscoverManifest: FfiConverterRustBuffer<OtaDiscoverManifest> {
    override fun read(buf: ByteBuffer): OtaDiscoverManifest {
        return OtaDiscoverManifest(
            FfiConverterUInt.read(buf),
            FfiConverterString.read(buf),
            FfiConverterMapStringTypeOtaManifestChannel.read(buf),
            FfiConverterMapStringTypeOtaManifestRelease.read(buf),
        )
    }

    override fun allocationSize(value: OtaDiscoverManifest) = (
            FfiConverterUInt.allocationSize(value.`manifestVersion`) +
            FfiConverterString.allocationSize(value.`updatedAt`) +
            FfiConverterMapStringTypeOtaManifestChannel.allocationSize(value.`channels`) +
            FfiConverterMapStringTypeOtaManifestRelease.allocationSize(value.`releases`)
    )

    override fun write(value: OtaDiscoverManifest, buf: ByteBuffer) {
            FfiConverterUInt.write(value.`manifestVersion`, buf)
            FfiConverterString.write(value.`updatedAt`, buf)
            FfiConverterMapStringTypeOtaManifestChannel.write(value.`channels`, buf)
            FfiConverterMapStringTypeOtaManifestRelease.write(value.`releases`, buf)
    }
}



data class OtaManifestChannel (
    var `name`: kotlin.String
    , 
    var `stability`: kotlin.String
    , 
    var `isDefault`: kotlin.Boolean
    , 
    var `latest`: kotlin.String
    , 
    var `releases`: List<kotlin.String>
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeOtaManifestChannel: FfiConverterRustBuffer<OtaManifestChannel> {
    override fun read(buf: ByteBuffer): OtaManifestChannel {
        return OtaManifestChannel(
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterBoolean.read(buf),
            FfiConverterString.read(buf),
            FfiConverterSequenceString.read(buf),
        )
    }

    override fun allocationSize(value: OtaManifestChannel) = (
            FfiConverterString.allocationSize(value.`name`) +
            FfiConverterString.allocationSize(value.`stability`) +
            FfiConverterBoolean.allocationSize(value.`isDefault`) +
            FfiConverterString.allocationSize(value.`latest`) +
            FfiConverterSequenceString.allocationSize(value.`releases`)
    )

    override fun write(value: OtaManifestChannel, buf: ByteBuffer) {
            FfiConverterString.write(value.`name`, buf)
            FfiConverterString.write(value.`stability`, buf)
            FfiConverterBoolean.write(value.`isDefault`, buf)
            FfiConverterString.write(value.`latest`, buf)
            FfiConverterSequenceString.write(value.`releases`, buf)
    }
}



data class OtaManifestRelease (
    var `version`: kotlin.String
    , 
    var `channel`: kotlin.String
    , 
    var `yanked`: kotlin.String?
    , 
    var `deprecated`: kotlin.Boolean
    , 
    var `builtinWebapps`: Map<kotlin.String, kotlin.String>
    , 
    var `artifacts`: OtaReleaseArtifacts?
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeOtaManifestRelease: FfiConverterRustBuffer<OtaManifestRelease> {
    override fun read(buf: ByteBuffer): OtaManifestRelease {
        return OtaManifestRelease(
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterBoolean.read(buf),
            FfiConverterMapStringString.read(buf),
            FfiConverterOptionalTypeOtaReleaseArtifacts.read(buf),
        )
    }

    override fun allocationSize(value: OtaManifestRelease) = (
            FfiConverterString.allocationSize(value.`version`) +
            FfiConverterString.allocationSize(value.`channel`) +
            FfiConverterOptionalString.allocationSize(value.`yanked`) +
            FfiConverterBoolean.allocationSize(value.`deprecated`) +
            FfiConverterMapStringString.allocationSize(value.`builtinWebapps`) +
            FfiConverterOptionalTypeOtaReleaseArtifacts.allocationSize(value.`artifacts`)
    )

    override fun write(value: OtaManifestRelease, buf: ByteBuffer) {
            FfiConverterString.write(value.`version`, buf)
            FfiConverterString.write(value.`channel`, buf)
            FfiConverterOptionalString.write(value.`yanked`, buf)
            FfiConverterBoolean.write(value.`deprecated`, buf)
            FfiConverterMapStringString.write(value.`builtinWebapps`, buf)
            FfiConverterOptionalTypeOtaReleaseArtifacts.write(value.`artifacts`, buf)
    }
}



data class OtaPatchDigest (
    var `size`: kotlin.ULong
    , 
    var `sha256`: kotlin.String
    , 
    var `sourceSha256`: kotlin.String?
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeOtaPatchDigest: FfiConverterRustBuffer<OtaPatchDigest> {
    override fun read(buf: ByteBuffer): OtaPatchDigest {
        return OtaPatchDigest(
            FfiConverterULong.read(buf),
            FfiConverterString.read(buf),
            FfiConverterOptionalString.read(buf),
        )
    }

    override fun allocationSize(value: OtaPatchDigest) = (
            FfiConverterULong.allocationSize(value.`size`) +
            FfiConverterString.allocationSize(value.`sha256`) +
            FfiConverterOptionalString.allocationSize(value.`sourceSha256`)
    )

    override fun write(value: OtaPatchDigest, buf: ByteBuffer) {
            FfiConverterULong.write(value.`size`, buf)
            FfiConverterString.write(value.`sha256`, buf)
            FfiConverterOptionalString.write(value.`sourceSha256`, buf)
    }
}



data class OtaPlanStep (
    var `id`: kotlin.UInt
    , 
    var `kind`: OtaStepKind
    , 
    var `label`: kotlin.String
    , 
    var `bytes`: kotlin.ULong
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeOtaPlanStep: FfiConverterRustBuffer<OtaPlanStep> {
    override fun read(buf: ByteBuffer): OtaPlanStep {
        return OtaPlanStep(
            FfiConverterUInt.read(buf),
            FfiConverterTypeOtaStepKind.read(buf),
            FfiConverterString.read(buf),
            FfiConverterULong.read(buf),
        )
    }

    override fun allocationSize(value: OtaPlanStep) = (
            FfiConverterUInt.allocationSize(value.`id`) +
            FfiConverterTypeOtaStepKind.allocationSize(value.`kind`) +
            FfiConverterString.allocationSize(value.`label`) +
            FfiConverterULong.allocationSize(value.`bytes`)
    )

    override fun write(value: OtaPlanStep, buf: ByteBuffer) {
            FfiConverterUInt.write(value.`id`, buf)
            FfiConverterTypeOtaStepKind.write(value.`kind`, buf)
            FfiConverterString.write(value.`label`, buf)
            FfiConverterULong.write(value.`bytes`, buf)
    }
}



data class OtaPollConfig (
    var `intervalSeconds`: kotlin.ULong
    , 
    var `autoPush`: kotlin.Boolean
    , 
    var `rootUrl`: kotlin.String?
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeOtaPollConfig: FfiConverterRustBuffer<OtaPollConfig> {
    override fun read(buf: ByteBuffer): OtaPollConfig {
        return OtaPollConfig(
            FfiConverterULong.read(buf),
            FfiConverterBoolean.read(buf),
            FfiConverterOptionalString.read(buf),
        )
    }

    override fun allocationSize(value: OtaPollConfig) = (
            FfiConverterULong.allocationSize(value.`intervalSeconds`) +
            FfiConverterBoolean.allocationSize(value.`autoPush`) +
            FfiConverterOptionalString.allocationSize(value.`rootUrl`)
    )

    override fun write(value: OtaPollConfig, buf: ByteBuffer) {
            FfiConverterULong.write(value.`intervalSeconds`, buf)
            FfiConverterBoolean.write(value.`autoPush`, buf)
            FfiConverterOptionalString.write(value.`rootUrl`, buf)
    }
}



data class OtaPollStatus (
    var `lastPolledAt`: kotlin.String?
    , 
    var `error`: kotlin.String?
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeOtaPollStatus: FfiConverterRustBuffer<OtaPollStatus> {
    override fun read(buf: ByteBuffer): OtaPollStatus {
        return OtaPollStatus(
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
        )
    }

    override fun allocationSize(value: OtaPollStatus) = (
            FfiConverterOptionalString.allocationSize(value.`lastPolledAt`) +
            FfiConverterOptionalString.allocationSize(value.`error`)
    )

    override fun write(value: OtaPollStatus, buf: ByteBuffer) {
            FfiConverterOptionalString.write(value.`lastPolledAt`, buf)
            FfiConverterOptionalString.write(value.`error`, buf)
    }
}



data class OtaReleaseArtifacts (
    var `daemon`: ArtifactDigest?
    , 
    var `daemonZst`: ArtifactDigest?
    , 
    var `imageSwu`: ArtifactDigest?
    , 
    var `imageZck`: ArtifactDigest?
    , 
    var `imageBootZck`: ArtifactDigest?
    , 
    var `webapps`: Map<kotlin.String, ArtifactDigest>
    , 
    var `daemonPatches`: Map<kotlin.String, OtaPatchDigest>
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeOtaReleaseArtifacts: FfiConverterRustBuffer<OtaReleaseArtifacts> {
    override fun read(buf: ByteBuffer): OtaReleaseArtifacts {
        return OtaReleaseArtifacts(
            FfiConverterOptionalTypeArtifactDigest.read(buf),
            FfiConverterOptionalTypeArtifactDigest.read(buf),
            FfiConverterOptionalTypeArtifactDigest.read(buf),
            FfiConverterOptionalTypeArtifactDigest.read(buf),
            FfiConverterOptionalTypeArtifactDigest.read(buf),
            FfiConverterMapStringTypeArtifactDigest.read(buf),
            FfiConverterMapStringTypeOtaPatchDigest.read(buf),
        )
    }

    override fun allocationSize(value: OtaReleaseArtifacts) = (
            FfiConverterOptionalTypeArtifactDigest.allocationSize(value.`daemon`) +
            FfiConverterOptionalTypeArtifactDigest.allocationSize(value.`daemonZst`) +
            FfiConverterOptionalTypeArtifactDigest.allocationSize(value.`imageSwu`) +
            FfiConverterOptionalTypeArtifactDigest.allocationSize(value.`imageZck`) +
            FfiConverterOptionalTypeArtifactDigest.allocationSize(value.`imageBootZck`) +
            FfiConverterMapStringTypeArtifactDigest.allocationSize(value.`webapps`) +
            FfiConverterMapStringTypeOtaPatchDigest.allocationSize(value.`daemonPatches`)
    )

    override fun write(value: OtaReleaseArtifacts, buf: ByteBuffer) {
            FfiConverterOptionalTypeArtifactDigest.write(value.`daemon`, buf)
            FfiConverterOptionalTypeArtifactDigest.write(value.`daemonZst`, buf)
            FfiConverterOptionalTypeArtifactDigest.write(value.`imageSwu`, buf)
            FfiConverterOptionalTypeArtifactDigest.write(value.`imageZck`, buf)
            FfiConverterOptionalTypeArtifactDigest.write(value.`imageBootZck`, buf)
            FfiConverterMapStringTypeArtifactDigest.write(value.`webapps`, buf)
            FfiConverterMapStringTypeOtaPatchDigest.write(value.`daemonPatches`, buf)
    }
}



data class OtaRun (
    var `runId`: kotlin.String
    , 
    var `deviceId`: kotlin.String
    , 
    var `kind`: OtaKind
    , 
    var `phase`: OtaRunPhase
    , 
    var `steps`: List<OtaPlanStep>
    , 
    var `stepId`: kotlin.UInt
    , 
    var `startedAtMs`: kotlin.ULong
    , 
    var `phaseStartedAtMs`: kotlin.ULong
    , 
    var `stageReceived`: kotlin.ULong?
    , 
    var `stageTotal`: kotlin.ULong?
    , 
    var `ratePerSec`: kotlin.Double?
    , 
    var `dwlPercent`: kotlin.UInt?
    , 
    var `outcome`: OtaRunOutcome?
    , 
    var `error`: kotlin.String?
    , 
    var `releaseVersion`: kotlin.String?
    , 
    var `daemonVersion`: kotlin.String?
    , 
    var `imageVersion`: kotlin.String?
    , 
    var `channel`: kotlin.String?
    , 
    var `rootUrl`: kotlin.String?
    , 
    var `resumable`: kotlin.Boolean
    , 
    var `webappId`: kotlin.String?
    , 
    var `webappName`: kotlin.String?
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeOtaRun: FfiConverterRustBuffer<OtaRun> {
    override fun read(buf: ByteBuffer): OtaRun {
        return OtaRun(
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterTypeOtaKind.read(buf),
            FfiConverterTypeOtaRunPhase.read(buf),
            FfiConverterSequenceTypeOtaPlanStep.read(buf),
            FfiConverterUInt.read(buf),
            FfiConverterULong.read(buf),
            FfiConverterULong.read(buf),
            FfiConverterOptionalULong.read(buf),
            FfiConverterOptionalULong.read(buf),
            FfiConverterOptionalDouble.read(buf),
            FfiConverterOptionalUInt.read(buf),
            FfiConverterOptionalTypeOtaRunOutcome.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterBoolean.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
        )
    }

    override fun allocationSize(value: OtaRun) = (
            FfiConverterString.allocationSize(value.`runId`) +
            FfiConverterString.allocationSize(value.`deviceId`) +
            FfiConverterTypeOtaKind.allocationSize(value.`kind`) +
            FfiConverterTypeOtaRunPhase.allocationSize(value.`phase`) +
            FfiConverterSequenceTypeOtaPlanStep.allocationSize(value.`steps`) +
            FfiConverterUInt.allocationSize(value.`stepId`) +
            FfiConverterULong.allocationSize(value.`startedAtMs`) +
            FfiConverterULong.allocationSize(value.`phaseStartedAtMs`) +
            FfiConverterOptionalULong.allocationSize(value.`stageReceived`) +
            FfiConverterOptionalULong.allocationSize(value.`stageTotal`) +
            FfiConverterOptionalDouble.allocationSize(value.`ratePerSec`) +
            FfiConverterOptionalUInt.allocationSize(value.`dwlPercent`) +
            FfiConverterOptionalTypeOtaRunOutcome.allocationSize(value.`outcome`) +
            FfiConverterOptionalString.allocationSize(value.`error`) +
            FfiConverterOptionalString.allocationSize(value.`releaseVersion`) +
            FfiConverterOptionalString.allocationSize(value.`daemonVersion`) +
            FfiConverterOptionalString.allocationSize(value.`imageVersion`) +
            FfiConverterOptionalString.allocationSize(value.`channel`) +
            FfiConverterOptionalString.allocationSize(value.`rootUrl`) +
            FfiConverterBoolean.allocationSize(value.`resumable`) +
            FfiConverterOptionalString.allocationSize(value.`webappId`) +
            FfiConverterOptionalString.allocationSize(value.`webappName`)
    )

    override fun write(value: OtaRun, buf: ByteBuffer) {
            FfiConverterString.write(value.`runId`, buf)
            FfiConverterString.write(value.`deviceId`, buf)
            FfiConverterTypeOtaKind.write(value.`kind`, buf)
            FfiConverterTypeOtaRunPhase.write(value.`phase`, buf)
            FfiConverterSequenceTypeOtaPlanStep.write(value.`steps`, buf)
            FfiConverterUInt.write(value.`stepId`, buf)
            FfiConverterULong.write(value.`startedAtMs`, buf)
            FfiConverterULong.write(value.`phaseStartedAtMs`, buf)
            FfiConverterOptionalULong.write(value.`stageReceived`, buf)
            FfiConverterOptionalULong.write(value.`stageTotal`, buf)
            FfiConverterOptionalDouble.write(value.`ratePerSec`, buf)
            FfiConverterOptionalUInt.write(value.`dwlPercent`, buf)
            FfiConverterOptionalTypeOtaRunOutcome.write(value.`outcome`, buf)
            FfiConverterOptionalString.write(value.`error`, buf)
            FfiConverterOptionalString.write(value.`releaseVersion`, buf)
            FfiConverterOptionalString.write(value.`daemonVersion`, buf)
            FfiConverterOptionalString.write(value.`imageVersion`, buf)
            FfiConverterOptionalString.write(value.`channel`, buf)
            FfiConverterOptionalString.write(value.`rootUrl`, buf)
            FfiConverterBoolean.write(value.`resumable`, buf)
            FfiConverterOptionalString.write(value.`webappId`, buf)
            FfiConverterOptionalString.write(value.`webappName`, buf)
    }
}



data class OtaRunProgress (
    var `percent`: kotlin.UInt
    , 
    var `stepIndex`: kotlin.UInt
    , 
    var `stepCount`: kotlin.UInt
    , 
    var `stepLabel`: kotlin.String?
    , 
    var `etaSeconds`: kotlin.ULong?
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeOtaRunProgress: FfiConverterRustBuffer<OtaRunProgress> {
    override fun read(buf: ByteBuffer): OtaRunProgress {
        return OtaRunProgress(
            FfiConverterUInt.read(buf),
            FfiConverterUInt.read(buf),
            FfiConverterUInt.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalULong.read(buf),
        )
    }

    override fun allocationSize(value: OtaRunProgress) = (
            FfiConverterUInt.allocationSize(value.`percent`) +
            FfiConverterUInt.allocationSize(value.`stepIndex`) +
            FfiConverterUInt.allocationSize(value.`stepCount`) +
            FfiConverterOptionalString.allocationSize(value.`stepLabel`) +
            FfiConverterOptionalULong.allocationSize(value.`etaSeconds`)
    )

    override fun write(value: OtaRunProgress, buf: ByteBuffer) {
            FfiConverterUInt.write(value.`percent`, buf)
            FfiConverterUInt.write(value.`stepIndex`, buf)
            FfiConverterUInt.write(value.`stepCount`, buf)
            FfiConverterOptionalString.write(value.`stepLabel`, buf)
            FfiConverterOptionalULong.write(value.`etaSeconds`, buf)
    }
}



data class PhoneCall (
    var `callId`: kotlin.String
    , 
    var `remoteId`: kotlin.String
    , 
    var `displayName`: kotlin.String
    , 
    var `status`: PhoneCallStatus
    , 
    var `direction`: PhoneCallDirection
    , 
    var `startedAtUnixS`: kotlin.UInt?
    , 
    var `label`: kotlin.String?
    , 
    var `addressBookId`: kotlin.String?
    , 
    var `service`: PhoneCallService?
    , 
    var `isConferenced`: kotlin.Boolean?
    , 
    var `conferenceGroup`: kotlin.UByte?
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypePhoneCall: FfiConverterRustBuffer<PhoneCall> {
    override fun read(buf: ByteBuffer): PhoneCall {
        return PhoneCall(
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterTypePhoneCallStatus.read(buf),
            FfiConverterTypePhoneCallDirection.read(buf),
            FfiConverterOptionalUInt.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalTypePhoneCallService.read(buf),
            FfiConverterOptionalBoolean.read(buf),
            FfiConverterOptionalUByte.read(buf),
        )
    }

    override fun allocationSize(value: PhoneCall) = (
            FfiConverterString.allocationSize(value.`callId`) +
            FfiConverterString.allocationSize(value.`remoteId`) +
            FfiConverterString.allocationSize(value.`displayName`) +
            FfiConverterTypePhoneCallStatus.allocationSize(value.`status`) +
            FfiConverterTypePhoneCallDirection.allocationSize(value.`direction`) +
            FfiConverterOptionalUInt.allocationSize(value.`startedAtUnixS`) +
            FfiConverterOptionalString.allocationSize(value.`label`) +
            FfiConverterOptionalString.allocationSize(value.`addressBookId`) +
            FfiConverterOptionalTypePhoneCallService.allocationSize(value.`service`) +
            FfiConverterOptionalBoolean.allocationSize(value.`isConferenced`) +
            FfiConverterOptionalUByte.allocationSize(value.`conferenceGroup`)
    )

    override fun write(value: PhoneCall, buf: ByteBuffer) {
            FfiConverterString.write(value.`callId`, buf)
            FfiConverterString.write(value.`remoteId`, buf)
            FfiConverterString.write(value.`displayName`, buf)
            FfiConverterTypePhoneCallStatus.write(value.`status`, buf)
            FfiConverterTypePhoneCallDirection.write(value.`direction`, buf)
            FfiConverterOptionalUInt.write(value.`startedAtUnixS`, buf)
            FfiConverterOptionalString.write(value.`label`, buf)
            FfiConverterOptionalString.write(value.`addressBookId`, buf)
            FfiConverterOptionalTypePhoneCallService.write(value.`service`, buf)
            FfiConverterOptionalBoolean.write(value.`isConferenced`, buf)
            FfiConverterOptionalUByte.write(value.`conferenceGroup`, buf)
    }
}



data class PhoneCallEnded (
    var `callId`: kotlin.String
    , 
    var `reason`: CallEndReason
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypePhoneCallEnded: FfiConverterRustBuffer<PhoneCallEnded> {
    override fun read(buf: ByteBuffer): PhoneCallEnded {
        return PhoneCallEnded(
            FfiConverterString.read(buf),
            FfiConverterTypeCallEndReason.read(buf),
        )
    }

    override fun allocationSize(value: PhoneCallEnded) = (
            FfiConverterString.allocationSize(value.`callId`) +
            FfiConverterTypeCallEndReason.allocationSize(value.`reason`)
    )

    override fun write(value: PhoneCallEnded, buf: ByteBuffer) {
            FfiConverterString.write(value.`callId`, buf)
            FfiConverterTypeCallEndReason.write(value.`reason`, buf)
    }
}



data class PhoneInitiate (
    var `kind`: InitiateCallType
    , 
    var `destinationId`: kotlin.String?
    , 
    var `service`: PhoneCallService?
    , 
    var `addressBookId`: kotlin.String?
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypePhoneInitiate: FfiConverterRustBuffer<PhoneInitiate> {
    override fun read(buf: ByteBuffer): PhoneInitiate {
        return PhoneInitiate(
            FfiConverterTypeInitiateCallType.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalTypePhoneCallService.read(buf),
            FfiConverterOptionalString.read(buf),
        )
    }

    override fun allocationSize(value: PhoneInitiate) = (
            FfiConverterTypeInitiateCallType.allocationSize(value.`kind`) +
            FfiConverterOptionalString.allocationSize(value.`destinationId`) +
            FfiConverterOptionalTypePhoneCallService.allocationSize(value.`service`) +
            FfiConverterOptionalString.allocationSize(value.`addressBookId`)
    )

    override fun write(value: PhoneInitiate, buf: ByteBuffer) {
            FfiConverterTypeInitiateCallType.write(value.`kind`, buf)
            FfiConverterOptionalString.write(value.`destinationId`, buf)
            FfiConverterOptionalTypePhoneCallService.write(value.`service`, buf)
            FfiConverterOptionalString.write(value.`addressBookId`, buf)
    }
}



data class PhoneState (
    var `activeCalls`: List<PhoneCall>
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypePhoneState: FfiConverterRustBuffer<PhoneState> {
    override fun read(buf: ByteBuffer): PhoneState {
        return PhoneState(
            FfiConverterSequenceTypePhoneCall.read(buf),
        )
    }

    override fun allocationSize(value: PhoneState) = (
            FfiConverterSequenceTypePhoneCall.allocationSize(value.`activeCalls`)
    )

    override fun write(value: PhoneState, buf: ByteBuffer) {
            FfiConverterSequenceTypePhoneCall.write(value.`activeCalls`, buf)
    }
}



data class Position (
    var `lat`: kotlin.Double
    , 
    var `lon`: kotlin.Double
    , 
    var `altM`: kotlin.Float?
    , 
    var `accuracyM`: kotlin.Float
    , 
    var `speedMps`: kotlin.Float?
    , 
    var `headingDeg`: kotlin.Float?
    , 
    var `tsUnixS`: kotlin.UInt
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypePosition: FfiConverterRustBuffer<Position> {
    override fun read(buf: ByteBuffer): Position {
        return Position(
            FfiConverterDouble.read(buf),
            FfiConverterDouble.read(buf),
            FfiConverterOptionalFloat.read(buf),
            FfiConverterFloat.read(buf),
            FfiConverterOptionalFloat.read(buf),
            FfiConverterOptionalFloat.read(buf),
            FfiConverterUInt.read(buf),
        )
    }

    override fun allocationSize(value: Position) = (
            FfiConverterDouble.allocationSize(value.`lat`) +
            FfiConverterDouble.allocationSize(value.`lon`) +
            FfiConverterOptionalFloat.allocationSize(value.`altM`) +
            FfiConverterFloat.allocationSize(value.`accuracyM`) +
            FfiConverterOptionalFloat.allocationSize(value.`speedMps`) +
            FfiConverterOptionalFloat.allocationSize(value.`headingDeg`) +
            FfiConverterUInt.allocationSize(value.`tsUnixS`)
    )

    override fun write(value: Position, buf: ByteBuffer) {
            FfiConverterDouble.write(value.`lat`, buf)
            FfiConverterDouble.write(value.`lon`, buf)
            FfiConverterOptionalFloat.write(value.`altM`, buf)
            FfiConverterFloat.write(value.`accuracyM`, buf)
            FfiConverterOptionalFloat.write(value.`speedMps`, buf)
            FfiConverterOptionalFloat.write(value.`headingDeg`, buf)
            FfiConverterUInt.write(value.`tsUnixS`, buf)
    }
}



data class ProviderInfo (
    var `id`: kotlin.String
    , 
    var `displayName`: kotlin.String
    , 
    var `available`: kotlin.Boolean
    , 
    var `connected`: kotlin.Boolean
    , 
    var `authState`: AuthState
    , 
    var `serviceHealth`: ServiceHealth
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeProviderInfo: FfiConverterRustBuffer<ProviderInfo> {
    override fun read(buf: ByteBuffer): ProviderInfo {
        return ProviderInfo(
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterBoolean.read(buf),
            FfiConverterBoolean.read(buf),
            FfiConverterTypeAuthState.read(buf),
            FfiConverterTypeServiceHealth.read(buf),
        )
    }

    override fun allocationSize(value: ProviderInfo) = (
            FfiConverterString.allocationSize(value.`id`) +
            FfiConverterString.allocationSize(value.`displayName`) +
            FfiConverterBoolean.allocationSize(value.`available`) +
            FfiConverterBoolean.allocationSize(value.`connected`) +
            FfiConverterTypeAuthState.allocationSize(value.`authState`) +
            FfiConverterTypeServiceHealth.allocationSize(value.`serviceHealth`)
    )

    override fun write(value: ProviderInfo, buf: ByteBuffer) {
            FfiConverterString.write(value.`id`, buf)
            FfiConverterString.write(value.`displayName`, buf)
            FfiConverterBoolean.write(value.`available`, buf)
            FfiConverterBoolean.write(value.`connected`, buf)
            FfiConverterTypeAuthState.write(value.`authState`, buf)
            FfiConverterTypeServiceHealth.write(value.`serviceHealth`, buf)
    }
}



data class ProviderTokens (
    var `accessToken`: kotlin.String
    , 
    var `refreshToken`: kotlin.String
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeProviderTokens: FfiConverterRustBuffer<ProviderTokens> {
    override fun read(buf: ByteBuffer): ProviderTokens {
        return ProviderTokens(
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
        )
    }

    override fun allocationSize(value: ProviderTokens) = (
            FfiConverterString.allocationSize(value.`accessToken`) +
            FfiConverterString.allocationSize(value.`refreshToken`)
    )

    override fun write(value: ProviderTokens, buf: ByteBuffer) {
            FfiConverterString.write(value.`accessToken`, buf)
            FfiConverterString.write(value.`refreshToken`, buf)
    }
}



data class ScreenshotFile (
    var `path`: kotlin.String
    , 
    var `byteSize`: kotlin.ULong
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeScreenshotFile: FfiConverterRustBuffer<ScreenshotFile> {
    override fun read(buf: ByteBuffer): ScreenshotFile {
        return ScreenshotFile(
            FfiConverterString.read(buf),
            FfiConverterULong.read(buf),
        )
    }

    override fun allocationSize(value: ScreenshotFile) = (
            FfiConverterString.allocationSize(value.`path`) +
            FfiConverterULong.allocationSize(value.`byteSize`)
    )

    override fun write(value: ScreenshotFile, buf: ByteBuffer) {
            FfiConverterString.write(value.`path`, buf)
            FfiConverterULong.write(value.`byteSize`, buf)
    }
}



data class ServiceHealth (
    var `kind`: ServiceHealthKind
    , 
    var `retryAfterSeconds`: kotlin.UInt?
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeServiceHealth: FfiConverterRustBuffer<ServiceHealth> {
    override fun read(buf: ByteBuffer): ServiceHealth {
        return ServiceHealth(
            FfiConverterTypeServiceHealthKind.read(buf),
            FfiConverterOptionalUInt.read(buf),
        )
    }

    override fun allocationSize(value: ServiceHealth) = (
            FfiConverterTypeServiceHealthKind.allocationSize(value.`kind`) +
            FfiConverterOptionalUInt.allocationSize(value.`retryAfterSeconds`)
    )

    override fun write(value: ServiceHealth, buf: ByteBuffer) {
            FfiConverterTypeServiceHealthKind.write(value.`kind`, buf)
            FfiConverterOptionalUInt.write(value.`retryAfterSeconds`, buf)
    }
}



data class SessionHostInfo (
    var `appName`: kotlin.String
    , 
    var `appVersion`: kotlin.String
    , 
    var `osName`: kotlin.String
    , 
    var `osVersion`: kotlin.String
    , 
    var `hostIdentifier`: kotlin.String
    , 
    var `libVersion`: kotlin.String
    , 
    var `libbridgethingVersion`: kotlin.String
    , 
    var `adapterVersion`: kotlin.String
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeSessionHostInfo: FfiConverterRustBuffer<SessionHostInfo> {
    override fun read(buf: ByteBuffer): SessionHostInfo {
        return SessionHostInfo(
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
        )
    }

    override fun allocationSize(value: SessionHostInfo) = (
            FfiConverterString.allocationSize(value.`appName`) +
            FfiConverterString.allocationSize(value.`appVersion`) +
            FfiConverterString.allocationSize(value.`osName`) +
            FfiConverterString.allocationSize(value.`osVersion`) +
            FfiConverterString.allocationSize(value.`hostIdentifier`) +
            FfiConverterString.allocationSize(value.`libVersion`) +
            FfiConverterString.allocationSize(value.`libbridgethingVersion`) +
            FfiConverterString.allocationSize(value.`adapterVersion`)
    )

    override fun write(value: SessionHostInfo, buf: ByteBuffer) {
            FfiConverterString.write(value.`appName`, buf)
            FfiConverterString.write(value.`appVersion`, buf)
            FfiConverterString.write(value.`osName`, buf)
            FfiConverterString.write(value.`osVersion`, buf)
            FfiConverterString.write(value.`hostIdentifier`, buf)
            FfiConverterString.write(value.`libVersion`, buf)
            FfiConverterString.write(value.`libbridgethingVersion`, buf)
            FfiConverterString.write(value.`adapterVersion`, buf)
    }
}



data class SessionPeer (
    var `id`: kotlin.String
    , 
    var `name`: kotlin.String
    , 
    var `status`: PeerLinkStatus
    , 
    var `linkError`: kotlin.String?
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeSessionPeer: FfiConverterRustBuffer<SessionPeer> {
    override fun read(buf: ByteBuffer): SessionPeer {
        return SessionPeer(
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterTypePeerLinkStatus.read(buf),
            FfiConverterOptionalString.read(buf),
        )
    }

    override fun allocationSize(value: SessionPeer) = (
            FfiConverterString.allocationSize(value.`id`) +
            FfiConverterString.allocationSize(value.`name`) +
            FfiConverterTypePeerLinkStatus.allocationSize(value.`status`) +
            FfiConverterOptionalString.allocationSize(value.`linkError`)
    )

    override fun write(value: SessionPeer, buf: ByteBuffer) {
            FfiConverterString.write(value.`id`, buf)
            FfiConverterString.write(value.`name`, buf)
            FfiConverterTypePeerLinkStatus.write(value.`status`, buf)
            FfiConverterOptionalString.write(value.`linkError`, buf)
    }
}



data class SessionSnapshot (
    var `hostInfo`: SessionHostInfo
    , 
    var `providers`: List<ProviderInfo>
    , 
    var `providerPriority`: List<kotlin.String>
    , 
    var `libraryProvider`: kotlin.String?
    , 
    var `peers`: List<SessionPeer>
    , 
    var `ancsAuthStatuses`: List<AncsAuthStatusEntry>
    , 
    var `nowPlaying`: NowPlaying?
    , 
    var `deviceMeta`: List<DeviceMetaEntry>
    , 
    var `capabilityFlags`: CapabilityFlags
    , 
    var `voiceModel`: VoiceModelState
    , 
    var `otaPollConfig`: OtaPollConfig?
    , 
    var `webapps`: List<DeviceWebappsEntry>
    , 
    var `otaRuns`: List<OtaRun>
    , 
    var `otaAvailable`: List<OtaAvailable>
    , 
    var `otaPoll`: OtaPollStatus
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeSessionSnapshot: FfiConverterRustBuffer<SessionSnapshot> {
    override fun read(buf: ByteBuffer): SessionSnapshot {
        return SessionSnapshot(
            FfiConverterTypeSessionHostInfo.read(buf),
            FfiConverterSequenceTypeProviderInfo.read(buf),
            FfiConverterSequenceString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterSequenceTypeSessionPeer.read(buf),
            FfiConverterSequenceTypeAncsAuthStatusEntry.read(buf),
            FfiConverterOptionalTypeNowPlaying.read(buf),
            FfiConverterSequenceTypeDeviceMetaEntry.read(buf),
            FfiConverterTypeCapabilityFlags.read(buf),
            FfiConverterTypeVoiceModelState.read(buf),
            FfiConverterOptionalTypeOtaPollConfig.read(buf),
            FfiConverterSequenceTypeDeviceWebappsEntry.read(buf),
            FfiConverterSequenceTypeOtaRun.read(buf),
            FfiConverterSequenceTypeOtaAvailable.read(buf),
            FfiConverterTypeOtaPollStatus.read(buf),
        )
    }

    override fun allocationSize(value: SessionSnapshot) = (
            FfiConverterTypeSessionHostInfo.allocationSize(value.`hostInfo`) +
            FfiConverterSequenceTypeProviderInfo.allocationSize(value.`providers`) +
            FfiConverterSequenceString.allocationSize(value.`providerPriority`) +
            FfiConverterOptionalString.allocationSize(value.`libraryProvider`) +
            FfiConverterSequenceTypeSessionPeer.allocationSize(value.`peers`) +
            FfiConverterSequenceTypeAncsAuthStatusEntry.allocationSize(value.`ancsAuthStatuses`) +
            FfiConverterOptionalTypeNowPlaying.allocationSize(value.`nowPlaying`) +
            FfiConverterSequenceTypeDeviceMetaEntry.allocationSize(value.`deviceMeta`) +
            FfiConverterTypeCapabilityFlags.allocationSize(value.`capabilityFlags`) +
            FfiConverterTypeVoiceModelState.allocationSize(value.`voiceModel`) +
            FfiConverterOptionalTypeOtaPollConfig.allocationSize(value.`otaPollConfig`) +
            FfiConverterSequenceTypeDeviceWebappsEntry.allocationSize(value.`webapps`) +
            FfiConverterSequenceTypeOtaRun.allocationSize(value.`otaRuns`) +
            FfiConverterSequenceTypeOtaAvailable.allocationSize(value.`otaAvailable`) +
            FfiConverterTypeOtaPollStatus.allocationSize(value.`otaPoll`)
    )

    override fun write(value: SessionSnapshot, buf: ByteBuffer) {
            FfiConverterTypeSessionHostInfo.write(value.`hostInfo`, buf)
            FfiConverterSequenceTypeProviderInfo.write(value.`providers`, buf)
            FfiConverterSequenceString.write(value.`providerPriority`, buf)
            FfiConverterOptionalString.write(value.`libraryProvider`, buf)
            FfiConverterSequenceTypeSessionPeer.write(value.`peers`, buf)
            FfiConverterSequenceTypeAncsAuthStatusEntry.write(value.`ancsAuthStatuses`, buf)
            FfiConverterOptionalTypeNowPlaying.write(value.`nowPlaying`, buf)
            FfiConverterSequenceTypeDeviceMetaEntry.write(value.`deviceMeta`, buf)
            FfiConverterTypeCapabilityFlags.write(value.`capabilityFlags`, buf)
            FfiConverterTypeVoiceModelState.write(value.`voiceModel`, buf)
            FfiConverterOptionalTypeOtaPollConfig.write(value.`otaPollConfig`, buf)
            FfiConverterSequenceTypeDeviceWebappsEntry.write(value.`webapps`, buf)
            FfiConverterSequenceTypeOtaRun.write(value.`otaRuns`, buf)
            FfiConverterSequenceTypeOtaAvailable.write(value.`otaAvailable`, buf)
            FfiConverterTypeOtaPollStatus.write(value.`otaPoll`, buf)
    }
}



data class SpeechSegment (
    var `text`: kotlin.String
    , 
    var `startMs`: kotlin.ULong
    , 
    var `endMs`: kotlin.ULong
    , 
    var `confidence`: kotlin.Float?
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeSpeechSegment: FfiConverterRustBuffer<SpeechSegment> {
    override fun read(buf: ByteBuffer): SpeechSegment {
        return SpeechSegment(
            FfiConverterString.read(buf),
            FfiConverterULong.read(buf),
            FfiConverterULong.read(buf),
            FfiConverterOptionalFloat.read(buf),
        )
    }

    override fun allocationSize(value: SpeechSegment) = (
            FfiConverterString.allocationSize(value.`text`) +
            FfiConverterULong.allocationSize(value.`startMs`) +
            FfiConverterULong.allocationSize(value.`endMs`) +
            FfiConverterOptionalFloat.allocationSize(value.`confidence`)
    )

    override fun write(value: SpeechSegment, buf: ByteBuffer) {
            FfiConverterString.write(value.`text`, buf)
            FfiConverterULong.write(value.`startMs`, buf)
            FfiConverterULong.write(value.`endMs`, buf)
            FfiConverterOptionalFloat.write(value.`confidence`, buf)
    }
}



data class SpotifyProviderConfig (
    var `workerBase`: kotlin.String
    , 
    var `psk`: kotlin.String
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeSpotifyProviderConfig: FfiConverterRustBuffer<SpotifyProviderConfig> {
    override fun read(buf: ByteBuffer): SpotifyProviderConfig {
        return SpotifyProviderConfig(
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
        )
    }

    override fun allocationSize(value: SpotifyProviderConfig) = (
            FfiConverterString.allocationSize(value.`workerBase`) +
            FfiConverterString.allocationSize(value.`psk`)
    )

    override fun write(value: SpotifyProviderConfig, buf: ByteBuffer) {
            FfiConverterString.write(value.`workerBase`, buf)
            FfiConverterString.write(value.`psk`, buf)
    }
}



data class Transcription (
    var `text`: kotlin.String
    , 
    var `alternatives`: List<kotlin.String>
    , 
    var `segments`: List<SpeechSegment>
    , 
    var `confidence`: kotlin.Float?
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeTranscription: FfiConverterRustBuffer<Transcription> {
    override fun read(buf: ByteBuffer): Transcription {
        return Transcription(
            FfiConverterString.read(buf),
            FfiConverterSequenceString.read(buf),
            FfiConverterSequenceTypeSpeechSegment.read(buf),
            FfiConverterOptionalFloat.read(buf),
        )
    }

    override fun allocationSize(value: Transcription) = (
            FfiConverterString.allocationSize(value.`text`) +
            FfiConverterSequenceString.allocationSize(value.`alternatives`) +
            FfiConverterSequenceTypeSpeechSegment.allocationSize(value.`segments`) +
            FfiConverterOptionalFloat.allocationSize(value.`confidence`)
    )

    override fun write(value: Transcription, buf: ByteBuffer) {
            FfiConverterString.write(value.`text`, buf)
            FfiConverterSequenceString.write(value.`alternatives`, buf)
            FfiConverterSequenceTypeSpeechSegment.write(value.`segments`, buf)
            FfiConverterOptionalFloat.write(value.`confidence`, buf)
    }
}



data class VoiceDebug (
    var `hasModel`: kotlin.Boolean
    , 
    var `armedBundle`: kotlin.String?
    , 
    var `transferAllowed`: kotlin.Boolean
    , 
    var `paths`: VoiceModelPaths
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeVoiceDebug: FfiConverterRustBuffer<VoiceDebug> {
    override fun read(buf: ByteBuffer): VoiceDebug {
        return VoiceDebug(
            FfiConverterBoolean.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterBoolean.read(buf),
            FfiConverterTypeVoiceModelPaths.read(buf),
        )
    }

    override fun allocationSize(value: VoiceDebug) = (
            FfiConverterBoolean.allocationSize(value.`hasModel`) +
            FfiConverterOptionalString.allocationSize(value.`armedBundle`) +
            FfiConverterBoolean.allocationSize(value.`transferAllowed`) +
            FfiConverterTypeVoiceModelPaths.allocationSize(value.`paths`)
    )

    override fun write(value: VoiceDebug, buf: ByteBuffer) {
            FfiConverterBoolean.write(value.`hasModel`, buf)
            FfiConverterOptionalString.write(value.`armedBundle`, buf)
            FfiConverterBoolean.write(value.`transferAllowed`, buf)
            FfiConverterTypeVoiceModelPaths.write(value.`paths`, buf)
    }
}



data class VoiceModelPaths (
    var `nluBundleDir`: kotlin.String?
    , 
    var `asrWeights`: kotlin.String?
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeVoiceModelPaths: FfiConverterRustBuffer<VoiceModelPaths> {
    override fun read(buf: ByteBuffer): VoiceModelPaths {
        return VoiceModelPaths(
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
        )
    }

    override fun allocationSize(value: VoiceModelPaths) = (
            FfiConverterOptionalString.allocationSize(value.`nluBundleDir`) +
            FfiConverterOptionalString.allocationSize(value.`asrWeights`)
    )

    override fun write(value: VoiceModelPaths, buf: ByteBuffer) {
            FfiConverterOptionalString.write(value.`nluBundleDir`, buf)
            FfiConverterOptionalString.write(value.`asrWeights`, buf)
    }
}



data class VoiceModelState (
    var `status`: VoiceModelStatus
    , 
    var `receivedBytes`: kotlin.ULong
    , 
    var `totalBytes`: kotlin.ULong
    , 
    var `version`: kotlin.String?
    , 
    var `error`: kotlin.String?
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeVoiceModelState: FfiConverterRustBuffer<VoiceModelState> {
    override fun read(buf: ByteBuffer): VoiceModelState {
        return VoiceModelState(
            FfiConverterTypeVoiceModelStatus.read(buf),
            FfiConverterULong.read(buf),
            FfiConverterULong.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
        )
    }

    override fun allocationSize(value: VoiceModelState) = (
            FfiConverterTypeVoiceModelStatus.allocationSize(value.`status`) +
            FfiConverterULong.allocationSize(value.`receivedBytes`) +
            FfiConverterULong.allocationSize(value.`totalBytes`) +
            FfiConverterOptionalString.allocationSize(value.`version`) +
            FfiConverterOptionalString.allocationSize(value.`error`)
    )

    override fun write(value: VoiceModelState, buf: ByteBuffer) {
            FfiConverterTypeVoiceModelStatus.write(value.`status`, buf)
            FfiConverterULong.write(value.`receivedBytes`, buf)
            FfiConverterULong.write(value.`totalBytes`, buf)
            FfiConverterOptionalString.write(value.`version`, buf)
            FfiConverterOptionalString.write(value.`error`, buf)
    }
}



data class VoiceTurn (
    var `deviceId`: kotlin.String
    , 
    var `streamId`: kotlin.String
    , 
    var `trigger`: VoiceTurnTrigger
    , 
    var `phase`: VoiceTurnPhase
    , 
    var `transcript`: kotlin.String?
    , 
    var `intent`: kotlin.String?
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeVoiceTurn: FfiConverterRustBuffer<VoiceTurn> {
    override fun read(buf: ByteBuffer): VoiceTurn {
        return VoiceTurn(
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterTypeVoiceTurnTrigger.read(buf),
            FfiConverterTypeVoiceTurnPhase.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
        )
    }

    override fun allocationSize(value: VoiceTurn) = (
            FfiConverterString.allocationSize(value.`deviceId`) +
            FfiConverterString.allocationSize(value.`streamId`) +
            FfiConverterTypeVoiceTurnTrigger.allocationSize(value.`trigger`) +
            FfiConverterTypeVoiceTurnPhase.allocationSize(value.`phase`) +
            FfiConverterOptionalString.allocationSize(value.`transcript`) +
            FfiConverterOptionalString.allocationSize(value.`intent`)
    )

    override fun write(value: VoiceTurn, buf: ByteBuffer) {
            FfiConverterString.write(value.`deviceId`, buf)
            FfiConverterString.write(value.`streamId`, buf)
            FfiConverterTypeVoiceTurnTrigger.write(value.`trigger`, buf)
            FfiConverterTypeVoiceTurnPhase.write(value.`phase`, buf)
            FfiConverterOptionalString.write(value.`transcript`, buf)
            FfiConverterOptionalString.write(value.`intent`, buf)
    }
}



data class VolumeLevel (
    var `level`: kotlin.Float
    , 
    var `muted`: kotlin.Boolean
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeVolumeLevel: FfiConverterRustBuffer<VolumeLevel> {
    override fun read(buf: ByteBuffer): VolumeLevel {
        return VolumeLevel(
            FfiConverterFloat.read(buf),
            FfiConverterBoolean.read(buf),
        )
    }

    override fun allocationSize(value: VolumeLevel) = (
            FfiConverterFloat.allocationSize(value.`level`) +
            FfiConverterBoolean.allocationSize(value.`muted`)
    )

    override fun write(value: VolumeLevel, buf: ByteBuffer) {
            FfiConverterFloat.write(value.`level`, buf)
            FfiConverterBoolean.write(value.`muted`, buf)
    }
}



data class WebappInfo (
    var `id`: kotlin.String
    , 
    var `name`: kotlin.String
    , 
    var `source`: WebappSource
    , 
    var `role`: WebappRole
    , 
    var `version`: kotlin.String
    , 
    var `provenance`: kotlin.String?
    , 
    var `description`: kotlin.String?
    , 
    var `iconHash`: kotlin.String?
    , 
    var `settingsHash`: kotlin.String?
    , 
    var `overlayHash`: kotlin.String?
    , 
    var `config`: List<ConfigField>
    , 
    var `permissions`: List<kotlin.String>
    , 
    var `extension`: ExtensionInfo?
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeWebappInfo: FfiConverterRustBuffer<WebappInfo> {
    override fun read(buf: ByteBuffer): WebappInfo {
        return WebappInfo(
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterTypeWebappSource.read(buf),
            FfiConverterTypeWebappRole.read(buf),
            FfiConverterString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterSequenceTypeConfigField.read(buf),
            FfiConverterSequenceString.read(buf),
            FfiConverterOptionalTypeExtensionInfo.read(buf),
        )
    }

    override fun allocationSize(value: WebappInfo) = (
            FfiConverterString.allocationSize(value.`id`) +
            FfiConverterString.allocationSize(value.`name`) +
            FfiConverterTypeWebappSource.allocationSize(value.`source`) +
            FfiConverterTypeWebappRole.allocationSize(value.`role`) +
            FfiConverterString.allocationSize(value.`version`) +
            FfiConverterOptionalString.allocationSize(value.`provenance`) +
            FfiConverterOptionalString.allocationSize(value.`description`) +
            FfiConverterOptionalString.allocationSize(value.`iconHash`) +
            FfiConverterOptionalString.allocationSize(value.`settingsHash`) +
            FfiConverterOptionalString.allocationSize(value.`overlayHash`) +
            FfiConverterSequenceTypeConfigField.allocationSize(value.`config`) +
            FfiConverterSequenceString.allocationSize(value.`permissions`) +
            FfiConverterOptionalTypeExtensionInfo.allocationSize(value.`extension`)
    )

    override fun write(value: WebappInfo, buf: ByteBuffer) {
            FfiConverterString.write(value.`id`, buf)
            FfiConverterString.write(value.`name`, buf)
            FfiConverterTypeWebappSource.write(value.`source`, buf)
            FfiConverterTypeWebappRole.write(value.`role`, buf)
            FfiConverterString.write(value.`version`, buf)
            FfiConverterOptionalString.write(value.`provenance`, buf)
            FfiConverterOptionalString.write(value.`description`, buf)
            FfiConverterOptionalString.write(value.`iconHash`, buf)
            FfiConverterOptionalString.write(value.`settingsHash`, buf)
            FfiConverterOptionalString.write(value.`overlayHash`, buf)
            FfiConverterSequenceTypeConfigField.write(value.`config`, buf)
            FfiConverterSequenceString.write(value.`permissions`, buf)
            FfiConverterOptionalTypeExtensionInfo.write(value.`extension`, buf)
    }
}



data class WebappResourceFile (
    var `path`: kotlin.String
    , 
    var `mime`: kotlin.String?
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeWebappResourceFile: FfiConverterRustBuffer<WebappResourceFile> {
    override fun read(buf: ByteBuffer): WebappResourceFile {
        return WebappResourceFile(
            FfiConverterString.read(buf),
            FfiConverterOptionalString.read(buf),
        )
    }

    override fun allocationSize(value: WebappResourceFile) = (
            FfiConverterString.allocationSize(value.`path`) +
            FfiConverterOptionalString.allocationSize(value.`mime`)
    )

    override fun write(value: WebappResourceFile, buf: ByteBuffer) {
            FfiConverterString.write(value.`path`, buf)
            FfiConverterOptionalString.write(value.`mime`, buf)
    }
}



data class WebappResourceOrigin (
    var `url`: kotlin.String
    , 
    var `sha256`: kotlin.String
    , 
    var `size`: kotlin.ULong
    , 
    var `mime`: kotlin.String?
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeWebappResourceOrigin: FfiConverterRustBuffer<WebappResourceOrigin> {
    override fun read(buf: ByteBuffer): WebappResourceOrigin {
        return WebappResourceOrigin(
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterULong.read(buf),
            FfiConverterOptionalString.read(buf),
        )
    }

    override fun allocationSize(value: WebappResourceOrigin) = (
            FfiConverterString.allocationSize(value.`url`) +
            FfiConverterString.allocationSize(value.`sha256`) +
            FfiConverterULong.allocationSize(value.`size`) +
            FfiConverterOptionalString.allocationSize(value.`mime`)
    )

    override fun write(value: WebappResourceOrigin, buf: ByteBuffer) {
            FfiConverterString.write(value.`url`, buf)
            FfiConverterString.write(value.`sha256`, buf)
            FfiConverterULong.write(value.`size`, buf)
            FfiConverterOptionalString.write(value.`mime`, buf)
    }
}



data class WebappSlots (
    var `launcher`: kotlin.String?
    , 
    var `overlay`: kotlin.String?
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeWebappSlots: FfiConverterRustBuffer<WebappSlots> {
    override fun read(buf: ByteBuffer): WebappSlots {
        return WebappSlots(
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
        )
    }

    override fun allocationSize(value: WebappSlots) = (
            FfiConverterOptionalString.allocationSize(value.`launcher`) +
            FfiConverterOptionalString.allocationSize(value.`overlay`)
    )

    override fun write(value: WebappSlots, buf: ByteBuffer) {
            FfiConverterOptionalString.write(value.`launcher`, buf)
            FfiConverterOptionalString.write(value.`overlay`, buf)
    }
}



data class WireNotification (
    var `id`: kotlin.String
    , 
    var `app`: NotificationApp
    , 
    var `category`: NotificationCategory
    , 
    var `title`: kotlin.String?
    , 
    var `subtitle`: kotlin.String?
    , 
    var `message`: kotlin.String?
    , 
    var `timestampUnixS`: kotlin.UInt?
    , 
    var `flags`: NotificationFlags
    , 
    var `positiveAction`: NotificationAction?
    , 
    var `negativeAction`: NotificationAction?
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeWireNotification: FfiConverterRustBuffer<WireNotification> {
    override fun read(buf: ByteBuffer): WireNotification {
        return WireNotification(
            FfiConverterString.read(buf),
            FfiConverterTypeNotificationApp.read(buf),
            FfiConverterTypeNotificationCategory.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalString.read(buf),
            FfiConverterOptionalUInt.read(buf),
            FfiConverterTypeNotificationFlags.read(buf),
            FfiConverterOptionalTypeNotificationAction.read(buf),
            FfiConverterOptionalTypeNotificationAction.read(buf),
        )
    }

    override fun allocationSize(value: WireNotification) = (
            FfiConverterString.allocationSize(value.`id`) +
            FfiConverterTypeNotificationApp.allocationSize(value.`app`) +
            FfiConverterTypeNotificationCategory.allocationSize(value.`category`) +
            FfiConverterOptionalString.allocationSize(value.`title`) +
            FfiConverterOptionalString.allocationSize(value.`subtitle`) +
            FfiConverterOptionalString.allocationSize(value.`message`) +
            FfiConverterOptionalUInt.allocationSize(value.`timestampUnixS`) +
            FfiConverterTypeNotificationFlags.allocationSize(value.`flags`) +
            FfiConverterOptionalTypeNotificationAction.allocationSize(value.`positiveAction`) +
            FfiConverterOptionalTypeNotificationAction.allocationSize(value.`negativeAction`)
    )

    override fun write(value: WireNotification, buf: ByteBuffer) {
            FfiConverterString.write(value.`id`, buf)
            FfiConverterTypeNotificationApp.write(value.`app`, buf)
            FfiConverterTypeNotificationCategory.write(value.`category`, buf)
            FfiConverterOptionalString.write(value.`title`, buf)
            FfiConverterOptionalString.write(value.`subtitle`, buf)
            FfiConverterOptionalString.write(value.`message`, buf)
            FfiConverterOptionalUInt.write(value.`timestampUnixS`, buf)
            FfiConverterTypeNotificationFlags.write(value.`flags`, buf)
            FfiConverterOptionalTypeNotificationAction.write(value.`positiveAction`, buf)
            FfiConverterOptionalTypeNotificationAction.write(value.`negativeAction`, buf)
    }
}



data class WsConnect (
    var `id`: kotlin.String
    , 
    var `url`: kotlin.String
    , 
    var `protocols`: List<kotlin.String>
    , 
    var `headers`: List<HttpHeader>
    
){
    

    

    
    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeWsConnect: FfiConverterRustBuffer<WsConnect> {
    override fun read(buf: ByteBuffer): WsConnect {
        return WsConnect(
            FfiConverterString.read(buf),
            FfiConverterString.read(buf),
            FfiConverterSequenceString.read(buf),
            FfiConverterSequenceTypeHttpHeader.read(buf),
        )
    }

    override fun allocationSize(value: WsConnect) = (
            FfiConverterString.allocationSize(value.`id`) +
            FfiConverterString.allocationSize(value.`url`) +
            FfiConverterSequenceString.allocationSize(value.`protocols`) +
            FfiConverterSequenceTypeHttpHeader.allocationSize(value.`headers`)
    )

    override fun write(value: WsConnect, buf: ByteBuffer) {
            FfiConverterString.write(value.`id`, buf)
            FfiConverterString.write(value.`url`, buf)
            FfiConverterSequenceString.write(value.`protocols`, buf)
            FfiConverterSequenceTypeHttpHeader.write(value.`headers`, buf)
    }
}




enum class AcceptCallAction {
    
    ACCEPT,
    END_AND_ACCEPT;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeAcceptCallAction: FfiConverterRustBuffer<AcceptCallAction> {
    override fun read(buf: ByteBuffer) = try {
        AcceptCallAction.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: AcceptCallAction) = 4UL

    override fun write(value: AcceptCallAction, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}






enum class AmAuthStatus {
    
    NOT_DETERMINED,
    AUTHORIZED,
    DENIED,
    RESTRICTED;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeAmAuthStatus: FfiConverterRustBuffer<AmAuthStatus> {
    override fun read(buf: ByteBuffer) = try {
        AmAuthStatus.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: AmAuthStatus) = 4UL

    override fun write(value: AmAuthStatus, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}






enum class AmKind {
    
    SONG,
    ALBUM,
    PLAYLIST,
    ARTIST,
    STATION;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeAmKind: FfiConverterRustBuffer<AmKind> {
    override fun read(buf: ByteBuffer) = try {
        AmKind.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: AmKind) = 4UL

    override fun write(value: AmKind, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}





sealed class AmLibraryScope {
    
    object Playlists : AmLibraryScope()
    
    
    object Albums : AmLibraryScope()
    
    
    object Artists : AmLibraryScope()
    
    
    object Songs : AmLibraryScope()
    
    
    object RecentlyPlayed : AmLibraryScope()
    
    
    data class Children(
        val `uri`: kotlin.String) : AmLibraryScope()
        
    {
        

        companion object
    }
    

    

    
    


    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeAmLibraryScope : FfiConverterRustBuffer<AmLibraryScope>{
    override fun read(buf: ByteBuffer): AmLibraryScope {
        return when(buf.getInt()) {
            1 -> AmLibraryScope.Playlists
            2 -> AmLibraryScope.Albums
            3 -> AmLibraryScope.Artists
            4 -> AmLibraryScope.Songs
            5 -> AmLibraryScope.RecentlyPlayed
            6 -> AmLibraryScope.Children(
                FfiConverterString.read(buf),
                )
            else -> throw RuntimeException("invalid enum value, something is very wrong!!")
        }
    }

    override fun allocationSize(value: AmLibraryScope): ULong = when(value) {
        is AmLibraryScope.Playlists -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
            )
        }
        is AmLibraryScope.Albums -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
            )
        }
        is AmLibraryScope.Artists -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
            )
        }
        is AmLibraryScope.Songs -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
            )
        }
        is AmLibraryScope.RecentlyPlayed -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
            )
        }
        is AmLibraryScope.Children -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterString.allocationSize(value.`uri`)
            )
        }
    }

    override fun write(value: AmLibraryScope, buf: ByteBuffer) {
        when(value) {
            is AmLibraryScope.Playlists -> {
                buf.putInt(1)
                Unit
            }
            is AmLibraryScope.Albums -> {
                buf.putInt(2)
                Unit
            }
            is AmLibraryScope.Artists -> {
                buf.putInt(3)
                Unit
            }
            is AmLibraryScope.Songs -> {
                buf.putInt(4)
                Unit
            }
            is AmLibraryScope.RecentlyPlayed -> {
                buf.putInt(5)
                Unit
            }
            is AmLibraryScope.Children -> {
                buf.putInt(6)
                FfiConverterString.write(value.`uri`, buf)
                Unit
            }
        }.let { /* this makes the `when` an expression, which ensures it is exhaustive */ }
    }
}





sealed class AmPlayerCommand {
    
    object Play : AmPlayerCommand()
    
    
    object Pause : AmPlayerCommand()
    
    
    object SkipNext : AmPlayerCommand()
    
    
    object SkipPrev : AmPlayerCommand()
    
    
    data class SeekTo(
        val `positionMs`: kotlin.UInt) : AmPlayerCommand()
        
    {
        

        companion object
    }
    
    data class SetShuffle(
        val `on`: kotlin.Boolean) : AmPlayerCommand()
        
    {
        

        companion object
    }
    
    data class SetRepeat(
        val `mode`: uniffi.bridgething_companion.AmRepeatMode) : AmPlayerCommand()
        
    {
        

        companion object
    }
    

    

    
    


    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeAmPlayerCommand : FfiConverterRustBuffer<AmPlayerCommand>{
    override fun read(buf: ByteBuffer): AmPlayerCommand {
        return when(buf.getInt()) {
            1 -> AmPlayerCommand.Play
            2 -> AmPlayerCommand.Pause
            3 -> AmPlayerCommand.SkipNext
            4 -> AmPlayerCommand.SkipPrev
            5 -> AmPlayerCommand.SeekTo(
                FfiConverterUInt.read(buf),
                )
            6 -> AmPlayerCommand.SetShuffle(
                FfiConverterBoolean.read(buf),
                )
            7 -> AmPlayerCommand.SetRepeat(
                FfiConverterTypeAmRepeatMode.read(buf),
                )
            else -> throw RuntimeException("invalid enum value, something is very wrong!!")
        }
    }

    override fun allocationSize(value: AmPlayerCommand): ULong = when(value) {
        is AmPlayerCommand.Play -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
            )
        }
        is AmPlayerCommand.Pause -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
            )
        }
        is AmPlayerCommand.SkipNext -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
            )
        }
        is AmPlayerCommand.SkipPrev -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
            )
        }
        is AmPlayerCommand.SeekTo -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterUInt.allocationSize(value.`positionMs`)
            )
        }
        is AmPlayerCommand.SetShuffle -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterBoolean.allocationSize(value.`on`)
            )
        }
        is AmPlayerCommand.SetRepeat -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterTypeAmRepeatMode.allocationSize(value.`mode`)
            )
        }
    }

    override fun write(value: AmPlayerCommand, buf: ByteBuffer) {
        when(value) {
            is AmPlayerCommand.Play -> {
                buf.putInt(1)
                Unit
            }
            is AmPlayerCommand.Pause -> {
                buf.putInt(2)
                Unit
            }
            is AmPlayerCommand.SkipNext -> {
                buf.putInt(3)
                Unit
            }
            is AmPlayerCommand.SkipPrev -> {
                buf.putInt(4)
                Unit
            }
            is AmPlayerCommand.SeekTo -> {
                buf.putInt(5)
                FfiConverterUInt.write(value.`positionMs`, buf)
                Unit
            }
            is AmPlayerCommand.SetShuffle -> {
                buf.putInt(6)
                FfiConverterBoolean.write(value.`on`, buf)
                Unit
            }
            is AmPlayerCommand.SetRepeat -> {
                buf.putInt(7)
                FfiConverterTypeAmRepeatMode.write(value.`mode`, buf)
                Unit
            }
        }.let { /* this makes the `when` an expression, which ensures it is exhaustive */ }
    }
}






enum class AmRepeatMode {
    
    OFF,
    ALL,
    ONE;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeAmRepeatMode: FfiConverterRustBuffer<AmRepeatMode> {
    override fun read(buf: ByteBuffer) = try {
        AmRepeatMode.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: AmRepeatMode) = 4UL

    override fun write(value: AmRepeatMode, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}






enum class AncsAuthStatus {
    
    UNKNOWN,
    PROBING,
    AUTHORIZED,
    UNAUTHORIZED;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeAncsAuthStatus: FfiConverterRustBuffer<AncsAuthStatus> {
    override fun read(buf: ByteBuffer) = try {
        AncsAuthStatus.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: AncsAuthStatus) = 4UL

    override fun write(value: AncsAuthStatus, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}






enum class AuthKind {
    
    IDLE,
    PENDING,
    AUTHENTICATED,
    FAILED;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeAuthKind: FfiConverterRustBuffer<AuthKind> {
    override fun read(buf: ByteBuffer) = try {
        AuthKind.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: AuthKind) = 4UL

    override fun write(value: AuthKind, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}





sealed class CallEndReason {
    
    object Local : CallEndReason()
    
    
    object Remote : CallEndReason()
    
    
    object Missed : CallEndReason()
    
    
    object Declined : CallEndReason()
    
    
    data class Failed(
        val `reason`: kotlin.String) : CallEndReason()
        
    {
        

        companion object
    }
    

    

    
    


    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeCallEndReason : FfiConverterRustBuffer<CallEndReason>{
    override fun read(buf: ByteBuffer): CallEndReason {
        return when(buf.getInt()) {
            1 -> CallEndReason.Local
            2 -> CallEndReason.Remote
            3 -> CallEndReason.Missed
            4 -> CallEndReason.Declined
            5 -> CallEndReason.Failed(
                FfiConverterString.read(buf),
                )
            else -> throw RuntimeException("invalid enum value, something is very wrong!!")
        }
    }

    override fun allocationSize(value: CallEndReason): ULong = when(value) {
        is CallEndReason.Local -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
            )
        }
        is CallEndReason.Remote -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
            )
        }
        is CallEndReason.Missed -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
            )
        }
        is CallEndReason.Declined -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
            )
        }
        is CallEndReason.Failed -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterString.allocationSize(value.`reason`)
            )
        }
    }

    override fun write(value: CallEndReason, buf: ByteBuffer) {
        when(value) {
            is CallEndReason.Local -> {
                buf.putInt(1)
                Unit
            }
            is CallEndReason.Remote -> {
                buf.putInt(2)
                Unit
            }
            is CallEndReason.Missed -> {
                buf.putInt(3)
                Unit
            }
            is CallEndReason.Declined -> {
                buf.putInt(4)
                Unit
            }
            is CallEndReason.Failed -> {
                buf.putInt(5)
                FfiConverterString.write(value.`reason`, buf)
                Unit
            }
        }.let { /* this makes the `when` an expression, which ensures it is exhaustive */ }
    }
}







sealed class CompanionException(message: String): kotlin.Exception(message) {
        
        class Cancelled(message: String) : CompanionException(message)
        
        class Runtime(message: String) : CompanionException(message)
        
        class NotConnected(message: String) : CompanionException(message)
        
        class Device(message: String) : CompanionException(message)
        
        class ResourceNotAvailable(message: String) : CompanionException(message)
        

    companion object ErrorHandler : UniffiRustCallStatusErrorHandler<CompanionException> {
        override fun lift(error_buf: RustBuffer.ByValue): CompanionException = FfiConverterTypeCompanionError.lift(error_buf)
    }
}

/**
 * @suppress
 */
public object FfiConverterTypeCompanionError : FfiConverterRustBuffer<CompanionException> {
    override fun read(buf: ByteBuffer): CompanionException {
        
            return when(buf.getInt()) {
            1 -> CompanionException.Cancelled(FfiConverterString.read(buf))
            2 -> CompanionException.Runtime(FfiConverterString.read(buf))
            3 -> CompanionException.NotConnected(FfiConverterString.read(buf))
            4 -> CompanionException.Device(FfiConverterString.read(buf))
            5 -> CompanionException.ResourceNotAvailable(FfiConverterString.read(buf))
            else -> throw RuntimeException("invalid error enum value, something is very wrong!!")
        }
        
    }

    override fun allocationSize(value: CompanionException): ULong {
        return 4UL
    }

    override fun write(value: CompanionException, buf: ByteBuffer) {
        when(value) {
            is CompanionException.Cancelled -> {
                buf.putInt(1)
                Unit
            }
            is CompanionException.Runtime -> {
                buf.putInt(2)
                Unit
            }
            is CompanionException.NotConnected -> {
                buf.putInt(3)
                Unit
            }
            is CompanionException.Device -> {
                buf.putInt(4)
                Unit
            }
            is CompanionException.ResourceNotAvailable -> {
                buf.putInt(5)
                Unit
            }
        }.let { /* this makes the `when` an expression, which ensures it is exhaustive */ }
    }

}




enum class ConfigKind {
    
    STRING,
    NUMBER,
    BOOLEAN,
    ENUM,
    SECRET;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeConfigKind: FfiConverterRustBuffer<ConfigKind> {
    override fun read(buf: ByteBuffer) = try {
        ConfigKind.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: ConfigKind) = 4UL

    override fun write(value: ConfigKind, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}






enum class DismissReason {
    
    USER_DISMISSED,
    ACTED,
    REMOTE_DISMISSED;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeDismissReason: FfiConverterRustBuffer<DismissReason> {
    override fun read(buf: ByteBuffer) = try {
        DismissReason.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: DismissReason) = 4UL

    override fun write(value: DismissReason, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}






enum class DtmfTone {
    
    D0,
    D1,
    D2,
    D3,
    D4,
    D5,
    D6,
    D7,
    D8,
    D9,
    STAR,
    HASH;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeDtmfTone: FfiConverterRustBuffer<DtmfTone> {
    override fun read(buf: ByteBuffer) = try {
        DtmfTone.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: DtmfTone) = 4UL

    override fun write(value: DtmfTone, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}






enum class EndCallAction {
    
    END,
    END_ALL;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeEndCallAction: FfiConverterRustBuffer<EndCallAction> {
    override fun read(buf: ByteBuffer) = try {
        EndCallAction.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: EndCallAction) = 4UL

    override fun write(value: EndCallAction, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}





sealed class ExtensionMessage {
    
    data class Text(
        val `text`: kotlin.String) : ExtensionMessage()
        
    {
        

        companion object
    }
    
    data class Json(
        val `json`: kotlin.String) : ExtensionMessage()
        
    {
        

        companion object
    }
    
    data class Binary(
        val `bytes`: kotlin.ByteArray) : ExtensionMessage()
        
    {
        

        companion object
    }
    

    

    
    


    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeExtensionMessage : FfiConverterRustBuffer<ExtensionMessage>{
    override fun read(buf: ByteBuffer): ExtensionMessage {
        return when(buf.getInt()) {
            1 -> ExtensionMessage.Text(
                FfiConverterString.read(buf),
                )
            2 -> ExtensionMessage.Json(
                FfiConverterString.read(buf),
                )
            3 -> ExtensionMessage.Binary(
                FfiConverterByteArray.read(buf),
                )
            else -> throw RuntimeException("invalid enum value, something is very wrong!!")
        }
    }

    override fun allocationSize(value: ExtensionMessage): ULong = when(value) {
        is ExtensionMessage.Text -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterString.allocationSize(value.`text`)
            )
        }
        is ExtensionMessage.Json -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterString.allocationSize(value.`json`)
            )
        }
        is ExtensionMessage.Binary -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterByteArray.allocationSize(value.`bytes`)
            )
        }
    }

    override fun write(value: ExtensionMessage, buf: ByteBuffer) {
        when(value) {
            is ExtensionMessage.Text -> {
                buf.putInt(1)
                FfiConverterString.write(value.`text`, buf)
                Unit
            }
            is ExtensionMessage.Json -> {
                buf.putInt(2)
                FfiConverterString.write(value.`json`, buf)
                Unit
            }
            is ExtensionMessage.Binary -> {
                buf.putInt(3)
                FfiConverterByteArray.write(value.`bytes`, buf)
                Unit
            }
        }.let { /* this makes the `when` an expression, which ensures it is exhaustive */ }
    }
}






enum class GeoAccuracy {
    
    COARSE,
    FINE;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeGeoAccuracy: FfiConverterRustBuffer<GeoAccuracy> {
    override fun read(buf: ByteBuffer) = try {
        GeoAccuracy.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: GeoAccuracy) = 4UL

    override fun write(value: GeoAccuracy, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}






enum class GeoError {
    
    PERMISSION_DENIED,
    NOT_DECLARED,
    UNAVAILABLE,
    UNKNOWN_TOKEN;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeGeoError: FfiConverterRustBuffer<GeoError> {
    override fun read(buf: ByteBuffer) = try {
        GeoError.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: GeoError) = 4UL

    override fun write(value: GeoError, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}





sealed class HttpMethod {
    
    object Get : HttpMethod()
    
    
    object Head : HttpMethod()
    
    
    object Post : HttpMethod()
    
    
    object Put : HttpMethod()
    
    
    object Patch : HttpMethod()
    
    
    object Delete : HttpMethod()
    
    
    object Options : HttpMethod()
    
    
    data class Other(
        val `verb`: kotlin.String) : HttpMethod()
        
    {
        

        companion object
    }
    

    

    
    


    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeHttpMethod : FfiConverterRustBuffer<HttpMethod>{
    override fun read(buf: ByteBuffer): HttpMethod {
        return when(buf.getInt()) {
            1 -> HttpMethod.Get
            2 -> HttpMethod.Head
            3 -> HttpMethod.Post
            4 -> HttpMethod.Put
            5 -> HttpMethod.Patch
            6 -> HttpMethod.Delete
            7 -> HttpMethod.Options
            8 -> HttpMethod.Other(
                FfiConverterString.read(buf),
                )
            else -> throw RuntimeException("invalid enum value, something is very wrong!!")
        }
    }

    override fun allocationSize(value: HttpMethod): ULong = when(value) {
        is HttpMethod.Get -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
            )
        }
        is HttpMethod.Head -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
            )
        }
        is HttpMethod.Post -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
            )
        }
        is HttpMethod.Put -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
            )
        }
        is HttpMethod.Patch -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
            )
        }
        is HttpMethod.Delete -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
            )
        }
        is HttpMethod.Options -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
            )
        }
        is HttpMethod.Other -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterString.allocationSize(value.`verb`)
            )
        }
    }

    override fun write(value: HttpMethod, buf: ByteBuffer) {
        when(value) {
            is HttpMethod.Get -> {
                buf.putInt(1)
                Unit
            }
            is HttpMethod.Head -> {
                buf.putInt(2)
                Unit
            }
            is HttpMethod.Post -> {
                buf.putInt(3)
                Unit
            }
            is HttpMethod.Put -> {
                buf.putInt(4)
                Unit
            }
            is HttpMethod.Patch -> {
                buf.putInt(5)
                Unit
            }
            is HttpMethod.Delete -> {
                buf.putInt(6)
                Unit
            }
            is HttpMethod.Options -> {
                buf.putInt(7)
                Unit
            }
            is HttpMethod.Other -> {
                buf.putInt(8)
                FfiConverterString.write(value.`verb`, buf)
                Unit
            }
        }.let { /* this makes the `when` an expression, which ensures it is exhaustive */ }
    }
}






enum class InitiateCallType {
    
    DESTINATION,
    VOICEMAIL,
    REDIAL;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeInitiateCallType: FfiConverterRustBuffer<InitiateCallType> {
    override fun read(buf: ByteBuffer) = try {
        InitiateCallType.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: InitiateCallType) = 4UL

    override fun write(value: InitiateCallType, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}






enum class LogLevel {
    
    TRACE,
    DEBUG,
    INFO,
    WARN,
    ERROR;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeLogLevel: FfiConverterRustBuffer<LogLevel> {
    override fun read(buf: ByteBuffer) = try {
        LogLevel.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: LogLevel) = 4UL

    override fun write(value: LogLevel, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}






enum class LogOrigin {
    
    DEVICE,
    HOST;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeLogOrigin: FfiConverterRustBuffer<LogOrigin> {
    override fun read(buf: ByteBuffer) = try {
        LogOrigin.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: LogOrigin) = 4UL

    override fun write(value: LogOrigin, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}






enum class LogStoreLevel {
    
    TRACE,
    DEBUG,
    INFO,
    NOTICE,
    WARN,
    ERROR,
    FATAL;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeLogStoreLevel: FfiConverterRustBuffer<LogStoreLevel> {
    override fun read(buf: ByteBuffer) = try {
        LogStoreLevel.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: LogStoreLevel) = 4UL

    override fun write(value: LogStoreLevel, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}





sealed class MediaControl {
    
    object Play : MediaControl()
    
    
    object Pause : MediaControl()
    
    
    object SkipNext : MediaControl()
    
    
    object SkipPrev : MediaControl()
    
    
    data class SeekTo(
        val `positionMs`: kotlin.Long) : MediaControl()
        
    {
        

        companion object
    }
    
    data class SkipToQueueItem(
        val `queueId`: kotlin.Long) : MediaControl()
        
    {
        

        companion object
    }
    
    data class SetShuffle(
        val `on`: kotlin.Boolean) : MediaControl()
        
    {
        

        companion object
    }
    
    data class SetRepeat(
        val `mode`: uniffi.bridgething_companion.MediaRepeatMode) : MediaControl()
        
    {
        

        companion object
    }
    
    data class SetSpeed(
        val `speed`: kotlin.Float) : MediaControl()
        
    {
        

        companion object
    }
    
    data class SetLiked(
        val `liked`: kotlin.Boolean) : MediaControl()
        
    {
        

        companion object
    }
    

    

    
    


    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeMediaControl : FfiConverterRustBuffer<MediaControl>{
    override fun read(buf: ByteBuffer): MediaControl {
        return when(buf.getInt()) {
            1 -> MediaControl.Play
            2 -> MediaControl.Pause
            3 -> MediaControl.SkipNext
            4 -> MediaControl.SkipPrev
            5 -> MediaControl.SeekTo(
                FfiConverterLong.read(buf),
                )
            6 -> MediaControl.SkipToQueueItem(
                FfiConverterLong.read(buf),
                )
            7 -> MediaControl.SetShuffle(
                FfiConverterBoolean.read(buf),
                )
            8 -> MediaControl.SetRepeat(
                FfiConverterTypeMediaRepeatMode.read(buf),
                )
            9 -> MediaControl.SetSpeed(
                FfiConverterFloat.read(buf),
                )
            10 -> MediaControl.SetLiked(
                FfiConverterBoolean.read(buf),
                )
            else -> throw RuntimeException("invalid enum value, something is very wrong!!")
        }
    }

    override fun allocationSize(value: MediaControl): ULong = when(value) {
        is MediaControl.Play -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
            )
        }
        is MediaControl.Pause -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
            )
        }
        is MediaControl.SkipNext -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
            )
        }
        is MediaControl.SkipPrev -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
            )
        }
        is MediaControl.SeekTo -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterLong.allocationSize(value.`positionMs`)
            )
        }
        is MediaControl.SkipToQueueItem -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterLong.allocationSize(value.`queueId`)
            )
        }
        is MediaControl.SetShuffle -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterBoolean.allocationSize(value.`on`)
            )
        }
        is MediaControl.SetRepeat -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterTypeMediaRepeatMode.allocationSize(value.`mode`)
            )
        }
        is MediaControl.SetSpeed -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterFloat.allocationSize(value.`speed`)
            )
        }
        is MediaControl.SetLiked -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterBoolean.allocationSize(value.`liked`)
            )
        }
    }

    override fun write(value: MediaControl, buf: ByteBuffer) {
        when(value) {
            is MediaControl.Play -> {
                buf.putInt(1)
                Unit
            }
            is MediaControl.Pause -> {
                buf.putInt(2)
                Unit
            }
            is MediaControl.SkipNext -> {
                buf.putInt(3)
                Unit
            }
            is MediaControl.SkipPrev -> {
                buf.putInt(4)
                Unit
            }
            is MediaControl.SeekTo -> {
                buf.putInt(5)
                FfiConverterLong.write(value.`positionMs`, buf)
                Unit
            }
            is MediaControl.SkipToQueueItem -> {
                buf.putInt(6)
                FfiConverterLong.write(value.`queueId`, buf)
                Unit
            }
            is MediaControl.SetShuffle -> {
                buf.putInt(7)
                FfiConverterBoolean.write(value.`on`, buf)
                Unit
            }
            is MediaControl.SetRepeat -> {
                buf.putInt(8)
                FfiConverterTypeMediaRepeatMode.write(value.`mode`, buf)
                Unit
            }
            is MediaControl.SetSpeed -> {
                buf.putInt(9)
                FfiConverterFloat.write(value.`speed`, buf)
                Unit
            }
            is MediaControl.SetLiked -> {
                buf.putInt(10)
                FfiConverterBoolean.write(value.`liked`, buf)
                Unit
            }
        }.let { /* this makes the `when` an expression, which ensures it is exhaustive */ }
    }
}






enum class MediaRepeatMode {
    
    OFF,
    ONE,
    ALL;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeMediaRepeatMode: FfiConverterRustBuffer<MediaRepeatMode> {
    override fun read(buf: ByteBuffer) = try {
        MediaRepeatMode.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: MediaRepeatMode) = 4UL

    override fun write(value: MediaRepeatMode, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}






enum class ModelArtifactKind {
    
    NLU_MODEL,
    ASR_MODEL;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeModelArtifactKind: FfiConverterRustBuffer<ModelArtifactKind> {
    override fun read(buf: ByteBuffer) = try {
        ModelArtifactKind.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: ModelArtifactKind) = 4UL

    override fun write(value: ModelArtifactKind, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}






enum class ModelPlatform {
    
    IOS,
    ANDROID,
    MACOS,
    LINUX,
    WINDOWS;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeModelPlatform: FfiConverterRustBuffer<ModelPlatform> {
    override fun read(buf: ByteBuffer) = try {
        ModelPlatform.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: ModelPlatform) = 4UL

    override fun write(value: ModelPlatform, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}







sealed class ModelValidationException(message: String): kotlin.Exception(message) {
        
        class Invalid(message: String) : ModelValidationException(message)
        

    companion object ErrorHandler : UniffiRustCallStatusErrorHandler<ModelValidationException> {
        override fun lift(error_buf: RustBuffer.ByValue): ModelValidationException = FfiConverterTypeModelValidationError.lift(error_buf)
    }
}

/**
 * @suppress
 */
public object FfiConverterTypeModelValidationError : FfiConverterRustBuffer<ModelValidationException> {
    override fun read(buf: ByteBuffer): ModelValidationException {
        
            return when(buf.getInt()) {
            1 -> ModelValidationException.Invalid(FfiConverterString.read(buf))
            else -> throw RuntimeException("invalid error enum value, something is very wrong!!")
        }
        
    }

    override fun allocationSize(value: ModelValidationException): ULong {
        return 4UL
    }

    override fun write(value: ModelValidationException, buf: ByteBuffer) {
        when(value) {
            is ModelValidationException.Invalid -> {
                buf.putInt(1)
                Unit
            }
        }.let { /* this makes the `when` an expression, which ensures it is exhaustive */ }
    }

}




enum class NluFastPathAmount {
    
    SMALL,
    MEDIUM,
    LARGE;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeNluFastPathAmount: FfiConverterRustBuffer<NluFastPathAmount> {
    override fun read(buf: ByteBuffer) = try {
        NluFastPathAmount.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: NluFastPathAmount) = 4UL

    override fun write(value: NluFastPathAmount, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}






enum class NluFastPathDirection {
    
    UP,
    DOWN;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeNluFastPathDirection: FfiConverterRustBuffer<NluFastPathDirection> {
    override fun read(buf: ByteBuffer) = try {
        NluFastPathDirection.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: NluFastPathDirection) = 4UL

    override fun write(value: NluFastPathDirection, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}






enum class NluFastPathRepeatMode {
    
    OFF,
    ALL,
    ONE;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeNluFastPathRepeatMode: FfiConverterRustBuffer<NluFastPathRepeatMode> {
    override fun read(buf: ByteBuffer) = try {
        NluFastPathRepeatMode.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: NluFastPathRepeatMode) = 4UL

    override fun write(value: NluFastPathRepeatMode, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}






enum class NluFastPathSpeed {
    
    ONE,
    ONE_POINT_TWO,
    ONE_POINT_FIVE,
    TWO;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeNluFastPathSpeed: FfiConverterRustBuffer<NluFastPathSpeed> {
    override fun read(buf: ByteBuffer) = try {
        NluFastPathSpeed.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: NluFastPathSpeed) = 4UL

    override fun write(value: NluFastPathSpeed, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}






enum class NluFastPathView {
    
    NOW_PLAYING,
    ARTIST,
    ALBUM,
    PLAYLIST,
    PLAYLISTS,
    LIBRARY,
    SONGS,
    PRESETS,
    QUEUE,
    SAVED_EPISODES,
    NEW_EPISODES;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeNluFastPathView: FfiConverterRustBuffer<NluFastPathView> {
    override fun read(buf: ByteBuffer) = try {
        NluFastPathView.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: NluFastPathView) = 4UL

    override fun write(value: NluFastPathView, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}







sealed class NluRejectionException(message: String): kotlin.Exception(message) {
        
        class HeadMismatch(message: String) : NluRejectionException(message)
        

    companion object ErrorHandler : UniffiRustCallStatusErrorHandler<NluRejectionException> {
        override fun lift(error_buf: RustBuffer.ByValue): NluRejectionException = FfiConverterTypeNluRejectionError.lift(error_buf)
    }
}

/**
 * @suppress
 */
public object FfiConverterTypeNluRejectionError : FfiConverterRustBuffer<NluRejectionException> {
    override fun read(buf: ByteBuffer): NluRejectionException {
        
            return when(buf.getInt()) {
            1 -> NluRejectionException.HeadMismatch(FfiConverterString.read(buf))
            else -> throw RuntimeException("invalid error enum value, something is very wrong!!")
        }
        
    }

    override fun allocationSize(value: NluRejectionException): ULong {
        return 4UL
    }

    override fun write(value: NluRejectionException, buf: ByteBuffer) {
        when(value) {
            is NluRejectionException.HeadMismatch -> {
                buf.putInt(1)
                Unit
            }
        }.let { /* this makes the `when` an expression, which ensures it is exhaustive */ }
    }

}



sealed class NluRejectionOutcome {
    
    data class Accept(
        val `intent`: kotlin.String) : NluRejectionOutcome()
        
    {
        

        companion object
    }
    
    object NoIntent : NluRejectionOutcome()
    
    
    data class Clarify(
        val `alternates`: List<kotlin.String>) : NluRejectionOutcome()
        
    {
        

        companion object
    }
    

    

    
    


    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeNluRejectionOutcome : FfiConverterRustBuffer<NluRejectionOutcome>{
    override fun read(buf: ByteBuffer): NluRejectionOutcome {
        return when(buf.getInt()) {
            1 -> NluRejectionOutcome.Accept(
                FfiConverterString.read(buf),
                )
            2 -> NluRejectionOutcome.NoIntent
            3 -> NluRejectionOutcome.Clarify(
                FfiConverterSequenceString.read(buf),
                )
            else -> throw RuntimeException("invalid enum value, something is very wrong!!")
        }
    }

    override fun allocationSize(value: NluRejectionOutcome): ULong = when(value) {
        is NluRejectionOutcome.Accept -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterString.allocationSize(value.`intent`)
            )
        }
        is NluRejectionOutcome.NoIntent -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
            )
        }
        is NluRejectionOutcome.Clarify -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterSequenceString.allocationSize(value.`alternates`)
            )
        }
    }

    override fun write(value: NluRejectionOutcome, buf: ByteBuffer) {
        when(value) {
            is NluRejectionOutcome.Accept -> {
                buf.putInt(1)
                FfiConverterString.write(value.`intent`, buf)
                Unit
            }
            is NluRejectionOutcome.NoIntent -> {
                buf.putInt(2)
                Unit
            }
            is NluRejectionOutcome.Clarify -> {
                buf.putInt(3)
                FfiConverterSequenceString.write(value.`alternates`, buf)
                Unit
            }
        }.let { /* this makes the `when` an expression, which ensures it is exhaustive */ }
    }
}







sealed class NluRunnerException: kotlin.Exception() {
    
    class NotLoaded(
        ) : NluRunnerException() {
        override val message
            get() = ""
    }
    
    class Failed(
        
        val `reason`: kotlin.String
        ) : NluRunnerException() {
        override val message
            get() = "reason=${ `reason` }"
    }
    

    


    companion object ErrorHandler : UniffiRustCallStatusErrorHandler<NluRunnerException> {
        override fun lift(error_buf: RustBuffer.ByValue): NluRunnerException = FfiConverterTypeNluRunnerError.lift(error_buf)
    }

    
}

/**
 * @suppress
 */
public object FfiConverterTypeNluRunnerError : FfiConverterRustBuffer<NluRunnerException> {
    override fun read(buf: ByteBuffer): NluRunnerException {
        

        return when(buf.getInt()) {
            1 -> NluRunnerException.NotLoaded()
            2 -> NluRunnerException.Failed(
                FfiConverterString.read(buf),
                )
            else -> throw RuntimeException("invalid error enum value, something is very wrong!!")
        }
    }

    override fun allocationSize(value: NluRunnerException): ULong {
        return when(value) {
            is NluRunnerException.NotLoaded -> (
                // Add the size for the Int that specifies the variant plus the size needed for all fields
                4UL
            )
            is NluRunnerException.Failed -> (
                // Add the size for the Int that specifies the variant plus the size needed for all fields
                4UL
                + FfiConverterString.allocationSize(value.`reason`)
            )
        }
    }

    override fun write(value: NluRunnerException, buf: ByteBuffer) {
        when(value) {
            is NluRunnerException.NotLoaded -> {
                buf.putInt(1)
                Unit
            }
            is NluRunnerException.Failed -> {
                buf.putInt(2)
                FfiConverterString.write(value.`reason`, buf)
                Unit
            }
        }.let { /* this makes the `when` an expression, which ensures it is exhaustive */ }
    }

}



sealed class NotificationActionError {
    
    data class NotFound(
        val `id`: kotlin.String) : NotificationActionError()
        
    {
        

        companion object
    }
    
    data class ActionRejected(
        val `reason`: kotlin.String) : NotificationActionError()
        
    {
        

        companion object
    }
    
    object NoTarget : NotificationActionError()
    
    

    

    
    


    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeNotificationActionError : FfiConverterRustBuffer<NotificationActionError>{
    override fun read(buf: ByteBuffer): NotificationActionError {
        return when(buf.getInt()) {
            1 -> NotificationActionError.NotFound(
                FfiConverterString.read(buf),
                )
            2 -> NotificationActionError.ActionRejected(
                FfiConverterString.read(buf),
                )
            3 -> NotificationActionError.NoTarget
            else -> throw RuntimeException("invalid enum value, something is very wrong!!")
        }
    }

    override fun allocationSize(value: NotificationActionError): ULong = when(value) {
        is NotificationActionError.NotFound -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterString.allocationSize(value.`id`)
            )
        }
        is NotificationActionError.ActionRejected -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterString.allocationSize(value.`reason`)
            )
        }
        is NotificationActionError.NoTarget -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
            )
        }
    }

    override fun write(value: NotificationActionError, buf: ByteBuffer) {
        when(value) {
            is NotificationActionError.NotFound -> {
                buf.putInt(1)
                FfiConverterString.write(value.`id`, buf)
                Unit
            }
            is NotificationActionError.ActionRejected -> {
                buf.putInt(2)
                FfiConverterString.write(value.`reason`, buf)
                Unit
            }
            is NotificationActionError.NoTarget -> {
                buf.putInt(3)
                Unit
            }
        }.let { /* this makes the `when` an expression, which ensures it is exhaustive */ }
    }
}






enum class NotificationCategory {
    
    OTHER,
    INCOMING_CALL,
    MISSED_CALL,
    VOICEMAIL,
    SOCIAL,
    SCHEDULE,
    EMAIL,
    NEWS,
    HEALTH_AND_FITNESS,
    BUSINESS_AND_FINANCE,
    LOCATION,
    ENTERTAINMENT;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeNotificationCategory: FfiConverterRustBuffer<NotificationCategory> {
    override fun read(buf: ByteBuffer) = try {
        NotificationCategory.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: NotificationCategory) = 4UL

    override fun write(value: NotificationCategory, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}






enum class OtaApplyPhase {
    
    STREAMING,
    VERIFYING,
    WRITING,
    CONFIRMING,
    REBOOT;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeOtaApplyPhase: FfiConverterRustBuffer<OtaApplyPhase> {
    override fun read(buf: ByteBuffer) = try {
        OtaApplyPhase.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: OtaApplyPhase) = 4UL

    override fun write(value: OtaApplyPhase, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}






enum class OtaKind {
    
    IMAGE,
    DAEMON,
    BUILTIN_WEBAPP,
    INSTALLED_WEBAPP,
    WAKEWORD_MODEL;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeOtaKind: FfiConverterRustBuffer<OtaKind> {
    override fun read(buf: ByteBuffer) = try {
        OtaKind.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: OtaKind) = 4UL

    override fun write(value: OtaKind, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}







sealed class OtaManifestException(message: String): kotlin.Exception(message) {
        
        class Parse(message: String) : OtaManifestException(message)
        

    companion object ErrorHandler : UniffiRustCallStatusErrorHandler<OtaManifestException> {
        override fun lift(error_buf: RustBuffer.ByValue): OtaManifestException = FfiConverterTypeOtaManifestError.lift(error_buf)
    }
}

/**
 * @suppress
 */
public object FfiConverterTypeOtaManifestError : FfiConverterRustBuffer<OtaManifestException> {
    override fun read(buf: ByteBuffer): OtaManifestException {
        
            return when(buf.getInt()) {
            1 -> OtaManifestException.Parse(FfiConverterString.read(buf))
            else -> throw RuntimeException("invalid error enum value, something is very wrong!!")
        }
        
    }

    override fun allocationSize(value: OtaManifestException): ULong {
        return 4UL
    }

    override fun write(value: OtaManifestException, buf: ByteBuffer) {
        when(value) {
            is OtaManifestException.Parse -> {
                buf.putInt(1)
                Unit
            }
        }.let { /* this makes the `when` an expression, which ensures it is exhaustive */ }
    }

}



sealed class OtaPhaseSnapshot {
    
    object Idle : OtaPhaseSnapshot()
    
    
    data class Downloading(
        val `asset`: kotlin.String, 
        val `received`: kotlin.ULong, 
        val `total`: kotlin.ULong, 
        val `ratePerSec`: kotlin.Double?) : OtaPhaseSnapshot()
        
    {
        

        companion object
    }
    
    data class Streaming(
        val `asset`: kotlin.String, 
        val `sent`: kotlin.ULong, 
        val `total`: kotlin.ULong, 
        val `ratePerSec`: kotlin.Double?, 
        val `etaSeconds`: kotlin.Double?) : OtaPhaseSnapshot()
        
    {
        

        companion object
    }
    
    data class Applying(
        val `phase`: uniffi.bridgething_companion.OtaApplyPhase, 
        val `writePercent`: kotlin.UInt, 
        val `dwlPercent`: kotlin.UInt, 
        val `dwlBytes`: kotlin.ULong) : OtaPhaseSnapshot()
        
    {
        

        companion object
    }
    
    object Staged : OtaPhaseSnapshot()
    
    
    object Completed : OtaPhaseSnapshot()
    
    
    data class Failed(
        val `reason`: kotlin.String) : OtaPhaseSnapshot()
        
    {
        

        companion object
    }
    

    

    
    


    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeOtaPhaseSnapshot : FfiConverterRustBuffer<OtaPhaseSnapshot>{
    override fun read(buf: ByteBuffer): OtaPhaseSnapshot {
        return when(buf.getInt()) {
            1 -> OtaPhaseSnapshot.Idle
            2 -> OtaPhaseSnapshot.Downloading(
                FfiConverterString.read(buf),
                FfiConverterULong.read(buf),
                FfiConverterULong.read(buf),
                FfiConverterOptionalDouble.read(buf),
                )
            3 -> OtaPhaseSnapshot.Streaming(
                FfiConverterString.read(buf),
                FfiConverterULong.read(buf),
                FfiConverterULong.read(buf),
                FfiConverterOptionalDouble.read(buf),
                FfiConverterOptionalDouble.read(buf),
                )
            4 -> OtaPhaseSnapshot.Applying(
                FfiConverterTypeOtaApplyPhase.read(buf),
                FfiConverterUInt.read(buf),
                FfiConverterUInt.read(buf),
                FfiConverterULong.read(buf),
                )
            5 -> OtaPhaseSnapshot.Staged
            6 -> OtaPhaseSnapshot.Completed
            7 -> OtaPhaseSnapshot.Failed(
                FfiConverterString.read(buf),
                )
            else -> throw RuntimeException("invalid enum value, something is very wrong!!")
        }
    }

    override fun allocationSize(value: OtaPhaseSnapshot): ULong = when(value) {
        is OtaPhaseSnapshot.Idle -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
            )
        }
        is OtaPhaseSnapshot.Downloading -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterString.allocationSize(value.`asset`)
                + FfiConverterULong.allocationSize(value.`received`)
                + FfiConverterULong.allocationSize(value.`total`)
                + FfiConverterOptionalDouble.allocationSize(value.`ratePerSec`)
            )
        }
        is OtaPhaseSnapshot.Streaming -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterString.allocationSize(value.`asset`)
                + FfiConverterULong.allocationSize(value.`sent`)
                + FfiConverterULong.allocationSize(value.`total`)
                + FfiConverterOptionalDouble.allocationSize(value.`ratePerSec`)
                + FfiConverterOptionalDouble.allocationSize(value.`etaSeconds`)
            )
        }
        is OtaPhaseSnapshot.Applying -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterTypeOtaApplyPhase.allocationSize(value.`phase`)
                + FfiConverterUInt.allocationSize(value.`writePercent`)
                + FfiConverterUInt.allocationSize(value.`dwlPercent`)
                + FfiConverterULong.allocationSize(value.`dwlBytes`)
            )
        }
        is OtaPhaseSnapshot.Staged -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
            )
        }
        is OtaPhaseSnapshot.Completed -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
            )
        }
        is OtaPhaseSnapshot.Failed -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterString.allocationSize(value.`reason`)
            )
        }
    }

    override fun write(value: OtaPhaseSnapshot, buf: ByteBuffer) {
        when(value) {
            is OtaPhaseSnapshot.Idle -> {
                buf.putInt(1)
                Unit
            }
            is OtaPhaseSnapshot.Downloading -> {
                buf.putInt(2)
                FfiConverterString.write(value.`asset`, buf)
                FfiConverterULong.write(value.`received`, buf)
                FfiConverterULong.write(value.`total`, buf)
                FfiConverterOptionalDouble.write(value.`ratePerSec`, buf)
                Unit
            }
            is OtaPhaseSnapshot.Streaming -> {
                buf.putInt(3)
                FfiConverterString.write(value.`asset`, buf)
                FfiConverterULong.write(value.`sent`, buf)
                FfiConverterULong.write(value.`total`, buf)
                FfiConverterOptionalDouble.write(value.`ratePerSec`, buf)
                FfiConverterOptionalDouble.write(value.`etaSeconds`, buf)
                Unit
            }
            is OtaPhaseSnapshot.Applying -> {
                buf.putInt(4)
                FfiConverterTypeOtaApplyPhase.write(value.`phase`, buf)
                FfiConverterUInt.write(value.`writePercent`, buf)
                FfiConverterUInt.write(value.`dwlPercent`, buf)
                FfiConverterULong.write(value.`dwlBytes`, buf)
                Unit
            }
            is OtaPhaseSnapshot.Staged -> {
                buf.putInt(5)
                Unit
            }
            is OtaPhaseSnapshot.Completed -> {
                buf.putInt(6)
                Unit
            }
            is OtaPhaseSnapshot.Failed -> {
                buf.putInt(7)
                FfiConverterString.write(value.`reason`, buf)
                Unit
            }
        }.let { /* this makes the `when` an expression, which ensures it is exhaustive */ }
    }
}





sealed class OtaPollEvent {
    
    data class ManifestPolled(
        val `updatedAt`: kotlin.String) : OtaPollEvent()
        
    {
        

        companion object
    }
    
    data class ManifestPollFailed(
        val `reason`: kotlin.String) : OtaPollEvent()
        
    {
        

        companion object
    }
    
    data class UpdateAvailable(
        val `deviceId`: kotlin.String, 
        val `release`: kotlin.String, 
        val `daemonVersion`: kotlin.String, 
        val `imageVersion`: kotlin.String) : OtaPollEvent()
        
    {
        

        companion object
    }
    
    data class Planned(
        val `deviceId`: kotlin.String, 
        val `kind`: uniffi.bridgething_companion.OtaKind, 
        val `release`: kotlin.String, 
        val `daemonVersion`: kotlin.String, 
        val `imageVersion`: kotlin.String, 
        val `channel`: kotlin.String, 
        val `rootUrl`: kotlin.String, 
        val `steps`: List<uniffi.bridgething_companion.OtaPlanStep>) : OtaPollEvent()
        
    {
        

        companion object
    }
    
    data class Progress(
        val `deviceId`: kotlin.String, 
        val `kind`: uniffi.bridgething_companion.OtaKind, 
        val `stepId`: kotlin.UInt, 
        val `snapshot`: uniffi.bridgething_companion.OtaPhaseSnapshot) : OtaPollEvent()
        
    {
        

        companion object
    }
    
    data class Updated(
        val `deviceId`: kotlin.String, 
        val `kind`: uniffi.bridgething_companion.OtaKind, 
        val `version`: kotlin.String) : OtaPollEvent()
        
    {
        

        companion object
    }
    
    data class Failed(
        val `deviceId`: kotlin.String, 
        val `kind`: uniffi.bridgething_companion.OtaKind, 
        val `reason`: kotlin.String) : OtaPollEvent()
        
    {
        

        companion object
    }
    

    

    
    


    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeOtaPollEvent : FfiConverterRustBuffer<OtaPollEvent>{
    override fun read(buf: ByteBuffer): OtaPollEvent {
        return when(buf.getInt()) {
            1 -> OtaPollEvent.ManifestPolled(
                FfiConverterString.read(buf),
                )
            2 -> OtaPollEvent.ManifestPollFailed(
                FfiConverterString.read(buf),
                )
            3 -> OtaPollEvent.UpdateAvailable(
                FfiConverterString.read(buf),
                FfiConverterString.read(buf),
                FfiConverterString.read(buf),
                FfiConverterString.read(buf),
                )
            4 -> OtaPollEvent.Planned(
                FfiConverterString.read(buf),
                FfiConverterTypeOtaKind.read(buf),
                FfiConverterString.read(buf),
                FfiConverterString.read(buf),
                FfiConverterString.read(buf),
                FfiConverterString.read(buf),
                FfiConverterString.read(buf),
                FfiConverterSequenceTypeOtaPlanStep.read(buf),
                )
            5 -> OtaPollEvent.Progress(
                FfiConverterString.read(buf),
                FfiConverterTypeOtaKind.read(buf),
                FfiConverterUInt.read(buf),
                FfiConverterTypeOtaPhaseSnapshot.read(buf),
                )
            6 -> OtaPollEvent.Updated(
                FfiConverterString.read(buf),
                FfiConverterTypeOtaKind.read(buf),
                FfiConverterString.read(buf),
                )
            7 -> OtaPollEvent.Failed(
                FfiConverterString.read(buf),
                FfiConverterTypeOtaKind.read(buf),
                FfiConverterString.read(buf),
                )
            else -> throw RuntimeException("invalid enum value, something is very wrong!!")
        }
    }

    override fun allocationSize(value: OtaPollEvent): ULong = when(value) {
        is OtaPollEvent.ManifestPolled -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterString.allocationSize(value.`updatedAt`)
            )
        }
        is OtaPollEvent.ManifestPollFailed -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterString.allocationSize(value.`reason`)
            )
        }
        is OtaPollEvent.UpdateAvailable -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterString.allocationSize(value.`deviceId`)
                + FfiConverterString.allocationSize(value.`release`)
                + FfiConverterString.allocationSize(value.`daemonVersion`)
                + FfiConverterString.allocationSize(value.`imageVersion`)
            )
        }
        is OtaPollEvent.Planned -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterString.allocationSize(value.`deviceId`)
                + FfiConverterTypeOtaKind.allocationSize(value.`kind`)
                + FfiConverterString.allocationSize(value.`release`)
                + FfiConverterString.allocationSize(value.`daemonVersion`)
                + FfiConverterString.allocationSize(value.`imageVersion`)
                + FfiConverterString.allocationSize(value.`channel`)
                + FfiConverterString.allocationSize(value.`rootUrl`)
                + FfiConverterSequenceTypeOtaPlanStep.allocationSize(value.`steps`)
            )
        }
        is OtaPollEvent.Progress -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterString.allocationSize(value.`deviceId`)
                + FfiConverterTypeOtaKind.allocationSize(value.`kind`)
                + FfiConverterUInt.allocationSize(value.`stepId`)
                + FfiConverterTypeOtaPhaseSnapshot.allocationSize(value.`snapshot`)
            )
        }
        is OtaPollEvent.Updated -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterString.allocationSize(value.`deviceId`)
                + FfiConverterTypeOtaKind.allocationSize(value.`kind`)
                + FfiConverterString.allocationSize(value.`version`)
            )
        }
        is OtaPollEvent.Failed -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterString.allocationSize(value.`deviceId`)
                + FfiConverterTypeOtaKind.allocationSize(value.`kind`)
                + FfiConverterString.allocationSize(value.`reason`)
            )
        }
    }

    override fun write(value: OtaPollEvent, buf: ByteBuffer) {
        when(value) {
            is OtaPollEvent.ManifestPolled -> {
                buf.putInt(1)
                FfiConverterString.write(value.`updatedAt`, buf)
                Unit
            }
            is OtaPollEvent.ManifestPollFailed -> {
                buf.putInt(2)
                FfiConverterString.write(value.`reason`, buf)
                Unit
            }
            is OtaPollEvent.UpdateAvailable -> {
                buf.putInt(3)
                FfiConverterString.write(value.`deviceId`, buf)
                FfiConverterString.write(value.`release`, buf)
                FfiConverterString.write(value.`daemonVersion`, buf)
                FfiConverterString.write(value.`imageVersion`, buf)
                Unit
            }
            is OtaPollEvent.Planned -> {
                buf.putInt(4)
                FfiConverterString.write(value.`deviceId`, buf)
                FfiConverterTypeOtaKind.write(value.`kind`, buf)
                FfiConverterString.write(value.`release`, buf)
                FfiConverterString.write(value.`daemonVersion`, buf)
                FfiConverterString.write(value.`imageVersion`, buf)
                FfiConverterString.write(value.`channel`, buf)
                FfiConverterString.write(value.`rootUrl`, buf)
                FfiConverterSequenceTypeOtaPlanStep.write(value.`steps`, buf)
                Unit
            }
            is OtaPollEvent.Progress -> {
                buf.putInt(5)
                FfiConverterString.write(value.`deviceId`, buf)
                FfiConverterTypeOtaKind.write(value.`kind`, buf)
                FfiConverterUInt.write(value.`stepId`, buf)
                FfiConverterTypeOtaPhaseSnapshot.write(value.`snapshot`, buf)
                Unit
            }
            is OtaPollEvent.Updated -> {
                buf.putInt(6)
                FfiConverterString.write(value.`deviceId`, buf)
                FfiConverterTypeOtaKind.write(value.`kind`, buf)
                FfiConverterString.write(value.`version`, buf)
                Unit
            }
            is OtaPollEvent.Failed -> {
                buf.putInt(7)
                FfiConverterString.write(value.`deviceId`, buf)
                FfiConverterTypeOtaKind.write(value.`kind`, buf)
                FfiConverterString.write(value.`reason`, buf)
                Unit
            }
        }.let { /* this makes the `when` an expression, which ensures it is exhaustive */ }
    }
}






enum class OtaRunOutcome {
    
    SUCCEEDED,
    FAILED,
    CANCELLED;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeOtaRunOutcome: FfiConverterRustBuffer<OtaRunOutcome> {
    override fun read(buf: ByteBuffer) = try {
        OtaRunOutcome.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: OtaRunOutcome) = 4UL

    override fun write(value: OtaRunOutcome, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}






enum class OtaRunPhase {
    
    IDLE,
    DOWNLOADING,
    STREAMING,
    VERIFYING,
    WRITING,
    CONFIRMING,
    REBOOT,
    COMPLETED,
    FAILED;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeOtaRunPhase: FfiConverterRustBuffer<OtaRunPhase> {
    override fun read(buf: ByteBuffer) = try {
        OtaRunPhase.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: OtaRunPhase) = 4UL

    override fun write(value: OtaRunPhase, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}






enum class OtaStepKind {
    
    DOWNLOAD,
    STREAM,
    APPLY,
    REBOOT;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeOtaStepKind: FfiConverterRustBuffer<OtaStepKind> {
    override fun read(buf: ByteBuffer) = try {
        OtaStepKind.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: OtaStepKind) = 4UL

    override fun write(value: OtaStepKind, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}





sealed class OtaStoreChange {
    
    data class Run(
        val `run`: OtaRun) : OtaStoreChange()
        
    {
        

        companion object
    }
    
    data class Available(
        val `available`: uniffi.bridgething_companion.OtaAvailable) : OtaStoreChange()
        
    {
        

        companion object
    }
    
    data class Poll(
        val `status`: uniffi.bridgething_companion.OtaPollStatus) : OtaStoreChange()
        
    {
        

        companion object
    }
    

    

    
    


    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeOtaStoreChange : FfiConverterRustBuffer<OtaStoreChange>{
    override fun read(buf: ByteBuffer): OtaStoreChange {
        return when(buf.getInt()) {
            1 -> OtaStoreChange.Run(
                FfiConverterTypeOtaRun.read(buf),
                )
            2 -> OtaStoreChange.Available(
                FfiConverterTypeOtaAvailable.read(buf),
                )
            3 -> OtaStoreChange.Poll(
                FfiConverterTypeOtaPollStatus.read(buf),
                )
            else -> throw RuntimeException("invalid enum value, something is very wrong!!")
        }
    }

    override fun allocationSize(value: OtaStoreChange): ULong = when(value) {
        is OtaStoreChange.Run -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterTypeOtaRun.allocationSize(value.`run`)
            )
        }
        is OtaStoreChange.Available -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterTypeOtaAvailable.allocationSize(value.`available`)
            )
        }
        is OtaStoreChange.Poll -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterTypeOtaPollStatus.allocationSize(value.`status`)
            )
        }
    }

    override fun write(value: OtaStoreChange, buf: ByteBuffer) {
        when(value) {
            is OtaStoreChange.Run -> {
                buf.putInt(1)
                FfiConverterTypeOtaRun.write(value.`run`, buf)
                Unit
            }
            is OtaStoreChange.Available -> {
                buf.putInt(2)
                FfiConverterTypeOtaAvailable.write(value.`available`, buf)
                Unit
            }
            is OtaStoreChange.Poll -> {
                buf.putInt(3)
                FfiConverterTypeOtaPollStatus.write(value.`status`, buf)
                Unit
            }
        }.let { /* this makes the `when` an expression, which ensures it is exhaustive */ }
    }
}






enum class PeerLinkStatus {
    
    CONNECTED,
    LINK_FAILED;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypePeerLinkStatus: FfiConverterRustBuffer<PeerLinkStatus> {
    override fun read(buf: ByteBuffer) = try {
        PeerLinkStatus.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: PeerLinkStatus) = 4UL

    override fun write(value: PeerLinkStatus, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}






enum class PhoneCallDirection {
    
    INCOMING,
    OUTGOING;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypePhoneCallDirection: FfiConverterRustBuffer<PhoneCallDirection> {
    override fun read(buf: ByteBuffer) = try {
        PhoneCallDirection.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: PhoneCallDirection) = 4UL

    override fun write(value: PhoneCallDirection, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}






enum class PhoneCallService {
    
    UNKNOWN,
    TELEPHONY,
    FACE_TIME_AUDIO,
    FACE_TIME_VIDEO;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypePhoneCallService: FfiConverterRustBuffer<PhoneCallService> {
    override fun read(buf: ByteBuffer) = try {
        PhoneCallService.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: PhoneCallService) = 4UL

    override fun write(value: PhoneCallService, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}






enum class PhoneCallStatus {
    
    DISCONNECTED,
    SENDING,
    RINGING,
    CONNECTING,
    ACTIVE,
    HELD,
    DISCONNECTING;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypePhoneCallStatus: FfiConverterRustBuffer<PhoneCallStatus> {
    override fun read(buf: ByteBuffer) = try {
        PhoneCallStatus.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: PhoneCallStatus) = 4UL

    override fun write(value: PhoneCallStatus, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}





sealed class PhoneCommand {
    
    data class Answer(
        val `callId`: kotlin.String) : PhoneCommand()
        
    {
        

        companion object
    }
    
    data class Accept(
        val `callId`: kotlin.String, 
        val `action`: uniffi.bridgething_companion.AcceptCallAction) : PhoneCommand()
        
    {
        

        companion object
    }
    
    data class Decline(
        val `callId`: kotlin.String) : PhoneCommand()
        
    {
        

        companion object
    }
    
    data class End(
        val `callId`: kotlin.String) : PhoneCommand()
        
    {
        

        companion object
    }
    
    data class EndTyped(
        val `callId`: kotlin.String, 
        val `action`: uniffi.bridgething_companion.EndCallAction) : PhoneCommand()
        
    {
        

        companion object
    }
    
    data class Hold(
        val `callId`: kotlin.String) : PhoneCommand()
        
    {
        

        companion object
    }
    
    data class Unhold(
        val `callId`: kotlin.String) : PhoneCommand()
        
    {
        

        companion object
    }
    
    data class Initiate(
        val `action`: uniffi.bridgething_companion.PhoneInitiate) : PhoneCommand()
        
    {
        

        companion object
    }
    
    object Swap : PhoneCommand()
    
    
    object Merge : PhoneCommand()
    
    
    data class Mute(
        val `muted`: kotlin.Boolean) : PhoneCommand()
        
    {
        

        companion object
    }
    
    data class Dtmf(
        val `callId`: kotlin.String?, 
        val `tone`: uniffi.bridgething_companion.DtmfTone) : PhoneCommand()
        
    {
        

        companion object
    }
    

    

    
    


    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypePhoneCommand : FfiConverterRustBuffer<PhoneCommand>{
    override fun read(buf: ByteBuffer): PhoneCommand {
        return when(buf.getInt()) {
            1 -> PhoneCommand.Answer(
                FfiConverterString.read(buf),
                )
            2 -> PhoneCommand.Accept(
                FfiConverterString.read(buf),
                FfiConverterTypeAcceptCallAction.read(buf),
                )
            3 -> PhoneCommand.Decline(
                FfiConverterString.read(buf),
                )
            4 -> PhoneCommand.End(
                FfiConverterString.read(buf),
                )
            5 -> PhoneCommand.EndTyped(
                FfiConverterString.read(buf),
                FfiConverterTypeEndCallAction.read(buf),
                )
            6 -> PhoneCommand.Hold(
                FfiConverterString.read(buf),
                )
            7 -> PhoneCommand.Unhold(
                FfiConverterString.read(buf),
                )
            8 -> PhoneCommand.Initiate(
                FfiConverterTypePhoneInitiate.read(buf),
                )
            9 -> PhoneCommand.Swap
            10 -> PhoneCommand.Merge
            11 -> PhoneCommand.Mute(
                FfiConverterBoolean.read(buf),
                )
            12 -> PhoneCommand.Dtmf(
                FfiConverterOptionalString.read(buf),
                FfiConverterTypeDtmfTone.read(buf),
                )
            else -> throw RuntimeException("invalid enum value, something is very wrong!!")
        }
    }

    override fun allocationSize(value: PhoneCommand): ULong = when(value) {
        is PhoneCommand.Answer -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterString.allocationSize(value.`callId`)
            )
        }
        is PhoneCommand.Accept -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterString.allocationSize(value.`callId`)
                + FfiConverterTypeAcceptCallAction.allocationSize(value.`action`)
            )
        }
        is PhoneCommand.Decline -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterString.allocationSize(value.`callId`)
            )
        }
        is PhoneCommand.End -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterString.allocationSize(value.`callId`)
            )
        }
        is PhoneCommand.EndTyped -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterString.allocationSize(value.`callId`)
                + FfiConverterTypeEndCallAction.allocationSize(value.`action`)
            )
        }
        is PhoneCommand.Hold -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterString.allocationSize(value.`callId`)
            )
        }
        is PhoneCommand.Unhold -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterString.allocationSize(value.`callId`)
            )
        }
        is PhoneCommand.Initiate -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterTypePhoneInitiate.allocationSize(value.`action`)
            )
        }
        is PhoneCommand.Swap -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
            )
        }
        is PhoneCommand.Merge -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
            )
        }
        is PhoneCommand.Mute -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterBoolean.allocationSize(value.`muted`)
            )
        }
        is PhoneCommand.Dtmf -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterOptionalString.allocationSize(value.`callId`)
                + FfiConverterTypeDtmfTone.allocationSize(value.`tone`)
            )
        }
    }

    override fun write(value: PhoneCommand, buf: ByteBuffer) {
        when(value) {
            is PhoneCommand.Answer -> {
                buf.putInt(1)
                FfiConverterString.write(value.`callId`, buf)
                Unit
            }
            is PhoneCommand.Accept -> {
                buf.putInt(2)
                FfiConverterString.write(value.`callId`, buf)
                FfiConverterTypeAcceptCallAction.write(value.`action`, buf)
                Unit
            }
            is PhoneCommand.Decline -> {
                buf.putInt(3)
                FfiConverterString.write(value.`callId`, buf)
                Unit
            }
            is PhoneCommand.End -> {
                buf.putInt(4)
                FfiConverterString.write(value.`callId`, buf)
                Unit
            }
            is PhoneCommand.EndTyped -> {
                buf.putInt(5)
                FfiConverterString.write(value.`callId`, buf)
                FfiConverterTypeEndCallAction.write(value.`action`, buf)
                Unit
            }
            is PhoneCommand.Hold -> {
                buf.putInt(6)
                FfiConverterString.write(value.`callId`, buf)
                Unit
            }
            is PhoneCommand.Unhold -> {
                buf.putInt(7)
                FfiConverterString.write(value.`callId`, buf)
                Unit
            }
            is PhoneCommand.Initiate -> {
                buf.putInt(8)
                FfiConverterTypePhoneInitiate.write(value.`action`, buf)
                Unit
            }
            is PhoneCommand.Swap -> {
                buf.putInt(9)
                Unit
            }
            is PhoneCommand.Merge -> {
                buf.putInt(10)
                Unit
            }
            is PhoneCommand.Mute -> {
                buf.putInt(11)
                FfiConverterBoolean.write(value.`muted`, buf)
                Unit
            }
            is PhoneCommand.Dtmf -> {
                buf.putInt(12)
                FfiConverterOptionalString.write(value.`callId`, buf)
                FfiConverterTypeDtmfTone.write(value.`tone`, buf)
                Unit
            }
        }.let { /* this makes the `when` an expression, which ensures it is exhaustive */ }
    }
}






enum class RegistrationStatus {
    
    UNKNOWN,
    NOT_REGISTERED,
    SEARCHING,
    DENIED,
    REGISTERED_HOME,
    REGISTERED_ROAMING,
    EMERGENCY_CALLS_ONLY;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeRegistrationStatus: FfiConverterRustBuffer<RegistrationStatus> {
    override fun read(buf: ByteBuffer) = try {
        RegistrationStatus.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: RegistrationStatus) = 4UL

    override fun write(value: RegistrationStatus, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}






enum class RepeatMode {
    
    OFF,
    ONE,
    ALL;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeRepeatMode: FfiConverterRustBuffer<RepeatMode> {
    override fun read(buf: ByteBuffer) = try {
        RepeatMode.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: RepeatMode) = 4UL

    override fun write(value: RepeatMode, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}






enum class ResumeTarget {
    
    PHONE_ONLY,
    ANY_SPEAKER;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeResumeTarget: FfiConverterRustBuffer<ResumeTarget> {
    override fun read(buf: ByteBuffer) = try {
        ResumeTarget.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: ResumeTarget) = 4UL

    override fun write(value: ResumeTarget, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}






enum class ServiceHealthKind {
    
    OK,
    RATE_LIMITED,
    UNREACHABLE;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeServiceHealthKind: FfiConverterRustBuffer<ServiceHealthKind> {
    override fun read(buf: ByteBuffer) = try {
        ServiceHealthKind.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: ServiceHealthKind) = 4UL

    override fun write(value: ServiceHealthKind, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}





sealed class SessionEvent {
    
    data class ProvidersChanged(
        val `providers`: List<uniffi.bridgething_companion.ProviderInfo>) : SessionEvent()
        
    {
        

        companion object
    }
    
    data class PeerConnected(
        val `peer`: uniffi.bridgething_companion.SessionPeer) : SessionEvent()
        
    {
        

        companion object
    }
    
    data class PeerDisconnected(
        val `deviceId`: kotlin.String) : SessionEvent()
        
    {
        

        companion object
    }
    
    data class PeerLinkFailed(
        val `peer`: uniffi.bridgething_companion.SessionPeer) : SessionEvent()
        
    {
        

        companion object
    }
    
    data class NowPlayingChanged(
        val `nowPlaying`: uniffi.bridgething_companion.NowPlaying?) : SessionEvent()
        
    {
        

        companion object
    }
    
    data class AncsAuthStatusChanged(
        val `deviceId`: kotlin.String, 
        val `status`: uniffi.bridgething_companion.AncsAuthStatus) : SessionEvent()
        
    {
        

        companion object
    }
    
    data class Log(
        val `origin`: uniffi.bridgething_companion.LogOrigin, 
        val `level`: uniffi.bridgething_companion.LogLevel, 
        val `target`: kotlin.String, 
        val `message`: kotlin.String) : SessionEvent()
        
    {
        

        companion object
    }
    
    data class WebappsChanged(
        val `entry`: uniffi.bridgething_companion.DeviceWebappsEntry) : SessionEvent()
        
    {
        

        companion object
    }
    
    data class WebappDocChanged(
        val `deviceId`: kotlin.String, 
        val `webappId`: kotlin.String, 
        val `key`: kotlin.String, 
        val `value`: kotlin.String?) : SessionEvent()
        
    {
        

        companion object
    }
    
    data class DeviceMetaChanged(
        val `deviceId`: kotlin.String, 
        val `meta`: uniffi.bridgething_companion.DeviceMeta) : SessionEvent()
        
    {
        

        companion object
    }
    
    data class VoiceModelStateChanged(
        val `state`: uniffi.bridgething_companion.VoiceModelState) : SessionEvent()
        
    {
        

        companion object
    }
    
    data class VoiceTurnChanged(
        val `turn`: uniffi.bridgething_companion.VoiceTurn) : SessionEvent()
        
    {
        

        companion object
    }
    
    data class OtaRunChanged(
        val `run`: uniffi.bridgething_companion.OtaRun) : SessionEvent()
        
    {
        

        companion object
    }
    
    data class OtaAvailableChanged(
        val `available`: uniffi.bridgething_companion.OtaAvailable) : SessionEvent()
        
    {
        

        companion object
    }
    
    data class OtaPollChanged(
        val `status`: uniffi.bridgething_companion.OtaPollStatus) : SessionEvent()
        
    {
        

        companion object
    }
    
    data class CompanionUpdateProgress(
        val `received`: kotlin.ULong, 
        val `total`: kotlin.ULong) : SessionEvent()
        
    {
        

        companion object
    }
    
    data class ScreenshotCaptured(
        val `deviceId`: kotlin.String, 
        val `transferId`: kotlin.String, 
        val `byteSize`: kotlin.UInt, 
        val `capturedAtMs`: kotlin.ULong) : SessionEvent()
        
    {
        

        companion object
    }
    
    object Resumed : SessionEvent()
    
    

    

    
    


    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeSessionEvent : FfiConverterRustBuffer<SessionEvent>{
    override fun read(buf: ByteBuffer): SessionEvent {
        return when(buf.getInt()) {
            1 -> SessionEvent.ProvidersChanged(
                FfiConverterSequenceTypeProviderInfo.read(buf),
                )
            2 -> SessionEvent.PeerConnected(
                FfiConverterTypeSessionPeer.read(buf),
                )
            3 -> SessionEvent.PeerDisconnected(
                FfiConverterString.read(buf),
                )
            4 -> SessionEvent.PeerLinkFailed(
                FfiConverterTypeSessionPeer.read(buf),
                )
            5 -> SessionEvent.NowPlayingChanged(
                FfiConverterOptionalTypeNowPlaying.read(buf),
                )
            6 -> SessionEvent.AncsAuthStatusChanged(
                FfiConverterString.read(buf),
                FfiConverterTypeAncsAuthStatus.read(buf),
                )
            7 -> SessionEvent.Log(
                FfiConverterTypeLogOrigin.read(buf),
                FfiConverterTypeLogLevel.read(buf),
                FfiConverterString.read(buf),
                FfiConverterString.read(buf),
                )
            8 -> SessionEvent.WebappsChanged(
                FfiConverterTypeDeviceWebappsEntry.read(buf),
                )
            9 -> SessionEvent.WebappDocChanged(
                FfiConverterString.read(buf),
                FfiConverterString.read(buf),
                FfiConverterString.read(buf),
                FfiConverterOptionalString.read(buf),
                )
            10 -> SessionEvent.DeviceMetaChanged(
                FfiConverterString.read(buf),
                FfiConverterTypeDeviceMeta.read(buf),
                )
            11 -> SessionEvent.VoiceModelStateChanged(
                FfiConverterTypeVoiceModelState.read(buf),
                )
            12 -> SessionEvent.VoiceTurnChanged(
                FfiConverterTypeVoiceTurn.read(buf),
                )
            13 -> SessionEvent.OtaRunChanged(
                FfiConverterTypeOtaRun.read(buf),
                )
            14 -> SessionEvent.OtaAvailableChanged(
                FfiConverterTypeOtaAvailable.read(buf),
                )
            15 -> SessionEvent.OtaPollChanged(
                FfiConverterTypeOtaPollStatus.read(buf),
                )
            16 -> SessionEvent.CompanionUpdateProgress(
                FfiConverterULong.read(buf),
                FfiConverterULong.read(buf),
                )
            17 -> SessionEvent.ScreenshotCaptured(
                FfiConverterString.read(buf),
                FfiConverterString.read(buf),
                FfiConverterUInt.read(buf),
                FfiConverterULong.read(buf),
                )
            18 -> SessionEvent.Resumed
            else -> throw RuntimeException("invalid enum value, something is very wrong!!")
        }
    }

    override fun allocationSize(value: SessionEvent): ULong = when(value) {
        is SessionEvent.ProvidersChanged -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterSequenceTypeProviderInfo.allocationSize(value.`providers`)
            )
        }
        is SessionEvent.PeerConnected -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterTypeSessionPeer.allocationSize(value.`peer`)
            )
        }
        is SessionEvent.PeerDisconnected -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterString.allocationSize(value.`deviceId`)
            )
        }
        is SessionEvent.PeerLinkFailed -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterTypeSessionPeer.allocationSize(value.`peer`)
            )
        }
        is SessionEvent.NowPlayingChanged -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterOptionalTypeNowPlaying.allocationSize(value.`nowPlaying`)
            )
        }
        is SessionEvent.AncsAuthStatusChanged -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterString.allocationSize(value.`deviceId`)
                + FfiConverterTypeAncsAuthStatus.allocationSize(value.`status`)
            )
        }
        is SessionEvent.Log -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterTypeLogOrigin.allocationSize(value.`origin`)
                + FfiConverterTypeLogLevel.allocationSize(value.`level`)
                + FfiConverterString.allocationSize(value.`target`)
                + FfiConverterString.allocationSize(value.`message`)
            )
        }
        is SessionEvent.WebappsChanged -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterTypeDeviceWebappsEntry.allocationSize(value.`entry`)
            )
        }
        is SessionEvent.WebappDocChanged -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterString.allocationSize(value.`deviceId`)
                + FfiConverterString.allocationSize(value.`webappId`)
                + FfiConverterString.allocationSize(value.`key`)
                + FfiConverterOptionalString.allocationSize(value.`value`)
            )
        }
        is SessionEvent.DeviceMetaChanged -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterString.allocationSize(value.`deviceId`)
                + FfiConverterTypeDeviceMeta.allocationSize(value.`meta`)
            )
        }
        is SessionEvent.VoiceModelStateChanged -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterTypeVoiceModelState.allocationSize(value.`state`)
            )
        }
        is SessionEvent.VoiceTurnChanged -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterTypeVoiceTurn.allocationSize(value.`turn`)
            )
        }
        is SessionEvent.OtaRunChanged -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterTypeOtaRun.allocationSize(value.`run`)
            )
        }
        is SessionEvent.OtaAvailableChanged -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterTypeOtaAvailable.allocationSize(value.`available`)
            )
        }
        is SessionEvent.OtaPollChanged -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterTypeOtaPollStatus.allocationSize(value.`status`)
            )
        }
        is SessionEvent.CompanionUpdateProgress -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterULong.allocationSize(value.`received`)
                + FfiConverterULong.allocationSize(value.`total`)
            )
        }
        is SessionEvent.ScreenshotCaptured -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterString.allocationSize(value.`deviceId`)
                + FfiConverterString.allocationSize(value.`transferId`)
                + FfiConverterUInt.allocationSize(value.`byteSize`)
                + FfiConverterULong.allocationSize(value.`capturedAtMs`)
            )
        }
        is SessionEvent.Resumed -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
            )
        }
    }

    override fun write(value: SessionEvent, buf: ByteBuffer) {
        when(value) {
            is SessionEvent.ProvidersChanged -> {
                buf.putInt(1)
                FfiConverterSequenceTypeProviderInfo.write(value.`providers`, buf)
                Unit
            }
            is SessionEvent.PeerConnected -> {
                buf.putInt(2)
                FfiConverterTypeSessionPeer.write(value.`peer`, buf)
                Unit
            }
            is SessionEvent.PeerDisconnected -> {
                buf.putInt(3)
                FfiConverterString.write(value.`deviceId`, buf)
                Unit
            }
            is SessionEvent.PeerLinkFailed -> {
                buf.putInt(4)
                FfiConverterTypeSessionPeer.write(value.`peer`, buf)
                Unit
            }
            is SessionEvent.NowPlayingChanged -> {
                buf.putInt(5)
                FfiConverterOptionalTypeNowPlaying.write(value.`nowPlaying`, buf)
                Unit
            }
            is SessionEvent.AncsAuthStatusChanged -> {
                buf.putInt(6)
                FfiConverterString.write(value.`deviceId`, buf)
                FfiConverterTypeAncsAuthStatus.write(value.`status`, buf)
                Unit
            }
            is SessionEvent.Log -> {
                buf.putInt(7)
                FfiConverterTypeLogOrigin.write(value.`origin`, buf)
                FfiConverterTypeLogLevel.write(value.`level`, buf)
                FfiConverterString.write(value.`target`, buf)
                FfiConverterString.write(value.`message`, buf)
                Unit
            }
            is SessionEvent.WebappsChanged -> {
                buf.putInt(8)
                FfiConverterTypeDeviceWebappsEntry.write(value.`entry`, buf)
                Unit
            }
            is SessionEvent.WebappDocChanged -> {
                buf.putInt(9)
                FfiConverterString.write(value.`deviceId`, buf)
                FfiConverterString.write(value.`webappId`, buf)
                FfiConverterString.write(value.`key`, buf)
                FfiConverterOptionalString.write(value.`value`, buf)
                Unit
            }
            is SessionEvent.DeviceMetaChanged -> {
                buf.putInt(10)
                FfiConverterString.write(value.`deviceId`, buf)
                FfiConverterTypeDeviceMeta.write(value.`meta`, buf)
                Unit
            }
            is SessionEvent.VoiceModelStateChanged -> {
                buf.putInt(11)
                FfiConverterTypeVoiceModelState.write(value.`state`, buf)
                Unit
            }
            is SessionEvent.VoiceTurnChanged -> {
                buf.putInt(12)
                FfiConverterTypeVoiceTurn.write(value.`turn`, buf)
                Unit
            }
            is SessionEvent.OtaRunChanged -> {
                buf.putInt(13)
                FfiConverterTypeOtaRun.write(value.`run`, buf)
                Unit
            }
            is SessionEvent.OtaAvailableChanged -> {
                buf.putInt(14)
                FfiConverterTypeOtaAvailable.write(value.`available`, buf)
                Unit
            }
            is SessionEvent.OtaPollChanged -> {
                buf.putInt(15)
                FfiConverterTypeOtaPollStatus.write(value.`status`, buf)
                Unit
            }
            is SessionEvent.CompanionUpdateProgress -> {
                buf.putInt(16)
                FfiConverterULong.write(value.`received`, buf)
                FfiConverterULong.write(value.`total`, buf)
                Unit
            }
            is SessionEvent.ScreenshotCaptured -> {
                buf.putInt(17)
                FfiConverterString.write(value.`deviceId`, buf)
                FfiConverterString.write(value.`transferId`, buf)
                FfiConverterUInt.write(value.`byteSize`, buf)
                FfiConverterULong.write(value.`capturedAtMs`, buf)
                Unit
            }
            is SessionEvent.Resumed -> {
                buf.putInt(18)
                Unit
            }
        }.let { /* this makes the `when` an expression, which ensures it is exhaustive */ }
    }
}






enum class VoiceModelStatus {
    
    ABSENT,
    DOWNLOADING,
    READY,
    FAILED;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeVoiceModelStatus: FfiConverterRustBuffer<VoiceModelStatus> {
    override fun read(buf: ByteBuffer) = try {
        VoiceModelStatus.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: VoiceModelStatus) = 4UL

    override fun write(value: VoiceModelStatus, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}






enum class VoiceTurnPhase {
    
    LISTENING,
    RESOLVED,
    CANCELLED;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeVoiceTurnPhase: FfiConverterRustBuffer<VoiceTurnPhase> {
    override fun read(buf: ByteBuffer) = try {
        VoiceTurnPhase.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: VoiceTurnPhase) = 4UL

    override fun write(value: VoiceTurnPhase, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}






enum class VoiceTurnTrigger {
    
    PUSH_TO_TALK,
    ASSISTANT,
    WAKE_WORD;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeVoiceTurnTrigger: FfiConverterRustBuffer<VoiceTurnTrigger> {
    override fun read(buf: ByteBuffer) = try {
        VoiceTurnTrigger.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: VoiceTurnTrigger) = 4UL

    override fun write(value: VoiceTurnTrigger, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}






enum class WakeReason {
    
    USER_PLAY,
    CONNECT_RESUME;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeWakeReason: FfiConverterRustBuffer<WakeReason> {
    override fun read(buf: ByteBuffer) = try {
        WakeReason.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: WakeReason) = 4UL

    override fun write(value: WakeReason, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}






enum class WebappResourceKind {
    
    ICON,
    SETTINGS,
    OVERLAY;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeWebappResourceKind: FfiConverterRustBuffer<WebappResourceKind> {
    override fun read(buf: ByteBuffer) = try {
        WebappResourceKind.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: WebappResourceKind) = 4UL

    override fun write(value: WebappResourceKind, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}






enum class WebappRole {
    
    STANDARD,
    LAUNCHER;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeWebappRole: FfiConverterRustBuffer<WebappRole> {
    override fun read(buf: ByteBuffer) = try {
        WebappRole.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: WebappRole) = 4UL

    override fun write(value: WebappRole, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}






enum class WebappSlot {
    
    LAUNCHER,
    OVERLAY;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeWebappSlot: FfiConverterRustBuffer<WebappSlot> {
    override fun read(buf: ByteBuffer) = try {
        WebappSlot.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: WebappSlot) = 4UL

    override fun write(value: WebappSlot, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}






enum class WebappSource {
    
    BUILTIN,
    INSTALLED;

    


    companion object
}


/**
 * @suppress
 */
public object FfiConverterTypeWebappSource: FfiConverterRustBuffer<WebappSource> {
    override fun read(buf: ByteBuffer) = try {
        WebappSource.values()[buf.getInt() - 1]
    } catch (e: IndexOutOfBoundsException) {
        throw RuntimeException("invalid enum value, something is very wrong!!", e)
    }

    override fun allocationSize(value: WebappSource) = 4UL

    override fun write(value: WebappSource, buf: ByteBuffer) {
        buf.putInt(value.ordinal + 1)
    }
}





sealed class WsFrame {
    
    data class Text(
        val `text`: kotlin.String) : WsFrame()
        
    {
        

        companion object
    }
    
    data class Binary(
        val `bytes`: kotlin.ByteArray) : WsFrame()
        
    {
        

        companion object
    }
    

    

    
    


    companion object
}

/**
 * @suppress
 */
public object FfiConverterTypeWsFrame : FfiConverterRustBuffer<WsFrame>{
    override fun read(buf: ByteBuffer): WsFrame {
        return when(buf.getInt()) {
            1 -> WsFrame.Text(
                FfiConverterString.read(buf),
                )
            2 -> WsFrame.Binary(
                FfiConverterByteArray.read(buf),
                )
            else -> throw RuntimeException("invalid enum value, something is very wrong!!")
        }
    }

    override fun allocationSize(value: WsFrame): ULong = when(value) {
        is WsFrame.Text -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterString.allocationSize(value.`text`)
            )
        }
        is WsFrame.Binary -> {
            // Add the size for the Int that specifies the variant plus the size needed for all fields
            (
                4UL
                + FfiConverterByteArray.allocationSize(value.`bytes`)
            )
        }
    }

    override fun write(value: WsFrame, buf: ByteBuffer) {
        when(value) {
            is WsFrame.Text -> {
                buf.putInt(1)
                FfiConverterString.write(value.`text`, buf)
                Unit
            }
            is WsFrame.Binary -> {
                buf.putInt(2)
                FfiConverterByteArray.write(value.`bytes`, buf)
                Unit
            }
        }.let { /* this makes the `when` an expression, which ensures it is exhaustive */ }
    }
}






/**
 * @suppress
 */
public object FfiConverterOptionalUByte: FfiConverterRustBuffer<kotlin.UByte?> {
    override fun read(buf: ByteBuffer): kotlin.UByte? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterUByte.read(buf)
    }

    override fun allocationSize(value: kotlin.UByte?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterUByte.allocationSize(value)
        }
    }

    override fun write(value: kotlin.UByte?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterUByte.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalUShort: FfiConverterRustBuffer<kotlin.UShort?> {
    override fun read(buf: ByteBuffer): kotlin.UShort? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterUShort.read(buf)
    }

    override fun allocationSize(value: kotlin.UShort?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterUShort.allocationSize(value)
        }
    }

    override fun write(value: kotlin.UShort?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterUShort.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalUInt: FfiConverterRustBuffer<kotlin.UInt?> {
    override fun read(buf: ByteBuffer): kotlin.UInt? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterUInt.read(buf)
    }

    override fun allocationSize(value: kotlin.UInt?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterUInt.allocationSize(value)
        }
    }

    override fun write(value: kotlin.UInt?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterUInt.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalInt: FfiConverterRustBuffer<kotlin.Int?> {
    override fun read(buf: ByteBuffer): kotlin.Int? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterInt.read(buf)
    }

    override fun allocationSize(value: kotlin.Int?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterInt.allocationSize(value)
        }
    }

    override fun write(value: kotlin.Int?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterInt.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalULong: FfiConverterRustBuffer<kotlin.ULong?> {
    override fun read(buf: ByteBuffer): kotlin.ULong? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterULong.read(buf)
    }

    override fun allocationSize(value: kotlin.ULong?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterULong.allocationSize(value)
        }
    }

    override fun write(value: kotlin.ULong?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterULong.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalLong: FfiConverterRustBuffer<kotlin.Long?> {
    override fun read(buf: ByteBuffer): kotlin.Long? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterLong.read(buf)
    }

    override fun allocationSize(value: kotlin.Long?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterLong.allocationSize(value)
        }
    }

    override fun write(value: kotlin.Long?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterLong.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalFloat: FfiConverterRustBuffer<kotlin.Float?> {
    override fun read(buf: ByteBuffer): kotlin.Float? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterFloat.read(buf)
    }

    override fun allocationSize(value: kotlin.Float?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterFloat.allocationSize(value)
        }
    }

    override fun write(value: kotlin.Float?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterFloat.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalDouble: FfiConverterRustBuffer<kotlin.Double?> {
    override fun read(buf: ByteBuffer): kotlin.Double? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterDouble.read(buf)
    }

    override fun allocationSize(value: kotlin.Double?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterDouble.allocationSize(value)
        }
    }

    override fun write(value: kotlin.Double?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterDouble.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalBoolean: FfiConverterRustBuffer<kotlin.Boolean?> {
    override fun read(buf: ByteBuffer): kotlin.Boolean? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterBoolean.read(buf)
    }

    override fun allocationSize(value: kotlin.Boolean?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterBoolean.allocationSize(value)
        }
    }

    override fun write(value: kotlin.Boolean?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterBoolean.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalString: FfiConverterRustBuffer<kotlin.String?> {
    override fun read(buf: ByteBuffer): kotlin.String? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterString.read(buf)
    }

    override fun allocationSize(value: kotlin.String?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterString.allocationSize(value)
        }
    }

    override fun write(value: kotlin.String?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterString.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalByteArray: FfiConverterRustBuffer<kotlin.ByteArray?> {
    override fun read(buf: ByteBuffer): kotlin.ByteArray? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterByteArray.read(buf)
    }

    override fun allocationSize(value: kotlin.ByteArray?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterByteArray.allocationSize(value)
        }
    }

    override fun write(value: kotlin.ByteArray?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterByteArray.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeAppleMusicBackend: FfiConverterRustBuffer<AppleMusicBackend?> {
    override fun read(buf: ByteBuffer): AppleMusicBackend? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeAppleMusicBackend.read(buf)
    }

    override fun allocationSize(value: AppleMusicBackend?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeAppleMusicBackend.allocationSize(value)
        }
    }

    override fun write(value: AppleMusicBackend?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeAppleMusicBackend.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeAudioBackend: FfiConverterRustBuffer<AudioBackend?> {
    override fun read(buf: ByteBuffer): AudioBackend? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeAudioBackend.read(buf)
    }

    override fun allocationSize(value: AudioBackend?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeAudioBackend.allocationSize(value)
        }
    }

    override fun write(value: AudioBackend?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeAudioBackend.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeConnectivityMonitor: FfiConverterRustBuffer<ConnectivityMonitor?> {
    override fun read(buf: ByteBuffer): ConnectivityMonitor? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeConnectivityMonitor.read(buf)
    }

    override fun allocationSize(value: ConnectivityMonitor?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeConnectivityMonitor.allocationSize(value)
        }
    }

    override fun write(value: ConnectivityMonitor?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeConnectivityMonitor.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeDeviceWaker: FfiConverterRustBuffer<DeviceWaker?> {
    override fun read(buf: ByteBuffer): DeviceWaker? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeDeviceWaker.read(buf)
    }

    override fun allocationSize(value: DeviceWaker?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeDeviceWaker.allocationSize(value)
        }
    }

    override fun write(value: DeviceWaker?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeDeviceWaker.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeExtensionHost: FfiConverterRustBuffer<ExtensionHost?> {
    override fun read(buf: ByteBuffer): ExtensionHost? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeExtensionHost.read(buf)
    }

    override fun allocationSize(value: ExtensionHost?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeExtensionHost.allocationSize(value)
        }
    }

    override fun write(value: ExtensionHost?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeExtensionHost.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeGeoProvider: FfiConverterRustBuffer<GeoProvider?> {
    override fun read(buf: ByteBuffer): GeoProvider? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeGeoProvider.read(buf)
    }

    override fun allocationSize(value: GeoProvider?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeGeoProvider.allocationSize(value)
        }
    }

    override fun write(value: GeoProvider?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeGeoProvider.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeImageScaler: FfiConverterRustBuffer<ImageScaler?> {
    override fun read(buf: ByteBuffer): ImageScaler? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeImageScaler.read(buf)
    }

    override fun allocationSize(value: ImageScaler?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeImageScaler.allocationSize(value)
        }
    }

    override fun write(value: ImageScaler?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeImageScaler.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeLinkTransport: FfiConverterRustBuffer<LinkTransport?> {
    override fun read(buf: ByteBuffer): LinkTransport? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeLinkTransport.read(buf)
    }

    override fun allocationSize(value: LinkTransport?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeLinkTransport.allocationSize(value)
        }
    }

    override fun write(value: LinkTransport?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeLinkTransport.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeMediaSessionBackend: FfiConverterRustBuffer<MediaSessionBackend?> {
    override fun read(buf: ByteBuffer): MediaSessionBackend? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeMediaSessionBackend.read(buf)
    }

    override fun allocationSize(value: MediaSessionBackend?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeMediaSessionBackend.allocationSize(value)
        }
    }

    override fun write(value: MediaSessionBackend?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeMediaSessionBackend.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeModelArtifactValidator: FfiConverterRustBuffer<ModelArtifactValidator?> {
    override fun read(buf: ByteBuffer): ModelArtifactValidator? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeModelArtifactValidator.read(buf)
    }

    override fun allocationSize(value: ModelArtifactValidator?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeModelArtifactValidator.allocationSize(value)
        }
    }

    override fun write(value: ModelArtifactValidator?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeModelArtifactValidator.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeNluModelRunner: FfiConverterRustBuffer<NluModelRunner?> {
    override fun read(buf: ByteBuffer): NluModelRunner? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeNluModelRunner.read(buf)
    }

    override fun allocationSize(value: NluModelRunner?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeNluModelRunner.allocationSize(value)
        }
    }

    override fun write(value: NluModelRunner?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeNluModelRunner.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeNotificationBackend: FfiConverterRustBuffer<NotificationBackend?> {
    override fun read(buf: ByteBuffer): NotificationBackend? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeNotificationBackend.read(buf)
    }

    override fun allocationSize(value: NotificationBackend?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeNotificationBackend.allocationSize(value)
        }
    }

    override fun write(value: NotificationBackend?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeNotificationBackend.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypePhoneBackend: FfiConverterRustBuffer<PhoneBackend?> {
    override fun read(buf: ByteBuffer): PhoneBackend? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypePhoneBackend.read(buf)
    }

    override fun allocationSize(value: PhoneBackend?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypePhoneBackend.allocationSize(value)
        }
    }

    override fun write(value: PhoneBackend?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypePhoneBackend.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeSpeechRecognizer: FfiConverterRustBuffer<SpeechRecognizer?> {
    override fun read(buf: ByteBuffer): SpeechRecognizer? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeSpeechRecognizer.read(buf)
    }

    override fun allocationSize(value: SpeechRecognizer?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeSpeechRecognizer.allocationSize(value)
        }
    }

    override fun write(value: SpeechRecognizer?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeSpeechRecognizer.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeTransferPolicy: FfiConverterRustBuffer<TransferPolicy?> {
    override fun read(buf: ByteBuffer): TransferPolicy? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeTransferPolicy.read(buf)
    }

    override fun allocationSize(value: TransferPolicy?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeTransferPolicy.allocationSize(value)
        }
    }

    override fun write(value: TransferPolicy?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeTransferPolicy.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeVolumeBackend: FfiConverterRustBuffer<VolumeBackend?> {
    override fun read(buf: ByteBuffer): VolumeBackend? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeVolumeBackend.read(buf)
    }

    override fun allocationSize(value: VolumeBackend?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeVolumeBackend.allocationSize(value)
        }
    }

    override fun write(value: VolumeBackend?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeVolumeBackend.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeWebappBundleSink: FfiConverterRustBuffer<WebappBundleSink?> {
    override fun read(buf: ByteBuffer): WebappBundleSink? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeWebappBundleSink.read(buf)
    }

    override fun allocationSize(value: WebappBundleSink?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeWebappBundleSink.allocationSize(value)
        }
    }

    override fun write(value: WebappBundleSink?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeWebappBundleSink.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeActiveWebapp: FfiConverterRustBuffer<ActiveWebapp?> {
    override fun read(buf: ByteBuffer): ActiveWebapp? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeActiveWebapp.read(buf)
    }

    override fun allocationSize(value: ActiveWebapp?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeActiveWebapp.allocationSize(value)
        }
    }

    override fun write(value: ActiveWebapp?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeActiveWebapp.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeAmEntry: FfiConverterRustBuffer<AmEntry?> {
    override fun read(buf: ByteBuffer): AmEntry? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeAmEntry.read(buf)
    }

    override fun allocationSize(value: AmEntry?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeAmEntry.allocationSize(value)
        }
    }

    override fun write(value: AmEntry?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeAmEntry.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeArtifactDigest: FfiConverterRustBuffer<ArtifactDigest?> {
    override fun read(buf: ByteBuffer): ArtifactDigest? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeArtifactDigest.read(buf)
    }

    override fun allocationSize(value: ArtifactDigest?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeArtifactDigest.allocationSize(value)
        }
    }

    override fun write(value: ArtifactDigest?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeArtifactDigest.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeExtensionInfo: FfiConverterRustBuffer<ExtensionInfo?> {
    override fun read(buf: ByteBuffer): ExtensionInfo? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeExtensionInfo.read(buf)
    }

    override fun allocationSize(value: ExtensionInfo?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeExtensionInfo.allocationSize(value)
        }
    }

    override fun write(value: ExtensionInfo?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeExtensionInfo.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeMediaArt: FfiConverterRustBuffer<MediaArt?> {
    override fun read(buf: ByteBuffer): MediaArt? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeMediaArt.read(buf)
    }

    override fun allocationSize(value: MediaArt?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeMediaArt.allocationSize(value)
        }
    }

    override fun write(value: MediaArt?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeMediaArt.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeNluFastPathHit: FfiConverterRustBuffer<NluFastPathHit?> {
    override fun read(buf: ByteBuffer): NluFastPathHit? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeNluFastPathHit.read(buf)
    }

    override fun allocationSize(value: NluFastPathHit?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeNluFastPathHit.allocationSize(value)
        }
    }

    override fun write(value: NluFastPathHit?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeNluFastPathHit.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeNotificationAction: FfiConverterRustBuffer<NotificationAction?> {
    override fun read(buf: ByteBuffer): NotificationAction? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeNotificationAction.read(buf)
    }

    override fun allocationSize(value: NotificationAction?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeNotificationAction.allocationSize(value)
        }
    }

    override fun write(value: NotificationAction?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeNotificationAction.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeNowPlaying: FfiConverterRustBuffer<NowPlaying?> {
    override fun read(buf: ByteBuffer): NowPlaying? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeNowPlaying.read(buf)
    }

    override fun allocationSize(value: NowPlaying?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeNowPlaying.allocationSize(value)
        }
    }

    override fun write(value: NowPlaying?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeNowPlaying.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeNowPlayingTrack: FfiConverterRustBuffer<NowPlayingTrack?> {
    override fun read(buf: ByteBuffer): NowPlayingTrack? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeNowPlayingTrack.read(buf)
    }

    override fun allocationSize(value: NowPlayingTrack?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeNowPlayingTrack.allocationSize(value)
        }
    }

    override fun write(value: NowPlayingTrack?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeNowPlayingTrack.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeOtaCompositeVersion: FfiConverterRustBuffer<OtaCompositeVersion?> {
    override fun read(buf: ByteBuffer): OtaCompositeVersion? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeOtaCompositeVersion.read(buf)
    }

    override fun allocationSize(value: OtaCompositeVersion?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeOtaCompositeVersion.allocationSize(value)
        }
    }

    override fun write(value: OtaCompositeVersion?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeOtaCompositeVersion.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeOtaPollConfig: FfiConverterRustBuffer<OtaPollConfig?> {
    override fun read(buf: ByteBuffer): OtaPollConfig? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeOtaPollConfig.read(buf)
    }

    override fun allocationSize(value: OtaPollConfig?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeOtaPollConfig.allocationSize(value)
        }
    }

    override fun write(value: OtaPollConfig?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeOtaPollConfig.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeOtaReleaseArtifacts: FfiConverterRustBuffer<OtaReleaseArtifacts?> {
    override fun read(buf: ByteBuffer): OtaReleaseArtifacts? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeOtaReleaseArtifacts.read(buf)
    }

    override fun allocationSize(value: OtaReleaseArtifacts?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeOtaReleaseArtifacts.allocationSize(value)
        }
    }

    override fun write(value: OtaReleaseArtifacts?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeOtaReleaseArtifacts.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeOtaRun: FfiConverterRustBuffer<OtaRun?> {
    override fun read(buf: ByteBuffer): OtaRun? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeOtaRun.read(buf)
    }

    override fun allocationSize(value: OtaRun?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeOtaRun.allocationSize(value)
        }
    }

    override fun write(value: OtaRun?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeOtaRun.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeOtaRunProgress: FfiConverterRustBuffer<OtaRunProgress?> {
    override fun read(buf: ByteBuffer): OtaRunProgress? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeOtaRunProgress.read(buf)
    }

    override fun allocationSize(value: OtaRunProgress?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeOtaRunProgress.allocationSize(value)
        }
    }

    override fun write(value: OtaRunProgress?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeOtaRunProgress.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeSpotifyProviderConfig: FfiConverterRustBuffer<SpotifyProviderConfig?> {
    override fun read(buf: ByteBuffer): SpotifyProviderConfig? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeSpotifyProviderConfig.read(buf)
    }

    override fun allocationSize(value: SpotifyProviderConfig?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeSpotifyProviderConfig.allocationSize(value)
        }
    }

    override fun write(value: SpotifyProviderConfig?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeSpotifyProviderConfig.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeWebappResourceOrigin: FfiConverterRustBuffer<WebappResourceOrigin?> {
    override fun read(buf: ByteBuffer): WebappResourceOrigin? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeWebappResourceOrigin.read(buf)
    }

    override fun allocationSize(value: WebappResourceOrigin?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeWebappResourceOrigin.allocationSize(value)
        }
    }

    override fun write(value: WebappResourceOrigin?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeWebappResourceOrigin.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeMediaRepeatMode: FfiConverterRustBuffer<MediaRepeatMode?> {
    override fun read(buf: ByteBuffer): MediaRepeatMode? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeMediaRepeatMode.read(buf)
    }

    override fun allocationSize(value: MediaRepeatMode?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeMediaRepeatMode.allocationSize(value)
        }
    }

    override fun write(value: MediaRepeatMode?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeMediaRepeatMode.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeModelPlatform: FfiConverterRustBuffer<ModelPlatform?> {
    override fun read(buf: ByteBuffer): ModelPlatform? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeModelPlatform.read(buf)
    }

    override fun allocationSize(value: ModelPlatform?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeModelPlatform.allocationSize(value)
        }
    }

    override fun write(value: ModelPlatform?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeModelPlatform.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeNluFastPathAmount: FfiConverterRustBuffer<NluFastPathAmount?> {
    override fun read(buf: ByteBuffer): NluFastPathAmount? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeNluFastPathAmount.read(buf)
    }

    override fun allocationSize(value: NluFastPathAmount?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeNluFastPathAmount.allocationSize(value)
        }
    }

    override fun write(value: NluFastPathAmount?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeNluFastPathAmount.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeNluFastPathDirection: FfiConverterRustBuffer<NluFastPathDirection?> {
    override fun read(buf: ByteBuffer): NluFastPathDirection? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeNluFastPathDirection.read(buf)
    }

    override fun allocationSize(value: NluFastPathDirection?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeNluFastPathDirection.allocationSize(value)
        }
    }

    override fun write(value: NluFastPathDirection?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeNluFastPathDirection.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeNluFastPathRepeatMode: FfiConverterRustBuffer<NluFastPathRepeatMode?> {
    override fun read(buf: ByteBuffer): NluFastPathRepeatMode? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeNluFastPathRepeatMode.read(buf)
    }

    override fun allocationSize(value: NluFastPathRepeatMode?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeNluFastPathRepeatMode.allocationSize(value)
        }
    }

    override fun write(value: NluFastPathRepeatMode?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeNluFastPathRepeatMode.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeNluFastPathSpeed: FfiConverterRustBuffer<NluFastPathSpeed?> {
    override fun read(buf: ByteBuffer): NluFastPathSpeed? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeNluFastPathSpeed.read(buf)
    }

    override fun allocationSize(value: NluFastPathSpeed?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeNluFastPathSpeed.allocationSize(value)
        }
    }

    override fun write(value: NluFastPathSpeed?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeNluFastPathSpeed.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeNluFastPathView: FfiConverterRustBuffer<NluFastPathView?> {
    override fun read(buf: ByteBuffer): NluFastPathView? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeNluFastPathView.read(buf)
    }

    override fun allocationSize(value: NluFastPathView?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeNluFastPathView.allocationSize(value)
        }
    }

    override fun write(value: NluFastPathView?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeNluFastPathView.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeOtaKind: FfiConverterRustBuffer<OtaKind?> {
    override fun read(buf: ByteBuffer): OtaKind? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeOtaKind.read(buf)
    }

    override fun allocationSize(value: OtaKind?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeOtaKind.allocationSize(value)
        }
    }

    override fun write(value: OtaKind?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeOtaKind.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeOtaRunOutcome: FfiConverterRustBuffer<OtaRunOutcome?> {
    override fun read(buf: ByteBuffer): OtaRunOutcome? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeOtaRunOutcome.read(buf)
    }

    override fun allocationSize(value: OtaRunOutcome?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeOtaRunOutcome.allocationSize(value)
        }
    }

    override fun write(value: OtaRunOutcome?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeOtaRunOutcome.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypePhoneCallService: FfiConverterRustBuffer<PhoneCallService?> {
    override fun read(buf: ByteBuffer): PhoneCallService? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypePhoneCallService.read(buf)
    }

    override fun allocationSize(value: PhoneCallService?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypePhoneCallService.allocationSize(value)
        }
    }

    override fun write(value: PhoneCallService?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypePhoneCallService.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterOptionalTypeRegistrationStatus: FfiConverterRustBuffer<RegistrationStatus?> {
    override fun read(buf: ByteBuffer): RegistrationStatus? {
        if (buf.get().toInt() == 0) {
            return null
        }
        return FfiConverterTypeRegistrationStatus.read(buf)
    }

    override fun allocationSize(value: RegistrationStatus?): ULong {
        if (value == null) {
            return 1UL
        } else {
            return 1UL + FfiConverterTypeRegistrationStatus.allocationSize(value)
        }
    }

    override fun write(value: RegistrationStatus?, buf: ByteBuffer) {
        if (value == null) {
            buf.put(0)
        } else {
            buf.put(1)
            FfiConverterTypeRegistrationStatus.write(value, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceInt: FfiConverterRustBuffer<List<kotlin.Int>> {
    override fun read(buf: ByteBuffer): List<kotlin.Int> {
        val len = buf.getInt()
        return List<kotlin.Int>(len) {
            FfiConverterInt.read(buf)
        }
    }

    override fun allocationSize(value: List<kotlin.Int>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterInt.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<kotlin.Int>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterInt.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceFloat: FfiConverterRustBuffer<List<kotlin.Float>> {
    override fun read(buf: ByteBuffer): List<kotlin.Float> {
        val len = buf.getInt()
        return List<kotlin.Float>(len) {
            FfiConverterFloat.read(buf)
        }
    }

    override fun allocationSize(value: List<kotlin.Float>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterFloat.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<kotlin.Float>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterFloat.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceDouble: FfiConverterRustBuffer<List<kotlin.Double>> {
    override fun read(buf: ByteBuffer): List<kotlin.Double> {
        val len = buf.getInt()
        return List<kotlin.Double>(len) {
            FfiConverterDouble.read(buf)
        }
    }

    override fun allocationSize(value: List<kotlin.Double>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterDouble.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<kotlin.Double>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterDouble.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceBoolean: FfiConverterRustBuffer<List<kotlin.Boolean>> {
    override fun read(buf: ByteBuffer): List<kotlin.Boolean> {
        val len = buf.getInt()
        return List<kotlin.Boolean>(len) {
            FfiConverterBoolean.read(buf)
        }
    }

    override fun allocationSize(value: List<kotlin.Boolean>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterBoolean.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<kotlin.Boolean>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterBoolean.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceString: FfiConverterRustBuffer<List<kotlin.String>> {
    override fun read(buf: ByteBuffer): List<kotlin.String> {
        val len = buf.getInt()
        return List<kotlin.String>(len) {
            FfiConverterString.read(buf)
        }
    }

    override fun allocationSize(value: List<kotlin.String>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterString.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<kotlin.String>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterString.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceTypeAmItem: FfiConverterRustBuffer<List<AmItem>> {
    override fun read(buf: ByteBuffer): List<AmItem> {
        val len = buf.getInt()
        return List<AmItem>(len) {
            FfiConverterTypeAmItem.read(buf)
        }
    }

    override fun allocationSize(value: List<AmItem>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterTypeAmItem.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<AmItem>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterTypeAmItem.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceTypeAmShelf: FfiConverterRustBuffer<List<AmShelf>> {
    override fun read(buf: ByteBuffer): List<AmShelf> {
        val len = buf.getInt()
        return List<AmShelf>(len) {
            FfiConverterTypeAmShelf.read(buf)
        }
    }

    override fun allocationSize(value: List<AmShelf>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterTypeAmShelf.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<AmShelf>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterTypeAmShelf.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceTypeAncsAuthStatusEntry: FfiConverterRustBuffer<List<AncsAuthStatusEntry>> {
    override fun read(buf: ByteBuffer): List<AncsAuthStatusEntry> {
        val len = buf.getInt()
        return List<AncsAuthStatusEntry>(len) {
            FfiConverterTypeAncsAuthStatusEntry.read(buf)
        }
    }

    override fun allocationSize(value: List<AncsAuthStatusEntry>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterTypeAncsAuthStatusEntry.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<AncsAuthStatusEntry>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterTypeAncsAuthStatusEntry.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceTypeConfigEntry: FfiConverterRustBuffer<List<ConfigEntry>> {
    override fun read(buf: ByteBuffer): List<ConfigEntry> {
        val len = buf.getInt()
        return List<ConfigEntry>(len) {
            FfiConverterTypeConfigEntry.read(buf)
        }
    }

    override fun allocationSize(value: List<ConfigEntry>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterTypeConfigEntry.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<ConfigEntry>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterTypeConfigEntry.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceTypeConfigField: FfiConverterRustBuffer<List<ConfigField>> {
    override fun read(buf: ByteBuffer): List<ConfigField> {
        val len = buf.getInt()
        return List<ConfigField>(len) {
            FfiConverterTypeConfigField.read(buf)
        }
    }

    override fun allocationSize(value: List<ConfigField>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterTypeConfigField.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<ConfigField>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterTypeConfigField.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceTypeDeviceAutoResume: FfiConverterRustBuffer<List<DeviceAutoResume>> {
    override fun read(buf: ByteBuffer): List<DeviceAutoResume> {
        val len = buf.getInt()
        return List<DeviceAutoResume>(len) {
            FfiConverterTypeDeviceAutoResume.read(buf)
        }
    }

    override fun allocationSize(value: List<DeviceAutoResume>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterTypeDeviceAutoResume.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<DeviceAutoResume>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterTypeDeviceAutoResume.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceTypeDeviceLogLine: FfiConverterRustBuffer<List<DeviceLogLine>> {
    override fun read(buf: ByteBuffer): List<DeviceLogLine> {
        val len = buf.getInt()
        return List<DeviceLogLine>(len) {
            FfiConverterTypeDeviceLogLine.read(buf)
        }
    }

    override fun allocationSize(value: List<DeviceLogLine>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterTypeDeviceLogLine.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<DeviceLogLine>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterTypeDeviceLogLine.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceTypeDeviceMetaEntry: FfiConverterRustBuffer<List<DeviceMetaEntry>> {
    override fun read(buf: ByteBuffer): List<DeviceMetaEntry> {
        val len = buf.getInt()
        return List<DeviceMetaEntry>(len) {
            FfiConverterTypeDeviceMetaEntry.read(buf)
        }
    }

    override fun allocationSize(value: List<DeviceMetaEntry>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterTypeDeviceMetaEntry.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<DeviceMetaEntry>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterTypeDeviceMetaEntry.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceTypeDeviceResumeTarget: FfiConverterRustBuffer<List<DeviceResumeTarget>> {
    override fun read(buf: ByteBuffer): List<DeviceResumeTarget> {
        val len = buf.getInt()
        return List<DeviceResumeTarget>(len) {
            FfiConverterTypeDeviceResumeTarget.read(buf)
        }
    }

    override fun allocationSize(value: List<DeviceResumeTarget>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterTypeDeviceResumeTarget.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<DeviceResumeTarget>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterTypeDeviceResumeTarget.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceTypeDeviceWebappsEntry: FfiConverterRustBuffer<List<DeviceWebappsEntry>> {
    override fun read(buf: ByteBuffer): List<DeviceWebappsEntry> {
        val len = buf.getInt()
        return List<DeviceWebappsEntry>(len) {
            FfiConverterTypeDeviceWebappsEntry.read(buf)
        }
    }

    override fun allocationSize(value: List<DeviceWebappsEntry>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterTypeDeviceWebappsEntry.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<DeviceWebappsEntry>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterTypeDeviceWebappsEntry.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceTypeDocEntry: FfiConverterRustBuffer<List<DocEntry>> {
    override fun read(buf: ByteBuffer): List<DocEntry> {
        val len = buf.getInt()
        return List<DocEntry>(len) {
            FfiConverterTypeDocEntry.read(buf)
        }
    }

    override fun allocationSize(value: List<DocEntry>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterTypeDocEntry.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<DocEntry>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterTypeDocEntry.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceTypeExtensionConfigEntry: FfiConverterRustBuffer<List<ExtensionConfigEntry>> {
    override fun read(buf: ByteBuffer): List<ExtensionConfigEntry> {
        val len = buf.getInt()
        return List<ExtensionConfigEntry>(len) {
            FfiConverterTypeExtensionConfigEntry.read(buf)
        }
    }

    override fun allocationSize(value: List<ExtensionConfigEntry>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterTypeExtensionConfigEntry.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<ExtensionConfigEntry>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterTypeExtensionConfigEntry.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceTypeHttpHeader: FfiConverterRustBuffer<List<HttpHeader>> {
    override fun read(buf: ByteBuffer): List<HttpHeader> {
        val len = buf.getInt()
        return List<HttpHeader>(len) {
            FfiConverterTypeHttpHeader.read(buf)
        }
    }

    override fun allocationSize(value: List<HttpHeader>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterTypeHttpHeader.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<HttpHeader>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterTypeHttpHeader.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceTypeLogArchive: FfiConverterRustBuffer<List<LogArchive>> {
    override fun read(buf: ByteBuffer): List<LogArchive> {
        val len = buf.getInt()
        return List<LogArchive>(len) {
            FfiConverterTypeLogArchive.read(buf)
        }
    }

    override fun allocationSize(value: List<LogArchive>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterTypeLogArchive.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<LogArchive>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterTypeLogArchive.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceTypeLogStoreLine: FfiConverterRustBuffer<List<LogStoreLine>> {
    override fun read(buf: ByteBuffer): List<LogStoreLine> {
        val len = buf.getInt()
        return List<LogStoreLine>(len) {
            FfiConverterTypeLogStoreLine.read(buf)
        }
    }

    override fun allocationSize(value: List<LogStoreLine>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterTypeLogStoreLine.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<LogStoreLine>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterTypeLogStoreLine.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceTypeLrcLine: FfiConverterRustBuffer<List<LrcLine>> {
    override fun read(buf: ByteBuffer): List<LrcLine> {
        val len = buf.getInt()
        return List<LrcLine>(len) {
            FfiConverterTypeLrcLine.read(buf)
        }
    }

    override fun allocationSize(value: List<LrcLine>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterTypeLrcLine.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<LrcLine>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterTypeLrcLine.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceTypeMediaQueueEntry: FfiConverterRustBuffer<List<MediaQueueEntry>> {
    override fun read(buf: ByteBuffer): List<MediaQueueEntry> {
        val len = buf.getInt()
        return List<MediaQueueEntry>(len) {
            FfiConverterTypeMediaQueueEntry.read(buf)
        }
    }

    override fun allocationSize(value: List<MediaQueueEntry>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterTypeMediaQueueEntry.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<MediaQueueEntry>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterTypeMediaQueueEntry.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceTypeMediaSessionSnapshot: FfiConverterRustBuffer<List<MediaSessionSnapshot>> {
    override fun read(buf: ByteBuffer): List<MediaSessionSnapshot> {
        val len = buf.getInt()
        return List<MediaSessionSnapshot>(len) {
            FfiConverterTypeMediaSessionSnapshot.read(buf)
        }
    }

    override fun allocationSize(value: List<MediaSessionSnapshot>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterTypeMediaSessionSnapshot.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<MediaSessionSnapshot>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterTypeMediaSessionSnapshot.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceTypeOtaAvailable: FfiConverterRustBuffer<List<OtaAvailable>> {
    override fun read(buf: ByteBuffer): List<OtaAvailable> {
        val len = buf.getInt()
        return List<OtaAvailable>(len) {
            FfiConverterTypeOtaAvailable.read(buf)
        }
    }

    override fun allocationSize(value: List<OtaAvailable>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterTypeOtaAvailable.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<OtaAvailable>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterTypeOtaAvailable.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceTypeOtaPlanStep: FfiConverterRustBuffer<List<OtaPlanStep>> {
    override fun read(buf: ByteBuffer): List<OtaPlanStep> {
        val len = buf.getInt()
        return List<OtaPlanStep>(len) {
            FfiConverterTypeOtaPlanStep.read(buf)
        }
    }

    override fun allocationSize(value: List<OtaPlanStep>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterTypeOtaPlanStep.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<OtaPlanStep>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterTypeOtaPlanStep.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceTypeOtaRun: FfiConverterRustBuffer<List<OtaRun>> {
    override fun read(buf: ByteBuffer): List<OtaRun> {
        val len = buf.getInt()
        return List<OtaRun>(len) {
            FfiConverterTypeOtaRun.read(buf)
        }
    }

    override fun allocationSize(value: List<OtaRun>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterTypeOtaRun.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<OtaRun>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterTypeOtaRun.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceTypePhoneCall: FfiConverterRustBuffer<List<PhoneCall>> {
    override fun read(buf: ByteBuffer): List<PhoneCall> {
        val len = buf.getInt()
        return List<PhoneCall>(len) {
            FfiConverterTypePhoneCall.read(buf)
        }
    }

    override fun allocationSize(value: List<PhoneCall>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterTypePhoneCall.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<PhoneCall>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterTypePhoneCall.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceTypeProviderInfo: FfiConverterRustBuffer<List<ProviderInfo>> {
    override fun read(buf: ByteBuffer): List<ProviderInfo> {
        val len = buf.getInt()
        return List<ProviderInfo>(len) {
            FfiConverterTypeProviderInfo.read(buf)
        }
    }

    override fun allocationSize(value: List<ProviderInfo>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterTypeProviderInfo.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<ProviderInfo>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterTypeProviderInfo.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceTypeSessionPeer: FfiConverterRustBuffer<List<SessionPeer>> {
    override fun read(buf: ByteBuffer): List<SessionPeer> {
        val len = buf.getInt()
        return List<SessionPeer>(len) {
            FfiConverterTypeSessionPeer.read(buf)
        }
    }

    override fun allocationSize(value: List<SessionPeer>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterTypeSessionPeer.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<SessionPeer>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterTypeSessionPeer.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceTypeSpeechSegment: FfiConverterRustBuffer<List<SpeechSegment>> {
    override fun read(buf: ByteBuffer): List<SpeechSegment> {
        val len = buf.getInt()
        return List<SpeechSegment>(len) {
            FfiConverterTypeSpeechSegment.read(buf)
        }
    }

    override fun allocationSize(value: List<SpeechSegment>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterTypeSpeechSegment.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<SpeechSegment>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterTypeSpeechSegment.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceTypeWebappInfo: FfiConverterRustBuffer<List<WebappInfo>> {
    override fun read(buf: ByteBuffer): List<WebappInfo> {
        val len = buf.getInt()
        return List<WebappInfo>(len) {
            FfiConverterTypeWebappInfo.read(buf)
        }
    }

    override fun allocationSize(value: List<WebappInfo>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterTypeWebappInfo.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<WebappInfo>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterTypeWebappInfo.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceTypeOtaStoreChange: FfiConverterRustBuffer<List<OtaStoreChange>> {
    override fun read(buf: ByteBuffer): List<OtaStoreChange> {
        val len = buf.getInt()
        return List<OtaStoreChange>(len) {
            FfiConverterTypeOtaStoreChange.read(buf)
        }
    }

    override fun allocationSize(value: List<OtaStoreChange>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterTypeOtaStoreChange.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<OtaStoreChange>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterTypeOtaStoreChange.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterSequenceSequenceFloat: FfiConverterRustBuffer<List<List<kotlin.Float>>> {
    override fun read(buf: ByteBuffer): List<List<kotlin.Float>> {
        val len = buf.getInt()
        return List<List<kotlin.Float>>(len) {
            FfiConverterSequenceFloat.read(buf)
        }
    }

    override fun allocationSize(value: List<List<kotlin.Float>>): ULong {
        val sizeForLength = 4UL
        val sizeForItems = value.map { FfiConverterSequenceFloat.allocationSize(it) }.sum()
        return sizeForLength + sizeForItems
    }

    override fun write(value: List<List<kotlin.Float>>, buf: ByteBuffer) {
        buf.putInt(value.size)
        value.iterator().forEach {
            FfiConverterSequenceFloat.write(it, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterMapStringString: FfiConverterRustBuffer<Map<kotlin.String, kotlin.String>> {
    override fun read(buf: ByteBuffer): Map<kotlin.String, kotlin.String> {
        val len = buf.getInt()
        return buildMap<kotlin.String, kotlin.String>(len) {
            repeat(len) {
                val k = FfiConverterString.read(buf)
                val v = FfiConverterString.read(buf)
                this[k] = v
            }
        }
    }

    override fun allocationSize(value: Map<kotlin.String, kotlin.String>): ULong {
        val spaceForMapSize = 4UL
        val spaceForChildren = value.map { (k, v) ->
            FfiConverterString.allocationSize(k) +
            FfiConverterString.allocationSize(v)
        }.sum()
        return spaceForMapSize + spaceForChildren
    }

    override fun write(value: Map<kotlin.String, kotlin.String>, buf: ByteBuffer) {
        buf.putInt(value.size)
        // The parens on `(k, v)` here ensure we're calling the right method,
        // which is important for compatibility with older android devices.
        // Ref https://blog.danlew.net/2017/03/16/kotlin-puzzler-whose-line-is-it-anyways/
        value.forEach { (k, v) ->
            FfiConverterString.write(k, buf)
            FfiConverterString.write(v, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterMapStringTypeArtifactDigest: FfiConverterRustBuffer<Map<kotlin.String, ArtifactDigest>> {
    override fun read(buf: ByteBuffer): Map<kotlin.String, ArtifactDigest> {
        val len = buf.getInt()
        return buildMap<kotlin.String, ArtifactDigest>(len) {
            repeat(len) {
                val k = FfiConverterString.read(buf)
                val v = FfiConverterTypeArtifactDigest.read(buf)
                this[k] = v
            }
        }
    }

    override fun allocationSize(value: Map<kotlin.String, ArtifactDigest>): ULong {
        val spaceForMapSize = 4UL
        val spaceForChildren = value.map { (k, v) ->
            FfiConverterString.allocationSize(k) +
            FfiConverterTypeArtifactDigest.allocationSize(v)
        }.sum()
        return spaceForMapSize + spaceForChildren
    }

    override fun write(value: Map<kotlin.String, ArtifactDigest>, buf: ByteBuffer) {
        buf.putInt(value.size)
        // The parens on `(k, v)` here ensure we're calling the right method,
        // which is important for compatibility with older android devices.
        // Ref https://blog.danlew.net/2017/03/16/kotlin-puzzler-whose-line-is-it-anyways/
        value.forEach { (k, v) ->
            FfiConverterString.write(k, buf)
            FfiConverterTypeArtifactDigest.write(v, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterMapStringTypeOtaManifestChannel: FfiConverterRustBuffer<Map<kotlin.String, OtaManifestChannel>> {
    override fun read(buf: ByteBuffer): Map<kotlin.String, OtaManifestChannel> {
        val len = buf.getInt()
        return buildMap<kotlin.String, OtaManifestChannel>(len) {
            repeat(len) {
                val k = FfiConverterString.read(buf)
                val v = FfiConverterTypeOtaManifestChannel.read(buf)
                this[k] = v
            }
        }
    }

    override fun allocationSize(value: Map<kotlin.String, OtaManifestChannel>): ULong {
        val spaceForMapSize = 4UL
        val spaceForChildren = value.map { (k, v) ->
            FfiConverterString.allocationSize(k) +
            FfiConverterTypeOtaManifestChannel.allocationSize(v)
        }.sum()
        return spaceForMapSize + spaceForChildren
    }

    override fun write(value: Map<kotlin.String, OtaManifestChannel>, buf: ByteBuffer) {
        buf.putInt(value.size)
        // The parens on `(k, v)` here ensure we're calling the right method,
        // which is important for compatibility with older android devices.
        // Ref https://blog.danlew.net/2017/03/16/kotlin-puzzler-whose-line-is-it-anyways/
        value.forEach { (k, v) ->
            FfiConverterString.write(k, buf)
            FfiConverterTypeOtaManifestChannel.write(v, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterMapStringTypeOtaManifestRelease: FfiConverterRustBuffer<Map<kotlin.String, OtaManifestRelease>> {
    override fun read(buf: ByteBuffer): Map<kotlin.String, OtaManifestRelease> {
        val len = buf.getInt()
        return buildMap<kotlin.String, OtaManifestRelease>(len) {
            repeat(len) {
                val k = FfiConverterString.read(buf)
                val v = FfiConverterTypeOtaManifestRelease.read(buf)
                this[k] = v
            }
        }
    }

    override fun allocationSize(value: Map<kotlin.String, OtaManifestRelease>): ULong {
        val spaceForMapSize = 4UL
        val spaceForChildren = value.map { (k, v) ->
            FfiConverterString.allocationSize(k) +
            FfiConverterTypeOtaManifestRelease.allocationSize(v)
        }.sum()
        return spaceForMapSize + spaceForChildren
    }

    override fun write(value: Map<kotlin.String, OtaManifestRelease>, buf: ByteBuffer) {
        buf.putInt(value.size)
        // The parens on `(k, v)` here ensure we're calling the right method,
        // which is important for compatibility with older android devices.
        // Ref https://blog.danlew.net/2017/03/16/kotlin-puzzler-whose-line-is-it-anyways/
        value.forEach { (k, v) ->
            FfiConverterString.write(k, buf)
            FfiConverterTypeOtaManifestRelease.write(v, buf)
        }
    }
}




/**
 * @suppress
 */
public object FfiConverterMapStringTypeOtaPatchDigest: FfiConverterRustBuffer<Map<kotlin.String, OtaPatchDigest>> {
    override fun read(buf: ByteBuffer): Map<kotlin.String, OtaPatchDigest> {
        val len = buf.getInt()
        return buildMap<kotlin.String, OtaPatchDigest>(len) {
            repeat(len) {
                val k = FfiConverterString.read(buf)
                val v = FfiConverterTypeOtaPatchDigest.read(buf)
                this[k] = v
            }
        }
    }

    override fun allocationSize(value: Map<kotlin.String, OtaPatchDigest>): ULong {
        val spaceForMapSize = 4UL
        val spaceForChildren = value.map { (k, v) ->
            FfiConverterString.allocationSize(k) +
            FfiConverterTypeOtaPatchDigest.allocationSize(v)
        }.sum()
        return spaceForMapSize + spaceForChildren
    }

    override fun write(value: Map<kotlin.String, OtaPatchDigest>, buf: ByteBuffer) {
        buf.putInt(value.size)
        // The parens on `(k, v)` here ensure we're calling the right method,
        // which is important for compatibility with older android devices.
        // Ref https://blog.danlew.net/2017/03/16/kotlin-puzzler-whose-line-is-it-anyways/
        value.forEach { (k, v) ->
            FfiConverterString.write(k, buf)
            FfiConverterTypeOtaPatchDigest.write(v, buf)
        }
    }
}







 fun `parseLrc`(`text`: kotlin.String): List<LrcLine> {
            return FfiConverterSequenceTypeLrcLine.lift(
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_func_parse_lrc(
    
        
        FfiConverterString.lower(`text`),_status)
}
    )
    }
    
 fun `nluFastPathMatch`(`transcript`: kotlin.String): NluFastPathHit? {
            return FfiConverterOptionalTypeNluFastPathHit.lift(
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_func_nlu_fast_path_match(
    
        
        FfiConverterString.lower(`transcript`),_status)
}
    )
    }
    
 fun `nluIntentCatalog`(): NluIntentCatalog {
            return FfiConverterTypeNluIntentCatalog.lift(
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_func_nlu_intent_catalog(
    
        _status)
}
    )
    }
    

    @Throws(NluRejectionException::class) fun `nluRejectionEvaluate`(`intentLogits`: List<kotlin.Double>, `inDomainLogit`: kotlin.Double, `policy`: NluRejectionPolicy): NluRejectionOutcome {
            return FfiConverterTypeNluRejectionOutcome.lift(
    uniffiRustCallWithError(NluRejectionException) { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_func_nlu_rejection_evaluate(
    
        
        FfiConverterSequenceDouble.lower(`intentLogits`),
        FfiConverterDouble.lower(`inDomainLogit`),
        FfiConverterTypeNluRejectionPolicy.lower(`policy`),_status)
}
    )
    }
    
 fun `otaArtifactUrls`(`rootUrl`: kotlin.String, `channel`: kotlin.String, `daemonVersion`: kotlin.String, `imageVersion`: kotlin.String, `imageVariant`: kotlin.String): OtaArtifactUrls {
            return FfiConverterTypeOtaArtifactUrls.lift(
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_func_ota_artifact_urls(
    
        
        FfiConverterString.lower(`rootUrl`),
        FfiConverterString.lower(`channel`),
        FfiConverterString.lower(`daemonVersion`),
        FfiConverterString.lower(`imageVersion`),
        FfiConverterString.lower(`imageVariant`),_status)
}
    )
    }
    
 fun `otaBuiltinWebappUrl`(`rootUrl`: kotlin.String, `channel`: kotlin.String, `name`: kotlin.String, `version`: kotlin.String): kotlin.String {
            return FfiConverterString.lift(
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_func_ota_builtin_webapp_url(
    
        
        FfiConverterString.lower(`rootUrl`),
        FfiConverterString.lower(`channel`),
        FfiConverterString.lower(`name`),
        FfiConverterString.lower(`version`),_status)
}
    )
    }
    
 fun `otaCancelledReason`(): kotlin.String {
            return FfiConverterString.lift(
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_func_ota_cancelled_reason(
    
        _status)
}
    )
    }
    
 fun `otaCompositeVersionString`(`version`: OtaCompositeVersion): kotlin.String {
            return FfiConverterString.lift(
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_func_ota_composite_version_string(
    
        
        FfiConverterTypeOtaCompositeVersion.lower(`version`),_status)
}
    )
    }
    
 fun `otaDaemonPatchUrl`(`rootUrl`: kotlin.String, `channel`: kotlin.String, `toVersion`: kotlin.String, `fromVersion`: kotlin.String): kotlin.String {
            return FfiConverterString.lift(
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_func_ota_daemon_patch_url(
    
        
        FfiConverterString.lower(`rootUrl`),
        FfiConverterString.lower(`channel`),
        FfiConverterString.lower(`toVersion`),
        FfiConverterString.lower(`fromVersion`),_status)
}
    )
    }
    
 fun `otaPatchSourceMatches`(`declared`: kotlin.String?, `running`: kotlin.String?): kotlin.Boolean {
            return FfiConverterBoolean.lift(
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_func_ota_patch_source_matches(
    
        
        FfiConverterOptionalString.lower(`declared`),
        FfiConverterOptionalString.lower(`running`),_status)
}
    )
    }
    
 fun `otaRunProgress`(`run`: OtaRun, `nowMs`: kotlin.ULong): OtaRunProgress {
            return FfiConverterTypeOtaRunProgress.lift(
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_func_ota_run_progress(
    
        
        FfiConverterTypeOtaRun.lower(`run`),
        FfiConverterULong.lower(`nowMs`),_status)
}
    )
    }
    
 fun `parseOtaCompositeVersion`(`raw`: kotlin.String): OtaCompositeVersion? {
            return FfiConverterOptionalTypeOtaCompositeVersion.lift(
    uniffiRustCall() { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_func_parse_ota_composite_version(
    
        
        FfiConverterString.lower(`raw`),_status)
}
    )
    }
    

    @Throws(OtaManifestException::class) fun `parseOtaDiscoverManifest`(`json`: kotlin.String): OtaDiscoverManifest {
            return FfiConverterTypeOtaDiscoverManifest.lift(
    uniffiRustCallWithError(OtaManifestException) { _status ->
    UniffiLib.uniffi_bridgething_companion_fn_func_parse_ota_discover_manifest(
    
        
        FfiConverterString.lower(`json`),_status)
}
    )
    }
    


